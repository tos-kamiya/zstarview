# Session 2026-03-14

- Scope: Review the existing planetarium CLI and `nominatim_search.py`, then define a specification for location search by place/station names without starting implementation.

- Topic: CLI location search by place or station names via Nominatim
  - Decision: Keep the existing positional `location` argument and add an explicit opt-in resolver path for online search, rather than silently changing current city-name behavior.
  - Rationale: Current `location` resolution is offline-first with GeoNames, viewpoint datasets, and lat/lon parsing. Replacing that behavior would change existing matches, introduce network dependency into normal startup, and make last-location persistence less predictable. An explicit online path preserves compatibility while still enabling station/place lookup.
  - Alternatives Considered: Full replacement of city-name resolution with Nominatim was rejected for now because it broadens the meaning of existing inputs and couples default startup behavior to network availability and Nominatim policy constraints.

- Topic: Nominatim result selection, display, and persistence
  - Decision: `--place` uses the first normalized Nominatim candidate for startup, prints all matched candidates to the terminal when there are multiple, shows failures on both terminal and splash, keeps the long `display_name` as the GUI-visible location string, and persists Nominatim-resolved locations as a tagged object containing the raw result JSON.
  - Rationale: The app remains non-interactive at startup while still exposing ambiguity to the user. Keeping the full display name helps users notice mismatches. Persisting the resolved object avoids re-querying on next launch and preserves the exact selected place instead of a potentially unstable free-form query.

- Summary:
  - Updated `docs/specification.md` with `--place`, `--place-countrycode`, `--place-lang`, default language `en`, Nominatim selection rules, failure behavior, and persistence requirements.
  - Updated `docs/design.md` with the dedicated online resolver path, timezone補完方針, and mixed-format last-location persistence design.
  - Implemented `--place` startup resolution, structured Nominatim persistence, and copied reusable search helpers into `src/zstarview/nominatim_search.py`.
  - Added CLI, startup, and config tests for the new path.
  - Updated `README_pypi.md` so the PyPI description also mentions `--place`, network-backed features, and Nominatim-related license/credit notes.

- 23:41 Ran: `uv run -p .venv/bin/python ruff check src/zstarview/cli_args.py src/zstarview/config.py src/zstarview/nominatim_search.py src/zstarview/startup.py src/zstarview/zstarview.py tests/test_cli_place_options.py tests/test_nominatim_startup_resolution.py tests/test_config_window_geometry.py`
  Output:
  ```text
  All checks passed!
  ```
  Result: Modified source and new tests passed static linting.

- 23:42 Ran: `python3 -m py_compile src/zstarview/cli_args.py src/zstarview/config.py src/zstarview/nominatim_search.py src/zstarview/startup.py src/zstarview/zstarview.py tests/test_cli_place_options.py tests/test_nominatim_startup_resolution.py tests/test_config_window_geometry.py`
  Output:
  ```text
  (no output)
  ```
  Result: Modified files compiled without syntax errors.

- 23:43 Ran: `PYTHONPATH=src uv run -p .venv/bin/python pytest tests/test_cli_place_options.py tests/test_nominatim_startup_resolution.py tests/test_config_window_geometry.py tests/test_tower_startup_resolution.py tests/test_cli_tower_options.py tests/test_cli_mountain_options.py tests/test_startup_city_persistence.py`
  Output:
  ```text
  ModuleNotFoundError: No module named 'appdirs'
  ModuleNotFoundError: No module named 'polars'
  ```

- Topic: Identify Japanese tower viewpoints from `zstarview --list-viewpoints t`
  - Decision: Use `src/zstarview/data/viewpoints/tower_viewpoints.json` as the source of truth and classify entries as Japanese when their coordinates fall within Japan's geographic bounds.
  - Rationale: The local viewpoint dataset is what the CLI resolves against, and coordinates are less ambiguous than inferring country from names alone.

- Topic: Import PLATEAU CityGML ZIP files into bundled skyline data
  - Decision: Add a thin orchestration script that accepts ZIP files, extracts them to a temporary directory, detects `udx/bldg`, and then reuses the existing derived-tile and tile-index builders. Optional one-off urban outline JSON export is supported behind a flag.
  - Rationale: The app already consumes `src/zstarview/data/plateau_derived/*/bldg` directly, so the lowest-risk path is to standardize ZIP import into that on-disk format instead of adding a new runtime code path first.
  Result: Runtime pytest verification is currently blocked by missing dependencies in the selected virtualenv.

- Topic: PyPI README sync after Nominatim documentation updates
  - Decision: Keep `README_pypi.md` short, but add the new `--place` example and options plus concise notes for network-backed features and runtime service/license expectations.
  - Rationale: The PyPI description should reflect the new user-facing startup behavior without duplicating the full repository README. Short notes are enough to avoid stale or misleading package metadata.

- Topic: Release metadata bump after PyPI README sync
  - Decision: Increment the package version from `0.29.0` to `0.29.1` as a patch release.
  - Rationale: The user requested a follow-up release bump after the PyPI metadata update, and the project stores the authoritative version in `src/zstarview/__about__.py`.

- Topic: Overture benchmark sample for area download timing
  - Decision: Add a `dev-samples/benchmark_overture_download.py` helper that computes a bbox from `lat/lon + radius`, shells out to the official `overturemaps` CLI, and reports elapsed time and output size without integrating the result into the app.
  - Rationale: The user wants a quick way to benchmark Overture fetch latency before considering product integration. Keeping it as a dev sample avoids adding network and third-party CLI assumptions to the main package.

- Topic: Experimental runtime switch from PLATEAU-derived urban outlines to Overture-derived cache data
  - Decision: Change the default runtime urban-outline lookup root from bundled `data/plateau_derived` to user cache `overture_buildings`, and add an experimental `zstarview-import-overture-buildings` utility that downloads Overture building data for a point-radius bbox and converts it into the existing derived-tile format.
  - Rationale: For a dev-branch experiment, reusing the existing derived JSON and `tile_index.json` reader path minimizes renderer churn while replacing the upstream acquisition pipeline. The PLATEAU code remains in-tree but is no longer the default runtime source.

- Topic: Remove the legacy PLATEAU pipeline after the Overture switch
  - Decision: Delete bundled `plateau_derived` assets, CityGML import/build scripts, and PLATEAU-named runtime helpers, while keeping only the generic derived-tile helpers and the Overture-backed runtime/cache path.
  - Rationale: After switching runtime urban outlines to Overture, the old PLATEAU path only added maintenance cost and naming confusion. Renaming the remaining shared helpers to source-neutral names keeps the renderer reusable without preserving a dead import pipeline.

- 23:58 Ran: `uv run -p .venv/bin/python ruff check src/zstarview/data/build_derived_tile_index.py src/zstarview/data/derived_tile_cache.py src/zstarview/data/urban_outline_from_buildings.py src/zstarview/data/import_overture_buildings.py src/zstarview/urban_debug_layer.py src/zstarview/paths.py tests/test_build_derived_tile_index.py tests/test_derived_tile_cache.py tests/test_urban_outline_from_buildings.py tests/test_import_overture_buildings.py tests/test_urban_debug_layer.py`
  Output:
  ```text
  All checks passed!
  ```
  Result: The renamed generic helpers, Overture importer, and updated tests passed linting.

- 23:59 Ran: `uv run -p .venv/bin/python pytest tests/test_build_derived_tile_index.py tests/test_derived_tile_cache.py tests/test_urban_outline_from_buildings.py tests/test_import_overture_buildings.py tests/test_urban_debug_layer.py tests/test_terrain_horizon_ui.py tests/test_cli_cloud_missing_tint_opacity.py`
  Output:
  ```text
  45 passed in 1.63s
  ```
  Result: The Overture-only urban-outline path and renamed helper modules passed targeted regression tests.
