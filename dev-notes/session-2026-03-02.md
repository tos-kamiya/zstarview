# Session 2026-03-02
- Scope: Star size baseline change to 2nd magnitude + bright-star diamond overlay

- Topic: 2nd-magnitude baseline for `star_base_radius`
  - Decision: Reinterpret `star_base_radius` as the square size baseline for 2nd-magnitude stars, and change defaults from 6.0 to 4.0 in CLI/UI.
  - Rationale: Reduce visual dominance of bright stars in default settings while preserving a simple single-parameter control.
  - Alternatives Considered: Keeping 1st-magnitude baseline with only a lower default was rejected because the parameter semantics remained unintuitive.

- Topic: Bright-star visual accent using rotated-square overlay
  - Decision: For stars with `vmag < 2.0`, add a 45-degree rotated square (diamond) overlay on the numpy canvas.
  - Rationale: Preserve compact square rendering for most stars and add a clear bright-star cue without reintroducing expensive QPainter per-star gradient drawing.
  - Alternatives Considered: Returning to radial gradients/spikes was rejected due to complexity and performance cost.

- 00:00 Ran: `uv run -p .venv/bin/python pytest tests/test_star_draw_lod.py tests/test_theme_parse.py`
  Output:
  ```text
  collected 4 items
  4 passed
  ```
  Result: Existing related tests passed after draw-path changes.

- 00:00 Ran: `uv run -p .venv/bin/python ruff check src/zstarview/render/draw.py src/zstarview/ui/window.py`
  Output:
  ```text
  All checks passed!
  ```
  Result: Modified rendering/window modules pass lint checks.

- Summary:
  - Commits: none (working tree changes only)
  - Tags: none
  - Pushes: none
  - Follow-ups:
    - Visual tuning candidate: adjust `_DIAMOND_OVERLAY_GAIN` if bright stars still appear too strong/weak.
