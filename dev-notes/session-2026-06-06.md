# Session 2026-06-06

- Scope: Optimize cloud image rendering by reusing shell-independent projection work within a single render.
- Notes:
  - Keep the existing cache keys and external API stable.
  - Add an internal projection context so each shell only recomputes the shell-dependent intersection step.
- Outcome:
  - Added `ProjectionContext` and split projection into context build + shell-specific intersection.
  - Updated cloud rendering to reuse the projection context across shells without changing cache keys.
  - Verified with targeted pytest and ruff checks.

- Topic: Specification structure cleanup
  - Decision: Replaced `docs/specification.md` with the refactored version by renaming the draft file into place and keeping the canonical path unchanged.
  - Rationale: The current specification repeats location, time, and overlay behavior across multiple sections, which weakens cohesion and makes updates easy to miss.
  - Next step: Compare the updated specification against `docs/design.md` and decide whether to do the same kind of refactor there, or whether to keep the existing design doc as-is for now.

- Topic: Design doc split and app model
  - Decision: Split `docs/design.md` into a directory-based set under `docs/design/`, kept `docs/design.md` as the index, and rewrote the overview to make `zstarview`, `zstarview-gui`, and `zstarview-export-image` first-class documented entry points.
  - Rationale: The monolithic design document had grown too broad for one file, and the product now behaves as three related applications with different startup and persistence semantics.
  - Next step: Keep the detailed subdocs aligned as implementation evolves, and update any references that still assume a single undivided design document.

- Topic: Data model separation
  - Decision: Added `docs/design/data-model.md` as an independent document and narrowed `docs/design/data-and-rendering.md` to rendering responsibilities.
  - Rationale: The shared state model has enough structure to justify its own document, and separating it from the renderer makes the design easier to navigate and maintain.
  - Next step: Keep `ViewerData`, `SkyWindowState`, and each overlay-specific state object documented in the data-model doc, while preserving the renderer as a separate concern.

- Topic: Runtime cleanup and rendering rename
  - Decision: Moved the remaining state-model summary out of `docs/design/runtime.md`, renamed the rendering document to `docs/design/rendering-pipeline.md`, and kept the runtime doc focused on threading, flow, errors, and cache policy.
  - Rationale: `rendering-pipeline.md` is clearer than `rendering.md` for a shared base-scene/HUD pipeline, and the runtime document reads better when it does not duplicate the state model.
  - Next step: Keep the data-model, runtime, and rendering-pipeline documents aligned as a three-part split, and update any docs that still imply a single combined design chapter.
