# Session 2026-06-06

- Topic: Revert satellite worker separation and prepare GUI worker pool update
  - Decision: Restore the satellite controller to use the shared GUI worker pool again, then change the pool size from 1 to 2 in a separate follow-up commit.
  - Rationale: The user wanted the intermediate separation backed out first so the tree could be committed in that state, then adjust concurrency by widening the shared pool instead of keeping a dedicated satellite executor.
  - Validation: Ran focused lint and tests for `src/zstarview/gui/satellite_controller.py` and `tests/satellites/test_satellite_controller.py`; both passed after the revert cleanup.

- Topic: Increase shared GUI worker pool size
  - Decision: Raise `src/zstarview/gui/worker_pool.py` from 1 to 2 workers and update the dedicated worker-pool test to expect two concurrent tasks.
  - Rationale: This keeps the shared pool model intact while reducing blocking between long-running background tasks such as Himawari fetching and satellite updates.
  - Validation: Ran `ruff check` for the worker-pool files and `pytest tests/test_worker_pool.py`; both passed.

- Topic: Align JPL search expectations and clean unused import
  - Decision: Update the window render sync test to expect the new `sct` JPL lookup pass before asteroid searches, and remove the unused import from `src/zstarview/render/sky_disc.py`.
  - Rationale: The added spacecraft search group changes the lookup order, and the render helper should not carry dead imports.
  - Validation: Ran `pytest tests/test_window_render_sync.py` and `ruff check src/zstarview/render/sky_disc.py tests/test_window_render_sync.py`; both passed.
