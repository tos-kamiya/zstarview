# Session 2026-06-06

- Scope: Optimize cloud image rendering by reusing shell-independent projection work within a single render.
- Notes:
  - Keep the existing cache keys and external API stable.
  - Add an internal projection context so each shell only recomputes the shell-dependent intersection step.
- Outcome:
  - Added `ProjectionContext` and split projection into context build + shell-specific intersection.
  - Updated cloud rendering to reuse the projection context across shells without changing cache keys.
  - Verified with targeted pytest and ruff checks.

- Topic: Specification structure cleanup
  - Decision: Replaced `docs/specification.md` with the refactored version by renaming the draft file into place and keeping the canonical path unchanged.
  - Rationale: The current specification repeats location, time, and overlay behavior across multiple sections, which weakens cohesion and makes updates easy to miss.
  - Next step: Compare the updated specification against `docs/design.md` and decide whether to do the same kind of refactor there, or whether to keep the existing design doc as-is for now.
