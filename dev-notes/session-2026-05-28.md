# Session 2026-05-28

- Scope: investigate urban outline performance and benchmark setup for `zstarview`.
- Notes:
  - Benchmark command provided by user: `uv run -p .venv/bin/python zstarview "@41.40410, 2.17384" -Zse -A5 --satellite-opacity 0 --water-surface-opacity 0 -a 0 -c 0`
  - Decision: document a score-based urban-outline decimation rule in spec/design, using outline-level width and apparent height, with circular azimuth span handling for 0/360 wraparound.
  - Ran: `uv run -p .venv/bin/python pytest tests/test_urban_outline_from_buildings.py`
    Output:
    ```text
    collected 13 items
    tests/test_urban_outline_from_buildings.py ............. [100%]
    ```
    Result: verified the new outline scoring and top-N limiting helpers.
  - Decision: replace the final outline truncation with a pre-sampling candidate heap so the expensive ring sampling step is applied only to the top-scoring rings.
    - Rationale: truncating after `sample_ring_points_xy()` does not reduce runtime enough; candidate pruning must happen before ring sampling to cut the hot path.
  - Decision: add `--urban-outline-max-candidates` as the tuning knob for the new pruning path, and keep `-b/--urban-outline-min-building-height-m` only as a deprecated compatibility filter.
    - Rationale: the new option controls the actual performance-sensitive candidate cap, while `-b` still has a semantic role but is not the main lever for speed.
  - Decision: show urban-outline item counts in the bottom status line when outlines are available.
    - Rationale: the state already carries the rendered polyline list, so the count can be shown without adding another cache or counter field. Matching the water overlay style makes the status line more informative.
  - Verification:
    - `uv run -p .venv/bin/python pytest tests/test_cli_export_image_args.py tests/test_cli_visibility_boost.py tests/test_urban_outline_from_buildings.py tests/test_export_image_layer_gating.py -q`
    - `uv run -p .venv/bin/python pytest tests/test_window_cloud_status.py -q`
    - `uv run -p .venv/bin/python ruff check src/zstarview/cli/args.py src/zstarview/cli/export_image.py src/zstarview/data/urban_outline_from_buildings.py src/zstarview/urban_outline_layer.py src/zstarview/gui/window_inputs.py src/zstarview/gui/window.py src/zstarview/gui/viewer.py src/zstarview/gui/urban_outline_controller.py tests/test_cli_export_image_args.py tests/test_cli_visibility_boost.py tests/test_urban_outline_from_buildings.py tests/test_export_image_layer_gating.py`
    - `uv run -p .venv/bin/python ruff check src/zstarview/gui/window_updates.py tests/test_window_cloud_status.py src/zstarview/gui/urban_outline_state.py`
  - Topic: Show pre-merge urban-outline counts in the status bar
    - Decision: Keep the rendered outline list merged for drawing, but store separate base and skyscraper outline counts in GUI state and display them as `base+skyscraper` in the bottom status line.
    - Rationale: The user wants the pre-merge number, and double-counting skyscraper outlines is acceptable. Surfacing the split counts is more informative than the merged total while keeping the rendering path unchanged.
    - Verification:
      - `uv run -p .venv/bin/python pytest tests/test_urban_outline_controller.py tests/test_window_cloud_status.py -q`
      - `uv run -p .venv/bin/python ruff check src/zstarview/gui/urban_outline_state.py src/zstarview/gui/urban_outline_controller.py src/zstarview/gui/window_updates.py tests/test_urban_outline_controller.py tests/test_window_cloud_status.py`
