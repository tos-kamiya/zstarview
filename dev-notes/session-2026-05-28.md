# Session 2026-05-28

- Scope: Repair failing tests in `zstarview`; determine whether any product-code fix is required before editing implementation files.

- Topic: Startup dialog initialization failure
  - Decision: `StartupDialog` currently raises `KeyError: 'geo_satellite'` while building overlay specs because the overlay section map does not include that field.
  - Rationale: This blocks dialog construction itself, so the failure cannot be resolved by test-only changes.
  - Status: Awaiting explicit approval before changing product code.

- Summary:
  - Removed `geo_satellite` from the startup dialog overlay specs so GUI startup no longer exposes the CLI-only feature.
  - Updated GUI test stubs to include the current `geosatellite_state` and `_geo_satellite_enabled` attributes expected by the window code.
  - Verification: `uv run -p .venv/bin/python -m pytest -q` passed with `983 passed`.
