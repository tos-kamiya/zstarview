# Session 2026-05-28

Scope: Fix GUI launch profile application so explicit CLI overlay values are not overwritten.

- Topic: `-a0` still showing aircraft in GUI launch mode
  - Decision: Apply the saved GUI launch profile only to arguments that are still at their parser defaults, and keep explicit CLI values intact.
  - Rationale: The GUI launcher currently loads a saved profile after parsing CLI args, so profile values can override explicit options such as `-a0`. Preserving non-default CLI values fixes the reported regression without changing the profile storage format.
  - Notes: Add a regression test for aircraft opacity and keep structured city profile payloads from overwriting explicit location CLI args.

- Summary
  - Implemented the profile-merge guard in `src/zstarview/gui/viewer.py`.
  - Fixed `src/zstarview/gui/window.py` so explicit `0.0` aircraft/satellite opacities are preserved across startup delta reconciliation.
  - Fixed `src/zstarview/gui/window.py` so explicit `0.0` cloud opacity is preserved across startup delta reconciliation, including when Geo-satellite is enabled.
  - Updated `docs/specification.md`, `docs/design.md`, `docs/cli-overlays.md`, and `docs/cli-overlays-ja_JP.md` to document that `-c 0` stays disabled for the session even with `--geo-satellite true`.
  - Added regression coverage in `tests/gui/test_startup_profile_application.py` and `tests/test_aircraft_startup_gating.py`.
  - Added regression coverage in `tests/test_cloud_startup_gating.py`.
  - Verified with `uv run -p .venv/bin/python pytest tests/gui/test_startup_profile_application.py -q`, `uv run -p .venv/bin/python pytest tests/test_aircraft_startup_gating.py tests/test_cloud_startup_gating.py tests/gui/test_startup_profile_application.py tests/test_viewer_startup_errors.py -q`, and `uv run -p .venv/bin/python ruff check src/zstarview/gui/window.py tests/test_aircraft_startup_gating.py tests/test_cloud_startup_gating.py tests/gui/test_startup_profile_application.py tests/test_viewer_startup_errors.py`.
