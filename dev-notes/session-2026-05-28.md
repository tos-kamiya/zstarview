# Session 2026-05-28

Scope: Evaluate whether `97493e19` can be made HEAD and note history-rewrite risks.

- Topic: Make `97493e19` the branch HEAD
  - Decision: Completed by moving `dev` to `97493e19`.
  - Rationale: The branch history was rewritten with `git reset --hard` after creating a backup branch to preserve the previous tip.
  - Notes: Backup branch created at `backup-dev-before-reset-20260528`. The worktree still contains untracked files that were not removed.

- Topic: Geo-satellite GUI design direction
  - Decision: Keep Geo-satellite as a dedicated GUI controller/state pair rather than folding it into the existing cloud controller, and expose it through the GUI startup/runtime toggle path.
  - Rationale: The Geo-satellite path has different lifecycle states and logging than GOES/Himawari, so a separate controller keeps the standard cloud controller simpler while still allowing the GUI to reuse the shared cloud rendering/compositing layer.
  - Notes: Updated `docs/design.md` to reflect the GUI integration plan, the separate controller/state split, and the short Geo-sat status-line wording.
