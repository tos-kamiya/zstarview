# Session 2026-05-15

- Scope: README note for Overpass water-surface download volume and cache behavior.

- Topic: README water-overlay usage note
  - Decision: Document that coastal or elevated viewpoints can cause a single Overpass fetch to return multi-megabyte water-overlay data, that the raw footprint is cached per viewpoint, and that frequent viewpoint changes can accumulate download volume toward the public Overpass instance's rough 1 GB/day safety guideline.
  - Rationale: The water overlay is the one runtime feature most likely to expose users to Overpass's fairness controls. The README should warn about the practical download cost without describing the public-instance guidance as a hard limit.

- Topic: README/specification structure for water-overlay note
  - Decision: Keep the README feature bullet short and link it to a dedicated "About Water Surface" subsection placed after the CLI option table, while mirroring the same warning in `docs/specification.md`.
  - Rationale: The feature list stays scannable, the operational note gets a stable anchor, and the specification keeps the behavioral warning close to the cache/Overpass description without mixing it into the top-level feature summary.

- Topic: Water-overlay subsection placement
  - Decision: Move `About Water Surface` directly under `--water-surface-opacity` and leave the other `About` subsections in place.
  - Rationale: The water warning is the only one that benefits from sitting immediately under its option row. The existing view-center, magnitude-limit, theme, and datetime notes already sit close enough to their respective content and do not need reordering.

- Topic: CLI reference split into docs files
  - Decision: Move the README CLI tables into dedicated Markdown files under `docs/`, and replace the README tables with short links to those files.
  - Rationale: The tables are too wide and too interleaved with prose for maintainable README editing. Separating them by topic keeps the README scannable while preserving the longer option details in focused docs pages.
  - Summary:
    - Added `docs/cli-argument.md`, `docs/cli-observing-location-and-time.md`, `docs/cli-search-objects.md`, `docs/cli-viewpoint-dataset-queries.md`, `docs/cli-sky-and-stars.md`, `docs/cli-overlays.md`, and `docs/cli-general.md`.
    - Replaced the CLI tables in `README.md` with a link list to those docs files.

- Topic: Move remaining CLI prose out of README
  - Decision: Relocate the remaining CLI prose blocks from `README.md` into the matching docs files, including `--place`, direct coordinates, tower/mountain input, datetime, overlay visibility, view-center, magnitude, theme, and supported asterisms.
  - Rationale: Once the option tables were split, keeping the explanatory prose in README would leave the document split-brained. Moving the prose into the same subdocuments keeps each topic self-contained and avoids duplicate maintenance.

- Topic: Keep CLI argument summary in README
  - Decision: Restore the short `Argument` table in `README.md` and keep the docs links focused on option groups.
  - Rationale: The positional argument summary is short enough to fit comfortably in README and helps readers orient before jumping into the longer option docs. The detailed group links still provide the maintainable split for the longer tables.

- Topic: Japanese CLI reference split
  - Decision: Apply the same topic-based split to `README_ja-JP.md`, keep the short argument table in the README, and move the detailed option groups into Japanese `docs/` files with a dedicated water-surface subsection.
  - Rationale: The Japanese README should match the English structure so both versions stay maintainable. Keeping the argument summary inline preserves the quick orientation step, while the detailed tables, examples, and the Overpass water warning live in focused topic pages with stable anchors.
  - Summary:
    - Added `docs/cli-observing-location-and-time-ja_JP.md`, `docs/cli-search-objects-ja_JP.md`, `docs/cli-viewpoint-dataset-queries-ja_JP.md`, `docs/cli-sky-and-stars-ja_JP.md`, `docs/cli-overlays-ja_JP.md`, and `docs/cli-general-ja_JP.md`.
    - Replaced the Japanese CLI prose block in `README_ja-JP.md` with link bullets to those docs files.
    - Added the Japanese water-surface note under `#### 水面について` and linked it from the README feature list.

- Topic: Japanese search-object wording
  - Decision: Translate the Japanese-facing `Search Objects...` label to `対象検索...` across the Japanese README and Japanese-facing docs/history notes.
  - Rationale: `対象検索` reads naturally in Japanese and keeps the GUI label, README, specification, design notes, and implementation history aligned. The English documentation and code-facing identifiers remain unchanged where they are still the canonical label.
  - Summary:
    - Updated `README_ja-JP.md`, `docs/cli-search-objects-ja_JP.md`, `docs/specification.md`, `docs/design.md`, and `docs/implementation-history.md`.

- Topic: Japanese viewpoint reference wording
  - Decision: Rename the Japanese-facing viewpoint reference section to `観測地点名参照ツール` and place it directly after `観測地点と時刻` in the README link list.
  - Rationale: The new name is closer to the function the user sees in the Japanese README, and grouping it next to the location/time topic makes the CLI reference flow more natural for readers.
  - Summary:
    - Updated the README link order and the heading in `docs/cli-viewpoint-dataset-queries-ja_JP.md`.

- Topic: Water mesh triangulation backend swap
  - Decision: Remove `shapely` from the core dependency set, add `mapbox-earcut` as the triangulation backend, and restore the water-surface mesh helper and dev-sample triangulation path on top of it.
  - Rationale: `mapbox-earcut` has prebuilt wheels on the target platforms, including Windows ARM64, so it avoids the source-build failure that blocked `pipx install`. The water mesh path only needs polygon triangulation, so the smaller dependency is sufficient for the same role.
  - Summary:
    - Added `src/zstarview/water_surface_mesh.py` and `tests/test_water_surface_mesh.py`.
    - Updated `dev-samples/overpass_water_overlay_probe.py` to triangulate via the shared mesh helper instead of `shapely`.

- Topic: Dev-sample grid-split prototype for thin water polygons
  - Decision: Add an optional `--triangulation-cell-m` mode to `dev-samples/overpass_water_overlay_probe.py` that clips each polygon against a local meter grid before passing the clipped pieces into the shared earcut-based mesh helper.
  - Rationale: The sample needs a way to probe whether grid-based subdivision reduces thin triangles without changing production code yet. Keeping the prototype in `dev-samples/` makes the experiment easy to turn on and off, and the existing shared mesh helper still owns the final triangulation step.
  - Validation:
    - `uv run -p .venv/bin/python ruff check dev-samples/overpass_water_overlay_probe.py`
    - `.venv/bin/python -m py_compile dev-samples/overpass_water_overlay_probe.py`
    - `.venv/bin/python -c "import sys; sys.path.append('dev-samples'); import overpass_water_overlay_probe as p; ..."`

- Topic: Dev-sample default grid size
  - Decision: Set the default `--triangulation-cell-m` value to `300.0` in the sample.
  - Rationale: Manual testing showed that 300m was sufficient to tame the thin polygon cases while keeping the output readable. Making it the default reduces friction when rerunning the experiment.

- Topic: Core water mesh grid refinement
  - Decision: Add a `split_cell_m` refinement step to `build_water_surface_mesh()` that clips earcut triangles to a meter grid and re-triangulates the clipped pieces.
  - Rationale: The core package stays free of `shapely`, but the mesh still benefits from cell-based subdivision when thin triangles show up. Doing the split after earcut keeps the implementation pure Python and local to the existing mesh helper, while preserving the original polygon area in tests.

- Topic: Fixed 300m split cell size
  - Decision: Hard-code the grid refinement cell size to `300.0` meters in the core mesh helper and remove the CLI knob from the sample.
  - Rationale: Manual testing showed that 300m was a good default, so exposing the value as a runtime option just adds surface area without practical benefit. Fixing it in the core keeps behavior consistent across callers and avoids carrying a sample-only control path.

- Topic: Restore pre-triangulation cell clipping
  - Decision: Move the 300m grid split back in front of earcut by clipping each shell and hole ring to grid cells before triangulation.
  - Rationale: Post-triangulation splitting changed the visual pattern. Clipping the rings first keeps the output closer to the original sample behavior while still avoiding a shapely dependency in the core package.
