# Session 2026-07-02

- Topic: Check historical presence of overturemaps UTF-8 handling and exe installer CLI
  - Decision: Investigation only; no implementation changes made.
  - Rationale: User asked to confirm whether the old CLI existed in history before reintroducing it.
  - Result: Confirmed `fix: force UTF-8 for overturemaps` in `d10c7314`. Found historical `zstarview-import-overture-buildings` console script in `47687d31` and release-note/archive references, but no git evidence for the exact `zstarview-install-overturemaps-exe-cli` name or any `install-overturemaps-exe` variant.
- Topic: Draft restore spec for Overture exe staging helper
  - Decision: Define `zstarview-install-overturemaps-exe-cli` as a copy-only staging helper that normalizes any downloaded Windows release asset to `CACHE_PATH/overturemaps.exe`.
  - Rationale: The current runtime path already accepts a concrete `--overturemaps-bin` path, so the helper should stay narrow and only solve local asset placement. Keeping the destination fixed makes versioned release asset names irrelevant and preserves a stable lookup target for future integration.
  - Notes: Documented that the helper does not fetch from the network, does not unpack the asset, and may overwrite an existing staged executable.
- Topic: README install guidance for Windows Arm64
  - Decision: Add a dedicated Arm64 Windows installation subsection that instructs users to install `zstarview` with `pipx`, download the x64 `overturemaps` release asset, and stage it with `zstarview-install-overturemaps-exe-cli.exe`.
  - Rationale: The helper exists specifically to bridge the Windows Arm64 gap while keeping the normal non-Arm64 `overturemaps` install guidance intact.
- Topic: OvertureMaps minimum version
  - Decision: State that the staged or separately installed `overturemaps` executable must be version 1.0.1 or newer.
  - Rationale: The Windows release flow and the helper both depend on the binary release packaging introduced in the 1.0.1 line, so the install guidance should make that floor explicit.
- Topic: README install order for Arm64 guidance
  - Decision: Move the Arm64-specific subsection to the end of the Installation block, after the normal install and first-run checks.
  - Rationale: Keeping the common `pipx` flow and first-run verification first prevents the Arm64 note from being read as if it applies to the entire installation sequence.
- Topic: README troubleshooting note placement
  - Decision: Move the troubleshooting note ahead of the Arm64 subsection and broaden its wording so it applies to general install issues instead of X11-only problems.
  - Rationale: The note is not Arm64-specific, so placing it before the Arm64 branch avoids implying that the following subsection owns the troubleshooting guidance.
- Topic: Arm64 Windows rationale
  - Decision: Add a short explanation that the Arm64 flow exists because one or more `overturemaps-py` dependencies do not currently ship Arm64 Windows wheels.
  - Rationale: The extra sentence makes the platform-specific staging step look like a compatibility workaround rather than an arbitrary alternative install path, while keeping the wording accurate about where the wheel gap lives.
- Topic: README translations
  - Decision: Mirror the Arm64 install guidance and the dependency-wheel rationale in `README_ja-JP.md` and `README_pypi.md`.
  - Rationale: The same install constraint should be visible in every user-facing install document so the Windows Arm64 path is not lost outside the main README.
- Topic: PyPI README link
  - Decision: Replace the long Arm64 note in `README_pypi.md` with a short pointer to the main README's Windows on Arm64 section.
  - Rationale: PyPI-facing copy should stay concise and avoid duplicating the full workaround, while still giving users a single canonical place for the detailed steps.
