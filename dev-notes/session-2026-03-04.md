# Session Log - 2026-03-04
- Scope: Review rendering pipeline refactor (cloud/sky/stars/planets) for dead code and duplication.

- Topic: Rendering pipeline refactor review (cloud/sky/stars/planets)
  - Decision: Keep runtime behavior unchanged and report dead/duplicate code candidates before any removal.
  - Rationale: Several symbols are currently test-only or compatibility stubs; pruning needs explicit scope confirmation to avoid breaking external call sites.
  - Alternatives Considered: Immediate cleanup commit was deferred because the request was review-only.

- {Time 00:00} Ran: `git log --oneline --decorate -n 20`
  Output:
  ```text
  978050b refactor: document sky layers and helpers
  ...
  ```
  Result: Located recent rendering refactor commit for review focus.

- {Time 00:00} Ran: `rg -n "..." src tests`
  Output:
  ```text
  src/zstarview/render/draw.py:129:def effective_star_draw_vmag_limit(...)
  tests/test_star_draw_lod.py:...
  src/zstarview/ui/window.py:76:def _initial_height_ratio_for_altitude(...)
  ```
  Result: Identified multiple test-only or unreferenced helpers.

- {Time 00:00} Ran: `.venv/bin/pytest -q`
  Output:
  ```text
  Fatal Python error: Aborted
  ... tests/test_planet_marker_style.py line 150 during collection
  ```
  Result: Full test run was not possible in current environment (Qt-related abort during collection).

## End Summary
- Commits: none (review-only)
- Tags/Pushes: none
- Follow-ups: optional cleanup PR to remove dead helpers and deduplicate sky-color model implementation.

- Topic: Cleanup pass after rendering pipeline review
  - Decision: Remove dead helpers and stale payload fields; keep only vectorized sky-color implementation in runtime path.
  - Rationale: Reduces drift risk and maintenance overhead without changing rendering behavior.
  - Alternatives Considered: Kept legacy compatibility stubs was rejected to avoid long-term divergence.

- {Time 00:00} Ran: `QT_QPA_PLATFORM=offscreen .venv/bin/pytest -q tests/test_sky_disc_viewport_and_ground_tint.py tests/test_cloud_hatch.py tests/test_planet_brightness.py`
  Output:
  ```text
  15 passed in 1.27s
  ```
  Result: Cleanup changes around sky disc/cloud hatch/planet brightness remained green.

## End Summary (updated)
- Commits: none yet
- Tags/Pushes: none
- Follow-ups:
  1. Run full test suite in a GUI-capable CI/runtime to validate PySide collection path.
  2. Optionally clean up legacy helper scripts under `src/zstarview/data/` if desired.

- {Time 00:00} Ran: `rg -n "test_star_draw_lod|effective_star_draw_vmag_limit" src tests`
  Output:
  ```text
  (no matches)
  ```
  Result: Confirmed removal of dead star draw LOD helper and its test.

- Topic: Cleanup of duplicated sky-color model under `src/zstarview/data/`
  - Decision: Replace standalone script-side sky/sun model code with runtime `draw_sky_disc` helpers.
  - Rationale: Prevent drift between debug/asset scripts and actual rendering output.
  - Alternatives Considered: Keeping script-local formulas was rejected due to repeated divergence risk.

- {Time 00:00} Ran: `PYTHONPATH=src .venv/bin/python src/zstarview/data/generate_sky_gradient.py`
  Output:
  ```text
  Generating sky gradient image...
  Image saved as 'sky_gradient_chart.png'
  ```
  Result: Script works after migration to runtime sky-color helper.

- {Time 00:00} Ran: `PYTHONPATH=src .venv/bin/python src/zstarview/data/sun_color_band.py ...`
  Output:
  ```text
  Saved color band to '/tmp/sun_color_band_test.png'
  ```
  Result: Runtime color-band script works with shared model.

- {Time 00:00} Ran: `PYTHONPATH=src QT_QPA_PLATFORM=offscreen .venv/bin/python src/zstarview/data/test_draw_sky_disc.py ...`
  Output:
  ```text
  Saved: /tmp/sky_test.png, Success: True
  ```
  Result: Sky-disc debug renderer now delegates to runtime renderer successfully.

- Topic: Separate mixed design/worklog document
  - Decision: Keep architecture/spec content in `docs/design.md` and move implementation/work history to `docs/implementation-history.md`.
  - Rationale: Design docs stay stable and readable while change history can grow independently.
  - Alternatives Considered: Keeping both in one file was rejected due to maintenance and readability costs.

- {Time 00:00} Ran: `git show HEAD:docs/design.md > /tmp/design_orig.md` and split by section boundary.
  Output:
  ```text
  docs/design.md and docs/implementation-history.md regenerated from HEAD source.
  ```
  Result: Separated without losing prior content.

- Topic: Reclassify implemented items in design.md
  - Decision: Move implemented "Famous Star Jump" entry out of future-work framing into a dedicated "機能設計（実装済み）" section.
  - Rationale: Keep future section focused on unresolved design work only.
  - Alternatives Considered: Keeping it under future work was rejected because it is already implemented and stable.

- Topic: 2px star batching optimization for large star base sizes
  - Decision: Add a dedicated batched path for `size_px == 2` stars, and skip 2px stars that would clip at window edges.
  - Rationale: As `star_base_radius` increases, the count of 2px stars rises sharply and Python per-star rectangle loops become the bottleneck.
  - Alternatives Considered: Keeping only `size_px==1` batching was retained as baseline but showed weaker scalability for larger base sizes.

- {Time 00:00} Ran: benchmark script comparing `batch_size2_stars=False` vs `True` (2560x1440, vmag<=10)
  Output:
  ```text
  base=8: size1=163616 size2=29709 size3+=4447
  without=344.43ms, with=327.91ms, speedup=4.8%

  base=10: size1=118497 size2=68211 size3+=11089
  without=401.92ms, with=355.43ms, speedup=11.6%
  ```
  Result: Optimization yields measurable speedup, increasing with 2px-star ratio.

- Topic: Finalize 2px-star batching as internal behavior
  - Decision: Remove public toggle and keep 2px batching path always enabled internally.
  - Rationale: Avoid API clutter while preserving the measured performance gain.

- Topic: Patch version bump
  - Decision: Bump package version by +0.0.1 from 0.19.0 to 0.19.1.
  - Rationale: Requested patch-level release increment.

- Topic: Star render downsample policy with visible clamp steps
  - Decision: Add `--star-render-expected-width` and apply star-layer-only downsampling with integer floor of `(width/expected)^0.5` when width exceeds expected.
  - Rationale: Preserve full quality at or below expected width while making clamp behavior explicit as requested.
  - Alternatives Considered: Hard threshold at 2x width was rejected due to abrupt quality drop.

- {Time 00:00} Ran: targeted tests + CLI parse checks
  Output:
  ```text
  20 passed
  --star-render-expected-width 600 -> accepted
  --star-render-expected-width 0 -> argparse error (expected)
  ```
  Result: New option and quantized scaling behavior validated.

- Topic: Adjust star-layer scaling formula after UX feedback
  - Decision: Replace integer-floor downsample-factor model with direct inverse-sqrt surface sizing (`render_w ~= sqrt(width * expected)`).
  - Rationale: Matches requested behavior (e.g., 1200px -> about 600*sqrt(2) internal width), avoiding no-op until 2400px.

- Topic: Quantize star render scaling by 100px bins and shorten CLI option name
  - Decision: Compute star-layer scale from quantized width buckets (`100px`) and rename option to `--expected-render-width` (keep old long name as alias).
  - Rationale: Make quality clamp transitions explicit and easier to perceive, while improving CLI usability.

- Topic: Switch star scaling reference from window width to celestial-disc width
  - Decision: Use `geometry.radius * 2` as scaling reference and quantize rendered disc width in 100px buckets.
  - Rationale: Matches perceived sky area, especially at low altitude where disc occupies less of the window.
  - Alternatives Considered: Window-width-based scaling was rejected as it over-downsampled when disc area was small.

- Topic: Apply stepped quantization to star blit destination size
  - Decision: Quantize star-layer blit target width in 100px bins and center the target rect in the window.
  - Rationale: Make quality transitions visible even when source-side quantization alone appears smooth.

- Topic: Roll back stepped quantization of star blit destination
  - Decision: Revert destination-rect quantization and keep star-layer blit target as full window rect.
  - Rationale: User feedback indicated non-monotonic visual behavior was undesirable.

- Topic: Runtime logging of star render resolution and CLI option cleanup
  - Decision: Log `window WxH` and star-layer `draw WxH` on change, and remove deprecated `--star-render-expected-width` alias.
  - Rationale: Improve observability from terminal logs and avoid carrying obsolete option names.

- Topic: Adjust star-layer quantization granularity and scaling curve
  - Decision: Change disc-width quantization step from 100px to 50px and switch scaling from linear ratio to square-root ratio.
  - Rationale: Match requested clamp granularity and use 1/2-power proportional scaling.

- Topic: Align star scaling formula with expected-width sqrt model
  - Decision: Change rendered disc size model to `expected * sqrt(quantized_disc / expected)`.
  - Rationale: Matches expectation that 2x disc width corresponds to about `expected * sqrt(2)` render width.

- Topic: Remove 50px quantization from star scaling
  - Decision: Drop disc-width quantization and keep continuous sqrt scaling only.
  - Rationale: Visual result became clear enough without stepped buckets.

- Topic: Patch version bump after scaling adjustments
  - Decision: Bump version from 0.19.1 to 0.19.2.
  - Rationale: Requested +0.0.1 increment in a separate commit.

- Topic: Document expected render width option in EN/JA README
  - Decision: Add `--expected-render-width EXPECTED_RENDER_WIDTH` to the options table in both `README.md` and `README_ja-JP.md`.
  - Rationale: Keep user-facing documentation aligned with implemented CLI options.

- Topic: Add short CLI alias for expected render width
  - Decision: Add `-W` as a short alias of `--expected-render-width` and update EN/JA README option tables.
  - Rationale: Improve CLI ergonomics while keeping the long option unchanged.

- {Time 23:59} Ran: `.venv/bin/python` parse smoke for `-W`
  Output:
  ```text
  700
  ```
  Result: Short option `-W` is parsed and mapped to `expected_render_width`.

- Topic: Adjust short option letter for expected render width
  - Decision: Change short option from `-W` to `-w` for `--expected-render-width` and update EN/JA README.
  - Rationale: Match requested lowercase short option style.

- {Time 23:59} Ran: `.venv/bin/python` parse smoke for `-w`
  Output:
  ```text
  700
  ```
  Result: Short option `-w` is accepted and mapped correctly.

- Topic: Add startup window geometry option and persistent geometry save
  - Decision: Add `--window-geometry restore|X,Y,W,H` to control startup position/size, and always save current position/size on close.
  - Rationale: Supports explicit startup geometry and restoration workflow while preserving location/size automatically.
  - Alternatives Considered: Separate restore flag was dropped in favor of one geometry option carrying both modes.

- Topic: Enforce minimum size and on-screen placement for startup geometry
  - Decision: Clamp startup geometry to minimum size and available screen area before `setGeometry`.

- Topic: Align low-resolution star-layer compositing with full-resolution path
  - Decision: Switch low-res star layer blit in `SkyWindow._draw_star_layer` to additive compositing (`CompositionMode_Plus`).
  - Rationale: SourceOver blitting made star pixels opaque over cloud stripes and reference lines; additive compositing matches the full-resolution star rendering impression.
  - Alternatives Considered: Reworking per-pixel alpha in `draw_stars` was deferred because the immediate artifact was caused by the low-res blit composition mode mismatch.

- {Time 07:16} Ran: `python3 -m py_compile src/zstarview/ui/window.py`
  Output:
  ```text
  (no output)
  ```
  Result: Syntax check passed after compositing-mode change.

- Topic: Remove unified `stars.csv` dependency and keep split-only star catalogs
  - Decision: Switch runtime catalog entrypoint to `stars/stars_base.csv`, load split files by threshold, and add `stars_extra_faint.csv` for `Vmag > 10`.
  - Rationale: Avoid duplicated payload where `stars.csv` fully overlaps `stars_base/extra7..10`; keep faint stars separately without bundling a second full catalog.
  - Alternatives Considered: Keeping unified `stars.csv` as the required source was rejected due to package size duplication.

- {Time 08:03} Ran: `.venv/bin/pytest -q tests/test_catalog_split_loading.py`
  Output:
  ```text
  ...                                                                      [100%]
  3 passed in 1.38s
  ```
  Result: Split-loading behavior works for `stars_base.csv` entrypoint, fallback path, and `>10` via `stars_extra_faint.csv`.

- Topic: Remove legacy unified star catalog file and document split-only generation
  - Decision: Delete `src/zstarview/data/stars.csv` and update developer guidance to use split outputs only.
  - Rationale: Runtime no longer depends on unified catalog; keeping it duplicates payload.

- {Time 08:05} Ran: `rm -f src/zstarview/data/stars.csv`
  Output:
  ```text
  (no output)
  ```
  Result: Removed legacy unified catalog file from workspace.

- Topic: DSO catalog generation track started (OpenNGC via pyongc)
  - Decision: Mark DSO catalog work as INPROGRESS in design doc and add a dedicated generator script `src/zstarview/data/dso/generate_dso_catalog.py`.
  - Rationale: Keep runtime dependency surface minimal by generating and bundling CSV, while using `pyongc` only in dev workflow.
  - Alternatives Considered: Runtime-time pyongc loading was rejected to avoid extra runtime dependency and startup/network coupling.

- Topic: Developer workflow for DSO catalog
  - Decision: Add `pyongc` to `dev` optional dependencies and document the generation flow in `docs/developer/dso-catalog-generation.md`.
  - Rationale: Make the DSO data pipeline reproducible and consistent with existing generated-data practice.

- {Time 08:20} Ran: `python3 -m py_compile src/zstarview/data/dso/generate_dso_catalog.py`
  Output:
  ```text
  (no output)
  ```
  Result: New DSO generation script passed syntax check.

- {Time 08:20} Ran: `python3 src/zstarview/data/dso/generate_dso_catalog.py --help`
  Output:
  ```text
  usage: generate_dso_catalog.py [-h] [--include INCLUDE] [--max-vmag MAX_VMAG] [--output OUTPUT]
  ```
  Result: CLI options are available and documented in runtime help text.

- {Time 08:23} Ran: `.venv/bin/python src/zstarview/data/dso/generate_dso_catalog.py --output src/zstarview/data/dso.csv`
  Output:
  ```text
  wrote: src/zstarview/data/dso.csv
  rows: 13972
  ```
  Result: DSO catalog was generated successfully in the venv that has `pyongc`.

- Topic: Narrow DSO scope to galaxies and clusters
  - Decision: Add `--types` filter and set default to `galaxy,open_cluster,globular_cluster`.
  - Rationale: Reduce dataset size and focus the first target scope as requested.

- {Time 08:27} Ran: `.venv/bin/python src/zstarview/data/dso/generate_dso_catalog.py --output src/zstarview/data/dso.csv`
  Output:
  ```text
  wrote: src/zstarview/data/dso.csv
  rows: 11346
  type counts: galaxy/open_cluster/globular_cluster only
  ```
  Result: Regenerated DSO CSV now contains only galaxy/cluster types.

- Topic: Limit DSO default magnitude to 12
  - Decision: Set `generate_dso_catalog.py` default `--max-vmag` to `12.0`.
  - Rationale: Very faint objects (e.g., >=16 mag) are out of scope for current rendering visibility.

- {Time 08:31} Ran: `.venv/bin/python src/zstarview/data/dso/generate_dso_catalog.py --output src/zstarview/data/dso.csv`
  Output:
  ```text
  wrote: src/zstarview/data/dso.csv
  rows: 977
  ```
  Result: Regenerated DSO CSV with defaults now yields only Vmag <= 12.0 objects.

- Topic: Resolve DSO INPROGRESS status in design doc
  - Decision: Move completed DSO catalog-generation scope to "機能設計（実装済み）" and keep only UI/render integration as future work.
  - Rationale: Keep INPROGRESS markers aligned with actual implementation status.

- Topic: Repository overview check from design document
  - Decision: Use `docs/design.md` as the current source of truth for architecture/module boundaries and runtime flow.
  - Rationale: The document was updated on 2026-03-04 and captures module responsibilities, thread/error models, and implemented feature scope in one place.
  - Alternatives Considered: Reading scattered implementation files first was skipped for this request because the user explicitly asked for design-doc-based understanding.

- {Time 07:02} Ran: `sed -n '1,260p' docs/design.md`
  Output:
  ```text
  zstarview 設計書（初版）
  最終更新: 2026-03-04
  ... (設計方針/モジュール構成/処理フロー/スレッドモデル/エラーモデル)
  ```
  Result: Confirmed project architecture and key runtime flows from the latest design doc.
  - Rationale: Prevent unusable tiny windows and avoid off-screen startup placements.

- {Time 23:59} Ran: `QT_QPA_PLATFORM=offscreen .venv/bin/pytest -q tests/test_window_geometry_option.py tests/test_config_window_geometry.py tests/test_startup_city_persistence.py`
  Output:
  ```text
  7 passed
  ```
  Result: New option parsing/config persistence behavior verified; existing city persistence invariant remains valid.

- Topic: Minor version bump
  - Decision: Bump package version from 0.19.2 to 0.20.0.
  - Rationale: Requested +0.1 increment.

- Topic: Increase ground and never-rises tint strength
  - Decision: Double `GROUND_TINT_STRENGTH` (0.2 -> 0.4) and `NEVER_RISES_TINT_STRENGTH` (0.16 -> 0.32).
  - Rationale: Requested stronger visibility for ground fill and non-rising-star region fill relative to sky/cloud layers.

- {Time 23:59} Ran: `QT_QPA_PLATFORM=offscreen .venv/bin/pytest -q tests/test_sky_disc_viewport_and_ground_tint.py tests/test_star_flare_profile.py tests/test_planet_brightness.py`
  Output:
  ```text
  12 passed
  ```
  Result: Tint-strength adjustment validated with related rendering tests.

- Topic: Patch version bump
  - Decision: Bump package version from 0.20.0 to 0.20.1.
  - Rationale: Requested +0.0.1 increment.
