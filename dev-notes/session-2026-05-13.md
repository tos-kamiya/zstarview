# Session 2026-05-13

- Scope: tighten the water-run scan demo so water plotting stays within a roughly 2 km square around the observer.
- Notes: record the design decision and validation steps for the water overlay clipping change.
- Topic: Water overlay extent for the scan demo
  - Decision: Add `--water-extent-km` with a default half-width of 1.0 km, project water polygons into observer-centered local XY space, and clip them to a square window before hit-testing and SVG rendering.
  - Rationale: This keeps the plotted water data bounded to about 2 km across without changing the DEM scan radius or introducing a second coordinate system for rendering.
  - Validation: `python3 -m py_compile dev-samples/water_run_scan_demo.py`
- Topic: Water sampling density for narrow rivers
  - Decision: Reduce the default ray sample spacing from 200 m to 50 m in `dev-samples/water_run_scan_demo.py`.
  - Rationale: Narrow rivers are easy to skip at 200 m spacing, so a tighter default is a better tradeoff for this exploratory demo.
  - Validation: `python3 -m py_compile dev-samples/water_run_scan_demo.py`
- Topic: Distance-aware water sampling
  - Decision: Switch from one fixed ray spacing to two stages: a dense near field and a coarser far field, with `--sample-step-m` kept as a compatibility alias for the far spacing.
  - Rationale: This improves detection of nearby narrow rivers without making the full 32 km scan uniformly expensive.
  - Validation: `python3 -m py_compile dev-samples/water_run_scan_demo.py`
- Topic: argparse alias collision
  - Decision: Keep `--sample-step-m` as a separate legacy alias field and copy it into `sample_step_far_m` after parsing.
  - Rationale: The previous hidden-arg approach overwrote the visible default with `None`, which caused the runtime crash.
  - Validation: `python3 -m py_compile dev-samples/water_run_scan_demo.py`
- Topic: Water points in zstarview
  - Decision: Add a background water overlay controller that fetches Overpass data around the current observer location, samples water polygons into projected point markers, and renders them as always-on blue dots in the terrain layer.
  - Rationale: This keeps the feature experimental and non-interactive, reuses the existing background-update pattern, and avoids introducing a new menu or toggle path.
  - Validation: `python3 -m py_compile src/zstarview/water_overlay.py src/zstarview/render/terrain.py src/zstarview/render/pipeline.py src/zstarview/gui/window_state.py src/zstarview/gui/window_render.py src/zstarview/gui/window_updates.py src/zstarview/gui/window.py src/zstarview/gui/water_overlay_state.py src/zstarview/gui/water_overlay_controller.py`
- Topic: Water overlay near-field refinement
  - Decision: Change the water-point distance schedule to start at 2 m, continue in 2 m increments through 512 m, then continue with a coarser tail out to the 2 km overlay radius; fade point alpha with distance.
  - Rationale: The near field needs to stay dense enough to catch small waterways, while the far field still benefits from coverage but should visually recede instead of competing with closer water marks.
  - Validation: `python3 -m py_compile src/zstarview/water_overlay.py src/zstarview/render/terrain.py src/zstarview/gui/water_overlay_controller.py`
- Topic: Water overlay fetch throttling
  - Decision: Add a small persistent cache under the app cache root for water overlay results, reuse recent successful results briefly, and preserve a failure backoff window across restarts for each observer-location scope key.
  - Rationale: This follows the OpeySky pattern of persisting failure metadata so repeated launches do not immediately hammer the upstream API after a 504 or similar transient error.
  - Validation: `python3 -m py_compile src/zstarview/water_overlay.py src/zstarview/gui/water_overlay_controller.py src/zstarview/gui/water_overlay_cache.py`
- Topic: Water overlay cache retention and retry policy
  - Decision: Keep fetched water-overlay results for 90 days in the app cache, and do not retry Overpass within the same execution when a fetch fails with HTTP/network errors, including 429.
  - Rationale: Long-lived cache reuse reduces repeated upstream pressure across restarts, while single-attempt failures keep the current run from hammering Overpass when it is already unhealthy.
  - Validation: `python3 -m py_compile src/zstarview/water_overlay.py src/zstarview/gui/water_overlay_controller.py src/zstarview/gui/water_overlay_cache.py`
- Topic: Water overlay cache freshness policy
  - Decision: Use a cached water overlay result directly when it is still within the 90-day retention window; otherwise attempt a fresh fetch and fall back to the stale cache if that fetch fails.
  - Rationale: This matches the DEM pattern more closely and avoids unnecessary network calls when a usable cached result already exists.
  - Validation: `python3 -m py_compile src/zstarview/water_overlay.py src/zstarview/gui/water_overlay_controller.py src/zstarview/gui/water_overlay_cache.py`
- Topic: Water overlay ground reference
  - Decision: Use DEM-based ground elevation when terrain horizon data is available, fall back to a zero-height plane when terrain is off or not yet ready, and re-run the water overlay when terrain becomes ready or fails.
  - Rationale: Water points should align with the same ground reference as the visible terrain when that data exists, but avoid fabricating a ground height when terrain is unavailable.
  - Validation: `python3 -m py_compile src/zstarview/gui/terrain_state.py src/zstarview/gui/terrain_controller.py src/zstarview/gui/water_overlay_cache.py src/zstarview/gui/water_overlay_controller.py src/zstarview/gui/window_updates.py src/zstarview/gui/window.py`
- Topic: Water overlay mode switching
  - Decision: Keep both sea-level and DEM-based water point sets in memory and switch between them based on terrain horizon availability, instead of rebuilding the overlay on every toggle.
  - Rationale: Terrain OFF should immediately use the sea-level set, terrain ON should use the DEM-based set once DEM data is ready, and the renderer can switch by swapping the active list without re-fetching Overpass data.
  - Validation: `python3 -m py_compile src/zstarview/gui/water_overlay_state.py src/zstarview/gui/water_overlay_controller.py src/zstarview/gui/window_updates.py src/zstarview/gui/window.py src/zstarview/gui/terrain_state.py src/zstarview/gui/terrain_controller.py`
- Topic: Coastline water polygons and island holes
  - Decision: Add `natural=coastline` to the Overpass query and reconstruct coastline faces with `shapely.polygonize` against the bbox boundary, keeping polygon holes so islands inside sea areas are cut out correctly.
  - Rationale: Bbox-scoped coastline data is available from Overpass, and polygonization lets the overlay keep sea areas while excluding enclosed land masses such as islands without adding a separate toggle or special-case path in the renderer.
  - Validation: `uv run -p .venv/bin/python pytest tests/test_water_overlay.py` and `uv run -p .venv/bin/python -m py_compile src/zstarview/water_overlay.py src/zstarview/gui/water_overlay_controller.py tests/test_water_overlay.py`
- Topic: Water overlay cache refresh from disk
  - Decision: Allow a newer on-disk water-overlay snapshot to replace the controller's in-memory cache for the same scope key.
  - Rationale: A long-running process should pick up an externally refreshed cache file without requiring a restart, which also makes coastline fixes visible when the stored snapshot changes after the process has already loaded an older version.
  - Validation: `uv run -p .venv/bin/python -m py_compile src/zstarview/gui/water_overlay_controller.py`
- Topic: Water overlay cache schema invalidation
  - Decision: Bump the water-overlay cache format version and reject older cache files on load so pre-coastline snapshots are not reused after restart.
  - Rationale: The stale 4-point behavior was coming from a recent-but-old-semantics on-disk cache, not from cross-process memory sharing; versioning the payload makes the coastline-aware behavior take effect without manual cache deletion.
  - Validation: `uv run -p .venv/bin/python pytest tests/test_water_overlay.py tests/test_water_overlay_cache.py` and `uv run -p .venv/bin/python -m py_compile src/zstarview/gui/water_overlay_cache.py tests/test_water_overlay_cache.py`
- Topic: Water overlay opacity control
  - Decision: Expose water overlay opacity through both GUI toggling and CLI startup options, wiring the value through window user options into the render pipeline and status line.
  - Rationale: The water layer now behaves like other overlay layers: it can be enabled/disabled from the menu, started at a chosen opacity from the CLI, and reported in the HUD without adding a separate UI path.
  - Validation: `uv run -p .venv/bin/python -m py_compile src/zstarview/cli/args.py src/zstarview/gui/window_inputs.py src/zstarview/gui/viewer.py src/zstarview/cli/export_image.py src/zstarview/gui/window.py src/zstarview/gui/window_render.py src/zstarview/gui/window_updates.py src/zstarview/render/pipeline.py`
- Topic: Water surface naming
  - Decision: Rename the user-facing water overlay label to "Water Surface" and switch the CLI to `--water-surface-opacity`.
  - Rationale: "Water surface" reads more naturally in the menu and CLI help, and there is no released compatibility surface to preserve yet.
  - Validation: `uv run -p .venv/bin/python -m py_compile src/zstarview/cli/args.py src/zstarview/gui/window.py src/zstarview/gui/window_updates.py src/zstarview/gui/water_overlay_controller.py`
- Topic: Shared ray scan grid for terrain and water
  - Decision: Introduce a shared `RayScanGrid` helper in `terrain/horizon.py` and use it for both terrain horizon sampling and water-surface point projection.
  - Rationale: Water points do not need line connectivity, but they do need to share the same observer-centered azimuth/distance scan structure as the terrain horizon so the two overlays stay geometrically consistent.
  - Validation: pending
- Topic: Water surface observer height
  - Decision: Use the observer's absolute height above the local ground for both sea-level and DEM-based water projections.
  - Rationale: The water-surface point geometry should be anchored to the same observer altitude in both modes; only the target surface height differs between sea-level and terrain-aware variants.
  - Validation: pending
- Topic: Water overlay initial ground reference
  - Decision: When terrain horizon is not ready yet, fall back to the resolved location's known ground elevation instead of a zero-height plane for the initial water overlay projection.
  - Rationale: This avoids a visible jump at startup where the first water points are rendered against a temporary sea-level baseline and then drop once terrain data arrives.
  - Validation: `uv run -p .venv/bin/python pytest tests/test_water_overlay.py tests/test_water_overlay_cache.py` and `uv run -p .venv/bin/python -m py_compile src/zstarview/water_overlay.py src/zstarview/gui/water_overlay_controller.py tests/test_water_overlay.py`
- Topic: Water surface fallback height
  - Decision: Let `sample_water_overlay_points()` accept a fallback water-surface height and use it for water footprints without explicit `ele` / `water_level` tags when no DEM target sampler is active.
  - Rationale: Inland lake water polygons usually lack explicit height tags, so projecting them against sea level before DEM readiness creates a visible startup jump for high-elevation lakes such as Crater Lake.
  - Validation: `uv run -p .venv/bin/python pytest tests/test_water_overlay.py tests/test_water_overlay_cache.py` and `uv run -p .venv/bin/python -m py_compile src/zstarview/water_overlay.py src/zstarview/gui/water_overlay_controller.py tests/test_water_overlay.py`
