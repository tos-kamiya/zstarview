# Session 2026-07-30

Scope: Tune the daytime atmospheric sky color.

- Topic: Correct Overture Maps installation instructions
  - Decision: Update README installation examples from `pipx install overturemaps-py` to `pipxu install -f overturemaps`.
  - Rationale: The prior instructions confused the GitHub repository name with the package and used the wrong installer command.

- Topic: Increase Mie forward anisotropy
  - Decision: Raise `MIE_ANISOTROPY` from `0.76` to `0.81`.
  - Rationale: Test a modest increase in forward-scattered aerosol light near the solar direction.

- Topic: Revert Mie forward anisotropy experiment
  - Decision: Restore `MIE_ANISOTROPY` to `0.76`.
  - Rationale: Return to the previous baseline after observing a possible narrowing of the warm horizon band.

- Topic: Reduce aerosol scale height
  - Decision: Change `AEROSOL_SCALE_HEIGHT_KM` from `1.4` to `1.0`.
  - Rationale: Test whether concentrating aerosols closer to the ground reduces the width and intensity of the warm horizon band.

- Topic: Revert aerosol scale height experiment
  - Decision: Restore `AEROSOL_SCALE_HEIGHT_KM` to `1.4`.
  - Rationale: The `1.0 km` setting made ground-level sunsets too white.

- Topic: Slightly increase the default sky opacity
  - Decision: Change the `--sky-opacity` default from `0.15` to `0.16`.
  - Rationale: Apply the requested small increase while retaining the existing `0.0`-to-`1.0` opacity semantics.

- Topic: Expand README sky-color explanation
  - Decision: Describe the sky disc as a dynamic spherical-atmosphere simulation combining Rayleigh and Mie scattering in both README language versions.
  - Rationale: Make the physical sky-color generation visible to users instead of describing only its display modes.

- Topic: Update README cloud feature description
  - Decision: Describe the current cloud presentation as a round-dot halftone overlay in both README language versions.
  - Rationale: Align the feature summary with the current default `halftone` renderer.

- Topic: Expand the PyPI README opening section
  - Decision: Add concise descriptions of the water-surface and AKARI IR bands overlays near the beginning of `README_pypi.md`.
  - Rationale: Make these two optional features visible before the screenshot and installation sections.

- Topic: Synchronize the PyPI README installation section
  - Decision: Update `README_pypi.md` with the current pipx-first installation, upgrade, coastline, AKARI cache, and urban-outline setup instructions from the English README.
  - Rationale: Keep PyPI users aligned with the current optional-data preparation workflow.

- Topic: Shorten PyPI optional-data installation notes
  - Decision: Keep only commands, the Windows Arm64 caution, and links to the detailed GitHub README instructions for optional coastline, AKARI, and urban-outline data.
  - Rationale: Keep the PyPI package page concise while retaining actionable setup guidance.

- Topic: Compact PyPI optional-data list
  - Decision: Replace the three optional-data subsections with compact bullet items.
  - Rationale: Reduce installation-section length while preserving commands and links to details.

- Topic: Ruff diagnostics after the version update
  - Finding: The environment runs Ruff 0.16.0 and reports 1,624 diagnostics across `src/zstarview` and `tests`; the project has no `[tool.ruff]` configuration.
  - Next step: Pin the supported Ruff range or explicitly define the intended rule set before applying fixes.
- Scope: Change the time-of-day marker sky-color sample altitude from 20 to 0 degrees.

- Topic: Lower time-of-day marker sky sampling altitude
  - Decision: Use altitude 0 degrees in the Sun's azimuth direction when sampling the sky color for the upper-left or lower-left time-of-day marker.
  - Rationale: Sample the sky at the horizon in the solar direction.
  - Validation: Added a regression test for the sampling-altitude constant.

- Topic: Release version 1.41.2
  - Decision: Bump the package version from `1.41.1` to `1.41.2` and document the time-of-day marker sampling change.
  - Rationale: Publish the requested patch-level update after the final 0-degree correction.
  - Validation: Version and release-note changes were reviewed without staging unrelated worktree files.

- Topic: Add display adjustment guidelines to the specification
  - Decision: Document a four-stage layer review order: celestial objects, terrain features, UI annotations, and background layers.
  - Rationale: Keep stars, planets, and the Moon as the visual focus, and make it possible to isolate the last-added layer when an adjustment harms readability.
  - Validation: Added a recovery rule, the guiding phrase "The subjects are celestial objects; the background is atmosphere; the UI guides; the terrain is a reference," and a compact implementation checklist to `docs/specification.md`.

- Topic: Diagnose PLATEAU preparation failure on a separate PC
  - Finding: The 2.80 GiB CityGML ZIP downloaded successfully, but extraction raised `OSError: [Errno 28] No space left on device`.
  - Cause: The CLI places both the downloaded ZIP and the extracted CityGML under Python's default temporary directory. On the reported machine `/tmp` is a 7.5 GiB tmpfs, while the post-failure `df -h` output shows ample space on `/`; cleanup then makes `/tmp` appear empty again.
  - Workaround: Set `TMPDIR` to a directory on the large root filesystem before rerunning the command.

- Topic: Add a PLATEAU temporary-directory option
  - Decision: Add `--temp-dir` to `zstarview-download-plateau-buildings`. When specified, both the downloaded ZIP and extracted CityGML use that directory; the default system temporary-directory behavior remains unchanged.
  - Rationale: Allow large PLATEAU preparations to avoid Debian 13's size-limited `/tmp` without making the normal command more complex.
  - Validation: PLATEAU building and cache tests passed; the new tests verify remote ZIP placement and local extraction cleanup.

- Topic: Guide users after PLATEAU temporary-space failure
  - Decision: Catch `ENOSPC` from PLATEAU preparation and print an ASCII-only retry example using `--temp-dir`.
  - Rationale: Turn the low-level `OSError: [Errno 28]` traceback into an actionable recovery path while preserving other `OSError` failures for diagnosis.
  - Validation: Added a regression test; all 23 PLATEAU building and cache tests passed.

- Topic: Document PLATEAU temporary-space recovery
  - Decision: Add PLATEAU `/tmp` capacity troubleshooting notes and a `--temp-dir` retry example to the English and Japanese README troubleshooting sections.
  - Rationale: Keep the normal installation instructions concise while making the recovery path discoverable after a large CityGML extraction failure.

- Topic: Release version 1.42.0
  - Decision: Bump the package version from `1.41.3` to `1.42.0` and document the PLATEAU temporary-directory recovery changes.
  - Rationale: Treat the new CLI option and actionable failure guidance as a minor user-facing release.

- Topic: Add bundled data version table
  - Decision: Add a separate bundled-data version table immediately before the license table in both README language versions.
  - Rationale: Keep source/version provenance distinct from license and attribution details while recording known DSO, catalog, font, and generated-data snapshots.
