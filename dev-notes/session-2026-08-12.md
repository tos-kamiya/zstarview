# Session 2026-08-12

- Scope: Implement phase one of GMN meteor trails, from published data
  discovery and caching through fixed celestial-coordinate trail generation.

- Topic: GMN daily-file discovery and phase-one boundary
  - Decision: Discover immutable `traj_summary_*_solrange_*.txt` files from the
    official daily directory index, load the nominal UTC dates around the
    requested window, and apply the exact 24-hour filter to record timestamps.
    Keep phase one GUI-independent and return ICRS endpoint pairs with source
    and partial-cache metadata.
  - Rationale: GMN daily summaries cover one degree of solar longitude rather
    than UTC midnight boundaries, so their variable `solrange` filenames cannot
    be derived from a UTC date alone. A celestial trail DTO gives the later GUI
    and export paths one shared, deterministic input.

- Summary: Implemented phase one of GMN meteor trails in a new GUI-independent
  `meteors` package. Added official directory-index discovery, semicolon
  trajectory parsing, six-hour index/recent-file caching, SHA-256 validation,
  stale and partial-file fallback, exact UTC-window filtering, 1000km candidate
  selection, WGS84 ECEF horizon clipping, and event-time Alt/Az to ICRS endpoint
  conversion. Smoke-tested the parser against current and 2018 GMN files.
  Added 14 focused tests; focused Ruff and mypy passed. The full suite passed
  with 1,491 tests and 5 conditional skips using Qt offscreen mode. Coverage
  reporting was unavailable because the active environment lacks `coverage`.

- Scope: Define the user-facing MVP specification for GMN meteor trail
  visualization from `memo.md`.

- Topic: GMN meteor trail MVP semantics
  - Decision: Specify an opt-in, 24-hour observation-history layer using GMN
    daily high-level data. Clip geographically visible trajectories at their
    event time, anchor the visible segment to celestial coordinates, and render
    it as a static line without shower or magnitude styling. Keep opacity fixed
    for the first 18 hours, then fade linearly to zero over the final 6 hours.
  - Rationale: Celestial anchoring preserves the relationship with the star
    field while a strict time window, subdued rendering, and explicit GMN
    wording distinguish observation history from a live meteor simulation. A
    long fixed-opacity interval avoids implying real-time freshness; the final
    linear fade only prevents an abrupt disappearance at the 24-hour boundary.
  - Alternatives Considered: Event-time Alt/Az persistence was rejected for
    the MVP because the trails would drift away from the current star field.
    REST API dependence was rejected because GMN's official data page presents
    the published high-level data set as the stable access route.

- Summary: Added the GMN meteor trail MVP to the functional specification,
  including opt-in behavior, the display-time-relative 24-hour window,
  event-time visibility clipping, celestial anchoring, an 18-hour fixed opacity
  followed by a 6-hour linear fade,
  official daily-file retrieval, caching, partial-data behavior, status text,
  attribution, and explicit initial exclusions. Verified the source fields,
  six-hour update statement, and CC BY 4.0 license against GMN's official data
  page and column documentation. No implementation or tests were changed.

- Scope: Complete the Japanese README feature list for forecast precipitation.
- Scope: Clarify README screenshot descriptions for viewpoint listings, the
  default bright-body mode, and forecast precipitation.

- Topic: Japanese forecast precipitation documentation
  - Decision: Add a forecast precipitation item to the atmospheric and man-made overlays section of `README_ja-JP.md`, matching the behavior and constraints documented in the English README.
  - Rationale: The Japanese feature list omitted the existing optional Open-Meteo forecast layer, including its model-forecast caveat, enablement, consent requirement, and export constraint.

- Summary: Added the missing forecast precipitation description to the Japanese README.

- Topic: Follow up recent-code review findings
  - Decision: Record the Road Lights location key only after a successful worker
    run, so a temporary failure remains retryable at the same location. Align the
    precipitation specification and design with the compact status display and
    the implemented `ceil(log2(1 + rate_mm_h / 2))` streak curve.
  - Rationale: A failed request must not be mistaken for a completed location
    cache. The precipitation implementation changes were intentional, so the
    user-facing specification and internal design should describe the shipped
    behavior. The broader calendar-time cache review remains deferred.
  - Validation: Focused Road Lights, precipitation, and status tests passed
    with 65 tests. The full suite passed with 1,471 tests and 5 conditional
    skips. Focused Ruff and `git diff --check` passed.

- Summary: Made same-location Road Lights retries possible after temporary
  failures, added regression coverage, and aligned precipitation documentation
  with the compact status and extended streak scale. Calendar-time cache
  handling remains a follow-up. Committed the focused changes; no tag or push
  was created.

- Summary: Updated the three requested README screenshot descriptions in the
  English README, Japanese README, and PyPI README. No tests were needed for
  this documentation-only change; `git diff --check` passed.

- Summary: Added common launch examples using `-a` and `-P` to the English and
  Japanese README usage sections.

- Summary: Changed the place example to `-p` and added shell comments
  explaining `-p`, `-a`, and `-P`.

- Summary: Localized the shell comments in the Japanese README examples.

- Summary: Added `-A 5 -Z n` view-center examples immediately after the
  `-p` example in both README usage sections.

- Summary: Bumped the package version from `1.48.2` to `1.48.3` and added the
  corresponding release-notes entry for the README documentation updates.
- Scope: Specify and document a Road Lights candidate-count limit.

- Topic: Align Road Lights performance limiting with urban outlines
  - Decision: Define `--road-light-max-candidates` with a default of `5000`, applied per OSM road `way` before simplification, DEM sampling, and projection. Keep the full fetched geometry in cache, and select candidates by visual-priority scoring rather than retrieval order.
  - Rationale: Road Lights can produce both a continuous stroke and lamp points from one way, so way-level accounting gives a stable and understandable limit while preserving both visual components. Keeping the cache uncapped allows changes in view or limit to reuse existing data.

- Topic: Road Lights status-line contents
  - Decision: Do not show candidate or selected counts in the normal status line, matching urban-outline status behavior. Candidate counts may be logged for diagnostics.
  - Rationale: Status lines should remain short and communicate source/availability rather than internal performance metrics.

- Topic: Road candidate ranking refinement
  - Decision: Rank candidate `way` objects only by their minimum observer distance, with a stable identifier as the deterministic tie-breaker. Do not use angular length, vertex count, highway class, retrieval order, or view direction.
  - Rationale: Road height is treated as constant, and angular length depends on how the source data splits a physical road into ways. Distance-only ranking avoids making that storage detail part of the visual selection policy.

- Summary: Implemented the Road Lights candidate limit. Added `--road-light-max-candidates` with a default of `5000`, selected nearest annulus-intersecting ways using a bounded heap and `way_id` tie-breaker, and applied the selection before simplification, DEM sampling, and projection in both GUI and image export paths. Added CLI and selection tests. Full pytest passed with 1,476 tests and 5 skips; changed-file Ruff passed. Full mypy remains blocked by pre-existing repository errors.

- Summary: Fixed the default Road Lights opacity at `0.12` after visual review and updated the CLI default test accordingly.

- Scope: Keep AKARI IR bands fully visible in simplified view.

- Topic: AKARI opacity in simplified view
  - Decision: Bypass the Sun-altitude fade while either simplified-view state is active and render AKARI IR bands at the configured maximum effective opacity.
  - Rationale: Simplified view is intended to emphasize the celestial layers, and a stable maximum AKARI intensity makes the infrared structure available regardless of daylight or twilight. Normal view retains its existing Sun-altitude fade.

- Summary: Simplified view now renders AKARI IR bands at the configured maximum opacity without the Sun-altitude fade. Added regression coverage and updated the functional specification. The focused tests passed with 2 tests and 23 deselected; changed-source Ruff and `git diff --check` passed.

- Scope: Add a standalone validation probe for the GMN meteor pipeline.

- Topic: Phase-one meteor data validation
  - Decision: Add a GUI-independent development sample that loads a selected UTC 24-hour window, reports selection and clipping counts, writes per-trail JSONL coordinates, verifies the ICRS-to-event-AltAz round trip, and optionally draws a red polar SVG. Support an explicit offline mode so the same cached input can be reproduced without network access.
  - Rationale: Numeric output makes coordinate-transform errors measurable before GUI rendering is introduced, while the SVG provides a quick geometry sanity check. Endpoint dots keep short near-zenith trails visible without altering their coordinates.

- Topic: Preserve GMN cache bytes during hash validation
  - Decision: Read cached trajectory text with newline conversion disabled before checking its SHA-256 digest.
  - Rationale: GMN trajectory files use CRLF line endings. Universal-newline conversion changed the cached text before hashing and incorrectly rejected a valid cache in offline mode.

- Summary: Added `dev-samples/gmn_meteor_trail_probe.py` and usage documentation. A live 2018 GMN sample produced one visible trail, and the subsequent offline run reproduced it from cache with zero reported round-trip error. Added regression tests for the probe and CRLF cache validation.

- Scope: Implement phase two of GMN meteor trails in the GUI.

- Topic: Meteor rendering and refresh behavior
  - Decision: Load only after the `Meteor trails` layer is enabled, draw fixed-ICRS endpoints as thin red lines, keep opacity constant through 18 hours, then fade linearly to zero at 24 hours. Refresh successful windows hourly and retry failures after 10 minutes.
  - Rationale: The layer remains opt-in and does no network work while disabled. Hourly refresh keeps the rolling 24-hour window current without treating historical observations as real-time telemetry.

- Summary: Completed phase two with an opt-in GUI toggle, background controller, status reporting, fixed-ICRS red-line rendering, and the 18-hour hold plus 6-hour fade. Added fade and state tests. Full pytest passed with 1,503 tests and 5 skips; changed-source Ruff, focused mypy, and `git diff --check` passed.

- Topic: Meteor layer placement and CLI startup opacity
  - Decision: Place `Meteor trails` in the celestial section of the Layers menu and add `--meteor-trails-opacity` with a default of `0.0`. A positive value enables startup loading at that opacity; zero disables fetching, rendering, and menu re-enabling for the session.
  - Rationale: Meteor observations are mapped to fixed celestial coordinates, so the celestial group communicates their display semantics. Treating zero as a session-level disable matches the other opacity-controlled layers.

- Topic: Empty meteor status
  - Decision: Show an available but empty meteor result as the compact count `M 0` instead of `No GMN meteor observations`.
  - Rationale: The status row already presents successful meteor results as counts, so zero should use the same compact representation.
