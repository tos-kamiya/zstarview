# Session 2026-03-26

- Scope: Add `--version` to the main `zstarview` CLI and cover it with a regression test.

- Topic: Main CLI version flag
  - Decision: Add `--version` via `argparse`'s built-in `version` action in the shared main parser builder.
  - Rationale: This keeps behavior standard, exits early before normal argument validation, and reuses the package version already defined in `src/zstarview/__about__.py`.

- 16:57 Ran: `uv run -p .venv/bin/python pytest tests/test_cli_arg_helpers.py tests/test_cli_place_options.py`
  Output:
  ```text
  11 passed in 0.49s
  ```
  Result: Targeted CLI parser tests passed after adding `--version`.

- Summary: Added main CLI `--version` support and a regression test for version output.

- Topic: Overlay accent colors for aircraft and satellites
  - Decision: Move the aircraft trail color and satellite marker color out of `render/draw.py` into the respective overlay constants modules.
  - Rationale: This removes magic numbers from the renderer, keeps overlay-specific styling near the rest of each feature's shared constants, and makes later tuning safer.

- 17:03 Ran: `uv run -p .venv/bin/python pytest tests/test_planet_marker_style.py`
  Output:
  ```text
  12 passed in 1.02s
  ```
  Result: Existing overlay marker render tests still pass after replacing inline color literals with named constants.
