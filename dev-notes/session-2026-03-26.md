# Session 2026-03-26

- Scope: Add Mount Everest to the bundled mountain viewpoint dataset so it can be resolved from the CLI.

- Topic: Restore Mount Everest as a bundled mountain viewpoint
  - Decision: Add a `Q513` Mount Everest entry to `src/zstarview/data/viewpoints/mountain_viewpoints.json` using the coordinates, elevation, and labels already present in the local mountain candidate review data.
  - Rationale: CLI mountain-name resolution depends on the bundled dataset, and Mount Everest was missing from that packaged JSON even though the local review artifacts already contained a vetted candidate. A data-only fix keeps the change small and avoids touching stable resolution logic.

- Topic: Verify CLI mountain resolution after the dataset update
  - Decision: Add regression coverage for `Mount Everest`, its alias `Sagarmatha`, and the CLI mountain-name listings.
  - Rationale: The missing entry was a packaged-data regression, so targeted tests ensure the bundled dataset continues to expose Everest through both direct resolution and list-oriented CLI options.

- Topic: Keep Mount Everest coordinates aligned with the bundled dataset policy
  - Decision: Use the Wikidata-derived coordinates `27.988055555,86.925000` for Mount Everest instead of the Google Maps summit pin.
  - Rationale: The mountain dataset is sourced from curated Wikidata-based artifacts. Keeping Everest on that same source policy is more consistent than mixing in a Google Maps-specific summit point, especially because Google Maps appears to distinguish a nearby south-summit-style marker.

- Summary
  - Commits: `fix: add mount everest mountain viewpoint`
  - Tags: none
  - Pushes: none
  - Follow-ups: Consider regenerating the bundled mountain dataset from the curated seed if more missing peaks are reported.
