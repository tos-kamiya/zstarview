# 2026-06-19

- Scope: ridge glow design update for `dev2`, using the main terrain ridge only and keeping night light behavior separate.
- Summary:
  - Commit: `9ac2c79c` (`feat: add independent ridge glow mask`)
  - Verified: `tests/test_glow_mask.py`, selected `tests/test_window_render_sync.py` cases for ridge glow and press-pending gating, `tests/test_cli_arg_helpers.py -k ridge_glow`
  - Follow-up: the full `tests/test_window_render_sync.py` file still has unrelated pre-existing failures outside the ridge glow path.
  - Update: lowered the default `night_light_opacity` to `0.05`; ridge glow remains independent and is not auto-linked to CLI night-light input.
  - Update: moved the ridge-glow distance target from `128 km` down to `20 km` to make the ridge contribution easier to observe in the current scenes.
  - Update: split ridge glow out of the sky/cloud composite and draw it as a separate post-terrain overlay so it is not hidden behind the terrain line layer; aligned its distance and horizon/vertical falloff with the night-light curve shape.
  - Update: added INFO logging for the actual night-light alpha-grid build path so terminal output shows when the cached profile is really computed and how long it took.
  - Update: verified the ridge-glow overlay path and the new logging with targeted pytest runs; the full `tests/test_window_render_sync.py` file still contains unrelated pre-existing failures outside the touched path.
