# 2026-04-16 Session Notes

- Scope: Compare `README.md` Features with `README_pypi.md` Highlights and assess drift.
- Decision: Review-only session; no repository changes yet.
- Notes:
  - `README.md` now includes more specific overlay and behavior details, especially for satellite clouds, terrain, urban outlines, and view-center interaction.
  - `README_pypi.md` Highlights still read as a shorter summary and appear slightly behind the main README.
  - Updated `README_pypi.md` Python support wording to include CPython 3.14, matching the main README.

- Topic: `zstarview` vs `zstarview-export-image`
  - Decision: Treat them as separate entry points with shared lower-level rendering and location-resolution code.
  - Rationale: `zstarview` is the GUI launcher plus dataset-query CLI path, while `zstarview-export-image` is the headless one-shot export path with file/SIXEL output and timeout/partial-data controls.
