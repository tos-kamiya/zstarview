# Session 2026-05-04

- Topic: Resize grip rendering investigation
  - Decision: Keep the custom chrome border behind the menu/grip artwork, and re-raise overlay widgets after resize handling completes.
  - Rationale: The bottom-right grip line could be obscured both by the optional outer border and by resize-time stacking/repaint ordering. Re-raising the overlay widgets after the resize path settles makes the grip visible immediately after a resize.
  - Alternatives Considered: Forcing a synchronous repaint only; rejected because the resize path also needs the overlay widgets restored to the top of the stack.
- Topic: Resize grip debug logging
  - Decision: Add debug logging around frameless chrome layout and client resize handling.
  - Rationale: The grip still appears to disappear after resize in practice, so logging the frame size, client size, grip geometry, visibility, and parent widget should show whether the widget is moving, being hidden, or merely painted over.
- Validation:
  - Ran: `uv run -p .venv/bin/python pytest tests/test_sky_disc_viewport_and_ground_tint.py -q`
  - Result: `26 passed`
  - Ran: `uv run -p .venv/bin/python pytest tests/terrain/test_terrain_horizon_ui.py -q`
  - Result: `45 passed`
- Topic: LOG_LEVEL support
  - Decision: Read LOG_LEVEL at startup and apply it to the root logger and file handler.
  - Rationale: The CLI was already using env LOG_LEVEL syntactically, but the app ignored it and stayed at INFO. Respecting the environment variable lets debug logs surface without changing the command line.
- Validation:
  - Ran: `uv run -p .venv/bin/python pytest tests/terrain/test_terrain_horizon_ui.py -q`
  - Result: `45 passed`
- Topic: Narrow chrome debug logging
  - Decision: Gate the window chrome logs behind ZSTARVIEW_DEBUG_WINDOW_CHROME instead of global DEBUG logging.
  - Rationale: Global DEBUG is too noisy; this keeps the resize-grip investigation focused on the frame/grip state only.
- Validation:
  - Ran: `uv run -p .venv/bin/python pytest tests/terrain/test_terrain_horizon_ui.py -q`
  - Result: `45 passed`
- Topic: Custom resize grip widget
  - Decision: Replace `QSizeGrip` with a small custom widget that paints the resize marker itself and handles drag-to-resize directly.
  - Rationale: The standard grip marker was not rendering reliably on the target setup even though the widget itself was visible. A custom widget removes the Qt style dependency and keeps the hit area and marker under application control.
  - Validation: `uv run -p .venv/bin/python pytest tests/terrain/test_terrain_horizon_ui.py -q` and `uv run -p .venv/bin/python pytest tests/test_sky_disc_viewport_and_ground_tint.py -q`
- Topic: Grip marker cleanup
  - Decision: Remove the old background-drawn grip line and keep only the custom widget marker, with the marker angle adjusted to a true 45-degree slash.
  - Rationale: The old `/` was still visible because it was drawn in the background chrome layer, and the new marker shape was too tall. Removing the duplicate and redrawing the custom marker made the handle clear and consistent.
- Topic: Sky disc default opacity
  - Decision: Lower the CLI sky disc default opacity from 0.17 to 0.12 and route GUI/export fallbacks through a shared constant.
  - Rationale: The requested default change needs to apply consistently across CLI parsing, export-image handling, and the window fallback path. A shared constant prevents these paths from drifting apart again.
- Topic: Sky disc default opacity adjustment
  - Decision: Adjust the shared sky disc default opacity from 0.12 to 0.13.
  - Rationale: The CLI default needed a small follow-up tweak, and keeping it on the shared constant preserves consistency across the CLI, GUI, and export-image paths.
- Topic: Location and viewpoint display
  - Decision: Show lat/lon alongside the resolved location name in the splash screen and static overlay text, and keep location-height labels visible when available.
  - Rationale: The user-facing launch/search context is clearer when the resolved coordinates are visible, and the shared overlay/export summary now reports the same location details consistently.
- Topic: Ground elevation display
  - Decision: Resolve DEM ground elevation for launch locations and show it alongside lat/lon; keep building height or tower/mountain height as a separate line when available.
  - Rationale: The user wanted the base ground height visible, with building height only when applicable. Reusing the same location summary helper keeps the GUI, splash, and export-image text aligned.
- Topic: Viewpoint height semantics
  - Decision: Treat DEM lookup failures as `0m`, treat non-roof/non-tower locations as `Building height 0m`, and compute the displayed observer height as `ground + building + 1.7m`.
  - Rationale: This makes the displayed numbers consistent and easy to interpret across the GUI overlay, splash text, and export-image summary.
  - Validation: `uv run -p .venv/bin/python pytest tests/test_location_display.py tests/test_planet_marker_style.py tests/test_export_image_sixel.py tests/test_export_image_layer_gating.py tests/test_window_render_sync.py tests/location_resolver/test_tower_startup_resolution.py tests/location_resolver/test_nominatim_startup_resolution.py -q`
    and `uv run -p .venv/bin/python ruff check src/zstarview/utils/location_display.py src/zstarview/location_resolver/resolve.py src/zstarview/render/background.py src/zstarview/gui/window_inputs.py src/zstarview/gui/window_render.py src/zstarview/gui/viewer.py src/zstarview/cli/export_image.py tests/test_location_display.py tests/test_planet_marker_style.py tests/test_export_image_sixel.py tests/test_export_image_layer_gating.py tests/test_window_render_sync.py tests/location_resolver/test_tower_startup_resolution.py tests/location_resolver/test_nominatim_startup_resolution.py`
- Topic: Location summary compression
  - Decision: Combine ground elevation and building height into a single line, omit observer height from the public location summary, and suppress duplicate lat/lon display when the resolved name is already the coordinate string.
  - Rationale: The overlay and export summary are easier to scan with one structural-height line, and coordinate launches should not repeat the same lat/lon text twice.
