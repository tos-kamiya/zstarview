# Session 2026-05-10
- Scope: classify docs/images PNGs referenced by README.md
- Topic: README image reference audit
  - Decision: Classified the PNGs under `docs/images/` into referenced and unreferenced sets by matching against `README.md`.
  - Rationale: The README contains both markdown image links and HTML `img` tags, so a direct path-level comparison avoids ambiguity and keeps the result reproducible.
- Topic: README image order update
  - Decision: Reordered the city screenshot tables so `Manarola` appears before `Sagrada Familia`, matching the east-to-west ordering requested by the user.
  - Rationale: The README variants should stay consistent with the main README so readers see the same geographic ordering everywhere.
- Topic: README feature grouping
  - Decision: Reorganized the README feature lists by GUI display-layer groups: sky content and guide overlays, atmospheric and moving overlays, and ground/orientation overlays.
  - Rationale: The feature list is easier to scan when it mirrors the menu structure users see in the app, and it keeps related overlays together across the English and Japanese READMEs.
- Topic: Night lights description
  - Decision: Removed the `--night-light-opacity` detail from the Night lights feature bullets so the description matches the granularity of the other overlay items.
  - Rationale: The feature bullets read more consistently when individual overlay descriptions stay at the same level of detail.
- Topic: Terrain status text
  - Decision: Replaced the GUI terrain status source label with `Dem: cache` instead of exposing the Copernicus DEM dataset name.
  - Rationale: The dataset name and 90m resolution are implementation details; the UI only needs to communicate that terrain data is being served from the cache.
- Topic: Urban status text
  - Decision: Replaced the GUI urban-outline source label with `Urban: cache` so the status line reads as a short `🂓 Urban: cache` indicator.
  - Rationale: The source label does not need to expose Overture/cache internals in the status bar, but it should still make clear that the layer is backed by cached data.
- Topic: README location and view-center bullets
  - Decision: Combined the location-input and view-center bullets into one README item, and shortened the view-center wording.
  - Rationale: Those controls are closely related user-input affordances, so grouping them reduces repetition and keeps the feature list tighter.
- Topic: Observation info placement
  - Decision: Moved the note about location and `alt/az` into the `Flexible location and view center` bullet instead of keeping it as a separate HUD description.
  - Rationale: The HUD note belongs with the controls that change location and view center, so the README is tighter when those details stay together.
- Topic: Celestial overlay heading
  - Decision: Renamed the overlay heading to `Celestial overlays` in English and `天体のオーバレイ` in Japanese.
  - Rationale: `Sky content` was broader than the actual feature set, while `celestial` better matches the astronomy-focused overlays shown in the app.
- Topic: Satellite overlay label
  - Decision: Renamed `Satellites` to `Artificial satellites` in English and tightened the Japanese wording around `人工衛星`.
  - Rationale: The items shown there are not natural satellites, so the more precise term reads better in the feature list.
- Topic: Status line prefix
  - Decision: Removed the leading `> ` from the HUD status line text.
  - Rationale: The prefix was visually noisy and did not add useful information.
