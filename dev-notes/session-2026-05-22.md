# 2026-05-22
Scope: split cloud data fetch from projection to reduce scheduler contention.

- Topic: Cloud update split
  - Decision: Split weather-satellite handling into a source-fetch phase and a separate projection phase, with `SkyWindow` requesting projection independently from fetch.
  - Rationale: Source fetches are now shorter and can finish without waiting for the heavier render path, which reduces contention with faster background updates. Projection can still be queued when idle or after source readiness without mixing the two concerns.
  - Notes: Added `cloud_source_ready` in the controller, introduced cloud projection scheduling state, and kept a compatibility fallback for older test doubles.
- Command transcript
  - Ran: `uv run -p .venv/bin/python pytest tests/clouddisc/test_cloud_controller_queueing.py tests/clouddisc/test_cloud_controller_epoch.py tests/test_window_cloud_partial_mode.py tests/test_scheduler_idle.py tests/terrain/test_terrain_horizon_ui.py -q`
    Output:
    ```text
    74 passed in 1.50s
    ```
  - Ran: `uv run -p .venv/bin/python pytest tests/test_window_render_sync.py tests/test_window_cloud_partial_mode.py -q`
    Output:
    ```text
    92 passed in 1.64s
    ```
  - Ran: `uv run -p .venv/bin/python pytest -q`
    Output:
    ```text
    914 passed in 46.83s
    ```

- Summary
  - Updated `docs/specification.md`, `docs/design.md`, and `docs/implementation-history.md` to reflect the finalized cloud fetch/projection split and the `Downloading` / `Projecting` status behavior.
  - Bumped `src/zstarview/__about__.py` to `1.27.12`.
- Topic: DSO hover color verification
  - Decision: Kept the current implementation unchanged; `draw_dso_hover_info()` already uses `DSO_LABEL_RGB`, while asterism hover/highlight uses `PALETTE_ASTERISM_RGB`.
  - Rationale: The code path is already split, and the existing tests confirm that DSO hover preserves the DSO color in all themes. There is no evidence of an active regression in the repository state.
  - Command transcript:
    - Ran: `uv run -p .venv/bin/python pytest tests/test_deep_sky_objects_render.py -q`
      Output:
      ```text
      2 passed in 0.69s
      ```
    - Ran: `uv run -p .venv/bin/python pytest tests/test_window_render_sync.py -q`
      Output:
      ```text
      90 passed in 1.62s
      ```
- Topic: Temporary DSO color change
  - Decision: Changed `DSO_LABEL_RGB` to a red tone so DSO shapes, hover outlines, and DSO labels all render red for now.
  - Rationale: The DSO color is centralized in one constant, so this is the smallest way to make the temporary visual change consistently across all DSO overlays.
- Topic: DSO color palette alignment
  - Decision: Shifted `DSO_LABEL_RGB` to a warm coral `RGB(240, 146, 122)`, matching the palette's shared `49.2%` saturation and `94.1%` value while keeping the hue in the red-orange range.
  - Rationale: This keeps DSO visually distinct from asterisms while making it fit the existing palette better than the previous stronger red.
- Topic: DSO hue adjustment away from day text
  - Decision: Moved `DSO_LABEL_RGB` further toward red as `RGB(240, 122, 130)` so it no longer sits on top of the `-t day` text color.
  - Rationale: Preserved the palette's bright, saturated look while separating DSO from the day-theme typography, which is slightly more orange.
- Topic: DSO red emphasis
  - Decision: Shifted `DSO_LABEL_RGB` to `RGB(240, 104, 118)` to make the DSO accent read more clearly as red.
  - Rationale: Lowering the green channel gives the marker more red contrast without changing the overall brightness class too much.
- Topic: Restore DSO blue
  - Decision: Reverted `DSO_LABEL_RGB` to the original blue `RGB(110, 185, 255)`.
  - Rationale: The red experiments did not improve visual clarity, so restoring the original blue keeps DSO styling consistent with the earlier design and matches the user's preference for the original look.
- Topic: DSO blue S/V alignment
  - Decision: Adjusted `DSO_LABEL_RGB` to `RGB(122, 183, 240)` so its saturation and value match the shared palette band while keeping the same blue hue family.
  - Rationale: This brings DSO closer to the palette's common HSV pattern used by the other accent colors, reducing the overly bright appearance of the previous blue.
- Topic: DSO hover alpha reduction
  - Decision: Lowered the DSO hover outline alpha from `220/230` to `180/190` depending on theme brightness.
  - Rationale: The outline was still too visually dominant; reducing alpha keeps the hover readable without overpowering the DSO fill or nearby labels.
- Topic: DSO blue emphasis
  - Decision: Shifted `DSO_LABEL_RGB` to `RGB(122, 173, 240)` to make the blue component read more strongly and reduce the whitish look.
  - Rationale: This keeps the color in the same brightness band while nudging it toward a clearer blue impression.
- Summary
  - Commit: bb77cb6
  - Follow-up: none
