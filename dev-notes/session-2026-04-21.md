# Session 2026-04-21

- Scope: Investigate and fix cloud stripe baseline gaps seen in debug renders.

- Topic: Cloud stripe baseline gaps in width mode
  - Decision: Reworked width-mode stripe sampling so cloud amount is read from the stripe baseline projected point only, rather than from positions between baselines.
  - Rationale: The debug frame showed isolated gaps where baseline segments dropped out while later diagonal lines remained. Sampling only the baseline removes the interior-of-band dependence that was letting the renderer skip the baseline in some local regions.
  - Validation: `uv run -p .venv/bin/python pytest tests/clouddisc/test_cloud_hatch.py tests/clouddisc/test_cloud_missing_overlay.py`
  - Validation: `uv run -p .venv/bin/python ruff check src/zstarview/gui/composite.py tests/clouddisc/test_cloud_hatch.py`

- Topic: Remove cloud stripe debug snapshot saving
  - Decision: Deleted the cloud stripe pre-compose debug image save path and its dedicated test.
  - Rationale: The debug-only file dump was no longer needed after the baseline-only sampling change, and removing it keeps the compositor simpler and avoids retaining an extra filesystem side effect.

- Topic: Cloud FOV overscan before clipping
  - Decision: Render cloud stripes with a small fixed FOV margin above the visible content FOV, then let the existing disc clipping remove anything outside the final view.
  - Rationale: The visible sky already needs the edge, but the cloud hatch looked like it stopped too early. Overscanning only the cloud pass preserves the final crop while allowing cloud stripes to extend into the outer ring before clipping.

- Topic: Reduce cloud FOV overscan
  - Decision: Trimmed the cloud render margin from `+20deg` to `+12deg`.
  - Rationale: The larger margin was acceptable but broader than necessary. Reducing it keeps the same edge coverage while limiting how far the cloud pass expands beyond the visible FOV.

- Topic: Raise default content FOV
  - Decision: Updated the default `content_fov_deg` from `100deg` to `115deg` across CLI parsing, GUI runtime defaults, export-image fallbacks, and the viewer data default.
  - Rationale: The user asked for a wider default content field so edge objects and clouds are less likely to drop out near the visible boundary. Keeping the default aligned across entry points avoids behavior drift between GUI, export, and direct viewer construction.

- Topic: Version bump
  - Decision: Bumped `zstarview` version from `1.20.7` to `1.20.8`.
  - Rationale: The moon hover marker-scale fix and the follow-up rendering adjustments are now ready to be captured as the next patch release.

- Topic: Show 90-degree boundary on sky disc
  - Decision: Added a very thin, low-alpha ring on top of the sky-disc layer at the 90-degree projection boundary.
  - Rationale: The user wanted a subtle visual cue for the historical 90-degree extent while keeping the newer overscanned content FOV. Drawing the ring on the sky-disc keeps it aligned with the projection and avoids mixing it into the background layer.
  - Validation: `uv run -p .venv/bin/python pytest tests/test_sky_disc_viewport_and_ground_tint.py`
  - Validation: `uv run -p .venv/bin/python ruff check src/zstarview/render/sky_disc.py tests/test_sky_disc_viewport_and_ground_tint.py`

- Topic: Remove 90-degree ring cue
  - Decision: Removed the experimental sky-disc ring cue after reviewing its visibility against the ground fill.
  - Rationale: The ring did not read cleanly in the lower half of the disc and was not providing a consistently useful cue. Removing it restores the original sky-disc appearance without leaving a confusing partial border.
  - Validation: Pending.

- Topic: Add square client-area resize action
  - Decision: Added a File-menu action named `Square Client Area` that resizes the client widget so its width and height match, and exposed the same action in the frameless root menu.
  - Rationale: The user wanted a window mode where the visible client region becomes square so the 90-degree FOV sits on all four edges. Resizing based on client dimensions keeps the behavior consistent whether the app uses title-bar chrome or the frameless wrapper.
  - Validation: `uv run -p .venv/bin/python pytest tests/terrain/test_terrain_horizon_ui.py -k "build_window_menu or square_client_area"`
  - Validation: `uv run -p .venv/bin/python ruff check src/zstarview/gui/window.py tests/terrain/test_terrain_horizon_ui.py`

- Topic: Scale satellite and search markers with window size
  - Decision: Propagated the star-layer upscale factor into satellite overlay markers and persistent search markers, while keeping each marker's existing base scale as the local multiplier.
  - Rationale: The planet cross marker already grows with the rendered disc size, so the other cross markers should behave consistently at larger window sizes. Multiplying the existing per-object scale by the same window factor preserves relative differences while keeping the overall UI proportional.
  - Validation: `uv run -p .venv/bin/python pytest tests/test_planet_marker_style.py tests/test_window_render_sync.py -k "satellite_overlay or persistent_search_overlay"`
  - Validation: `uv run -p .venv/bin/python ruff check src/zstarview/render/satellites.py src/zstarview/render/search_overlay.py src/zstarview/render/pipeline.py src/zstarview/gui/window_render.py src/zstarview/cli/export_image.py tests/test_planet_marker_style.py tests/test_window_render_sync.py`

- Topic: Raise default V-magnitude limit
  - Decision: Changed the default `-V/--vmag-limit` from `6.0` to `7.0` in the CLI parser, GUI user options, and export-image fallback paths.
  - Rationale: The user wanted the default star cutoff widened by one magnitude step. Keeping the parser defaults and the GUI-side defaults aligned avoids a mismatch between launch paths.
  - Validation: `uv run -p .venv/bin/python pytest tests/test_cli_arg_helpers.py tests/test_cli_content_fov.py tests/test_cli_export_image_args.py`
  - Validation: `uv run -p .venv/bin/python ruff check src/zstarview/cli/args.py src/zstarview/cli/export_image.py src/zstarview/gui/window_inputs.py tests/test_cli_arg_helpers.py`

- Topic: Sync README defaults
  - Decision: Updated the README option table and magnitude-limit note so the documented defaults now match the new `--content-fov-deg 115` and `-V 7.0` behavior.
  - Rationale: The CLI help text needs to reflect the runtime defaults users see, otherwise the README becomes a source of stale configuration guidance.

- Topic: Sync Japanese README defaults
  - Decision: Applied the same default-value updates to `README_ja-JP.md` so the Japanese documentation matches the English README and current CLI behavior.
  - Rationale: Both language variants should advertise the same defaults to avoid confusion when users switch between them.

- Summary: No commits, tags, or pushes were made in this session.
