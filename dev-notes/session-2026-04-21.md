# Session 2026-04-21

- Scope: Confirm whether key overlay line widths scale up with larger windows.
- Notes: Investigated render pipeline and drawing code for asterisms, urban outlines, and Earth guide lines.
- Decision: Keep asterisms and urban outline line widths fixed in the render buffer by passing `line_width_scale=1.0` from the pipeline.
- Rationale: The user clarified that the goal is to avoid resolution-level thickening during downsampled rendering, not to compensate for window enlargement at draw time.
- Verification: Updated the pipeline and the sync test expectations; direct AST parsing succeeded. Pytest could not run in this environment because `astropy` is not installed in the active Python environment.
- Topic: Planet and moon marker scaling
  - Decision: Pass the same render upscale factor used by the star layer into the solar-system drawing path and multiply planet disc radius, moon radius, bloom radius, and gauge cross dimensions by it.
  - Rationale: Planet markers are drawn directly on the final painter, so without this multiplier they do not follow the same low-resolution buffer scaling as stars and lines.
  - Verification: Added tests covering direct solar-system marker scaling and the pipeline handoff; `uv run pytest tests/test_planet_marker_style.py tests/test_window_render_sync.py -q` passed.
