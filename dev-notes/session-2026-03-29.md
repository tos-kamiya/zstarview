# Session 2026-03-29

- Scope: remove duplicated star catalog storage for the GUI/export paths

- Topic: Lighten `white` and `day` theme label text
  - Decision: Shift the `white` and `day` preset text colors from saturated blue toward a paler blue-white, and match the explicitly overridden asterism/DSO label text to that lighter tone.
  - Rationale: The previous colors read too blue against the light themes. A lighter tint keeps contrast and theme identity while reducing the heavy blue cast.
  - Alternatives Considered: Changing all blue accent elements would broaden the visual change unnecessarily; this adjustment stays scoped to text.

- Topic: Switch `white` and `day` text to orange constants
  - Decision: Replace the light-theme text colors with shared orange constants in `paths.py`, and make the explicitly overridden asterism/DSO label text consume the same constant set.
  - Rationale: The lighter blue variant was still hard to read. Centralizing the new orange palette makes further tuning easier and avoids duplicated literal color values.
  - Alternatives Considered: Tweaking each label color independently would increase drift between text layers and make future tuning slower.

- Topic: Remove duplicated prepared star catalogs
  - Decision: Replace `star_catalog_full_np` + `star_catalog_lod6_np` with one prepared star catalog plus a precomputed `vmag <= 6.0` index array.
  - Rationale: The previous design duplicated all prepared numeric arrays, even though the LOD path only needed row selection. Passing a subset index keeps the hot-path math unchanged while removing duplicated storage.
  - Alternatives Considered: Keeping two prepared catalogs was simpler to read but wasted memory; recomputing the bright subset mask on every update would avoid duplication but add repeated work.

- 00:18 Ran: `uv run -p .venv/bin/python pytest tests/terrain/test_terrain_horizon_ui.py tests/test_export_image_layer_gating.py tests/test_export_image_sixel.py tests/test_window_render_sync.py tests/data/test_star_catalog_prepare_arrays.py`
  Output:
  ```text
  71 passed in 1.87s
  ```
  Result: GUI interaction, export-image, and catalog prep paths passed after the single-catalog LOD refactor.

- 00:19 Ran: `uv run -p .venv/bin/python pytest tests/test_startup_ephemeris.py tests/test_asterism_render.py tests/test_star_overscan_render.py tests/test_cli_export_image_args.py`
  Output:
  ```text
  24 passed in 0.93s
  ```
  Result: Startup-adjacent, asterism, overscan, and export CLI argument paths remained green.

- Summary: Implemented single-catalog star LOD selection with precomputed indices and verified the affected test slices.

- Topic: Cloud source sampler cache
  - Decision: Cache the brightness-temperature sampler on `CloudSourceData` and reuse it across repeated renders for the same fetched cloud source.
  - Rationale: During Alt/Az interaction, the source satellite data often stays the same while only the camera changes. Reusing the sampler avoids repeated `build_bt_sampler()` setup without changing any user-visible rendering behavior.
  - Alternatives Considered: Caching full rendered images or projection grids would affect behavior and cache invalidation more broadly; sampler reuse is narrower and lower risk.

- 00:33 Ran: `uv run -p .venv/bin/python pytest tests/clouddisc/test_clouddisc_fetch_render_split.py tests/clouddisc/test_cloud_controller_queueing.py tests/clouddisc/test_cloud_controller_epoch.py`
  Output:
  ```text
  7 passed in 0.74s
  ```
  Result: Source/render split behavior and new sampler reuse test passed.

- 00:34 Ran: `uv run -p .venv/bin/python pytest tests/clouddisc/test_cloud_missing_overlay.py tests/clouddisc/test_cloud_keys.py tests/clouddisc/test_goes_failover_visibility.py tests/test_window_cloud_partial_mode.py tests/test_window_cloud_status.py`
  Output:
  ```text
  17 passed in 1.47s
  ```
  Result: Cloud UI and helper behavior stayed green after sampler caching.

- Topic: Patch version bump
  - Decision: Bump the package version from `1.7.0` to `1.7.1`.
  - Rationale: The recent rendering, memory-layout, and cloud-behavior fixes should ship as a patch release without changing the public feature set.

- Topic: Remove Pillow from the cloud runtime path
  - Decision: Change cloud rendering to produce `numpy` RGBA buffers instead of `PIL.Image`, and convert those buffers directly to `QImage` in the GUI/export layers.
  - Rationale: The cloud pipeline already computes pixel values in `numpy`, so using Pillow there only added an extra image wrapper and conversion step. Returning RGBA arrays keeps the cloud path lighter while preserving Qt integration.
  - Alternatives Considered: Replacing Pillow with another image library would add dependency churn without simplifying the pipeline; converting only part of the path would still leave the cloud runtime dependent on Pillow.

- Topic: Keep splash error visible on startup location failures
  - Decision: Move launch location resolution inside the startup `LaunchSetupError` handling block so location lookup failures use the same splash-delay path as other setup errors.
  - Rationale: A mistyped city name should leave the splash error visible briefly instead of bubbling out as an uncaught exception and terminating immediately.
  - Alternatives Considered: Handling `LocationResolveError` separately would duplicate startup cleanup and make the failure paths diverge again.

- Topic: Start removing Pillow from moon rendering
  - Decision: Change moon phase generation to return `numpy` RGBA buffers and draw the moon through `QImage` plus painter transforms instead of rotating a Pillow image.
  - Rationale: This keeps the moon path aligned with the newer cloud path and removes another runtime dependency edge on Pillow without changing the rendered result intentionally.
  - Alternatives Considered: Keeping the Pillow rotation path would be simpler short-term but would block further runtime dependency reduction.

- Topic: Move runtime Qt image helpers out of `utils/qt.py`
  - Decision: Split NumPy/QImage conversion helpers into `render/qt_image.py` and leave `utils/qt.py` for Pillow compatibility helpers only.
  - Rationale: The runtime render/export path should import the Qt/NumPy helpers directly without pulling in a mixed utility module that also carries optional Pillow glue.
  - Alternatives Considered: Keeping everything in `utils/qt.py` would still work, but it hides the runtime dependency boundary and makes future cleanup harder.

- Topic: Keep cloud layer state in NumPy until final draw
  - Decision: Change the cloud image state, export-image cloud fetch path, and cloud missing-mask path to keep `numpy` arrays as the stored/intermediate representation, converting to `QImage` only at the final composition output.
  - Rationale: The earlier path converted cloud RGBA data and missing masks into `QImage` eagerly, only to convert them back to `numpy` during hatching, masking, and compositing. Holding cloud data as `numpy` avoids those round-trips, removes unnecessary 4-channel expansion for the missing mask, and keeps GUI and export-image on the same representation.
  - Alternatives Considered: Keeping only the missing mask as `numpy` would help a little but still leave the main cloud image bouncing between `QImage` and `numpy`; replacing the cache with direct `QImage.bits()` sharing would be more brittle in PySide and harder to reason about safely.

- 01:34 Ran: `uv run -p .venv/bin/python pytest tests/test_cli_export_image_args.py tests/test_export_image_layer_gating.py tests/test_export_image_sixel.py tests/test_window_cloud_partial_mode.py tests/clouddisc/test_cloud_hatch.py tests/clouddisc/test_cloud_missing_overlay.py tests/test_window_render_sync.py -q`
  Output:
  ```text
  65 passed in 1.32s
  ```
  Result: Export-image and GUI cloud rendering stayed green after switching cloud image and missing-mask storage to NumPy arrays.

- Summary: Tuned `white`/`day` text from blue to a brighter orange-red palette, centralized the light-theme text colors as constants, and left tests unrun because the change is visual-only.

- Topic: Warm `white` and `day` text slightly toward gold
  - Decision: Nudge the shared light-theme text, status-line text, and label text constants a bit further toward gold without changing their outline colors or any non-light-theme presets.
  - Rationale: The existing orange tint was close, but the requested adjustment is narrower than a full palette retune. Updating the shared constants keeps the visual shift consistent across light-theme text layers while staying low-risk.
  - Alternatives Considered: Adjusting only one text layer would make the light-theme text colors drift apart; changing outline colors as well would broaden the visual change beyond the requested scope.

- Topic: Unify text outline width across all themes
  - Decision: Remove the light-theme-specific outline-width override and use the shared `3.0` outlined-text stroke width for every preset.
  - Rationale: Keeping `white` and `day` thinner than the other themes made the behavior inconsistent and reduced readability on bright backgrounds. A single width is simpler and matches the user's intended adjustment.
  - Alternatives Considered: Keeping a separate `white`/`day` width would preserve finer control, but it adds special-case behavior without enough benefit here.

- Topic: Shift `white` and `day` outlines to warm orange
  - Decision: Replace the `white` and `day` outline colors in both the general text palette and the status-line palette with orange-brown RGBA values closer to the light-theme text color family.
  - Rationale: The previous blue-toned outlines no longer matched the warmed gold/orange text. Using a slightly darker warm outline keeps the text coherent and should improve readability on light backgrounds without introducing a stark black edge.
  - Alternatives Considered: Using pure black would maximize contrast but change the visual style more aggressively; reusing the exact fill color for outlines would reduce edge separation too much.

- Topic: Align `white` and `day` splash text with main text palette
  - Decision: Change the default splash info-message color for `white` and `day` to read from the shared main text palette instead of using separate dark-blue literals in `splash.py`.
  - Rationale: The splash screen had drifted from the warmed main-window text colors. Reusing the shared palette keeps the startup text visually consistent without changing splash warning/error semantics.
  - Alternatives Considered: Duplicating the new orange values inside `splash.py` would preserve the current structure but reintroduce palette drift.

- Topic: Remove fixed outline-width helper and centralize text style lookup
  - Decision: Delete `_get_text_outline_width()` from `render/text.py`, inline the fixed `3.0` width at call sites, and replace the private `_get_text_style()` helper with a public `get_text_style(..., status_line=False)` utility shared by main rendering and splash setup.
  - Rationale: A helper that only returns one constant adds indirection without value. Consolidating text-style lookup in one public utility reduces duplication and gives splash and main rendering a single palette access path.
  - Alternatives Considered: Keeping the helper for future flexibility would preserve an unnecessary abstraction; adding a separate splash-specific text-style helper would duplicate preset lookup logic again.

- Topic: Rename text color constants for theme consistency
  - Decision: Rename the remaining generic text color constants in `paths.py` to theme-family-based names such as `NIGHT_THEME_TEXT_COLOR`, `NIGHT_THEME_STATUS_TEXT_COLOR`, `DARK_THEME_LABEL_TEXT_COLOR`, and `NIGHT_THEME_TEXT_OUTLINE_RGBA`.
  - Rationale: The previous mix of generic and theme-specific names made the palette harder to scan and reason about. Using one naming pattern clarifies which preset family each constant belongs to.
  - Alternatives Considered: Leaving the old names in place would avoid churn, but it would keep the inconsistency that prompted the change.

- Topic: Use typed text-style objects instead of nested dict fields
  - Decision: Replace the preset style tables in `paths.py` from nested dict literals to a frozen `TextStyle` dataclass with `text` and `outline` fields, and make `get_text_style()` read from those typed objects.
  - Rationale: The old shape mixed direct literals with referenced constants and relied on string-key access. A small typed object makes the structure explicit, removes fragile `"text"`/`"outline"` indexing, and gives both main rendering and splash setup a clearer palette interface.
  - Alternatives Considered: Keeping nested dicts would remain flexible, but the structure is fixed enough that a dataclass is easier to read and safer to refactor.

- Topic: Make `TextStyle` objects the primary text palette source
  - Decision: Remove the intermediate text color tuple constants in `paths.py` and define named `TextStyle` objects directly for each main-text and status-line theme variant, with downstream code reading colors from those objects.
  - Rationale: Keeping both raw tuple constants and `TextStyle` objects duplicated the palette source of truth. Making the style objects primary reduces indirection and keeps all text palette lookups consistent with the new typed structure.
  - Alternatives Considered: Retaining the tuple constants would preserve a little reuse, but it would keep the split ownership the user wanted to eliminate.

- Topic: Rename and decouple the urban outline layer color
  - Decision: Rename `URBAN_DEBUG_LAYER_LINE_COLOR` to `URBAN_OUTLINE_LAYER_LINE_COLOR` and define it as its own color tuple instead of deriving it from a text style.
  - Rationale: The old name no longer matched the feature's role, and the line color is conceptually separate from text palette data. A dedicated constant makes that boundary explicit.
  - Alternatives Considered: Keeping the old name would preserve continuity but remain misleading; continuing to reuse a text color would keep unrelated styling coupled.

- Topic: Add guideline opacity control for CLI and GUI
  - Decision: Represent the visibility of sky guidelines as `guideline_opacity`, expose it on the CLI as `--guideline-opacity`, and add a GUI menu toggle that switches between `0.0` and the configured non-zero opacity.
  - Rationale: The existing rendering controls use opacity rather than separate show/hide flags, so guidelines fit that model better than a boolean startup option. One shared opacity also keeps the geometric horizon, celestial equator, ecliptic, direction labels, and zenith marker in sync.
  - Alternatives Considered: A separate `--show-guidelines-initial` boolean would be simpler, but it would add a second visibility convention alongside the existing opacity-based overlays.

- 16:43 Ran: `uv run -p .venv/bin/python pytest tests/test_cli_cloud_missing_tint_opacity.py tests/terrain/test_terrain_horizon_ui.py tests/test_window_render_sync.py -q`
  Output:
  ```text
  78 passed in 1.32s
  ```
  Result: CLI parsing, GUI guideline toggle behavior, and render pipeline changes passed in the focused test slice.
