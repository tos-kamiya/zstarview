# Session Notes - 2026-02-28

- Scope: プロジェクト概要の調査（構成、主要機能、実行方式、アーキテクチャ確認）

- Topic: プロジェクト全体像の把握
  - Decision: README / pyproject / エントリポイント / 設計書を一次情報として概要を抽出した。
  - Rationale: 利用者向け説明と実装実態の両方を短時間で整合的に把握するため。

- {Time 00:00} Ran: `rg --files`
  Output:
  ```text
  主要構成: src/zstarview, tests, docs, dev-notes, pyproject.toml, README.md
  ```
  Result: モジュール配置とテスト/ドキュメント配置を確認。

- {Time 00:00} Ran: `sed -n '1,220p' README.md`
  Output:
  ```text
  zstarview は都市指定で星空を表示し、雲オーバーレイ(Himawari/GOES)に対応。
  ```
  Result: 機能・CLIオプション・運用上の注意を確認。

- {Time 00:00} Ran: `sed -n '1,260p' pyproject.toml`
  Output:
  ```text
  Python>=3.10, GUIはPySide6、天文計算はastropy/skyfield、雲はsatpy/boto3系依存。
  ```
  Result: 依存関係とパッケージ設定を確認。

- Topic: 主要アーキテクチャ確認
  - Decision: `zstarview.py` を起点に、`docs/design.md` の責務分離方針（UI/計算/クラウド）を確認した。
  - Rationale: 実装の入口と設計意図を一致させて概要説明の精度を上げるため。

- Summary:
  - 調査のみ（コード変更なし）。
  - follow-up: 必要なら次回は実行フロー図（起動→ワーカー→描画）を追加する。

- Topic: 方位ラベル（N/NE...）の色を地平線色へ統一
  - Decision: `draw_direction_labels()` の文字色を `TEXT_COLOR` から `HORIZON_LINE_COLOR` に変更した。
  - Rationale: 方位ラベルを地平線の緑ラインと同色に揃えるため。

- {Time 18:20} Ran: `python3 -m py_compile src/zstarview/render/draw.py`
  Output:
  ```text
  (no output)
  ```
  Result: 構文エラーなしを確認。

- Topic: 方角位置を示す短い縦線マーカーの追加
  - Decision: `draw_direction_labels()` で各方角の地平線座標に短い縦線（tick）を描画する処理を追加した。
  - Rationale: N/NE などの正確な方角位置を視覚的に示すため。

- {Time 18:22} Ran: `python3 -m py_compile src/zstarview/render/draw.py`
  Output:
  ```text
  (no output)
  ```
  Result: 変更後も構文エラーなし。

- Topic: 方位ラベルを8方位表示へ簡略化
  - Decision: 方位ラベル描画対象を16方位から8方位（N/NE/E/SE/S/SW/W/NW）に限定した。
  - Rationale: NNE/ENE などを省き、視認性と情報密度のバランスを改善するため。

- {Time 18:24} Ran: `python3 -m py_compile src/zstarview/render/draw.py`
  Output:
  ```text
  (no output)
  ```
  Result: 構文エラーなし。

- Topic: DIRECTIONS定数を16方位から8方位へ直接変更
  - Decision: `paths.py` の `DIRECTIONS` を8方位（45度刻み）のみへ変更し、描画とAz方位表示の変換ロジックも8方位へ統一した。
  - Rationale: 表示・入力・内部定義の方位粒度を一致させるため。

- {Time 18:25} Ran: `python3 -m py_compile src/zstarview/paths.py src/zstarview/render/draw.py`
  Output:
  ```text
  (no output)
  ```
  Result: 構文エラーなし。

- Topic: 方位定数の重複定義を解消
  - Decision: `draw.py` 内の `az_to_compass()` で独自方位配列を廃止し、`paths.py` の `DIRECTIONS` キーを直接参照するように変更した。
  - Rationale: 方位定義の単一ソース化により、将来の変更漏れを防ぐため。

- {Time 18:26} Ran: `python3 -m py_compile src/zstarview/render/draw.py src/zstarview/paths.py`
  Output:
  ```text
  (no output)
  ```
  Result: 構文エラーなし。

- Topic: パッチバージョンを +0.0.1 バンプ
  - Decision: `__version__` を `0.13.0` から `0.13.1` に更新した。
  - Rationale: ユーザー依頼に基づき、直近変更に対するパッチリリース番号を付与するため。

- Topic: 雲ストライプの低雲量抑制仕様（設計のみ）
  - Decision: 雲量 `w` にソフトしきい値（0.03/0.08）を適用する設計案を `docs/design.md` へ追記した。
  - Rationale: 晴天域で低雲量ノイズ由来のストライプ可視化を抑えつつ、固定5%カットで起きる境界点滅を避けるため。

- Topic: ローカル作業ファイルの除外設定
  - Decision: `.gitignore` に `.codex/`, `package.json`, `package-lock.json` を追加した。
  - Rationale: 開発ローカルの補助ファイルをコミット対象から外し、差分ノイズを減らすため。

- {Time 21:50} Ran: `git add .gitignore && git commit -m "chore: ignore local codex and npm metadata files"`
  Output:
  ```text
  [main bc67d78] chore: ignore local codex and npm metadata files
  ```
  Result: `.gitignore` 変更をコミット。

- Topic: 雲アルファの低雲量抑制を実装
  - Decision: `convert_bt_to_la_image()` の雲量変換に、`floor=0.03`, `knee=0.08` のソフト抑制を適用した。
  - Rationale: 晴天域の薄いストライプを抑えつつ、低雲量域の立ち上がりを急峻にしないため。

- {Time 21:51} Ran: `python3 -m pytest -q tests/test_cloud_la_encoding.py tests/test_cloud_hatch.py`
  Output:
  ```text
  /usr/bin/python3: No module named pytest
  ```
  Result: テストランナー未導入のため実行不能。構文検証へ切り替え。

- {Time 21:51} Ran: `python3 -m py_compile src/zstarview/clouddisc/render/grayscale.py tests/test_cloud_la_encoding.py`
  Output:
  ```text
  (no output)
  ```
  Result: 変更ファイルの構文エラーなし。

- Topic: 開発用依存（dev extras）の追加
  - Decision: `pyproject.toml` に `[project.optional-dependencies]` の `dev` セクションを追加し、`pytest` を登録した。
  - Rationale: `uv pip install ".[dev]"` でテスト実行に必要な依存を一括導入できるようにするため。

- Topic: 開発者向けドキュメントに dev インストール手順を反映
  - Decision: `docs/developer/uv-workflow.md` と `docs/developer/README.md` に `uv pip install -e ".[dev]"` の案内を追記した。
  - Rationale: 実際のセットアップ手順と `pyproject.toml` の optional dependencies を一致させるため。

- Topic: パッチバージョンを +0.0.1 バンプ（2回目）
  - Decision: `__version__` を `0.13.1` から `0.13.2` に更新した。
  - Rationale: ユーザー依頼に基づき、現時点の変更をパッチリリースとして区切るため。

- Topic: 地平線方位マーカー仕様の進行管理更新
  - Decision: `docs/design.md` の既存進行中表記（雲ストライプ低雲量抑制）を整理し、新規に「方位マーカーを縦線から円盤へ変更」の仕様を追記した。
  - Rationale: 現在の議論対象を明確にし、完了済み項目と進行中項目を分離するため。

- Topic: 地平線方位マーカーを円盤描画へ実装
  - Decision: `draw_direction_labels()` の画面相対縦線を廃止し、方位点に円盤マーカーを描画する実装へ置換した。ラベル重なり時は外周方向へ `+4px` オフセットする。
  - Rationale: 天頂表示や低仰角表示での不自然な縦線視認を避けるため。

- {Time 22:00} Ran: `python3 -m py_compile src/zstarview/render/draw.py`
  Output:
  ```text
  (no output)
  ```
  Result: 方位マーカー変更後の構文エラーなし。

- Topic: 方位マーカーを「そろばん珠」風に調整
  - Decision: 画面上の単純な円ではなく、地平線接線方向を短軸にした向き付き楕円（中心に短い接線方向の弦）で描画するように変更した。
  - Rationale: 地平線がマーカーを貫く見え方を担保し、天頂付近でも不自然な見え方を避けるため。

- {Time 22:03} Ran: `python3 -m py_compile src/zstarview/render/draw.py`
  Output:
  ```text
  (no output)
  ```
  Result: 調整後も構文エラーなし。

- Topic: 方位マーカーを地平線垂直の固定長線へ再調整
  - Decision: 向き付き楕円（そろばん珠風）を撤回し、各方位点で地平線接線に垂直な方向の短線を固定ピクセル長で描く方式へ変更した。
  - Rationale: 仰角に依存せず一定の見た目長を維持する要件に合わせるため。

- Topic: パッチバージョンを +0.0.1 バンプ（3回目）
  - Decision: `__version__` を `0.13.2` から `0.13.3` に更新した。
  - Rationale: ユーザー依頼に基づき、最新変更に対してパッチリリース番号を進めるため。

- Topic: 空ディスク描画の NumPy 1パス化と円形フル描画
  - Decision: `draw_sky_color_disc()` を同心リング走査から、画素グリッドの逆投影 + ベクトル化色計算に置換した。地平線による描画抑制は廃止し、円ディスク内は常時描画する。
  - Rationale: 描画アルゴリズムを簡素化しつつ、保守性を上げるため。ユーザー要望どおり地平線で欠けない円形ディスクを維持するため。
  - Alternatives Considered: 従来リング方式の簡略化（パラメータ削減）も可能だが、実装複雑性が残るため採用しなかった。

- {Time 23:20} Ran: `python3 -m py_compile src/zstarview/render/draw_sky_disc.py`
  Output:
  ```text
  (no output)
  ```
  Result: 構文エラーなし。

- {Time 23:20} Ran: `PYTHONPATH=src python3 - <<'PY' ... draw_sky_color_disc(...) ... PY`
  Output:
  ```text
  ModuleNotFoundError: No module named 'PySide6'
  ```
  Result: 実行環境に PySide6 がないため、ランタイム描画確認は未実施。

- Topic: 空ディスク色強度の旧挙動復元
  - Decision: `draw_sky_color_disc()` の最終RGBに `alpha`（sky_opacity）を乗算する処理を追加した（`eclipse_factor` と併用）。
  - Rationale: NumPy 1パス化時に抜けていた色強度スケールを復元し、従来の見え方に近づけるため。

- {Time 23:30} Ran: `python3 -m py_compile src/zstarview/render/draw_sky_disc.py`
  Output:
  ```text
  (no output)
  ```
  Result: 構文エラーなし。

- Topic: 雲描画の地平線下マージンを無効化
  - Decision: `alt_min_deg` を実運用値・デフォルト値ともに `0.0` へ統一した（`window.py`, `clouddisc/config.py`, `clouddisc/projectors/az.py`）。
  - Rationale: 雲ストライプを地平線より下へ描かない方針に合わせ、設定検索時の `-2.0/-1.0` 混在による混乱を解消するため。

- {Time 23:45} Ran: `python3 -m py_compile src/zstarview/ui/window.py src/zstarview/clouddisc/config.py src/zstarview/clouddisc/projectors/az.py`
  Output:
  ```text
  (no output)
  ```
  Result: 構文エラーなし。

- Topic: 雲更新失敗時の表示ポリシーを「非表示」に統一
  - Decision: Cloud fetch/render 失敗時は `clear_image=True` を返し、UI 側でも常に `cloud_state.image` / `stripe_density` を消去してコンポジットキャッシュを無効化するよう変更した。
  - Rationale: 失敗時に古い雲画像が残る/見え方が不安定になるのを防ぎ、「失敗表示 + 雲は描かない」を確実にするため。

- {Time 23:55} Ran: `python3 -m py_compile src/zstarview/ui/cloud_controller.py src/zstarview/ui/window.py`
  Output:
  ```text
  (no output)
  ```
  Result: 構文エラーなし。

- Topic: 設計文書の雲仕様を現行実装へ同期
  - Decision: `docs/design.md` の雲更新フロー/エラーモデルへ「失敗時は雲をクリアして非表示」を明記し、雲の地平線下マージン方針（`alt_min_deg=0.0`）を追記した。
  - Rationale: 直近で確定した運用仕様と設計文書の記述差分を解消するため。

- Topic: 全天球円形ビュー + 地平線下ティント仕様の策定（設計のみ）
  - Decision: `docs/design.md` に、ビュー固定円形化と地平線下ティント表示仕様を追記した。色/強度は VR 実装を参照し、`groundTint=(0.12,0.19,0.27)`, 強度 `0.30` を初期値とした。
  - Rationale: 仰角変更時の見切れ変動を抑え、地平線下の視認性を向上するため。雲は `alt_min_deg=0.0` 維持で地平線下非表示とし、背景色のみを見せる方針を明確化するため。

- Topic: 全天球固定円ビュー + 地平線下ティントの実装
  - Decision: `get_screen_geometry()` を仰角非依存の固定円（画面中央・半径=min(可用幅,可用高)/2）へ変更し、空ディスク描画で `alt<0` に VR 参照のティント（`RGB=(0.12,0.19,0.27)`, 強度 `0.30`）を適用した。
  - Rationale: 仰角変更時に円位置が移動しない表示へ統一しつつ、地平線下を完全黒ではなく薄い色で視認可能にするため。

- {Time 00:20} Ran: `python3 -m py_compile src/zstarview/render/draw.py src/zstarview/render/draw_sky_disc.py`
  Output:
  ```text
  (no output)
  ```
  Result: 構文エラーなし。

- Topic: 固定円ビュー/地平線下ティントの回帰テスト追加
  - Decision: `tests/test_sky_disc_viewport_and_ground_tint.py` を追加し、(1) 仰角に依存しない固定円ジオメトリ、(2) 地平線下ティントの描画を検証するテストを実装した。
  - Rationale: 表示仕様変更の再発防止と、今後の描画調整時の安全網を確保するため。

- {Time 00:30} Ran: `.venv/bin/python -m pytest -q tests/test_sky_disc_viewport_and_ground_tint.py`
  Output:
  ```text
  2 passed in 1.33s
  ```
  Result: 追加テストは通過。

- Topic: 地平線下への星・補助線表示拡張
  - Decision: `astro.py` で星、天の赤道、黄道の点生成にある地平線下カット条件を外し、FOV条件のみで採用するよう変更した。
  - Rationale: 固定円ビュー＋地平線下ティント仕様に合わせ、地平線下にも星と主要補助線を表示するため。

- {Time 00:45} Ran: `python3 -m py_compile src/zstarview/astro.py`
  Output:
  ```text
  (no output)
  ```
  Result: 構文エラーなし。

- Topic: 星空表示FOVの縮小
  - Decision: `FIELD_OF_VIEW_DEG` を `115` から `105` に変更した。
  - Rationale: 全天球化後の見え方として、外周の広がりが大き過ぎるため表示範囲をやや狭めるため。

- {Time 01:05} Ran: `python3 -m py_compile src/zstarview/paths.py src/zstarview/astro.py src/zstarview/render/draw.py`
  Output:
  ```text
  (no output)
  ```
  Result: 構文エラーなし。

- Topic: デフォルトウィンドウ形状を正方形へ変更
  - Decision: `SkyWindow` の初期サイズ計算を仰角依存の縦横比から固定値へ変更し、`WINDOW_WIDTH x WINDOW_HEIGHT`（既定 600x600）で起動、最小サイズも `400x400` に統一した。
  - Rationale: 全天球固定円ビュー仕様に合わせ、起動時ウィンドウ形状を安定した正方形にするため。

- {Time 01:20} Ran: `python3 -m py_compile src/zstarview/ui/window.py`
  Output:
  ```text
  (no output)
  ```
  Result: 構文エラーなし。

- Topic: 地平線下ティントへの sky_opacity 適用除外
  - Decision: `draw_sky_color_disc()` で `sky_opacity(alpha)` は地平線上（`alt>=0`）の空色にのみ適用し、地平線下ティント（`alt<0`）には適用しないよう変更した。
  - Rationale: 地面側の薄色が `sky_opacity` で過度に減衰して視認しづらくなる問題を解消するため。

- {Time 01:30} Ran: `python3 -m py_compile src/zstarview/render/draw_sky_disc.py`
  Output:
  ```text
  (no output)
  ```
  Result: 構文エラーなし。

- Topic: 地平線下ティント強度の調整
  - Decision: `GROUND_TINT_STRENGTH` を `0.30` から `0.10` に変更した。
  - Rationale: 地面側の色が強すぎるため、視認性を保ちつつ主張を弱めるため。

- {Time 01:40} Ran: `python3 -m py_compile src/zstarview/render/draw_sky_disc.py`
  Output:
  ```text
  (no output)
  ```
  Result: 構文エラーなし。

- Topic: 地平線下ティント強度の再調整
  - Decision: `GROUND_TINT_STRENGTH` を `0.10` から `0.05` に変更した。
  - Rationale: さらに控えめな地面ティント表示にするため。

- {Time 01:45} Ran: `python3 -m py_compile src/zstarview/render/draw_sky_disc.py`
  Output:
  ```text
  (no output)
  ```
  Result: 構文エラーなし。

- Topic: 地平線下色の生成方式を暗化ベースへ変更
  - Decision: 地平線下は `skyColor` を直接ティント混合するのではなく、先に `GROUND_BASE_DARKNESS=0.12` で暗化したベースを作ってから `GROUND_TINT_STRENGTH=0.01` でティント混合する方式へ変更した。
  - Rationale: ティント強度の調整感度を上げ、微小値でも見え方を制御しやすくするため。

- {Time 01:55} Ran: `python3 -m py_compile src/zstarview/render/draw_sky_disc.py`
  Output:
  ```text
  (no output)
  ```
  Result: 構文エラーなし。

- Topic: 不可視半球（恒久的に昇らない天球領域）への赤ティント導入
  - Decision: `draw_sky_color_disc()` に観測緯度依存の「never rises」判定を追加し、該当画素へ赤系ティント（`(0.42, 0.07, 0.07) * 0.08`）を加える実装を追加した。`SkyDataWorker` から観測緯度を渡すように更新した。
  - Rationale: VR版と同様に、観測地点で恒久的に見えない天球領域を視覚的に識別しやすくするため。

- {Time 02:10} Ran: `python3 -m py_compile src/zstarview/render/draw_sky_disc.py src/zstarview/ui/sky_worker.py`
  Output:
  ```text
  (no output)
  ```
  Result: 構文エラーなし。

- Topic: 不可視半球赤ティントの適用順調整
  - Decision: 「never rises」赤ティントを地面暗化/ティント処理の後段へ移動し、最終色へ加算する順序に変更した。
  - Rationale: 先行適用だと地平線下の暗化処理で赤ティントが視認しづらくなるため。

- {Time 02:25} Ran: `python3 -m py_compile src/zstarview/render/draw_sky_disc.py`
  Output:
  ```text
  (no output)
  ```
  Result: 構文エラーなし。

- Topic: 地面/不可視半球ティント仕様の設計文書同期
  - Decision: `docs/design.md` を現行実装に合わせて更新し、地平線下色の暗化ベース合成（`base=0.03`, `strength=0.20`）、`sky_opacity` の適用範囲（地平線上のみ）、never-rises 赤ティント（`strength=0.16`、最終段適用）を明記した。
  - Rationale: チューニング後の実装状態と設計文書の乖離を解消するため。

- Topic: 矢印キー上下で仰角変更を追加
  - Decision: `keyPressEvent()` に `↑/↓` を追加し、`_rotate_view(d_alt=±rotation_step)` を呼ぶようにした。ビュー更新理由ラベルは `az-change` から `view-change` へ一般化した。README のキー操作説明にも反映した。
  - Rationale: 左右の方位変更と同じ操作感で仰角を調整できるようにし、入力仕様を明確化するため。

- {Time 00:27} Ran: `python3 -m py_compile src/zstarview/ui/window.py`
  Output:
  ```text
  (no output)
  ```
  Result: 矢印キー上下対応後も構文エラーなし。

- Topic: 天頂マーカーの対向点（Nadir）追加
  - Decision: `draw_zenith_marker()` を拡張し、`alt=+90`（Zenith）と `alt=-90`（Nadir）の双方に同じ `X` マーカーを描くようにした。描画は従来どおり FOV 内のみ実施する。
  - Rationale: 天頂基準だけでなく観測者下方向の天球位置も同一記号で参照できるようにするため。

- {Time 00:33} Ran: `python3 -m py_compile src/zstarview/render/draw.py`
  Output:
  ```text
  (no output)
  ```
  Result: Nadir 追加後も構文エラーなし。

- Topic: バージョンを +0.1 バンプ
  - Decision: `__version__` を `0.13.3` から `0.14.0` に更新した。
  - Rationale: ユーザー依頼の +0.1（マイナー）バージョン更新に合わせるため。

- Topic: 星描画FOVと背景FOVの分離
  - Decision: 背景側の `FIELD_OF_VIEW_DEG=100` は維持し、星の抽出判定だけ `STAR_FIELD_OF_VIEW_DEG=95` を導入して適用した。`is_in_fov` / `is_in_fov_vectorized` は `fov_deg` 引数対応に拡張した。
  - Rationale: 背景グラデーションの見え方は維持しつつ、星の表示範囲のみ少し絞る要件に合わせるため。

- {Time 00:44} Ran: `python3 -m py_compile src/zstarview/paths.py src/zstarview/astro.py`
  Output:
  ```text
  (no output)
  ```
  Result: FOV分離変更後も構文エラーなし。

- Topic: 背景FOV定数の明確化と値調整
  - Decision: 背景グラデーション用の定数を `BACKGROUND_FIELD_OF_VIEW_DEG` として新設し、値を `105` に設定した。`draw_radial_background()` はこの背景専用定数を参照するように変更した。
  - Rationale: 背景向けFOVであることを名前から判別できるようにしつつ、ユーザー要望どおり背景の見え方を広げるため。

- {Time 00:51} Ran: `python3 -m py_compile src/zstarview/paths.py src/zstarview/render/draw.py`
  Output:
  ```text
  (no output)
  ```
  Result: 背景FOV定数分離後も構文エラーなし。

- Topic: README の機能説明を最新表示仕様に更新
  - Decision: Features セクションの雲オーバレイ画像を `screenshot5.png` に差し替え、雲説明の直後に never-rises 領域（赤ティント）の説明を追加した。
  - Rationale: 現在の表示仕様（雲 + never-rises 可視化）と README の冒頭説明を一致させるため。

- Topic: 日本語READMEの機能説明も同期更新
  - Decision: `README_ja-JP.md` の Features 節で雲オーバレイ画像を `screenshot5.png` に差し替え、雲説明の直後に never-rises 領域（赤ティント）の説明を追加した。
  - Rationale: 英語版READMEと日本語版READMEの説明・画像を一致させるため。

- Topic: バージョンを 0.14.1 に更新
  - Decision: `__version__` を `0.14.0` から `0.14.1` に更新した。
  - Rationale: ユーザー依頼に基づくパッチリリースのバージョンバンプ。

- Topic: バージョンを 0.14.2 に更新
  - Decision: `__version__` を `0.14.1` から `0.14.2` に更新した。
  - Rationale: ユーザー依頼に基づく予防的パッチバージョン更新。

- Topic: 横長ウィンドウ時の天球レイアウト調整（地面側を画面下に隠す）
  - Decision: アスペクト比が横長かつ2:1以下のとき、天球直径をウィンドウ幅基準にし、天球上端が上マージンに内接する配置へ変更した。2:1を超える横長や通常比率では従来どおり中央配置を維持する。
  - Rationale: 横長時に地面側の表示比率を下げ、上空側の視認性を優先するため。極端な横長では従来挙動を維持し、過度なはみ出しを避けるため。

- {Time 01:45} Ran: `uv run -p .venv/bin/python -m pytest -q tests/test_sky_disc_viewport_and_ground_tint.py`
  Output:
  ```text
  ...                                                                      [100%]
  3 passed in 1.51s
  ```
  Result: 画面ジオメトリ変更と関連スカイディスク描画テストの通過を確認。

- Topic: 超横長(2:1超)時の天球ジオメトリ調整
  - Decision: 横幅が高さの2倍を超える場合は、天球半径をウィンドウ内側高さ基準（`height-20`）にし、天球上端を上マージンへ内接する配置に変更した。
  - Rationale: 横長化が進んだレイアウトでも、地面側をより強く画面下へ逃がし、上空側の表示面積を優先するため。

- {Time 01:52} Ran: `uv run -p .venv/bin/python -m pytest -q tests/test_sky_disc_viewport_and_ground_tint.py`
  Output:
  ```text
  ...                                                                      [100%]
  3 passed in 1.27s
  ```
  Result: 横長/超横長の新ジオメトリ分岐を含むテストが通過。

- Topic: 横長時の天球縦配置を仰角依存へ変更
  - Decision: 横長モードでは、円の上端ではなく「現在仰角における天球の最上点」が上マージンに一致するよう中心Yを計算する方式へ変更した（`center_y = margin + radius * (1 - alt/90)`）。
  - Rationale: 仰角0以外で上空側が不要に隠れる問題を抑え、地面以外を可能な限り可視にするため。

- {Time 02:01} Ran: `uv run -p .venv/bin/python -m pytest -q tests/test_sky_disc_viewport_and_ground_tint.py`
  Output:
  ```text
  ....                                                                     [100%]
  4 passed in 1.23s
  ```
  Result: 仰角依存レイアウトを含むテストが通過。

- Topic: 横長時に天球が上端からはみ出す不具合の修正
  - Decision: 横長モードでは天球円の上端が常に上マージンへ内接するように中心Yを `margin + radius` で固定し、半径を `min((width-20)/2, (height-20)/(1+alt/90))` で決定する方式に変更した。
  - Rationale: 「天球が上端からはみ出さない」条件を満たしつつ、仰角に応じて地平線位置が下寄りになるようスケールして地面以外の隠れを抑えるため。

- {Time 02:10} Ran: `uv run -p .venv/bin/python -m pytest -q tests/test_sky_disc_viewport_and_ground_tint.py`
  Output:
  ```text
  ....                                                                     [100%]
  4 passed in 1.21s
  ```
  Result: 上端内接仕様への修正後も関連テストが通過。

- Topic: 横長時ビュー円ディスク条件の仕様・設計ドキュメント反映
  - Decision: 現行実装に合わせて、横長時の「上端内接保証」と仰角連動半径式（`r = min((w-20)/2, (h-20)/(1+alt/90))`）を `docs/design.md` と `docs/specification.md` に追記した。
  - Rationale: 実装済みの表示ルールを仕様・設計へ同期し、今後の改修時に挙動基準を参照可能にするため。

- Topic: パッチバージョンを +0.0.1 バンプ（0.14.2 -> 0.14.3）
  - Decision: `__version__` を `0.14.3` に更新した。
  - Rationale: ユーザー依頼により、直近の表示仕様更新をパッチリリースとして区切るため。

- Topic: 惑星マーカーへブルーム効果を追加（-1.5クリップ維持）
  - Decision: 惑星ディスクの半径/不透明度は従来どおり `-1.5` 等級でクリップし、ブルーム強度のみ実等級（`-1.5` より明るい領域）を追加反映する方式を導入した。
  - Rationale: 金星など非常に明るい惑星で、サイズ肥大を避けつつ明るさ差を視覚的に表現するため。
  - Alternatives Considered: ディスクサイズのクリップ解除は、明るい惑星の見た目肥大が大きくUIバランスを崩しやすいため不採用。

- {Time 04:59} Ran: `uv run -p .venv/bin/python -m pytest -q tests/test_planet_brightness.py tests/test_planet_marker_style.py`
  Output:
  ```text
  .......                                                                  [100%]
  7 passed in 1.37s
  ```
  Result: 惑星ブルーム追加と既存惑星マーカー挙動の回帰テストが通過。

- Topic: 惑星ラベルの常時表示
  - Decision: 惑星（Sun/Moonを除く）を描画する際、マーカーの右上へ名称ラベルを常時表示するように変更した（ホバー依存を廃止）。
  - Rationale: 惑星が見分けにくい状況でも、ラベルによって即時識別できるようにするため。

- {Time 05:14} Ran: `uv run -p .venv/bin/python -m pytest -q tests/test_planet_marker_style.py tests/test_planet_brightness.py`
  Output:
  ```text
  .......                                                                  [100%]
  7 passed in 1.24s
  ```
  Result: 惑星ラベル常時表示とブルーム描画を含む関連テストが通過。

- Topic: 惑星がほぼ表示されない問題の修正
  - Decision: 惑星描画と惑星ホバー判定の条件を `body.is_visible` 依存から `is_in_fov(...)` 依存へ変更し、地平線下でもFOV内なら表示対象にした。
  - Rationale: 全天球表示方針に合わせ、時刻によって惑星がほぼ出ない状態を避けるため。

- {Time 05:23} Ran: `uv run -p .venv/bin/python -m pytest -q tests/test_planet_marker_style.py tests/test_planet_brightness.py`
  Output:
  ```text
  ........                                                                 [100%]
  8 passed in 1.25s
  ```
  Result: 惑星可視判定変更後も惑星描画/ラベル/ブルーム/ホバーのテストが通過。

- Topic: `ANGLE_BELOW_HORIZON` 定数の削除クリーンアップ
  - Decision: `paths.py` から `ANGLE_BELOW_HORIZON` を削除し、`astro.calculate_planets()` の `is_visible` 判定を地平線条件付きからFOV判定のみに統一した。
  - Rationale: 現在の描画方針（地平線下も表示、FOV基準）と整合しない未使用ロジックを排除するため。

- {Time 05:33} Ran: `uv run -p .venv/bin/python -m pytest -q tests/test_planet_marker_style.py tests/test_planet_brightness.py`
  Output:
  ```text
  ........                                                                 [100%]
  8 passed in 1.22s
  ```
  Result: 定数削除後も惑星描画/ラベル/ブルーム/ホバー回帰テストが通過。

- Topic: 惑星表示仕様の設計ドキュメント同期
  - Decision: `docs/design.md` の惑星表示節を現行実装へ更新し、常時ラベル表示・FOV基準表示（地平線下含む）・ブルームによる超高輝度表現を追記した。
  - Rationale: 直近実装との差異（ホバー依存ラベルなど）を解消し、保守時の参照仕様を一致させるため。

- Topic: Venusブルーム過大のチューニング
  - Decision: `planet_bloom_profile_from_vmag()` の半径倍率とアルファを下方調整し、ハロー最大径をおおむねコア半径の約2.4倍に抑えるよう変更した。
  - Rationale: VR版より過大に見える惑星ブルームを抑え、惑星識別性と画面バランスを両立するため。

- {Time 05:49} Ran: `uv run -p .venv/bin/python -m pytest -q tests/test_planet_brightness.py tests/test_planet_marker_style.py`
  Output:
  ```text
  ........                                                                 [100%]
  8 passed in 1.24s
  ```
  Result: ブルーム調整後も惑星描画/ラベル/ホバーの回帰テストが通過。

- Topic: マイナーバージョンを +0.1 バンプ
  - Decision: `__version__` を `0.14.3` から `0.15.0` に更新した。
  - Rationale: ユーザー依頼により、直近の機能変更群をマイナーアップデートとして区切るため。

- Topic: 恒星の既定サイズを 6.0 へ調整
  - Decision: `--star-base-radius` の既定値を `8.0` から `6.0` へ変更し、`SkyWindow` 側の既定値と README（日英）の記載も同じ値へ統一した。
  - Rationale: 恒星のサイズ飽和を抑え、天の川の濃淡・形状が読み取りやすい見え方を既定状態で得るため。

- Topic: 暗い恒星の持ち上がりを緩和
  - Decision: 恒星描画の `min_area_px` を `0.5` から `0.35` に下げた。
  - Rationale: `vmag<=9` 表示時に暗い星が同一サイズ床へ張り付く傾向を緩和し、暗い等級側の階調を残すため。

- Topic: パッチバージョンを +0.0.1 バンプ
  - Decision: アプリケーション版を `0.15.0` から `0.15.1` へ更新した。
  - Rationale: ユーザー依頼に基づき、直近調整をパッチリリースとして区切るため。

- Topic: 星カタログ再生成計画（Hipparcos + Tycho-2）をINPROGRESS化
  - Decision: `docs/design.md` の「今後の設計課題」に、`zstarview` を星データ生成の正本とする統合再生成プランを `INPROGRESS` で追加した。
  - Rationale: Desktop/VRで同一データ系統へ統一し、`vmag<=9` 表示時の密度改善を再現可能な運用で進めるため。

- Topic: 星カタログ再生成プラン 1/7（仕様固定）の実施
  - Decision: `docs/developer/input-data-contract.md` に「星カタログ生成契約（v1）」を追加し、Hipparcos+Tycho-2 の入力、出力スキーマ、等級レンジ、重複解決、欠損処理、受け入れ基準を固定した。
  - Rationale: 実装着手前にデータ仕様を先に固定し、Desktop/VR 間のデータ整合と検証基準を明確化するため。

- Topic: 星データ生成スクリプトの設計・実装（Hipparcos + Tycho-2）
  - Decision: `src/zstarview/data/stars/generate_star_catalog.py` を追加し、Hipparcos必須 + Tycho-2任意の統合生成、重複解決、`base/extra7/8/9` 分割出力、生成統計出力を実装した。
  - Rationale: `zstarview` を生成の正本として、Desktop/VR で同一データ系統を運用できるようにするため。

- Topic: 星カタログ再生成の開発者向け導線追加
  - Decision: `docs/developer/star-catalog-generation.md` を新規追加し、実行手順と主要パラメータ、検証観点を明文化した。
  - Rationale: 再生成作業の属人化を避け、再現可能な運用手順を共有するため。

- Topic: 星データ実生成とアプリ読込確認
  - Decision: 新規生成スクリプトで `src/zstarview/data/stars.csv` と `stars_base/extra7/extra8/extra9.csv` を生成し、`load_star_catalog()` で `<=6:5041`, `<=9:83337` を確認した。
  - Rationale: 実データを生成して既存アプリの読込経路で利用できることを確定するため。

- Topic: 星データ生成手順のドキュメント導線追加
  - Decision: README（日英）と `docs/developer/README.md` へ生成スクリプト実行方法と参照先ドキュメントを追記した。
  - Rationale: 生成手順を利用者と開発者の双方から見つけやすくするため。

- Topic: 旧星データの削除
  - Decision: `src/zstarview/data/stars/` 配下の旧生成物 `stars.csv` と `stars_hip.csv` を削除した。
  - Rationale: 新しい生成フローの成果物（`src/zstarview/data/stars.csv` と `stars_base/extra7/8/9.csv`）へ一本化し、重複管理を避けるため。

- Topic: Tycho-2 I/259 直接取込と生成更新
  - Decision: `generate_star_catalog.py` に `--tycho-i259-dir` を追加し、`tyc2.dat.*.gz` 直接読込を実装した。あわせて `stars.csv` と分割CSVをTycho統合版へ再生成した。
  - Rationale: 正規化CSVの事前作成を不要にし、I/259の標準配布形式から直接更新できるようにするため。

- Topic: I/259 作業データの誤コミット防止
  - Decision: `.gitignore` に `src/zstarview/data/I-259/` を追加した。
  - Rationale: 大容量の外部取得データをリポジトリ履歴に含めない運用を明確化するため。

- Topic: 旧星データ生成スクリプトの削除
  - Decision: `src/zstarview/data/stars/add_name.py` と `convert_to_csv.py` を削除した。
  - Rationale: 新しい統合生成スクリプト `generate_star_catalog.py` へ一本化し、保守対象を明確化するため。

- Topic: extra10 分割導入（`9 < vmag <= 10`）
  - Decision: 生成スクリプトに `stars_extra10.csv` 出力を追加し、ローダーも `vmag<=10` で `base..extra10` を分割読込するよう更新した。
  - Rationale: 高等級データ利用時でも統合CSV全量を常時読む必要を減らし、運用の見通しを維持するため。
