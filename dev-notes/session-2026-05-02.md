# 2026-05-02

Scope: Investigate GUI search behavior when `-A/-Z` are supplied on startup.

- Topic: Search jump vs. fixed CLI view center
  - Decision: The current implementation keeps the CLI-specified view center when `-A` or `-Z` are explicitly provided, even after a place search result is selected.
  - Rationale: `SkyWindow._jump_to_search_target()` preserves the base `view_center` whenever the explicit-altitude/azimuth flags are set. The visible target highlight is temporary and lasts about 3 seconds, so the UI can look like it "returns" after the highlight fades.
  - Alternatives Considered: None yet. If the intended behavior is for place search to override CLI view-center arguments, that would require a code change in the jump logic.
- Summary
  - Implemented per-search control for keeping CLI Alt/Az in both search dialogs.
  - Verified with `pytest tests/test_place_search_dialog.py tests/test_window_render_sync.py` and targeted `ruff check` on the edited files.

- Topic: README gallery order for added location screenshots
  - Decision: Reordered the `Urban outline and terrain horizon examples from several cities worldwide:` gallery so the images read left-to-right, top-to-bottom in a westward progression from Tokyo, and added the new Mt. Fuji, Cape of Good Hope, and Uyuni screenshots into that flow.
  - Rationale: The new screenshots fit the existing geographic tour better when the gallery is treated as a sequence rather than two unrelated rows. Expanding to three rows keeps the layout balanced while preserving the visual rhythm of three-up image groups.

- Topic: README gallery order from Sydney westward
  - Decision: Switched the gallery to start at Sydney and continue westward across the world: Sydney, Tokyo, Mt. Fuji, Singapore, Dubai, Cape of Good Hope, Jungfraujoch, Barcelona, and Uyuni.
  - Rationale: Starting from Sydney makes the east-to-west longitude progression easier to read because the sequence naturally moves from positive eastern longitudes toward western longitudes. That matches the user's intent better than a Tokyo-first arrangement.

- Topic: Localized README gallery parity
  - Decision: Applied the same Sydney-first westward gallery order to `README_ja-JP.md` and `README_pypi.md`.
  - Rationale: Keeping the localized docs aligned avoids drift between the main README, Japanese README, and PyPI README while preserving the same longitude-based reading order everywhere.

- Topic: Uyuni label normalization
  - Decision: Standardized the English label to `Uyuni Salt Flat` and the Japanese label to `ユウニ塩湖` across the README family.
  - Rationale: `Uyuni Salt Flat` is the clearer English place name for the formation, while `ユウニ塩湖` is the user-preferred Japanese form and reads naturally in the localized docs.

- Topic: Mt. Fuji caption refinement
  - Decision: Renamed the Mt. Fuji screenshot caption to `View of Mt. Fuji` in English and `山中湖から望む富士山` in Japanese.
  - Rationale: The image is a scenic viewpoint rather than a close-up location marker, so a view-oriented caption describes it more accurately without adding unnecessary geographic detail in the English README.
