# Session 2026-05-06

- Scope: Adjust sky disc opacity defaults and document theme-vs-CLI default sources.
- Topic: Sky disc opacity defaults
  - Decision: Changed the CLI `--sky-opacity` default from `0.13` to `0.1` in `src/zstarview/cli/args.py`, and updated the README and CLI test expectations to match.
  - Rationale: The CLI default should be a rounder, simpler value and match the requested behavior without changing the theme preset behavior.
  - Note: The `transparent` theme's sky-disc opacity remains `0.4`; it is defined in `src/zstarview/paths.py` inside the `THEME_STYLES_BY_PRESET["transparent"]` preset, not in the CLI default. Tests passed for the targeted CLI and theme checks.
