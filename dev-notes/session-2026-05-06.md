# Session 2026-05-06

- Scope: Investigate adding a top-left resize grip to the frameless window.

- Topic: Frameless window resize grips
  - Decision: Added a second resize grip in the top-left corner, using the same frameless chrome path as the existing bottom-right grip.
  - Rationale: This preserves the custom frameless affordance while giving users a symmetric resize handle for top-left drags. The manual fallback now adjusts both geometry and position so it behaves correctly even when system resize support is unavailable.
  - Validation: `uv run -p .venv/bin/python -m py_compile src/zstarview/gui/window.py tests/terrain/test_terrain_horizon_ui.py`
    and `uv run -p .venv/bin/python -m pytest tests/terrain/test_terrain_horizon_ui.py -q` passed.

- Topic: Revert top-left grip and add default-size menu action
  - Decision: Removed the top-left resize grip and added a `Default Window Size` action to the file menu and frameless popup menu.
  - Rationale: The top-left grip caused awkward resize behavior in some environments, while a menu action gives the same size-reset capability without introducing another edge-handle interaction.
  - Validation: Updated menu and window-helper tests to cover the new action and the restored default-size path.

- Topic: Version bump
  - Decision: Bumped the package version to `1.21.24`.
  - Rationale: The current change set is ready to tag as a patch-level release.
