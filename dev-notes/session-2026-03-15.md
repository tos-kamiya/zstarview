# Session 2026-03-15

- Scope: Update package metadata after the 1.0 release by changing the PyPI development status classifier from beta to production/stable.

- Topic: PyPI development status after version 1.0
  - Decision: Change the `pyproject.toml` classifier from `Development Status :: 4 - Beta` to `Development Status :: 5 - Production/Stable`.
  - Rationale: The package has reached major version 1, so the published metadata should match the intended release maturity shown on PyPI and other package indexes.

- Topic: README platform support note for Windows Arm64
  - Decision: Add installation notes to `README.md`, `README_ja-JP.md`, and `README_pypi.md` stating that Windows Arm64 is currently unsupported, and mention the observed `shapely` build failure as the reason.
  - Rationale: A real installation attempt on 2026-03-15 failed on Windows Arm64 during native dependency build, so users should see that limitation before trying `pipx install`, including on the PyPI project page.

- Topic: Suppress planet labels when the body marker is off-screen
  - Decision: Only enqueue planet and moon labels when the rendered marker still intersects the current `QPainter` viewport, instead of relying only on the spherical field-of-view check.
  - Rationale: In very wide, low-altitude views, a body can remain inside the 90-degree FOV while its projected marker falls completely outside the actual window. Matching label visibility to marker visibility removes orphaned labels such as off-screen Jupiter/Uranus in low-horizon views.

- Topic: Keep the hamburger menu readable and responsive over heavy overlays
  - Decision: Give the top-right menu button an opaque preset-matched background, explicitly raise overlay widgets after layout changes, and enter viewport-interaction rendering while the menu is open. Menu rotation actions now reuse the same lightweight viewport path as arrow-key view changes.
  - Rationale: The transparent button was hard to read over labels and sky content, and opening the menu while urban outlines were visible kept the full render path active. Reusing the existing simplified render mode improves menu responsiveness without adding a separate interaction state.

- Topic: Cache the final rendered frame between identical paints
  - Decision: Add a final-frame `QImage` cache in the window renderer keyed by geometry, render inputs, overlay-relevant state, and current interaction mode, and draw the cached image directly when nothing changed.
  - Rationale: Qt can still request repainting for widget/menu interactions even when the sky content is unchanged. Reusing the previous fully rendered frame removes repeated heavy layer drawing from those redundant paints without needing to suppress Qt repaint events themselves.

- Topic: Candidate non-Japan cities for urban-outline screenshots
  - Decision: Recommend a small set of non-Japan cities that combine recognizable skyline/building-top variation with likely good contrast against the sky dome, instead of listing only the largest metros.
  - Rationale: Screenshot value depends more on silhouette readability and geographic variety than on population alone. A mixed set across latitude, skyline density, and coastal/desert contexts is more useful for showcasing the Overture-derived urban outline feature.

- Topic: English README screenshot gallery placement
  - Decision: Keep the existing two large introductory screenshots unchanged and insert a compact four-image city gallery directly below them in `README.md`.
  - Rationale: The large images still explain the UI best, while the smaller gallery immediately demonstrates geographic variety without pushing the installation section too far down the page.

- Topic: Final city set for README screenshot gallery
  - Decision: Use Tokyo, Downtown Dubai, Marina Bay, and Circular Quay (Sydney) for the compact screenshot gallery, and update the English, Japanese, and PyPI READMEs accordingly.
  - Rationale: This set keeps strong geographic spread while avoiding city-center viewpoints that leave too little visible sky. Sydney also replaces a misidentified Auckland/Brisbane caption with a clearer and more recognizable location.

- Summary:
  - Updated the package classifier in `pyproject.toml`.
  - Added Windows Arm64 installation limitation notes to the English, Japanese, and PyPI READMEs.
  - Suppressed solar-system labels when the body marker is fully outside the window viewport.
  - Added a render test covering the off-screen planet-label case.
  - Made the hamburger menu button opaque and moved menu interactions onto the simplified viewport render path.
  - Added a final-frame cache so identical repaint requests reuse the previous rendered image.
  - Reviewed non-Japan city candidates suitable for urban-outline screenshots.
  - Added a compact four-city screenshot gallery to the English README.
  - Synced the English, Japanese, and PyPI screenshot galleries to use Sydney as the fourth city.
