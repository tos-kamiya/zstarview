# Session 2026-04-13

- Topic: Version bump
  - Decision: Bump the package version from `1.9.12` to `1.9.13`.
  - Rationale: The change is a patch-level release bump requested after the theme outline-width fix.
- Topic: Theme label outline width check
  - Decision: `day` and `white` now use the same `3.0` outline width as `night` and `black` for outlined labels.
  - Rationale: The label rendering helpers resolve outline width directly from the theme preset, so matching the preset values removes the visible thickness difference without changing the rest of the theme colors.
- Topic: Color palette alignment for overlay rendering
  - Decision: Re-map the main overlay colors to the four swatches from `color-palette.png`: earth guide and terrain horizon use the first swatch, aircraft and satellite markers use the second, horizon lines and direction labels use the third, and asterisms use the fourth.
  - Rationale: This keeps the visual language consistent across the main navigation overlays while preserving the existing layer structure and opacity behavior.
- Topic: Ground-tone follow-up
  - Decision: Move the terrain horizon and earth guide to the olive fifth swatch from `color-palette.png`.
  - Rationale: The ground-related layers read more naturally as a separate family from the sky-facing overlays, while keeping terrain and earth guide visually matched.
- Topic: Sixth-swatch trial
  - Decision: Move the terrain horizon and earth guide again, this time to the sixth swatch from `color-palette.png`.
  - Rationale: This lets us compare a darker, more muted ground tone against the previous olive tone while keeping both layers linked.
- Topic: Equator/ecliptic prominence
  - Decision: Increase the line width of the celestial equator and ecliptic slightly, without changing the horizon line thickness.
  - Rationale: The extra width restores visibility for the two reference lines most likely to get lost against the background while preserving the existing horizon styling.
- Topic: Asterism de-emphasis
  - Decision: Thin the base and highlighted asterism strokes slightly.
  - Rationale: The asterism overlay was too visually dominant after the color changes, so reducing the stroke widths keeps the pattern readable without competing with the main reference layers.
- Topic: Highlight line parity
  - Decision: Match the highlighted asterism's bright inner stroke to the urban outline line width.
  - Rationale: This keeps the hover emphasis visible, but not heavier than the other structural sky layers that use the same width class.
- Topic: Palette reference docs
  - Decision: Add the full `color-palette.png` RGB table to the design document for future palette tuning.
  - Rationale: The palette now drives multiple overlay families, so keeping the exact swatches in the design doc reduces guesswork during later visual adjustments.
- Topic: Spec color roles
  - Decision: Add a brief user-facing summary of the palette roles to the specification document.
  - Rationale: The spec should describe the visible role of each palette family without repeating the full RGB reference table from the design doc.
- Topic: README color note
  - Decision: Update the README terrain-horizon / earth-guide description to reflect the new dark ground tone.
  - Rationale: The README should describe the visible effect users now see, especially where the ground-related layers no longer read as the older ocher style.
- Topic: Never-rises tint
  - Decision: Rebase the never-rises tint on the first palette swatch so it matches the palette-driven ground family.
  - Rationale: This keeps the under-horizon warning color aligned with the same ground-tone palette used by the earth guide and terrain horizon.
- Topic: Palette correction
  - Decision: Use the top-left palette swatch specifically for the never-rises tint, and keep the earth guide / terrain horizon on the dark ground-tone swatch.
  - Rationale: The never-rises warning should stand out as its own warm accent instead of blending into the ground family.
- Topic: Never-rises regression
  - Decision: Update the compositor/sky-disc tests to match the new palette-driven never-rises tint and add a direct constant check.
  - Rationale: The tint change affects both the raw sky-disc path and the compositor path, so the tests should lock down the new shared color instead of a derived sample alone.
- Topic: README Japanese palette text
  - Decision: Mirror the README palette wording updates into `README_ja-JP.md`, using the current dark ground tone for terrain/earth-guide text and the top-left warm swatch for never-rises.
  - Rationale: The Japanese README should describe the same visible colors as the English README so the docs stay aligned when screenshots are refreshed.
- End Summary: Updated the shared palette constants and the dependent overlay renderers, moved the ground-related layers from the olive fifth swatch to the sixth swatch for comparison, slightly thickened the celestial equator and ecliptic, then thinned the asterism strokes a bit and matched the highlight stroke to the urban-outline width, and verified the affected tests and lint checks.
- Scope: Adjust the urban outline rendering color to be slightly bluer while keeping the existing white-ish look.
- Summary: Updated `URBAN_OUTLINE_LAYER_LINE_COLOR` from `(246, 249, 255)` to `(235, 242, 255)` in `src/zstarview/paths.py`.
- Update: Shifted `URBAN_OUTLINE_LAYER_LINE_COLOR` further toward a cool LED-like blue, from `(235, 242, 255)` to `(214, 232, 255)`.
- Update: Changed the variable-width cloud stripe fade curve from linear to a gentler ease-out so alpha declines more slowly away from the baseline.
- Topic: Overture cache refresh behavior
  - Decision: Document that urban-outline caches may be refreshed immediately when the upstream Overture release changes, while preserving offline fallback behavior.
  - Rationale: TTL alone does not detect upstream data publication changes. Recording the release identifier alongside `fetched_at_utc` keeps the runtime behavior aligned with the latest published Overture data without breaking offline reuse.
- Topic: Overture release check cadence
  - Decision: Limit release checks to startup or initial enablement, but skip the check when the last successful lookup is within 24 hours.
  - Rationale: This keeps the cache refresh path responsive to upstream publication changes without adding unnecessary catalog traffic on every launch.
- Topic: Overture release metadata layout
  - Decision: Store `overture_release` alongside each urban-outline cache entry, and keep `last_release_check_utc` / `last_seen_overture_release` in a root-level release-check metadata file.
  - Rationale: Per-cache release IDs make stale detection local to each dataset or tile, while the shared timestamp avoids duplicating one network-check result across every cache directory.
- Topic: Overture release metadata schema
  - Decision: Use `cache_meta.json` sidecars for dataset-level metadata and `overture_buildings_release_check_meta.json` at the cache root for release lookup state.
  - Rationale: The split keeps the cache-key metadata colocated with each derived dataset while preserving a single 24-hour suppression clock for release catalog checks.
- Topic: Overture release metadata failure mode
  - Decision: If release metadata is missing or unreadable, keep the cache and fall back to TTL-based stale handling instead of deleting data.
  - Rationale: Release metadata is advisory. Treating it as required would turn a small metadata problem into an unnecessary full cache invalidation.
- Topic: Overture release check implementation flow
  - Decision: Read the root-level release-check metadata first, skip the release catalog query when the last check is within 24 hours, and update `overture_release` in dataset sidecars only after successful refresh.
  - Rationale: This minimizes catalog traffic, keeps refresh decisions local to each dataset or tile, and preserves the existing TTL-based fallback path when release metadata is missing or stale.
- Topic: Legacy cache timestamp fallback
  - Decision: For caches missing `fetched_at_utc`, use `cache_meta.json` mtime first and `bldg` directory mtime second as the initial freshness timestamp.
  - Rationale: This is closer to the actual cache update time than migrating everything to `now`, while still avoiding a destructive invalidation of old caches.
