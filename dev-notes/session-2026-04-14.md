# Session 2026-04-14

- Topic: Himawari partial-slot policy clarification
  - Decision: Update the documentation to state that Himawari ISatSS data can be used when the tiles needed for observer-specific rendering and warm-threshold estimation are present, even if the slot does not contain all 88 tiles.
  - Rationale: The current implementation is stricter than necessary because it rejects a slot before checking which tiles are actually required. The docs should reflect the intended behavior before the code is relaxed.

- Summary: Commit `183c99c`
  - Scope: Updated `docs/specification.md`, `docs/design.md`, and `docs/implementation-history.md` to describe the partial-slot acceptance policy for Himawari ISatSS data.
  - Follow-up: Implement the slot selection change in `src/zstarview/clouddisc/providers/hima.py` so only required tiles gate acceptance.
