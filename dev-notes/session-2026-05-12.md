# Session 2026-05-12

- Scope: Investigate Dependabot PR #4 for dependency vulnerability updates.
- Topic: Dependabot PR #4 (pillow vulnerability)
  - Decision: No repository change was needed; current `uv.lock` already pins `pillow` to 12.2.0, matching the PR fix.
  - Rationale: The local `main` branch already contains the lockfile update in commit `8ac4d64`, so the vulnerable version is no longer present in this workspace.
# Session 2026-05-12

- Scope: Confirm whether OpenSky aircraft timeouts are swallowed by the GUI path or surface as uncaught errors.
- Topic: Aircraft OpenSky timeout logging
  - Decision: Removed traceback emission for aircraft fetch failures and kept a plain warning log only.
  - Rationale: The timeout is already handled by the controller and reflected in the UI; the full traceback was noisy for a recoverable network failure.
  - Validation: `uv run -p .venv/bin/python pytest tests/test_controller_shutdown_waits.py -q`
