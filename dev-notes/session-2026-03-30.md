# Session 2026-03-30

- Scope: Continue window frame and resize affordance adjustments.

- Topic: Hide the top-left observation overlay by default and expose it from the menu
  - Decision: Added a dedicated GUI toggle for the static top-left observation overlay and changed the main window default to hidden.
  - Rationale: The location/time/Alt-Az/Vmag block competes with the sky view, while the existing render pipeline already supports conditional overlay drawing with a narrow UI-side change.

- Topic: Restore observation overlay default visibility and move it to the bottom-left
  - Decision: Changed the observation overlay default back to visible and anchored its static text block to the window's bottom-left margin instead of the top-left.
  - Rationale: This preserves quick access to observation context while keeping it out of the upper sky area and aligned with the requested layout.

- Topic: Reorder observation overlay lines and move `Vmag limit` into the menu
  - Decision: Changed the on-screen observation overlay order to `Alt/Az`, local time, then location-related lines, and removed `Vmag limit` from the HUD in favor of a disabled menu item.
  - Rationale: The requested order prioritizes immediate pointing context, while `Vmag limit` is still visible without consuming overlay space.

- Topic: Make observation overlay dodge the cursor by switching corners
  - Decision: Restored the default anchor to the top-left, changed the on-screen order to location, time, then Alt/Az, and made the overlay move to the bottom-left whenever the mouse is in the window's upper half.
  - Rationale: This keeps the observation block available by default, preserves the menu toggle, and avoids covering the upper sky area under active pointer use without fully hiding the overlay.

- Topic: Add hysteresis to observation overlay corner switching
  - Decision: Changed the corner-switch rule so the overlay moves only when the cursor enters the top third or bottom third of the window, and keeps the previous corner while the cursor stays in the middle third.
  - Rationale: This removes rapid corner flipping around the vertical midpoint while preserving the same top-left and bottom-left escape behavior.


- Topic: Fix the corner-triangle resize marker to a constant size
  - Decision: Set the bottom-right triangle marker to a fixed size of 12 pixels instead of deriving it from frame width.
  - Rationale: Theme-dependent frame widths made the resize marker look inconsistent between presets, especially day versus white.

- 00:01 Ran: `uv run -p .venv/bin/python pytest tests/test_sky_disc_viewport_and_ground_tint.py tests/terrain/test_terrain_horizon_ui.py tests/test_window_render_sync.py`
  Output:
  ```text
  72 passed in 1.38s
  ```
  Result: Fixed-size triangle marker preserved the targeted rendering and UI behavior.

- Topic: Draw the hamburger menu background as part of the frame instead of widget styling
  - Decision: Made the menu button widget transparent and added a top-right square panel in the frame renderer, using the same frame color/alpha as the resize affordance marker.
  - Rationale: This matches the resize indicator approach, removes the rounded-corner button treatment, and makes the menu background read as part of the window frame.

- 00:08 Ran: `uv run -p .venv/bin/python pytest tests/test_sky_disc_viewport_and_ground_tint.py tests/terrain/test_terrain_horizon_ui.py tests/test_window_render_sync.py`
  Output:
  ```text
  73 passed in 1.35s
  ```
  Result: Frame-drawn menu background and transparent button styling passed the targeted tests.
- Topic: Align the top-left location label top margin with its left margin
  - Decision: Adjusted the overlay-info baseline calculation so the first line's visible top edge sits at the same inset as the left edge.
  - Rationale: The city label was using the same horizontal inset but a smaller effective top inset because the baseline started too close to the top edge.

- 00:15 Ran: `uv run -p .venv/bin/python pytest tests/test_planet_marker_style.py tests/test_window_render_sync.py`
  Output:
  ```text
  43 passed in 1.54s
  ```
  Result: Overlay-info margin adjustment passed the targeted tests.
- Topic: Bump application patch version
  - Decision: Incremented the main package version from `1.7.6` to `1.7.7` in `src/zstarview/__about__.py`.
  - Rationale: The package version is sourced dynamically from `__about__.py`, so the requested patch bump only needs the single source-of-truth update.
- Topic: Replace the hamburger glyph with drawn menu lines
  - Decision: Removed the `☰` button label and drew the three hamburger lines directly inside the frame-rendered top-right menu square.
  - Rationale: The glyph size depended on font rendering, while explicit line drawing keeps the menu icon dimensions stable across fonts and platforms.

- 00:22 Ran: `uv run -p .venv/bin/python pytest tests/test_sky_disc_viewport_and_ground_tint.py tests/terrain/test_terrain_horizon_ui.py tests/test_window_render_sync.py`
  Output:
  ```text
  74 passed in 1.41s
  ```
  Result: Drawn hamburger lines passed the targeted rendering and UI tests.
- Topic: Move the static top-left overlay text above object labels
  - Decision: Reordered the base-scene pipeline so the static overlay info is drawn after the label layer instead of before it.
  - Rationale: Moon and object labels could overlap the location/time text, so the static top-left overlay now renders last and stays readable.

- 00:31 Ran: `uv run -p .venv/bin/python pytest tests/test_window_render_sync.py tests/test_planet_marker_style.py`
  Output:
  ```text
  43 passed in 1.59s
  ```
  Result: Overlay-order change passed the targeted rendering tests.
- Topic: Hide the static top-left overlay when the mouse enters its vertical band
  - Decision: Added a coarse hover suppression rule that skips drawing the static overlay text when the mouse `y` position falls within the overlay's vertical range.
  - Rationale: Exact hit testing was unnecessary; a simple vertical-band check is enough to get the text out of the way when the cursor approaches that area.

- 00:42 Ran: `uv run -p .venv/bin/python pytest tests/test_window_render_sync.py tests/test_planet_marker_style.py`
  Output:
  ```text
  44 passed in 1.71s
  ```
  Result: Mouse-triggered overlay suppression passed the targeted tests.
- Topic: Move the static top-left overlay out of the cached base frame
  - Decision: Removed the static overlay from the base-scene pass and now draw it in the HUD pass after hover overlays.
  - Rationale: The overlay hide/show response depended on mouse movement, so keeping it inside the cached frame made it lag behind hover updates.

- 00:49 Ran: `uv run -p .venv/bin/python pytest tests/test_window_render_sync.py tests/test_planet_marker_style.py`
  Output:
  ```text
  44 passed in 1.57s
  ```
  Result: HUD-side overlay rendering passed the targeted tests.
- Topic: Restore hover feedback for the top-right hamburger hit area
  - Decision: Added widget-side hover/pressed background highlights and a pointing-hand cursor to the transparent hamburger button.
  - Rationale: After moving the visible menu panel into the frame renderer, the button itself lost visible hover feedback even though the click target still existed.

- 01:03 Ran: `uv run -p .venv/bin/python pytest tests/terrain/test_terrain_horizon_ui.py tests/test_window_render_sync.py`
  Output:
  ```text
  59 passed in 1.32s
  ```
  Result: Hover-feedback restoration passed the targeted UI tests.
- Topic: Bump application patch version again
  - Decision: Incremented the main package version from `1.7.7` to `1.7.8` in `src/zstarview/__about__.py`.
  - Rationale: The requested patch bump is still sourced from the single version definition in `__about__.py`.

- Topic: Remove the `transparent` option from theme presets
  - Decision: Deleted `transparent` from CLI theme parsing and removed its preset-specific rendering/style branches, while keeping generic transparent drawing behavior untouched.
  - Rationale: The supported theme list should now be limited to `night`, `day`, `white`, and `black`, so retaining a hidden fifth preset would leave dead paths and misleading tests.

- 10:39 Ran: `uv run -p .venv/bin/python pytest tests/test_theme_parse.py tests/test_sky_disc_viewport_and_ground_tint.py tests/terrain/test_terrain_horizon_ui.py`
  Output:
  ```text
  47 passed in 1.38s
  ```
  Result: Theme removal did not regress the targeted parser, rendering, or terrain UI behavior.

- Topic: Align specification and design docs with the reduced theme set
  - Decision: Updated the user-facing specification and internal design documents to state that supported theme presets are `night`, `day`, `white`, and `black`.
  - Rationale: The code no longer accepts `transparent` as a theme preset, so both documents need to describe the same supported set to avoid user confusion and stale internal guidance.

- Topic: Align README with the reduced theme set
  - Decision: Removed `transparent` from the README CLI option table and the theme-preset overview section.
  - Rationale: README is the primary quick-reference document for users, so it must match the currently accepted theme presets and avoid advertising unsupported options.

- Topic: Align localized and PyPI README variants with the reduced theme set
  - Decision: Removed `transparent` from `README_ja-JP.md` and `README_pypi.md`, keeping their advertised `--theme` values aligned with the English README and the CLI parser.
  - Rationale: Users should see the same supported theme set regardless of which README variant they read, and PyPI metadata should not advertise an unsupported option.

- Topic: Bump application patch version
  - Decision: Incremented the main package version from `1.7.8` to `1.7.9` in `src/zstarview/__about__.py`.
  - Rationale: The requested `+0.0.1` bump is sourced from the single application version definition.

- Topic: Reconcile mountain-viewpoint TODO with existing implementation records
  - Decision: Updated `docs/implementation-history.md` so the mountain-viewpoint TODO refers to documenting the dataset generation/update flow, and added implementation-history entries summarizing the already completed mountain dataset integration from prior `dev-notes`.
  - Rationale: The mountain resolver and bundled dataset are already implemented, so leaving the old TODO as if the feature itself were still pending was misleading. The remaining gap is documenting the generation workflow cleanly.

- Topic: Split render background and guides code out of `draw.py`
  - Decision: Moved window background/frame rendering into `src/zstarview/render/background.py` and moved sky-guide helpers into `src/zstarview/render/guides.py`, while keeping backward-compatible re-exports in `render.draw`.
  - Rationale: `draw.py` had grown past two thousand lines and mixed unrelated responsibilities. Extracting the static window/guide logic first reduces file size with low behavior risk and keeps existing imports stable during the transition.

- Topic: Preserve `render.draw` test monkeypatch behavior after guide extraction
  - Decision: Kept `draw_sky_reference_lines()` as a thin wrapper in `render.draw` that forwards to `render.guides.draw_sky_reference_lines()` while injecting the current `render.draw` projection helpers.
  - Rationale: Some focused tests monkeypatch `render.draw.is_in_fov` and `render.draw.altaz_to_normalized_xy`. Re-exporting the moved function directly would bypass those patches and break test expectations even though runtime behavior was unchanged.

- Topic: Update window-frame pixel tests to the current frame width
  - Decision: Adjusted the bottom-right grip and top-right menu pixel assertions in `tests/test_sky_disc_viewport_and_ground_tint.py` to match the current `frame_width = 16` rendering.
  - Rationale: The old assertions assumed narrower right/top frame coverage and were already stale relative to the committed frame rendering, so they no longer described the intended output after the recent frame adjustment.

- 13:28 Ran: `uv run -p .venv/bin/python ruff check src/zstarview/render/draw.py src/zstarview/render/background.py src/zstarview/render/guides.py tests/test_sky_disc_viewport_and_ground_tint.py --select F401,F821`
  Output:
  ```text
  All checks passed!
  ```
  Result: The extracted modules and compatibility layer were free of missing-name and unused-import issues.

- 13:29 Ran: `uv run -p .venv/bin/python pytest tests/test_sky_disc_viewport_and_ground_tint.py tests/test_window_render_sync.py tests/terrain/test_terrain_horizon_ui.py`
  Output:
  ```text
  75 passed in 1.42s
  ```
  Result: Background/guide extraction and the updated frame assertions passed the targeted rendering and UI tests.

- Topic: Extract `draw_overlay_info()` without breaking test patch points
  - Decision: Moved the overlay-info implementation into `src/zstarview/render/overlay_info.py` and kept `render.draw.draw_overlay_info()` as a thin wrapper that injects text/dso helpers.
  - Rationale: The overlay renderer has a separate responsibility from star/planet/terrain drawing, but several focused tests monkeypatch `render.draw.draw_outlined_text` and related helpers. Keeping the wrapper preserves those patch points while still shrinking `draw.py`.

- 13:37 Ran: `uv run -p .venv/bin/python ruff check src/zstarview/render/draw.py src/zstarview/render/overlay_info.py --select F401,F821`
  Output:
  ```text
  All checks passed!
  ```
  Result: The overlay-info extraction introduced no missing-name or unused-import issues.

- 13:38 Ran: `uv run -p .venv/bin/python pytest tests/test_planet_marker_style.py tests/test_window_render_sync.py tests/test_sky_disc_viewport_and_ground_tint.py tests/terrain/test_terrain_horizon_ui.py`
  Output:
  ```text
  89 passed in 1.70s
  ```
  Result: Overlay-info extraction preserved the focused rendering and monkeypatch-based behavior tests.

- Topic: Extract terrain and urban-outline renderers with patch-compatible wrappers
  - Decision: Moved terrain-horizon and urban-outline drawing into `src/zstarview/render/terrain.py`, while keeping `render.draw` wrappers that inject projection and clipping helpers.
  - Rationale: These functions are a separate rendering concern and have relatively shallow dependencies, but focused tests monkeypatch `render.draw` projection helpers directly. Injecting those helpers from the wrapper keeps the existing test seam intact while continuing to shrink `draw.py`.

- 13:47 Ran: `uv run -p .venv/bin/python ruff check src/zstarview/render/draw.py src/zstarview/render/terrain.py --select F401,F821`
  Output:
  ```text
  All checks passed!
  ```
  Result: Terrain extraction left no missing-name or unused-import issues.

- 13:48 Ran: `uv run -p .venv/bin/python pytest tests/test_window_render_sync.py tests/terrain/test_terrain_horizon_ui.py tests/test_sky_disc_viewport_and_ground_tint.py`
  Output:
  ```text
  75 passed in 1.49s
  ```
  Result: Terrain and urban-outline extraction preserved targeted rendering, UI, and monkeypatch-based tests.

- Topic: Remove `render.draw` compatibility re-exports after module extraction
  - Decision: Switched production callers and focused tests to import `background`, `guides`, `overlay_info`, `terrain`, `geometry`, and `text` directly, then removed the extracted-function re-exports and wrappers from `render.draw`.
  - Rationale: Keeping all extracted entry points mirrored through `render.draw` was turning that file into a compatibility barrel and obscuring its actual ownership. Moving callers to the real modules made the refactor complete and reduced `draw.py` to the remaining star/planet/asterism rendering responsibilities.

- Topic: Replace wrapper-based monkeypatch seams with explicit dependency injection in tests
  - Decision: Updated focused tests to patch or pass helper functions directly to the extracted modules instead of relying on `render.draw` wrappers to forward monkeypatches.
  - Rationale: Once the wrappers were removed, tests needed to target the real module boundaries. Passing injected helpers explicitly keeps those tests precise without reintroducing broad compatibility glue into production code.

- 14:06 Ran: `uv run -p .venv/bin/python ruff check src/zstarview/render/draw.py src/zstarview/render/pipeline.py src/zstarview/gui/window_render.py src/zstarview/gui/window.py src/zstarview/gui/sky_worker.py src/zstarview/cli/export_image.py tests/test_window_render_sync.py tests/test_planet_marker_style.py tests/test_sky_disc_viewport_and_ground_tint.py src/zstarview/data/test_draw_sky_disc.py --select F401,F821`
  Output:
  ```text
  All checks passed!
  ```
  Result: Removing the compatibility exports and updating call sites introduced no missing-name or unused-import issues.

- 14:07 Ran: `uv run -p .venv/bin/python pytest tests/test_window_render_sync.py tests/test_planet_marker_style.py tests/test_sky_disc_viewport_and_ground_tint.py tests/terrain/test_terrain_horizon_ui.py`
  Output:
  ```text
  89 passed in 1.64s
  ```
  Result: Direct module imports and the reduced `render.draw` surface preserved the focused rendering and UI behavior tests.

- Topic: Extract solar-system rendering from `render.draw`
  - Decision: Moved moon, planet, and hovered-moon rendering into `src/zstarview/render/solar_system.py`, and updated `render.pipeline` plus focused planet tests to call that module directly.
  - Rationale: Solar-system rendering is a coherent subsystem with its own photometry, moon-phase, and label behavior. Pulling it out removes another large responsibility block from `draw.py` and clarifies the remaining file as mostly star/DSO/asterism rendering.

- Topic: Retarget planet tests to the solar-system module
  - Decision: Updated focused tests to patch `render.solar_system` helpers such as `draw_planet_disc`, `draw_planet_bloom`, `draw_moon`, `draw_gauge_cross`, and `draw_outlined_text` instead of patching `render.draw`.
  - Rationale: After the extraction, patching `render.draw` would no longer affect the production call path. Moving the patch points to the actual module keeps the tests aligned with the implementation boundary.

- 14:17 Ran: `uv run -p .venv/bin/python ruff check src/zstarview/render/draw.py src/zstarview/render/solar_system.py src/zstarview/render/pipeline.py tests/test_planet_marker_style.py tests/test_window_render_sync.py --select F401,F821`
  Output:
  ```text
  All checks passed!
  ```
  Result: The solar-system extraction introduced no missing-name or unused-import issues in the touched modules.

- 14:18 Ran: `uv run -p .venv/bin/python pytest tests/test_planet_marker_style.py tests/test_window_render_sync.py tests/test_sky_disc_viewport_and_ground_tint.py tests/terrain/test_terrain_horizon_ui.py`
  Output:
  ```text
  89 passed in 1.57s
  ```
  Result: Solar-system extraction preserved the focused render, hover, and UI tests.

- Topic: Extract satellite and aircraft overlays from `render.draw`
  - Decision: Moved the satellite and aircraft overlay renderers into `src/zstarview/render/overlays.py` and updated the pipeline plus focused overlay test to target that module directly.
  - Rationale: These overlays form a separate layer with shallow dependencies on projection, text style, and gauge markers. Extracting them further reduces `draw.py` without introducing complicated cross-module ownership.

- 14:26 Ran: `uv run -p .venv/bin/python ruff check src/zstarview/render/draw.py src/zstarview/render/overlays.py src/zstarview/render/pipeline.py tests/test_planet_marker_style.py --select F401,F821`
  Output:
  ```text
  All checks passed!
  ```
  Result: Overlay extraction introduced no missing-name or unused-import issues in the touched files.

- 14:27 Ran: `uv run -p .venv/bin/python pytest tests/test_planet_marker_style.py tests/test_window_render_sync.py tests/test_sky_disc_viewport_and_ground_tint.py tests/terrain/test_terrain_horizon_ui.py`
  Output:
  ```text
  89 passed in 1.63s
  ```
  Result: Satellite and aircraft overlay extraction preserved the focused rendering and UI tests.

- Topic: Rename the temporary overlay module into concrete satellite and aircraft modules
  - Decision: Replaced the temporary `render/overlays.py` split with two concrete modules: `render/satellites.py` and `render/aircraft.py`.
  - Rationale: “Overlays” was too abstract for the actual ownership. Satellite and aircraft rendering are operationally separate concerns with distinct data sources and styles, so naming the modules after the objects they render is clearer than introducing a broad artificial-object bucket.

- 14:33 Ran: `uv run -p .venv/bin/python ruff check src/zstarview/render/pipeline.py src/zstarview/render/satellites.py src/zstarview/render/aircraft.py tests/test_planet_marker_style.py --select F401,F821`
  Output:
  ```text
  All checks passed!
  ```
  Result: Renaming the temporary overlay module and switching imports introduced no missing-name or unused-import issues.

- 14:34 Ran: `uv run -p .venv/bin/python pytest tests/test_planet_marker_style.py tests/test_window_render_sync.py tests/test_sky_disc_viewport_and_ground_tint.py tests/terrain/test_terrain_horizon_ui.py`
  Output:
  ```text
  89 passed in 1.63s
  ```
  Result: The concrete `satellites.py` / `aircraft.py` split preserved the focused rendering and UI behavior tests.

- Topic: Extract DSO rendering into `render/deep_sky_objects.py`
  - Decision: Moved named-DSO shape generation, hover hit testing, filled-shape rendering, and DSO hover rendering into `src/zstarview/render/deep_sky_objects.py`, then pointed `render.pipeline`, `gui.window_render`, and `render.overlay_info` directly at that module.
  - Rationale: DSO rendering had become a self-contained subsystem with its own geometry helpers, constants, and hover behavior. Pulling it out reduced `draw.py` substantially and removed the need to thread DSO-specific helpers through `overlay_info`.

- Topic: Extract asterism rendering into `render/asterisms.py`
  - Decision: Moved `draw_asterisms()` into `src/zstarview/render/asterisms.py` and switched `render.pipeline` plus focused asterism tests to call that module directly, while leaving `find_highlighted_object()` in `render.draw`.
  - Rationale: The draw path for asterisms is coherent and now independent, but hover selection still belongs with the star-highlight search in `draw.py`. Splitting only the renderer keeps ownership clear without forcing a broader highlight-selection refactor.

- 14:49 Ran: `uv run -p .venv/bin/python ruff check src/zstarview/render/draw.py src/zstarview/render/deep_sky_objects.py src/zstarview/render/asterisms.py src/zstarview/render/overlay_info.py src/zstarview/render/pipeline.py src/zstarview/gui/window_render.py tests/test_planet_marker_style.py tests/test_asterism_render.py tests/test_window_render_sync.py --select F401,F821`
  Output:
  ```text
  All checks passed!
  ```
  Result: The DSO and asterism extractions introduced no missing-name or unused-import issues in the touched modules and tests.

- 14:49 Ran: `uv run -p .venv/bin/python pytest tests/test_planet_marker_style.py tests/test_asterism_render.py tests/test_window_render_sync.py tests/test_sky_disc_viewport_and_ground_tint.py tests/terrain/test_terrain_horizon_ui.py`
  Output:
  ```text
  95 passed in 1.66s
  ```
  Result: Focused rendering, hover, and UI tests still pass after the DSO and asterism modules were split out of `render.draw`.

- Topic: Rename the remaining star renderer module from `draw.py` to `stars.py`
  - Decision: Renamed `src/zstarview/render/draw.py` to `src/zstarview/render/stars.py`, switched production callers to import `render.stars` directly, and retargeted tests that still referenced `render.draw`.
  - Rationale: After background, guide, terrain, solar-system, aircraft, satellite, DSO, and asterism extraction, the remaining module was overwhelmingly star rendering and object-picking logic. `stars.py` matches the ownership better than the old abstract `draw.py` name and avoids reintroducing a compatibility barrel.

- Topic: Rename the sky-disc renderer module from `draw_sky_disc.py` to `sky_disc.py`
  - Decision: Renamed `src/zstarview/render/draw_sky_disc.py` to `src/zstarview/render/sky_disc.py` and switched GUI, data utilities, and tests to import `render.sky_disc` directly.
  - Rationale: The sky-disc renderer is a coherent module like `background.py`, `guides.py`, and `stars.py`. Dropping the `draw_` prefix makes the render package naming consistent around ownership rather than action names.

- Topic: Reconcile `design.md` with renamed render modules
  - Decision: Updated `docs/design.md` so its render-module inventory now points to `stars.py`, `sky_disc.py`, `asterisms.py`, `guides.py`, and `satellites.py`.
  - Rationale: The document still referred to pre-refactor module names and grouped responsibilities that have already been split in code. Correcting the module references keeps the design document aligned with the current renderer layout.

- Topic: Mark internal-only render helpers with `_` prefixes
  - Decision: Renamed `render.pipeline` stage helpers, `render.sky_disc.grade_color`, and several NumPy-only helpers in `gui.composite` to `_`-prefixed names, and updated focused tests to patch the internal names directly where needed.
  - Rationale: After removing the compatibility re-exports, these functions were still public in name only and were being kept open mostly for tests. Giving them internal names makes the module boundaries reflect the actual API surface and clarifies which functions are implementation details.

- Topic: Bump package version after renderer refactors
  - Decision: Updated the package version from `1.7.9` to `1.8.0`.
  - Rationale: The render package layout, module naming, and internal API boundaries changed materially across this session, so a patch-level bump would understate the amount of maintenance-facing change.
