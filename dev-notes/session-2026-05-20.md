# 2026-05-20 Session Notes

- Scope: trim rendering compatibility shims and simplify test-facing
  context construction.
- Topic: view-center dialog repaint recovery
  - Decision: when `Set View Center...` or a search jump changes the
    view, enter viewport interaction mode long enough to paint one fast
    frame, then schedule a release on the next event-loop turn.
  - Rationale: the dialog and search-jump paths should mirror the
    keyboard interaction feel by showing the simplified frame once
    before the full redraw. Deferring the release with
    `QTimer.singleShot(0, ...)` gives the UI a chance to present the
    fast frame and then transition back to the normal renderer without
    waiting for a human key-up event.
  - Validation: added a regression test in
    `tests/test_window_render_sync.py` and
    `tests/terrain/test_terrain_horizon_ui.py`, then ran the targeted
    pytest subset plus Ruff on the touched files.
- Topic: `FrameContext` follow-up cleanup
  - Decision: remove the thin `radec_to_altaz()` shim from
    `gui/window.py`, keep `FrameContext` as the shared render input, and
    reduce the test-compatibility `getattr()` paths that had accumulated
    in `window_render.py` and `export_image.py`.
  - Rationale: the shim had no behavior of its own, and the render
    context now has enough structure that direct attribute access is
    clearer than defensive fallback access in the common code paths.
- Topic: export-image and render test alignment
  - Decision: update the layer-gating and window-render sync tests to
    provide the fields that production code now reads directly.
  - Rationale: keeping tests aligned with the production dataclass shape
    removes the need for extra compatibility branches.
- Validation: `uv run -p .venv/bin/python pytest
  tests/test_export_image_layer_gating.py tests/test_window_render_sync.py
  tests/test_viewer_startup_errors.py tests/test_place_search_dialog.py`
  and `uv run -p .venv/bin/python ruff check
  src/zstarview/gui/window_render.py src/zstarview/cli/export_image.py
  tests/test_window_render_sync.py tests/test_export_image_layer_gating.py`
- Summary: prepared the cleanup for commit after the test suite passed;
  unrelated untracked workspace files were left untouched.
- Topic: export-image defensive `getattr()` cleanup
  - Decision: remove the remaining clearly redundant `getattr()` calls
    in `src/zstarview/cli/export_image.py` and switch them to direct
    attribute access for CLI args, resolved locations, prepared catalogs,
    celestial data, and water points.
  - Rationale: `parse_export_image_args()` and the dataclasses involved
    already guarantee those fields in production, so the fallback reads
    were mostly masking the real data contract. Direct access makes the
    export path easier to read and keeps the CLI aligned with the typed
    inputs used elsewhere.
  - Exceptions: keep the `use_building_top` and `height_add_m`
    fallbacks because the lightweight test stubs used by the layer-gating
    tests do not provide those attributes.
  - Validation: `uv run -p .venv/bin/python ruff check
    src/zstarview/cli/export_image.py tests/test_export_image_layer_gating.py`
    and `uv run -p .venv/bin/python pytest
    tests/test_cli_export_image_args.py tests/test_export_image_layer_gating.py`
- Topic: internal GUI state `getattr()` cleanup
  - Decision: remove compatibility-oriented `getattr()` calls from
    `src/zstarview/gui/window.py` and `src/zstarview/gui/window_updates.py`
    where the code is reading internal dataclass fields or private window
    state that already exists in production.
  - Rationale: these reads were masking the real contracts between the
    window shell, render/update mixins, and `SkyWindowState`. Direct
    access makes the expected state shape explicit and reduces the amount
    of test-only fallback logic in the core code paths.
  - Exceptions: keep `getattr()` for external or intentionally dynamic
    inputs such as Qt event objects, widget hooks, controller feature
    probes, and legacy refresh-field compatibility in projection helpers.
  - Validation: `uv run -p .venv/bin/python ruff check
    src/zstarview/gui/window.py src/zstarview/gui/window_updates.py
    tests/test_window_render_sync.py` and `uv run -p .venv/bin/python
    pytest tests/test_window_render_sync.py`
- Topic: test stub alignment after GUI cleanup
  - Decision: update `tests/terrain/test_terrain_horizon_ui.py` and
    `tests/test_export_image_sixel.py` to provide the concrete internal
    attributes now read directly by production code.
  - Rationale: the cleanup removed test-compatibility branches from the
    production path, so the remaining failures were due to sparse
    `SimpleNamespace` stubs in targeted tests rather than behavior
    regressions.
  - Validation: `uv run -p .venv/bin/python pytest
    tests/terrain/test_terrain_horizon_ui.py tests/test_export_image_sixel.py`
    and `uv run -p .venv/bin/python ruff check
    tests/terrain/test_terrain_horizon_ui.py tests/test_export_image_sixel.py`
- Topic: remove leftover production debug code
  - Decision: remove the JPL and window-chrome debug print hooks, the
    `debug_main()` alias, and the `zstarview-debug` entry point. Keep
    `ZSTARVIEW_DEBUG_SAVE_AIRCRAFT_READY_FRAME` because it is still used
    for screenshot capture.
  - Rationale: the remaining debug hooks were no longer needed in the
    production path and were emitting ad hoc stderr output. The aircraft
    ready-frame hook is the one exception because it is still a supported
    capture mechanism.
  - Validation: `uv run -p .venv/bin/python ruff check

- Topic: cloud startup fetch timing
  - Decision: start the first cloud fetch immediately after startup
    instead of waiting for the normal refresh interval.
  - Rationale: the previous startup path only set `cloud_next_refresh_utc`
    to `now + CLOUD_UPDATE_INTERVAL`, which left the status line at
    `HIMAWARI idle` for up to ten minutes before any network activity.
    Immediate startup fetches make the overlay behave like the other
    startup data loaders and avoid the misleading idle state.
  - Validation: added a regression test for the startup path in
    `tests/terrain/test_terrain_horizon_ui.py`, then ran
    `uv run -p .venv/bin/python pytest tests/terrain/test_terrain_horizon_ui.py -q`,
    `uv run -p .venv/bin/python pytest tests/test_window_cloud_status.py tests/test_scheduler_idle.py -q`,
    and `uv run -p .venv/bin/python ruff check src/zstarview/gui/window.py tests/terrain/test_terrain_horizon_ui.py`.

- Topic: aircraft debug snapshot timing
  - Decision: defer `ZSTARVIEW_DEBUG_SAVE_AIRCRAFT_READY_FRAME` saves until after the next `paintEvent` and capture the actual painted frame image.
  - Rationale: saving immediately from `_on_aircraft_ready()` captured a freshly rendered off-screen image, which could precede the visible repaint. Deferring through the paint path keeps the saved frame aligned with what the user actually saw and still uses the latest ready payload.
  - Validation: updated the aircraft debug snapshot tests to cover the queued save path and verified `uv run -p .venv/bin/python pytest tests/terrain/test_terrain_horizon_ui.py -q` plus `uv run -p .venv/bin/python ruff check src/zstarview/gui/window.py src/zstarview/gui/window_render.py src/zstarview/gui/window_updates.py tests/terrain/test_terrain_horizon_ui.py`.
    src/zstarview/gui/window.py src/zstarview/gui/window_updates.py
    src/zstarview/__init__.py src/zstarview/cli/export_image.py
    pyproject.toml` and `uv run -p .venv/bin/python pytest
    tests/test_cli_export_image_args.py
    tests/test_export_image_layer_gating.py
    tests/test_export_image_sixel.py
    tests/terrain/test_terrain_horizon_ui.py`
- Topic: import cleanup pass
  - Decision: run `ruff check --select I --fix` across `src/zstarview`
    and `tests`, then follow with a full `ruff check` pass to catch any
    path-hack exceptions such as `E402`.
  - Rationale: the repo had accumulated noisy import ordering drift after
    the debug-hook cleanup. Using Ruff's import sorter is the least risky
    way to normalize the layout without touching runtime behavior.
  - Validation: `uv run -p .venv/bin/python ruff check --select I --fix
    src/zstarview tests` and `uv run -p .venv/bin/python ruff check
    src/zstarview tests`
- Topic: remove thin GUI wrappers and aliases
  - Decision: replace the `_sync_overlay_projection_timer_if_available`
    wrapper with direct method calls, remove the old projection-refresh
    compatibility helper, and make the frameless host window the actual
    `SkyWindow` class instead of a `SkyWindow = FramelessSkyWindow`
    alias.
  - Rationale: these shims were adding indirection without real
    abstraction value. Direct calls and a concrete `SkyWindow` class make
    the public API and the update flow easier to follow.
  - Validation: `uv run -p .venv/bin/python ruff check
    src/zstarview/gui/window.py src/zstarview/gui/window_updates.py
    src/zstarview/gui/viewer.py tests/test_viewer_startup_errors.py` and
    `uv run -p .venv/bin/python pytest tests/test_viewer_startup_errors.py
    tests/test_scheduler_idle.py tests/test_window_cloud_status.py
    tests/test_window_cloud_partial_mode.py tests/test_window_render_sync.py
    tests/terrain/test_terrain_horizon_ui.py`
- Topic: remove no-op overlay timer sync hook
  - Decision: delete the now-empty `_sync_overlay_projection_timer()`
    method and stop calling it from satellite/aircraft overlay refresh
    paths.
  - Rationale: the method had no runtime effect after the wrapper cleanup
    and only preserved a placeholder hook. Removing it makes the timer
    behavior explicit: the refresh paths update state and request a
    repaint directly.
  - Validation: `uv run -p .venv/bin/python ruff check
    src/zstarview/gui/window.py src/zstarview/gui/window_updates.py
    tests/test_window_render_sync.py tests/test_scheduler_idle.py` and
    `uv run -p .venv/bin/python pytest tests/test_window_render_sync.py
    tests/test_scheduler_idle.py`
- Topic: remove redundant satellite observer arguments
  - Decision: change `draw_satellite_overlay()` and
    `find_highlighted_satellite()` to read observer location only from
    `viewer_data`, and update the direct satellite overlay test to pass a
    `ViewerData` stub.
  - Rationale: the satellite renderer already used `viewer_data` as the
    authoritative source in the main pipeline, so the extra
    `observer_lat/lon/height_m` arguments were redundant.
  - Validation: `uv run -p .venv/bin/python ruff check
    src/zstarview/render/satellites.py src/zstarview/render/pipeline.py
    tests/test_planet_marker_style.py` and `uv run -p .venv/bin/python
    pytest tests/test_planet_marker_style.py
    tests/satellites/test_satellite_controller.py
    tests/satellites/test_satellite_fetch.py`
- Topic: remove ViewerData-derived overlay parameters
  - Decision: keep `viewer_data` as the only source for observer
    location and field-of-view values in aircraft and satellite overlay
    rendering, and drop the duplicated `view_center`/FOV parameters and
    related fallback logic.
  - Rationale: the renderer already has `ViewerData` at each call site,
    so duplicating its fields in function arguments only increases the
    chance of skew between the caller and callee. The remaining `None`
    checks are limited to actual optional inputs such as `viewer_data`
    and `time_obj`.
  - Validation: `uv run -p .venv/bin/python ruff check
    src/zstarview/render/aircraft.py src/zstarview/render/satellites.py
    tests/test_planet_marker_style.py` and `uv run -p .venv/bin/python
    pytest tests/test_planet_marker_style.py
    tests/aircraft/test_aircraft_project.py
    tests/satellites/test_satellite_controller.py
    tests/satellites/test_satellite_fetch.py`
- Topic: trim night-light glow implementation arguments
  - Decision: remove the unused internal arguments from
    `_draw_night_light_glow_impl()` and keep the outer wrappers as
    compatibility adapters for now.
  - Rationale: `viewport_rect`, `content_fov_deg`, and the terrain
    distance lists were being threaded through the implementation but
    not consulted there. Trimming the private implementation makes the
    dependency surface easier to follow without forcing a broader API
    break yet.
  - Validation: `uv run -p .venv/bin/python ruff check
    src/zstarview/render/night_lights.py tests/test_night_lights.py`
    and `uv run -p .venv/bin/python pytest tests/test_night_lights.py`
- Topic: extract GUI widget classes from `window.py`
  - Decision: move the early window chrome / overlay widget classes into
    `window_widgets.py` and re-export them from `window.py` so existing
    imports keep working.
  - Rationale: the widget cluster was self-contained enough to separate
    cleanly, while `window.py` still owns the host-window orchestration
    logic. Keeping the public names available from `window.py` avoids a
    wider API break.
  - Validation: `uv run -p .venv/bin/python ruff check
    src/zstarview/gui/window.py src/zstarview/gui/window_widgets.py
    tests/test_startup_log.py tests/test_window_shutdown_message.py`
    and `uv run -p .venv/bin/python pytest tests/test_startup_log.py
    tests/test_window_shutdown_message.py`
- Topic: remove resize-grip marker compatibility export
  - Decision: point the terrain-horizon test directly at
    `window_widgets._draw_resize_grip_marker()` and remove the
    `window.py` wrapper export.
  - Rationale: the helper now has a clear home in the new widget module.
    The test can import the real implementation directly, so keeping a
    second export in `window.py` was just compatibility noise.
  - Validation: `uv run -p .venv/bin/python ruff check
    src/zstarview/gui/window.py src/zstarview/gui/window_widgets.py
    tests/terrain/test_terrain_horizon_ui.py` and `uv run -p .venv/bin/python
    pytest tests/terrain/test_terrain_horizon_ui.py -k
    resize_grip_widget_paints_a_visible_marker`
- Topic: let `SkyDataWorker` consume `ViewerData`
  - Decision: change `SkyDataWorker.update()` / `_run_update()` to take a
    `ViewerData` object instead of threading observer latitude/longitude,
    height, view center, and FOV values through separately.
  - Rationale: the window already owns a complete `ViewerData`, and the
    worker only needs those fields to compute the sky snapshot. This
    keeps the worker API smaller without touching the broader
    `compute_sky_snapshot()` helper yet.
  - Validation: `uv run -p .venv/bin/python ruff check
    src/zstarview/gui/sky_worker.py src/zstarview/gui/window_updates.py
    tests/test_scheduler_idle.py tests/terrain/test_terrain_horizon_ui.py`
    and `uv run -p .venv/bin/python pytest tests/test_scheduler_idle.py
    tests/terrain/test_terrain_horizon_ui.py tests/test_startup_ephemeris.py`
  - Follow-up: update `tests/test_controller_shutdown_waits.py` to pass a
    `ViewerData` instance into `SkyDataWorker.update()` after the API
    change.
- Topic: let `compute_sky_snapshot()` consume `ViewerData` and geometry
  - Decision: change `compute_sky_snapshot()` to accept `ViewerData` plus
    the render `ScreenGeometry`, and update the GUI worker, export-image
    CLI, and startup test to build the geometry at the call site.
  - Rationale: `ViewerData` already owns the observer/location and FOV
    state, while `ScreenGeometry` captures the projection frame used for
    the sky-disc render. Splitting the inputs this way removes duplicated
    observer/FOV arguments without overloading either object.
  - Validation: `uv run -p .venv/bin/python ruff check
    src/zstarview/gui/sky_worker.py src/zstarview/gui/window_updates.py
    src/zstarview/cli/export_image.py tests/test_controller_shutdown_waits.py
    tests/test_startup_ephemeris.py` and `uv run -p .venv/bin/python
    pytest tests/test_controller_shutdown_waits.py
    tests/test_startup_ephemeris.py tests/test_scheduler_idle.py
    tests/terrain/test_terrain_horizon_ui.py`
- Topic: remove redundant render size pair from sky snapshot flow
  - Decision: replace `render_width_px` / `render_height_px` with a
    single `image_size` tuple at the call sites, and use `geometry`
    in the sky payload for stale-render detection.
  - Rationale: the width/height pair duplicated information already
    carried by the call site, while `geometry` is the more stable identity
    for deciding whether a sky snapshot still matches the current frame.
    Keeping the canvas size as one tuple preserves the existing render
    surface without threading two separate integers through the worker API.
  - Validation: `uv run -p .venv/bin/python ruff check
    src/zstarview/gui/sky_worker.py src/zstarview/gui/window_updates.py
    src/zstarview/cli/export_image.py tests/test_startup_ephemeris.py
    tests/test_controller_shutdown_waits.py tests/test_window_render_sync.py`
    and `uv run -p .venv/bin/python pytest tests/test_export_image_sixel.py
    tests/test_cli_export_image_args.py tests/test_startup_ephemeris.py
    tests/test_controller_shutdown_waits.py tests/test_window_render_sync.py
    tests/test_scheduler_idle.py tests/terrain/test_terrain_horizon_ui.py`
- Topic: bump package version after refactor
  - Decision: increment `src/zstarview/__about__.py` to `1.27.6`.
  - Rationale: the API cleanup and related behavior changes were
    validated, so a patch version bump is appropriate before the next
    commit.
- Topic: bump package version after view-change fix
  - Decision: increment `src/zstarview/__about__.py` to `1.27.8`.
  - Rationale: the view-center/search-jump rendering fix was committed
    separately, so the requested `+0.0.1` version bump belongs in its
    own follow-up commit.

## Session Summary

- Commits:
  - `6e1260f` `fix: make view changes render transient fast frame`
- Follow-ups:
  - `1.27.8` version bump committed separately as requested.
- Pushes:
  - None.
- Topic: downsample fast-mode base scene
  - Decision: render the fast-mode base scene into a capped low-resolution
    buffer with a 600 px maximum edge, then scale it up into the final
    window-sized frame while keeping the status line drawn at full size.
  - Rationale: the expensive sky/base scene is what dominates fast-mode
    repaint cost at high window sizes, while the HUD/status line remains
    cheap and should stay crisp. Rendering the scene itself into a smaller
    buffer directly reduces the expensive work without sacrificing the fast
    interaction affordance.
  - Validation: `uv run -p .venv/bin/python pytest
    tests/test_window_render_sync.py -q` and `uv run -p .venv/bin/python
    ruff check src/zstarview/gui/window_render.py tests/test_window_render_sync.py`
- Topic: suppress menu chrome in fast-mode
  - Decision: draw direction labels on the final window-sized painter, hide
    the actual menu widget while viewport interaction is active, and skip the
    menu-button ornament inside `draw_window_border()` during fast-mode.
  - Rationale: the right-top "menu" that remained visible was not the button
    widget itself but the decorative chrome painted by the framed window
    background. Keeping the border while omitting only the menu ornament
    preserves the window frame without showing the misleading menu glyph.
  - Validation: `uv run -p .venv/bin/python pytest
    tests/test_window_render_sync.py -q` and `uv run -p .venv/bin/python
    ruff check src/zstarview/gui/window.py src/zstarview/gui/window_updates.py
    src/zstarview/gui/window_render.py src/zstarview/render/pipeline.py
    src/zstarview/render/background.py tests/test_window_render_sync.py`
- Topic: bump package version after fast-mode menu fix
  - Decision: increment `src/zstarview/__about__.py` to `1.27.9`.
  - Rationale: the fast-mode menu visibility and chrome changes were
    committed separately, so the requested patch-level bump belongs in a
    follow-up commit.
