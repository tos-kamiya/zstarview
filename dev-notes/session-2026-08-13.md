# Development Session - 2026-08-13

- Scope: Review licensing and request frequency before enabling GMN meteor trails by default.

- Topic: Enable meteor trails by default
  - Decision: Change the `--meteor-trails-opacity` default to `0.4` for the interactive viewer and add the required GMN attribution and modification notice to the bundled license summary and README. Keep `0` as the explicit session-level disable. Do not advertise the option for image export until that path implements meteor loading and rendering.
  - Rationale: GMN publishes its high-level products under CC BY 4.0 and states that they update every six hours. No explicit request-rate limit is published. The existing shared cache limits directory-index and recent-file network refreshes to once per six hours, matching the source update cadence; historical files are reused indefinitely.

- Summary: Set the interactive viewer default meteor opacity to `0.4`, added GMN attribution, and kept image export free of the unsupported option. Full pytest passed with 1,508 tests and 5 skips; changed-source Ruff, focused mypy, and `git diff --check` passed.

- Topic: Release version
  - Decision: Bump the package from `1.48.5` to `1.49.0`, incrementing the minor component and resetting the patch component to zero.
  - Rationale: The release adds the new user-visible GMN meteor trail layer and its CLI control.

- Scope: Restrict Open-Meteo forecast precipitation to real-time views.

- Topic: Forecast precipitation time gating
  - Decision: Add precipitation to the shared real-time overlay availability
    policy and enforce it for interactive startup, background refreshes, menu
    state, and image export.
  - Rationale: Open-Meteo values represent the current forecast snapshot, so
    showing them against a past or future sky time is misleading. Reusing the
    existing five-minute real-time tolerance keeps precipitation consistent
    with clouds and tropical cyclones.

- Summary: Disabled Open-Meteo precipitation outside real-time views and added
  focused coverage for past/future export gating and interactive menu state.
  Focused tests passed with 66 tests; Ruff and `git diff --check` passed.

- Scope: Hide GMN meteor trails while fast-mode rendering is active.

- Topic: Meteor trails in fast-mode
  - Decision: Skip GMN meteor trail drawing in the fast viewport frame while
    preserving the configured opacity and cached observations for the next
    normal frame.
  - Rationale: Meteor trails are a historical detail layer and need not add
    work or visual noise during interactive viewport movement.

- Summary: Fast viewport frames now omit GMN meteor trails. Added coverage for
  fast-frame flag propagation and renderer suppression; 36 focused tests
  passed and `git diff --check` passed.

- Topic: Patch release version
  - Decision: Bump the package from `1.49.0` to `1.49.1` and move the two
    completed overlay fixes into the corresponding release-notes entry.
  - Rationale: The changes are backward-compatible corrections to display
    gating and fast-mode rendering, so a patch increment is appropriate.
