# Session 2026-05-11

- Scope: Fix `search object` dialog `Keep marker` crash in the GUI.
- Topic: `Keep marker` crash on search jump targets
  - Decision: Initialize the persistent-marker state from the current search target before the kind-specific branches, and guard the persistent-target logger behind `getattr()` so local stars and other non-JPL targets can keep a marker without raising `UnboundLocalError` or `AttributeError`.
  - Rationale: The checkbox is exposed in the general search dialog, so the jump path must tolerate non-JPL targets. The persistent overlay already only refreshes JPL bodies, so non-JPL targets can safely retain a static marker state.
  - Alternatives Considered: Restricting the checkbox to JPL results only, but that would remove a UI affordance that already exists for all search results.
- 19:39 Ran: `uv run -p .venv/bin/python pytest tests/terrain/test_terrain_horizon_ui.py -q -k 'jump_to_search_target_can_keep_marker_for_local_star or jump_to_search_target_keeps_negative_target_alt_for_highlight or jump_to_jpl_small_body_target_can_set_persistent_overlay or jump_to_jpl_small_body_target_uses_state_vector_when_present'`
  Output:
  ```text
  2 passed, 47 deselected in 1.22s
  ```
  Result: Verified the fixed jump path for a local star and the existing JPL keep-marker cases.
- 19:40 Ran: `uv run -p .venv/bin/python pytest tests/test_place_search_dialog.py -q`
  Output:
  ```text
  13 passed in 1.06s
  ```
  Result: Confirmed the search dialog behavior still passes its dedicated tests.
- Topic: Test expectation drift in status/source labels
  - Decision: Update the affected assertions to match the current public payloads and status line formatting (`⎮` separators, `Dem: cache`, `Urban: cache`).
  - Rationale: The implementation already emits these strings, so the failing tests were stale rather than surfacing a regression from the `Keep marker` fix.
  - Alternatives Considered: Changing the production labels back, but that would regress the existing UI/status conventions in the codebase.
- Topic: Persistent marker disappearing after jump
  - Decision: Sync `state.render_view_center` immediately in `_jump_to_search_target()` when the jump target is applied.
  - Rationale: The persistent search overlay is projected against `render_view_center`, while the jump path already updates `viewer_data.view_center` immediately. Syncing the render center removes the transient mismatch that can make the kept marker vanish once the temporary jump highlight expires.
  - Validation: `uv run -p .venv/bin/python pytest tests/terrain/test_terrain_horizon_ui.py tests/test_place_search_dialog.py -q`
    Output: `62 passed`
- Topic: Stale sky snapshot overwrote the jump center
  - Decision: Reject sky snapshots whose emitted `view_center` no longer matches the current `viewer_data.view_center` before updating `state.render_view_center`.
  - Rationale: An older background sky computation can finish after a jump and roll the render center back to the pre-jump view. That makes the persistent marker project off-screen even though the stored target is still present.
  - Validation: `uv run -p .venv/bin/python pytest tests/test_window_render_sync.py -q -k 'on_sky_data_calculated'`
    Output: `5 passed, 76 deselected`
- Topic: Roll back production-side changes
  - Decision: Restore `src/zstarview/gui/window.py`, `src/zstarview/gui/window_updates.py`, and `src/zstarview/render/search_overlay.py` to the previous commit state while leaving tests and image assets untouched.
  - Rationale: The previous session drifted in the wrong direction for production code, so the safest recovery was to revert only the runtime implementation and preserve the existing verification artifacts.
- Topic: Keep marker for local star search results
  - Decision: Allow non-JPL search targets such as Dubhe to be stored as persistent marker targets, sync the render view center when applying a search jump, draw the persistent target through the edge FOV, and pass persistent-search labels through the HUD label layer.
  - Rationale: The temporary jump highlight uses its own timer, so if the persistent target is not stored, a checked `Keep marker` appears to work for only a few seconds. In the live GUI path, the persistent marker is rendered by `render_hud_overlay_into_painter()`; that call was explicitly using `draw_label=False`, so the marker could remain after the jump highlight expired while the adjacent label disappeared.
  - Validation: `uv run -p .venv/bin/python pytest tests/terrain/test_terrain_horizon_ui.py -q -k 'jump_to_search_target_can_keep_marker_for_local_star or jump_to_search_target_keeps_negative_target_alt_for_highlight'`; `uv run -p .venv/bin/python pytest tests/test_label_layer.py -q -k 'draw_search_target_overlay'`; `uv run -p .venv/bin/python pytest tests/test_place_search_dialog.py -q -k 'keep_marker or cli_view_center'`; `uv run -p .venv/bin/python pytest tests/test_window_render_sync.py -q -k 'persistent_search or draw_persistent_search_overlay or render_hud_overlay_draws_persistent_search_label'`.
