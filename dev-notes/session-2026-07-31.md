# Session 2026-07-31

Scope: Clarify bundled star-catalog provenance and record a future Hipparcos update.

- Topic: Identify the bundled Hipparcos catalog version
  - Finding: `src/zstarview/data/stars/hip_main.dat.zip` contains 118,218 records and matches CDS VizieR catalog `I/239`, The Hipparcos and Tycho Catalogues (ESA 1997). The optional Tycho-2 input is catalog `I/259`.
  - Decision: Update the English and Japanese README bundled-data version tables with `Hipparcos I/239 (ESA 1997)` and `Tycho-2 I/259`.
  - Rationale: Record the known source identifiers without changing the currently bundled star catalog.

- Topic: Future Hipparcos catalog update
  - Decision: Migrate the bundled star generation input to Hipparcos New Reduction `I/311` (van Leeuwen 2007), using the fixed-width `hip2.dat.gz` format and retaining Tycho-2 `I/259` as the optional faint-star supplement.
  - Validation: The generator was extended for I/311 byte positions and regenerated all six split catalogs at `--max-vmag 10.5`; Hipparcos input yielded 111,734 usable rows and the merged output remained valid.
  - Rationale: I/311 provides the improved astrometric reduction while preserving the existing runtime CSV schema and Tycho merge policy.

- Topic: Future IAU Catalog of Star Names update
  - Finding: The bundled IAU WGSN Catalog of Star Names CSV has `2025/06/17` as its latest `Date of Adoption` value. The online CSN is continually updated, so this date identifies the bundled snapshot rather than a formal catalog version.
  - Decision: Refresh the bundled IAU catalog from the current exopla.net table and record the retrieval date together with the latest adoption date.
  - Finding: The refreshed table contains 605 rows with latest adoption date `2026-07-16`; it was retrieved on `2026-07-31`.
  - Rationale: The source does not provide a stable release version, while the latest adoption date provides a reproducible content marker for the bundled data.

- Topic: Future GeoNames data update
  - Finding: The bundled GeoNames files were committed on `2025-07-29`; the latest record date in `cities1000.txt` is `2025-07-27`. The official download listing showed `2026-07-30` data when checked on `2026-07-31`.
  - Decision: Refresh the bundled `cities1000.txt` and `admin1CodesASCII.txt` from the GeoNames distribution dated `2026-07-30`.
  - Finding: The refreshed `cities1000.txt` has latest record date `2026-07-29`; the source listing and both files were retrieved on `2026-07-31`.
  - Rationale: GeoNames publishes updated dump files independently of the application, so the bundled snapshot should be refreshed and explicitly identified when changed.

- Topic: DSO catalog update check
  - Finding: Regenerating `dso.csv` with the installed PyOngc `1.2.2` produced 977 rows and an identical SHA-256 to the bundled file.
  - Decision: Keep `dso.csv` unchanged and retain its existing `Generated 2026-07-30` provenance.
  - Rationale: The current generator and source package produce no data change to review or ship.

- Topic: Earth guide and coastline source separation
  - Decision: Keep the Earth guide based on the stable Natural Earth 1:110m source, and update the optional coastline overlay from the latest OSM Water Polygons package instead of rebuilding the Earth guide from water polygons.
  - Finding: OSM Water Polygons has a separate land-polygon product, but the complete WGS84 download is very large and requires additional handling for large rings, holes, antimeridian wrapping, and coarse-guide rendering. A trial output showed visible horizontal artifacts when those topology details were not handled.
  - Rationale: Separating the two products preserves the existing Earth guide behavior while allowing the coastline overlay to follow the regularly updated OSM-derived source.

- Topic: Coastline source update
  - Decision: Record the 2026-07-30 OSM Water Polygons package as the current source candidate and document the existing vector-tile and sea-mask regeneration pipeline in `src/zstarview/data/earth_guide_land_regeneration.md`.
  - Finding: The downloaded package contains 53,330 WGS84 polygon features and has SHA-256 `2f705bae3f7a7af7a7ef4740cfd6621ce8004df0aaf7fe01d914ff1498afdf17`.
  - TODO: Regenerate and validate the coastline vector release assets and 25m sea-mask assets from the 2026-07-30 source before changing the release tag or downloader defaults.

- Topic: Bundled catalog refresh
  - Summary: Updated GeoNames `cities1000.txt` and `admin1CodesASCII.txt` to the 2026-07-30 distribution; refreshed the IAU star-name table; migrated generated stars to Hipparcos I/311 with Tycho-2 I/259 supplementation; retained the old I/239 input for compatibility.
  - Finding: PyOngc 1.2.2 regeneration produced a byte-identical `dso.csv`, so no DSO data change was needed.
  - Validation: The six star CSVs contain 535,116 unique source IDs with valid coordinate ranges; related pytest checks passed (10 tests); `git diff --check` passed.
