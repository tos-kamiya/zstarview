# Session 2026-06-18

Scope: Align ridge glow documentation with the current implementation.

- Topic: ridge glow documentation drift
  - Decision: Update specification and design docs to describe ridge glow as the current code does: a GlowMask-based glow layer with fixed tinting and opacity linked to night-light opacity, rather than a separate sky-glow mix with sky-disc color.
  - Rationale: The docs still described an older sky-glow concept that no longer matches the code path, which made the ridge glow behavior hard to reason about.

Summary: committed as `8d9d77cc` (`docs: align ridge glow docs`).
