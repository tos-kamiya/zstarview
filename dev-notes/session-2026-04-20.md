# Session 2026-04-20

- Scope: Investigate whether Himawari cloud download timeouts are treated as "no clouds" in the GUI.
- Findings:
  - A Himawari timeout raises `TimeoutError` in the S3 helper and is surfaced as a cloud failure banner.
  - The cloud failure handler does not clear an existing cloud image by itself.
  - Cloud buffers are explicitly cleared when viewport interaction starts unless `preserve_cloud_buffers=True`.
- Conclusion: The blank-cloud state is more likely caused by stale-cloud clearing during interaction plus a failed refresh, not by timeout being interpreted as clear sky.
- Follow-up:
  - Updated the cloud status-line formatter so banner text like `Clouds fetch timed out` becomes `download failed` instead of being misread as `downloading`.
  - Kept the cloud repaint request on failure even during interaction mode so the status bar can reflect the error immediately.
  - Validation: `python3 -m compileall` succeeded for the changed files; full pytest run could not complete in this environment because the local `.venv` lacks runtime dependencies such as `pytest`, `astropy`, and `PySide6`.
- Follow-up 2:
  - While a cloud fetch or render is in flight, viewport interaction now preserves the current cloud buffers instead of clearing them.
  - This avoids the blank-cloud interval between "start fetching" and the eventual success/failure signal.
- Docs:
  - Updated `docs/specification.md` to state that existing cloud imagery may be preserved while a refetch is in progress, and that failure/interaction transitions should not blank the layer prematurely.
  - Updated `docs/design.md` to document the in-flight cloud-buffer preservation rule in the cloud update flow and error handling notes.
- Topic: Documentation readability cleanup
  - Decision: Keep this pass limited to wording, heading numbering, and bullet hierarchy in `docs/specification.md` and `docs/design.md`.
  - Rationale: The user asked for doc readability and consistency improvements only, without consulting source or tests. Structural cleanup improves scanability while keeping behavior unchanged.
- Topic: Design heading depth alignment
  - Decision: Promote `docs/design.md` `4.2.x` subsections from `###` to `####` so they match the nesting depth implied by the section numbering.
  - Rationale: The `4.2` branch is a parent section, and the numbered subsections read more consistently when their markdown depth reflects that hierarchy.
- Topic: Additional heading alignment
  - Decision: Promote `docs/design.md` `5.6.x` and `6.2.x` subsections from `###` to `####` for the same hierarchy reason.
  - Rationale: These sections are nested under their parent numbered sections, and the markdown heading depth should match that structure for consistent scanning.
- Topic: Numeric subsection renumbering
  - Decision: Replace the alphabetic `4.2.2a` / `4.2.2b` / `4.2.4a` style headings in `docs/design.md` with numeric-only numbering.
  - Rationale: Numeric-only numbering makes the subsection order easier to scan and keeps the `4.2` branch consistent with the rest of the document.
- Topic: Specification heading depth cleanup
  - Decision: Promote `docs/specification.md` sections `7.2.1`, `7.4.0`, `7.4.1`, `8.2.1`, and `8.2.2` from `####` to `###`.
  - Rationale: The document had those entries one level deeper than the surrounding numbered structure suggested. Raising them one level improves scanability while preserving the existing section hierarchy.
- Topic: Specification heading renumbering
  - Decision: Reassign the affected `docs/specification.md` branch numbers so the promoted headings remain collision-free and the surrounding section order stays sequential.
  - Rationale: Once those headings move up a level, the old subsection numbers would collide with their former parent sections. Repacking the local branch keeps the outline readable without changing the underlying prose.
- Topic: Specification color-role split
  - Decision: Promote `7.1.1` into an independent `7.2` section and renumber the following `docs/specification.md` branch accordingly.
  - Rationale: The color-role guidance reads like a first-class specification topic rather than a subordinate note under display targets, so giving it its own numbered section improves locality and scanability.
- Topic: Earth guide merge
  - Decision: Merge the former `7.7` Earth-guide detail into `7.7 地球裏面ガイド表示`, remove the "future expansion" wording, and keep the implemented land-outline behavior in the same section.
  - Rationale: The feature is already implemented, so the spec should describe the current behavior directly instead of splitting it into a future-looking subsection.
- Topic: Earth guide simplification
  - Decision: Compress `7.7 地球裏面ガイド表示` into a shorter set of bullets organized around control, displayed content, implementation source, and approximation limits.
  - Rationale: The section had become a long inventory of near-duplicate constraints. Grouping them by concern makes the spec easier to scan while preserving the implemented behavior.
- Topic: Cloud overlay simplification
  - Decision: Compress `7.10 雲オーバーレイ` into a smaller set of bullets covering display purpose, projection approximation, visibility limits, cache/failure handling, and viewport-change behavior.
  - Rationale: The original section mixed several layers of detail in a long list. Grouping those concerns makes the current behavior easier to read without dropping the important operational rules.
- Topic: Urban outline tile split
  - Decision: Rewrite `7.12 都市アウトライン` so the normal near-field tiles and the distant skyscraper tiles are described as separate data sets and separate cache roots.
  - Rationale: The old wording mixed two storage models into one list. Making the tile split explicit clarifies which rules apply to the normal building layer versus the skyscraper-only supplement.
- Topic: Aircraft overlay approximation
  - Decision: Reorder `7.13 航空機オーバーレイ` so the `5分` fetch cadence is explained as a coarse interval that turns the displayed aircraft positions into approximate values between refreshes.
  - Rationale: The old ordering made the TTL look like an isolated numeric choice. Leading with the approximate-display assumption makes the refresh cadence and the 2-second projection ticks easier to understand together.
- Topic: Satellite layer ISS split
  - Decision: Reorder `7.14 人工衛星レイヤー` so `ISS` is clearly separated from the `JWST` / `Voyager` / `Parker` set, with its own data source and freshness rules.
  - Rationale: `ISS` follows the Earth-satellite path with TLE/OMM-style caching and a different acquisition source, while the other targets use Horizons. Making that split explicit keeps the spec aligned with the implementation.
- Topic: Render design current-state cleanup
  - Decision: Keep `4.3.5` and `4.3.7` as current-state documentation backed by `src/zstarview/render/pipeline.py` and `src/zstarview/gui/window_render.py`, and rewrite `4.3.6` to describe the implemented base/HUD split rather than a future refactor.
  - Rationale: The source already contains the split pipeline and cache behavior, so the design document should describe the present implementation instead of an in-progress plan.
- Topic: Render refactor wording cleanup
  - Decision: Rewrite `4.3.4` as a current-state description of the drawing pipeline boundaries and responsibility split, instead of a future refactoring policy.
  - Rationale: The code already implements the reusable pipeline, GUI/CLI separation, and thin window-render wrapper, so the design text should reflect that state directly.
- Topic: Cloud data structure relocation
  - Decision: Move the cloud layer internal representation details into `5.4 雲関連` and leave `6.2.1` as a short reference note.
  - Rationale: Cloud state and image buffers are data structures, so they fit the main data-structure section better than the update-flow section.
- Topic: Sky update flow scope clarification
  - Decision: Clarify that `6.2 星空更新フロー` covers the shared sky disc and celestial update path, while `6.3` through `6.8` handle the per-layer update flows.
  - Rationale: The later sections are already the layer-specific flows, so the section introduction should state that boundary explicitly to avoid implying that `6.2` is the full update model.
- Topic: Remove redundant cloud subsection
  - Decision: Delete `6.2.1` from `docs/design.md` because cloud internal representation now lives in `5.4 雲関連`, leaving `6.2` for shared sky updates only.
  - Rationale: The subsection duplicated data-structure content and created a misleading impression that cloud state belongs in the sky update flow instead of the main data-structure section.
- Topic: Earth guide placement
  - Decision: Remove the Earth guide projection subsection from `6.2` and keep Earth guide in `5.6.1` plus the main drawing flow instead.
  - Rationale: Earth guide is a separate render layer, not part of the shared sky update path, so it fits better as a layer definition and a draw-order item.
- Topic: Remove redundant 6.2 subsections
  - Decision: Delete `6.2.1` and `6.2.2` from `docs/design.md`, leaving `6.2` as the shared sky update flow only.
  - Rationale: `6.2.1` duplicated viewport geometry already covered by the rendering section, and `6.2.2` duplicated cache behavior that is already documented in section 10.
- Topic: Frame cache location
  - Decision: Add an explicit note in `4.4` that the `window_render.py` cache is a runtime frame cache, while persistent cache behavior remains in section `10`.
  - Rationale: This keeps the responsibility split clear after removing the cache subsection from `6.2` and avoids implying that the render cache is part of the long-lived cache model.
- Topic: Search documentation deduplication
  - Decision: Keep `4.2.7` as the authoritative search-spec section and reduce `4.4.7` to a short module-placement summary.
  - Rationale: The search request model and resolution strategy were duplicated between the two sections, so separating "what it means" from "where it lives" makes the document easier to scan.
