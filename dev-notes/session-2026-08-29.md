# Session 2026-08-29

Scope: Extract representative night-light and edge-glow colors from the
2012 Black Marble JPEG in `raw-data/`.

- Topic: Representative color extraction
  - Decision: Report both a high-brightness color band and the brightest
    highlight pixels for city light, while sampling the Antarctic interior
    for edge glow.
  - Rationale: The brightest city pixels are nearly white, so they are useful
    as an endpoint but not sufficient to preserve the warm color character.

Summary: Added and ran `raw-data/extract_black_marble_colors.py`. No commits,
tags, or pushes were made.

- Topic: Existing color comparison
  - Finding: `NIGHT_LIGHTS_GLOW_RGB` is `(244, 246, 248)` and is reused as
    `GLOW_MASK_TINT_RGB` for both night-light and edge-glow compositing.
  - Comparison: The extracted display colors are warm white `(255, 249, 234)`
    for city light and saturated blue `(128, 155, 255)` when the Antarctic
    edge-glow color is normalized.

- Topic: Trial color change
  - Decision: Set night light to `(255, 249, 234)` and use a separate
    edge-glow tint `(128, 155, 255)`.
  - Validation: Ruff passed and `tests/test_glow_mask.py` passed (13 tests).
  - Scope: No tests or user-facing documentation were changed.

- Topic: Edge-glow default strength
  - Decision: Increase `RIDGE_GLOW_DEFAULT_OPACITY` from `0.04` to `0.08`
    to compensate for the darker perceived result of the new blue tint.
  - Scope: Explicit opacity values and the edge-glow formula remain unchanged.

- Topic: Follow-up alignment
  - Decision: Update default-value assertions, the test support stub, and CLI/
    specification documentation to `0.08`.
  - Validation: 136 focused tests passed; Ruff and `git diff --check` passed.
