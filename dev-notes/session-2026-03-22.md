# Session 2026-03-22

- Scope: Simplify and reorganize the `Features` section in `README.md`.

- Topic: Trim README feature wording for the aircraft overlay
  - Decision: Keep the feature list entry focused on visible behavior and remove implementation/control details such as startup opacity, GUI toggling, and cache reuse.
  - Rationale: The `Features` section should stay scannable. Those details are lower-level controls or implementation notes rather than the core capability being advertised.

- Topic: Reorder README features around visual layers first
  - Decision: Reordered the feature bullets to present visible layers first in this order: celestial layers, satellite clouds, aircraft, urban outlines, terrain horizon, and guide overlays; CLI interaction and Python support follow afterward.
  - Rationale: This makes the section read like a layered rendering stack instead of mixing scene content, overlays, controls, and runtime support in one sequence.

- Topic: Keep the Japanese README feature list aligned with the English README
  - Decision: Applied the same aircraft-overlay simplification and layer-first feature ordering to `README_ja-JP.md`, including consolidating guide-related items into one bullet.
  - Rationale: The English and Japanese READMEs should describe the same product structure and avoid drifting in emphasis or detail level.

- Topic: Mention sixel-capable terminal export in the feature list
  - Decision: Added a `zstarview-export-image` feature bullet immediately before Python support in both READMEs, describing headless image export and direct display in sixel-capable terminals.
  - Rationale: This is a user-visible CLI capability and belongs with the non-layer usage features near the end of the list.

- Topic: Split the main screenshots into their own section
  - Decision: Added a dedicated screenshots section in both READMEs and clarified that the first image shows the asterism overlay plus the never-rises region, while the second image shows the aircraft overlay.
  - Rationale: The screenshots should be introduced explicitly instead of appearing as an unlabeled image block after the feature list.

- Topic: Move the magnitude-limit screenshot into the screenshots section
  - Decision: Moved the `-V10 -s4.5` screenshot out of the CLI reference and into the screenshots section in both READMEs, then added a note under that screenshot linking to the magnitude-limit explanation.
  - Rationale: Example images should live with the other screenshots, while the CLI reference should stay text-focused and linkable.

- Topic: Align the PyPI README screenshot order with the main README
  - Decision: Reworked `README_pypi.md` to use the same three-screenshot order and descriptions as the main README, including the magnitude-limit note linking to the GitHub README anchor.
  - Rationale: The PyPI page should present the same visual story and screenshot sequence as the repository README even though its links must target GitHub URLs.

- Topic: Re-add the sixel export example to screenshot sections
  - Decision: Restored `docs/images/screenshot6.png` and added it back as the fourth screenshot in the main, Japanese, and PyPI READMEs, describing it as a `zstarview-export-image` sixel output example.
  - Rationale: The screenshot had been lost during a git operation, but the screenshot sections should continue to document the terminal export feature visually.

- Topic: Skip incomplete Himawari ISatSS slots when selecting the latest cloud source
  - Decision: Updated the Himawari provider to reject a newly listed slot when its key count is still below the product's expected tile count, and fall back to the next older 10-minute slot instead.
  - Rationale: NOAA's Himawari S3 prefix can become visible before all tiles for a timeslot are published. Treating any non-empty prefix as complete caused Japan-visible scenes to regress from full coverage to 0% when a partially published newer slot replaced a stable older one.

- Topic: Bump the application version for the follow-up fix release
  - Decision: Increased the package version from `1.4.0` to `1.4.1` in `src/zstarview/__about__.py`.
  - Rationale: The Himawari incomplete-slot fix changes runtime behavior and should ship as a distinct patch release.

- Topic: Reorganize the PyPI README highlights to match the main README
  - Decision: Reordered `README_pypi.md` so the highlights now start with the core celestial content, then move through environmental overlays, and end with input/control and Python support. Also folded the never-rises region into the asterism/guides bullet instead of keeping it as a standalone item.
  - Rationale: The PyPI page should mirror the main README's feature hierarchy while keeping a slightly coarser, faster-scanning bullet granularity that emphasizes user-visible capabilities over implementation details.

- Topic: Align the PyPI README common options with the new highlight order
  - Decision: Reordered `README_pypi.md` common options to start with location and view-center controls, then list cloud, aircraft, terrain, and urban-outline controls, followed by the remaining general rendering/time options. Added `-Z` and `-A` because the reordered highlights explicitly call out adjustable view center.
  - Rationale: The quick option list should reinforce the same story as the highlights section and should not omit the most direct controls for a feature that is now emphasized near the end of that section.

- Topic: Update the PyPI README note for Windows on Arm64 installation
  - Decision: Replaced the outdated "not supported for installation" note with a note that installation is now possible, while warning that Windows Security can still block Python extension modules during startup on some systems.
  - Rationale: The old note no longer matches the current runtime and dependency situation. The PyPI README should reflect that the install blocker is gone without hiding the remaining Windows-specific startup risk.

- Topic: Scope Google Maps URL support for direct observing-location input
  - Decision: Keep the session note at the requirements-discussion level for now: direct coordinate input may expand beyond `lat;lon` to include Google Maps-derived forms, but the accepted prefixes, suffix handling, timezone behavior, and interaction with `--observer-height-m` still need to be fixed in the written spec before code changes are finalized.
  - Rationale: The open questions are about parsing boundaries and user-facing behavior rather than implementation mechanics. Recording the unresolved points first avoids locking the code into a URL grammar that might be too broad or too narrow.

- Topic: Document the planned direct-coordinate URL grammar on `dev`
  - Decision: Wrote the target behavior into `docs/specification.md` and the matching README section: `lat;lon` remains supported, new direct forms are `@lat,lon` and selected Google Maps `/maps/@lat,lon...` URLs under `maps.google.com/`, `www.google.com/maps/`, or `google.com/maps/` with optional `https://`.
  - Rationale: Restricting the accepted URL family keeps parsing predictable, avoids the ambiguity of bare `lat,lon`, and makes it explicit that Google Maps trailing zoom or camera metadata is ignored.

- Topic: Align timezone and observer-height rules across direct coordinate forms
  - Decision: Specified that `lat;lon`, `@lat,lon`, and accepted Google Maps URLs should all resolve timezone the same way as `--place`, with a planned `--timezone` override taking precedence; observer height remains controlled only by `--observer-height-m`.
  - Rationale: Input syntax should not silently change how local time is chosen, and Google Maps camera metadata should not be mistaken for physical observer height.

- Topic: Implement direct coordinate parsing for `@lat,lon` and Google Maps URLs
  - Decision: Added a shared direct-coordinate parser in `startup.py` that accepts existing `lat;lon`, new `@lat,lon`, and restricted Google Maps `/maps/@lat,lon...` URLs from `maps.google.com`, `www.google.com/maps/`, and `google.com/maps/`, with optional `https://`.
  - Rationale: Keeping all direct-coordinate forms in one parser preserves consistent validation and avoids scattering URL-specific parsing across CLI or viewer setup code.

- Topic: Use resolved location timezones by default for direct coordinates
  - Decision: Changed direct coordinate input to resolve timezone from the nearest GeoNames city, matching `--place`, and added a new `--timezone` override that feeds both on-screen timezone display and `--datetime` parsing when the datetime string omits an explicit timezone.
  - Rationale: Users expect pasted coordinates and pasted map URLs to behave like place lookups for local time, while still needing an explicit override for edge cases and alternate viewing contexts.

- Topic: Narrow Google Maps host matching and prefer place-pin coordinates
  - Decision: Refined the Google Maps parser to accept only `maps.google.com/` and `www.google.com/maps/` shared URLs, prefer `!3dLAT!4dLON` when present, and fall back to `@LAT,LON` only when the explicit pin coordinates are absent.
  - Rationale: Real shared `place/...` URLs often contain both a viewport center and a place pin, and the pin coordinates better match user intent. Dropping the broader `google.com/maps/` host also keeps the accepted URL family closer to the currently observed shared-link patterns.

- Topic: Align the Japanese README with the new direct-coordinate rules
  - Decision: Updated `README_ja-JP.md` so its `location` argument table, `--datetime` explanation, `--timezone` option, and direct-coordinate section match the implemented support for `@lat,lon`, restricted Google Maps shared URLs, `!3dLAT!4dLON` priority, and coordinate-based timezone resolution.
  - Rationale: The English and Japanese READMEs should describe the same accepted input forms and timezone behavior so the CLI contract does not drift by language.

- Topic: Mention Google Maps coordinate URLs in feature summaries
  - Decision: Added a short mention of supported Google Maps coordinate URLs to the `Flexible location input` / `柔軟な場所指定` feature bullets in the English and Japanese READMEs.
  - Rationale: The feature list should reflect that this input path is now a first-class user-visible capability, without forcing readers to dig into the CLI reference first.

- Summary: Updated README variants for clearer feature/screenshot presentation, fixed Himawari cloud-source selection to skip incomplete latest slots, added a regression test, bumped the app version to `1.4.1`, and implemented documented support for Google Maps-based direct location input, resolved-coordinate timezones, and the `--timezone` override.
