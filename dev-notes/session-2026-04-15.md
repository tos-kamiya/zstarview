# Session 2026-04-15

Scope: Dependabot reported dependency bumps for `pytest` and `Pillow`, but the requested update target (`pyproject.toml` vs `uv.lock`) is still ambiguous.

- Topic: Dependency bump handling
  - Decision: Paused before editing dependency files.
  - Rationale: `pyproject.toml` currently uses an unpinned `pytest` and `Pillow>=12,<13`, so the Dependabot versions likely map to lockfile updates unless the minimum supported versions should be raised intentionally.
  - Alternatives Considered: Update `pyproject.toml` directly by raising lower bounds; rejected for now because it could change the supported-version policy without confirmation.
