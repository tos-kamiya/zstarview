- Scope: Restrict diffuse sky and sky-colour disc to the region above the terrain horizon.
  - Decision: Apply the shared terrain mask while each low-resolution sky surface is generated, then upscale as before.
  - Rationale: This avoids a separate full-window mask pass and keeps the boundary aligned with the sky surface resolution. Missing terrain data preserves the previous behavior.
- Validation: `uv run -p .venv/bin/python pytest tests/test_ground_mask.py tests/test_molecular_cloud_overlay.py tests/test_sky_disc_viewport_and_ground_tint.py tests/test_window_render_overlays.py` passed (80 tests); targeted Ruff check passed.
- Follow-up: No commit or push was performed.
- Experiment: Disabled the compositor's full-window ground reset call while retaining the helper and its parameter for easy rollback/comparison.
- Validation: After the experiment, the relevant 92-test set passed; the compositor regression test now verifies that lower-disc pixels are not replaced by the reset color.
- Decision: Remove the Atlas ground-tint draw call as well; retain the option/data fields for compatibility, but do not paint a ground tint.
- Validation: 69 relevant tests passed; one unrelated window-render-context test could not construct its existing stub because `asterism_opacity` is missing. Direct Ruff check passed.
- Decision: Replace low-resolution hard masking with a post-upscale full-resolution clip over the combined sky-disc, cloud, and diffuse-sky image. Use 1.5 display pixels of soft edge to reduce stair-stepping.
- Validation: `tests/test_sky_disc_viewport_and_ground_tint.py`, `tests/clouddisc/test_cloud_missing_overlay.py`, and `tests/test_molecular_cloud_overlay.py` passed (56 tests); targeted Ruff check passed.
- Implementation: Removed the now-unneeded low-resolution terrain-mask plumbing from sky-disc and diffuse-sky generation; the compositor now performs the sole post-upscale clip.
- Follow-up: Gate the post-faint-star cloud pass on non-simplified mode so Space-key display cycling permits clouds in inverted-city mode but suppresses them in simplified modes.
- Validation: Targeted Ruff and compileall passed. Qt-based window tests abort in this headless environment during collection, so they were not usable for this follow-up check.
- Bug fix: Also gate the cached faint-star surface's post-star cloud redraw on non-simplified mode; this was the remaining Space-key path.
- Validation: `QT_QPA_PLATFORM=offscreen .venv/bin/pytest tests/test_window_render_overlays.py -q` passed (35 tests), with targeted Ruff and compileall passing.
- Regression fix: Keep the cached faint-star surface's bright-star redraw outside the simplified-only cloud gate, so simplified mode hides clouds without hiding stars.
- Cleanup: Remove the ground-tint setting, CLI option, GUI field, render field, dead renderer, Atlas profile entry, and current documentation references.
- Validation: Ground-tint-related and option-dependent tests passed except for one pre-existing `sky_disc_alpha` expectation mismatch in `test_prepare_window_user_options_normalizes_terrain_horizon_fields`; targeted Ruff and compileall passed.
- Topic: Earth guide line width
  - Decision: Increase the Earth guide foreground line width from 1.5 px to 2.25 px (1.5x).
  - Rationale: Make the guide easier to read while preserving the existing rendering path and opacity behavior.
- Topic: Earth guide latitude-aligned line width
  - Decision: Set the fill/latitude-aligned line width to 2.2 px, and use its named constant at draw time.
  - Rationale: Improve visibility of the latitude-aligned guide lines.
- Topic: Star clipping at sky-disc edges
  - Decision: Apply the sky-disc circular clip to direct star passes and to cached/interpolated star-surface compositing.
  - Rationale: Prevent stars from appearing outside the sky disc, including at asymmetric viewport corners.
- Follow-up: Match the star clip radius to the sky-disc 0.75-degree overscan so faint stars are not clipped inside the visible disc edge.
- Follow-up: Defer clipping of the downsampled faint-star surface until after upscaling, so its low-resolution boundary does not move inward.
- Regression fix: Keep the cached/interpolated faint-star source surface unclipped and apply the circular sky-disc clip only after its presentation transform. This prevents the stale source-circle edge from moving inside the sky background near all four corners.
- Validation: Star interpolation, overscan, and star-layer tests passed (42 tests); the additional render-context/cache/overlay set passed 61 tests with one pre-existing stub failure because `asterism_opacity` is missing. Targeted Ruff and `git diff --check` passed.

- Topic: Interactive rendering slowdown investigation
  - Decision: Identify the full-resolution terrain-horizon sky clip added in
    `aa85919d` as the primary regression; do not change implementation during
    this diagnostic task.
  - Rationale: `_clip_below_terrain_horizon()` converts the complete composed
    image to RGBA, inverse-projects every pixel, interpolates the terrain
    horizon, and applies a float coverage blend. On this machine, an isolated
    warm benchmark measured about 80 ms at 1600 x 900, 183 ms at 2560 x 1440,
    and 403 ms at 3840 x 2160 per call. The compositor cache key includes the
    view center, so a direction change necessarily reruns this work. A
    comparison benchmark for the new star-disc `QPainterPath` clip remained
    around 1 ms at 4K and does not explain the regression.
  - Alternatives Considered: The August 26 cloud path was considered because
    its first redraw is still substantial, but it predates the latest
    regression, renders at the faint-star internal resolution, and caches its
    completed raster for presentation-only refreshes. Hover-only refreshes
    also reuse the normal composed frame, so the new terrain pass mainly
    explains rotation, resize, and other base-frame invalidation latency rather
    than an isolated mouse-hover slowdown.
- Validation: Ran isolated offscreen Qt timing loops for the terrain clip and
  clipped/unclipped `QImage` presentation. No product source or tests were
  changed.

- Topic: Narrow-band terrain-horizon clipping
  - Decision: Classify definite ground and a conservative ridge guard at the
    sky-disc render scale, clear definite ground after expansion, and perform
    display-resolution inverse projection and soft clipping only inside the
    expanded guard.
  - Rationale: Preserve extra low-resolution surface content past the ridge so
    upscaling cannot expose a jagged mask, while avoiding full-resolution
    projection for pixels far from the ridge. The guard spans two low-resolution
    pixels on both sides and is expanded by two cells before nearest-neighbour
    mapping to prevent corner and disc-edge classification gaps.
  - Validation: Comparison against the full-resolution horizon classification
    found no retained pixels below the soft edge at 1600 x 900, 2560 x 1440,
    or 3840 x 2160. Warm isolated timings improved from approximately 80 to
    28 ms, 183 to 58 ms, and 403 to 121 ms respectively. The 57 relevant sky,
    cloud, and molecular-cloud tests passed; targeted Ruff, compileall, and
    `git diff --check` passed.
