# Session 2026-06-24 — Cloud stripe count now absolute

## Scope
Change `--cloud-stripe` `COUNT` semantics from "density relative to 600x600 star surface" to an absolute stripe count that does not scale with window size.

## Decision Log

- Topic: Cloud stripe `COUNT` scaling behavior
  - Decision: `COUNT` is now interpreted as the absolute number of stripes/cells across the disc. It is no longer multiplied by the render surface size / 600.
  - Rationale: The user asked that a value like `jellybean,20` keep 20 divisions even when the window is enlarged, so stripe spacing grows with the window instead of density staying constant.
  - Alternatives Considered: Adding a new option (e.g. `jellybean,20,absolute`). Rejected because the desired behavior is simpler and matches the literal meaning of the count parameter.

## Implementation Notes

- `src/zstarview/gui/composite.py`:
  - `_scaled_cloud_target_stripes()` now returns `max(1, int(target_stripes))` unchanged, ignoring the reference size parameters (kept for API compatibility).
  - For `jellybean` mode, grid spacing `delta` is now computed from the actual output diameter (`min(w, h)`) divided by `target_stripes`, instead of the star-layer reference size. This makes the number of jellybean chains constant across window sizes.
  - `width`/`alpha` modes already used `_scaled_cloud_target_stripes()` to derive `period = diameter / stripes`, so with the helper returning the absolute count, they naturally keep the same stripe count on larger windows (period grows proportionally).

- `src/zstarview/cli/args.py`: Updated `--cloud-stripe` help text to say count is absolute and not scaled.

- `docs/cli-overlays.md` / `docs/cli-overlays-ja_JP.md`: Updated user-facing descriptions of `COUNT` to match the new behavior.

- `tests/clouddisc/test_cloud_hatch.py`:
  - Renamed `test_scaled_cloud_target_stripes_tracks_buffer_size` to `test_scaled_cloud_target_stripes_is_absolute` and adjusted assertions.
  - Renamed `test_variable_width_cloud_stripes_use_star_surface_density` to `test_variable_width_cloud_stripes_keep_same_count_across_sizes`, removed the `density_reference_size` test path, and compared 800x800 vs 1200x1200 outputs to verify equal stripe counts (sizes chosen to stay above the minimum period clamp).



- Topic: Default cloud options changed to jellybean,24 / opacity 0.05
  - Decision: Changed the default `--cloud-stripe` to `jellybean,24,1.0` and `--cloud-opacity` to `0.05` for all entry points (`zstarview`, `zstarview-gui`, `zstarview-export-image`).
  - Rationale: User requested these defaults. The GUI launch profile derives from `parse_args([])`, so updating the CLI argument defaults automatically propagates to the GUI startup dialog.
  - Files changed: `src/zstarview/cli/args.py`, `docs/cli-overlays.md`, `docs/cli-overlays-ja_JP.md`.


- Topic: Subtle jellybean outline
  - Decision: Added a faint outline pass behind each jellybean circle/chain before drawing the main shape.
  - Implementation: In `_render_jellybean_cloud_rgba_from_altaz_grid`, draw each shape once with `diam + 3.0` px width and 20% of the main alpha, then draw the normal shape on top. Because the same RoundCap geometry is used, the halo blends naturally around the round line ends.
  - Rationale: User wanted a barely-there contour around clouds. Since the jellybean style renders as round-cap line segments, a true polygon outline is not available; a slightly thicker low-alpha stroke is the natural equivalent.


- Topic: Updated design/spec docs for cloud stripe changes
  - Decision: Updated `docs/design/rendering-pipeline.md` and `docs/specification.md` to reflect the new jellybean default, absolute `COUNT` semantics, and subtle outline pass.
  - Details:
    - `docs/specification.md`: Added section `6.8.1 雲ストライプ` describing `--cloud-stripe`, the three modes, default `jellybean,24,1.0`, and cloud opacity default `0.05`.
    - `docs/design/rendering-pipeline.md`: Replaced outdated MVP circle description with the three stripe modes, moved the detailed jellybean algorithm into `4.2a 雲ストライプ描画モード`, and documented the low-alpha outline pass.

## Validation

- `ruff check src/zstarview/gui/composite.py src/zstarview/cli/args.py tests/clouddisc/test_cloud_hatch.py` — passed.
- `python -m compileall src/zstarview/gui/composite.py src/zstarview/cli/args.py` — passed.
- `python -m pytest tests/clouddisc/test_cloud_hatch.py -q` — 20 passed.
- Manual spot check: `jellybean` with `target_stripes=20` produced 20 chain groups across the center line for both 600x600 and 1200x1200 outputs.

## Follow-ups

- Verify visual behavior at startup dialog / saved profiles after merging, since `COUNT` semantics changed for existing saved values.
- Consider whether the default counts (width=50, alpha=50, jellybean=100) still look reasonable on very large windows, since visual density now drops as the window grows.

- Topic: Reverted all jellybean grid/experiments
  - Decision: Reverted the working tree to the previous commit with `git reset --hard`, discarding the brick layout, interpolated boundaries, and radius-shrink attempts in `_render_jellybean_cloud_rgba_from_altaz_grid`.
  - Rationale: User reported that even after shrinking segments by the cap radius, chain ends still visibly overlapped. Rather than layering further heuristics, return to the baseline and pursue a different approach.
  - Command: `git reset --hard`
  - Result: HEAD is now at `039b3b06 feat: make cloud-stripe count absolute and set jellybean/opacity defaults`. `src/zstarview/gui/composite.py` restored to its pre-experiment state.


- Topic: Render jellybean style as individual circles instead of chains
  - Decision: Replaced chain-based line segments in `_render_jellybean_cloud_rgba_from_altaz_grid` with per-cell circles. Each non-empty grid cell is drawn as a single round dot; adjacent cells are no longer connected.
  - Rationale: Previous attempts to hide aligned chain endpoints (brick layout, boundary interpolation, cap-radius shrink) all introduced visible artifacts. Connecting circles into segments inherently creates aligned endpoints along the same 45° screen lines, which the user found distracting. Drawing isolated circles removes the "chain endpoint" concept entirely.
  - Implementation: `src/zstarview/gui/composite.py`:
    - Removed the `processed` set, chain extraction loop, and `segments` list.
    - Iterate over sampled cells and append a circle for each cell in `cell_level`.
    - Removed segment drawing passes from the QPainter path; only the per-circle outline and fill passes remain.
    - Updated the function docstring to describe the dots-only style.

- 21:50 Validation
  - Ran: `uv run -p .venv/bin/python ruff check src/zstarview/gui/composite.py`
    Output: `All checks passed!`
    Result: No new lint issues.
  - Ran: `uv run -p .venv/bin/python pytest tests/test_window_cloud_partial_mode.py tests/test_window_render_sync.py tests/test_clouddisc_altaz_debug.py tests/clouddisc/test_cloud_hatch.py -q`
    Output: `134 passed in 2.74s`
    Result: Cloud-related tests pass.


- Topic: Scale jellybean circles up for overlap-based hatching
  - Decision: Increased the default `width_factor` for jellybean mode from `1.0` to `2.0` and changed the diameter scale formula so diameters are proportional to `delta` instead of `delta / sqrt(2) * 0.95`. This makes the largest quantized circles overlap their diagonal neighbours, producing a hatching-style fill for dense cloud while smaller levels still read as individual dots.
  - Rationale: User wanted a unified width_factor scaling so that all 8 quantized diameters grow together, avoiding discontinuous size gaps between levels. A larger default factor lets heavy cloud regions merge visually while preserving the full level ramp.
  - Implementation:
    - `src/zstarview/gui/composite.py`: updated the diameter comment and changed `diam_scale = delta / 30.0 * wf / math.sqrt(2.0) * 0.95` to `diam_scale = delta / 30.0 * wf`.
    - `src/zstarview/cli/args.py`: changed `--cloud-stripe` default from `("jellybean", 24, 1.0)` to `("jellybean", 24, 2.0)` and updated the help text.

- 22:00 Validation
  - Ran: `uv run -p .venv/bin/python ruff check src/zstarview/gui/composite.py src/zstarview/cli/args.py tests/clouddisc/test_cloud_hatch.py`
    Output: `All checks passed!`
    Result: No new lint issues.
  - Ran: `uv run -p .venv/bin/python pytest tests/test_window_cloud_partial_mode.py tests/test_window_render_sync.py tests/test_clouddisc_altaz_debug.py tests/clouddisc/test_cloud_hatch.py -q`
    Output: `134 passed in 2.78s`
    Result: Cloud-related tests pass.


- Topic: Reduce jellybean default width_factor from 2.0 to 1.4
  - Decision: Lowered the default jellybean `width_factor` from `2.0` to `1.4` after observing that `2.0` made the largest and second-largest quantized circles overlap so heavily that they became indistinguishable solid fill.
  - Rationale: `1.4` keeps the largest circles close to the diagonal cell pitch so neighbouring heavy-cloud dots still overlap for a hatching effect, while preserving visible diameter differences between the top levels.
  - Implementation: `src/zstarview/cli/args.py`: changed default tuple from `("jellybean", 24, 2.0)` to `("jellybean", 24, 1.4)` and updated the help string.

- 22:08 Validation
  - Ran: `uv run -p .venv/bin/python ruff check src/zstarview/cli/args.py src/zstarview/gui/composite.py`
    Output: `All checks passed!`
    Result: No new lint issues.
  - Ran: `uv run -p .venv/bin/python pytest tests/test_window_cloud_partial_mode.py tests/test_window_render_sync.py tests/test_clouddisc_altaz_debug.py tests/clouddisc/test_cloud_hatch.py -q`
    Output: `134 passed in 2.89s`
    Result: Cloud-related tests pass.


- Topic: Reduce jellybean default width_factor from 1.4 to 1.1
  - Decision: Lowered the default jellybean `width_factor` again, from `1.4` to `1.1`, because `1.4` still produced too much overlap between the largest circles.
  - Rationale: `1.1` brings the largest quantized diameter close to the diagonal cell pitch without exceeding it, giving a subtle hatching effect while keeping all 8 quantized sizes visually distinct.
  - Implementation: `src/zstarview/cli/args.py`: changed default tuple from `("jellybean", 24, 1.4)` to `("jellybean", 24, 1.1)` and updated the help string.

- 22:12 Validation
  - Ran: `uv run -p .venv/bin/python ruff check src/zstarview/cli/args.py src/zstarview/gui/composite.py`
    Output: `All checks passed!`
    Result: No new lint issues.
  - Ran: `uv run -p .venv/bin/python pytest tests/test_window_cloud_partial_mode.py tests/test_window_render_sync.py tests/test_clouddisc_altaz_debug.py tests/clouddisc/test_cloud_hatch.py -q`
    Output: `134 passed in 2.58s`
    Result: Cloud-related tests pass.


- Topic: Treat width_factor as a radius scale for jellybean circles
  - Decision: Halved the jellybean diameter scale so that `width_factor` now controls the maximum circle *radius* rather than diameter. With the default `1.1`, the largest circle has a radius of roughly one grid cell (`delta`) and a diameter of about `delta`, so dense clouds touch but do not merge into solid fill.
  - Rationale: User felt previous values overlapped too much; the original formula had been scaling diameters as if they were radii. Halving restores the intuitive "radius = pitch" relationship.
  - Implementation: `src/zstarview/gui/composite.py`: changed `diam_scale = delta / 30.0 * wf` to `diam_scale = delta / 30.0 * wf * 0.5` and updated the comment.

- 22:16 Validation
  - Ran: `uv run -p .venv/bin/python ruff check src/zstarview/gui/composite.py src/zstarview/cli/args.py`
    Output: `All checks passed!`
    Result: No new lint issues.
  - Ran: `uv run -p .venv/bin/python pytest tests/test_window_cloud_partial_mode.py tests/test_window_render_sync.py tests/test_clouddisc_altaz_debug.py tests/clouddisc/test_cloud_hatch.py -q`
    Output: `134 passed in 2.62s`
    Result: Cloud-related tests pass.


- Topic: Increase jellybean default width_factor from 1.1 to 1.4
  - Decision: Raised the default jellybean `width_factor` from `1.1` to `1.4` to let the largest circles lightly touch their diagonal neighbours while keeping the level sizes distinct.
  - Rationale: After fixing the diameter-to-radius interpretation, `1.1` produced no overlap at all; `1.4` brings the maximum diameter close to the diagonal cell pitch for a subtle hatching effect.
  - Implementation: `src/zstarview/cli/args.py`: changed default tuple from `("jellybean", 24, 1.1)` to `("jellybean", 24, 1.4)` and updated the help string.

- 22:20 Validation
  - Ran: `uv run -p .venv/bin/python ruff check src/zstarview/cli/args.py src/zstarview/gui/composite.py`
    Output: `All checks passed!`
    Result: No new lint issues.
  - Ran: `uv run -p .venv/bin/python pytest tests/test_window_cloud_partial_mode.py tests/test_window_render_sync.py tests/test_clouddisc_altaz_debug.py tests/clouddisc/test_cloud_hatch.py -q`
    Output: `134 passed in 2.77s`
    Result: Cloud-related tests pass.


- Topic: Increase jellybean default width_factor from 1.4 to 1.6
  - Decision: Raised the default jellybean `width_factor` from `1.4` to `1.6` to increase overlap slightly.
  - Implementation: `src/zstarview/cli/args.py`: changed default tuple from `("jellybean", 24, 1.4)` to `("jellybean", 24, 1.6)` and updated the help string.

- 22:24 Validation
  - Ran: `uv run -p .venv/bin/python ruff check src/zstarview/cli/args.py src/zstarview/gui/composite.py`
    Output: `All checks passed!`
  - Ran: `uv run -p .venv/bin/python pytest tests/test_window_cloud_partial_mode.py tests/test_window_render_sync.py tests/test_clouddisc_altaz_debug.py tests/clouddisc/test_cloud_hatch.py -q`
    Output: `134 passed in 2.63s`


- Topic: Adopt user-tuned jellybean defaults: `jellybean,50,1.7`
  - Decision: Set the default `--cloud-stripe` to `("jellybean", 50, 1.7)` after the user tested various combinations and found this one the most effective.
  - Rationale: `count=50` gives finer grid cells than the previous `24`, and `width_factor=1.7` provides slightly more overlap than `1.6` for a denser hatching look without losing the dot-style readability.
  - Implementation: `src/zstarview/cli/args.py`: changed default tuple from `("jellybean", 24, 1.6)` to `("jellybean", 50, 1.7)` and updated the help string.

- 22:28 Validation
  - Ran: `uv run -p .venv/bin/python ruff check src/zstarview/cli/args.py src/zstarview/gui/composite.py`
    Output: `All checks passed!`
  - Ran: `uv run -p .venv/bin/python pytest tests/test_window_cloud_partial_mode.py tests/test_window_render_sync.py tests/test_clouddisc_altaz_debug.py tests/clouddisc/test_cloud_hatch.py -q`
    Output: `134 passed in 2.69s`

