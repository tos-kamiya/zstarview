- Topic: Night-light and ridge-glow default tune
  - Decision: Change the defaults to `--night-light-opacity 0.04` and `--ridge-glow-opacity 0.02` across CLI parsing, GUI defaults, renderer defaults, docs, and the tests that pin those defaults.
  - Rationale: The new pair keeps the base street-light layer slightly stronger than the ridge glow layer while preserving the separate control surface already exposed to users.
- Topic: Night-light and ridge-glow startup warmup
  - Decision: Treat startup night-light warmup as required when either `night_light_opacity` or `ridge_glow_opacity` is above zero, so the shared glow profile is prepared whenever either visible layer needs it.
  - Rationale: Night light and ridge glow share the same computed profile but are rendered independently. Using an OR condition keeps the preparation path aligned with the displayed layers while preserving separate display toggles.
- Topic: Ridge-glow CLI exposure
  - Decision: Add `--ridge-glow-opacity` as a first-class CLI option and wire it through the GUI/export startup path so ridge glow can be tuned independently of night light.
  - Rationale: Ridge glow is already a separate render-layer input, so exposing it directly keeps the public interface aligned with the internal model and avoids overloading `--night-light-opacity` with two different roles.
- Topic: Night-light and ridge-glow azimuth sigma adjustment
  - Decision: Lower `NIGHT_LIGHTS_NEIGHBORHOOD_SIGMA_DEG` from `8.0` to `6.0` so the azimuth-neighborhood weighting falls off faster for both night light and ridge glow.
  - Rationale: The sigma controls the shape of the shared azimuth kernel in the glow accumulation path, so reducing it tightens both effects without changing the explicit azimuth cutoff used for pruning.
- Topic: Ridge-glow opacity default update
  - Decision: Lower the default `ridge_glow_opacity` from `0.02666666666666667` to `0.025` across the GUI user options, render-style defaults, compositor defaults, and internal tests.
  - Rationale: The slightly lower default keeps the additive ridge layer subtle while preserving the existing tuning model, and aligning the defaults avoids drift between the UI state and the renderer.
- Topic: Night-light opacity default update
  - Decision: Raise the default `--night-light-opacity` from `0.02` to `0.05` across the CLI, GUI startup defaults, renderer defaults, and documentation.
  - Rationale: The new default keeps the street-light layer visible without making it as strong as the previous tuning, and syncing the defaults prevents the GUI, export path, and help text from diverging.
- Topic: Night light and ridge glow performance design
  - Decision: Treat the work as a two-stage optimization: first precompute terrain visibility thresholds into an azimuth-distance grid for ridge glow, then reduce Python overhead in glow accumulation by reusing azimuth kernels and shifting band work toward chunked NumPy operations.
  - Rationale: The current hot path is not the final QImage compositing step; it is the repeated terrain-threshold scanning and the per-sample accumulation inside `night_lights.py`. Precomputing the threshold grid removes repeated azimuth-by-distance terrain walks, while kernel reuse and chunked accumulation reduce repeated Gaussian lookup and Python loop overhead without changing the visible model.
  - Alternatives Considered: A full 4D or 5D tensor formulation would be more vectorized but is likely to increase memory use too much for limited benefit. The compositor-side interpolation loop is still worth cleaning up later, but it is secondary because the mask is already built on a reduced grid.
- Topic: Investigate night light glow alpha not rendering
  - Decision: Start tracing the rendering path and configuration source for the glow alpha.
  - Update: Rolled tracked files back to `71d1f36c` to remove the ridge glow branch, then trimmed ridge glow-specific tests and a few brittle terrain expectations to match the restored code path.
  - Validation: `pytest -q tests/test_glow_mask.py tests/test_window_render_sync.py tests/test_cli_arg_helpers.py tests/test_viewer_startup_errors.py tests/test_export_image_layer_gating.py tests/test_sky_disc_viewport_and_ground_tint.py tests/terrain/test_terrain_horizon_ui.py tests/test_night_lights.py`
  - Update: Replaced the night-light distance attenuation with a linear distance boost that grows toward the 128 km band edge, so the accumulated glow gets stronger with distance instead of decaying.
  - Validation: `pytest -q tests/test_night_lights.py tests/test_glow_mask.py tests/test_window_render_sync.py tests/test_sky_disc_viewport_and_ground_tint.py tests/test_export_image_layer_gating.py tests/test_cli_arg_helpers.py tests/terrain/test_terrain_horizon_ui.py`
  - Update: Split the current effect into two composited layers: keep the existing night light profile as the base layer, and add a separate edge glow layer built from the new edge alpha grid so the additive behavior is explicit in the compositor.
  - Validation: `uv run -p .venv/bin/python pytest -q tests/test_night_lights.py tests/test_glow_mask.py tests/test_window_render_sync.py tests/test_sky_disc_viewport_and_ground_tint.py tests/test_export_image_layer_gating.py tests/test_cli_arg_helpers.py tests/terrain/test_terrain_horizon_ui.py`
  - Update: Reworked the edge glow source so it is driven by DEM-derived terrain heights plus distance only; removed the night-light tile brightness from the edge layer and keep the distance boost as a separate multiplier.
  - Validation: `uv run -p .venv/bin/python pytest -q tests/test_night_lights.py tests/test_glow_mask.py tests/test_window_render_sync.py tests/test_sky_disc_viewport_and_ground_tint.py tests/test_export_image_layer_gating.py tests/test_cli_arg_helpers.py tests/terrain/test_terrain_horizon_ui.py`
  - Update: Reduced the DEM-derived edge glow input by dividing the relative terrain elevation by 3 before the existing accumulation path, so ridge glow stays visibly present but much subtler.
  - Validation: `uv run -p .venv/bin/python pytest -q tests/test_night_lights.py tests/test_glow_mask.py`
  - Update: Separated the base night-light opacity from the edge glow opacity in the compositor, then removed stale cross-wired argument names from `_resolve_edge_glow_mask()` / `_build_edge_glow_mask()` so the edge path now receives only `ridge_glow_opacity`.
  - Validation: `uv run -p .venv/bin/python ruff check src/zstarview/gui/composite.py`; `uv run -p .venv/bin/python pytest -q tests/test_glow_mask.py -k 'build_glow_mask or compositor_builds_edge_glow_mask_separately'`
  - Update: Simplified the public specification and internal design notes so they now describe night light as the base layer and ridge glow as a separate additive layer with its own opacity.
  - Update: Bumped the package version to `1.31.10` and added a release-note entry for the separate night-light / ridge-glow opacity controls.
- Topic: Investigate glow vs cloud draw order
  - Decision: Keep this as a design-only investigation for now and do not change rendering code yet.
  - Rationale: The current base scene compositor builds a single sky/cloud image first, then adds night-light and ridge-glow onto that composited image in `SkyCompositorCache.draw()`. To make glow appear in front of clouds in a controlled way, the glow pass would need to be split out of the shared sky/cloud composite so it can be layered explicitly relative to clouds instead of being baked into the same cached image.
  - Result: Identified the relevant boundaries in `src/zstarview/gui/composite.py` and `src/zstarview/render/pipeline.py`; no implementation or tests were changed.
- Topic: Repaint trigger for glow visibility
  - Decision: The useful design split is not "cloud missing => skip cloud composite", because that branch already exists. The meaningful change would be to separate sky/glow repaint triggers from cloud projection/render triggers so a completed sky snapshot can become visible immediately, even if cloud fetching or cloud projection is still pending.
  - Rationale: `request_client_update()` only schedules a Qt repaint; the actual frame is still governed by the shared frame cache and by whichever background callback last invalidated it. Because the base frame cache currently keys sky, glow, and cloud state together, cloud progress can effectively coalesce the visible update even when glow is already computed.
  - Update: Reflected this separation in `docs/design/rendering-pipeline.md` and added a short note to `docs/design.md` that glow visibility should not wait on cloud download completion.
- Scope: Startup sequencing for static overlay cache warm-up vs delayed dynamic overlays.
- Notes: Investigate whether DEM/terrain/night-light/ridge-glow/water/urban outline can be warmed before the main GUI reveal, while clouds/sky-disc/aircraft/satellites remain deferred until after the window is shown.
- Summary: The current startup gate already excludes clouds from the initial reveal path; next design step is to split "static derived overlays" from "live overlays" more explicitly before any code changes.
- Topic: `zstarview-export-image` layer acquisition order
  - Decision: Allow export-image to prioritize short-lived layers first, with aircraft and satellites ahead of cloud, while keeping cloud as an independent fetch path and leaving static layers to follow afterward.
  - Rationale: export-image is a single-shot command, so maximizing freshness matters more than mirroring the GUI splash sequence. Cloud still benefits from an independent download path, but it does not need to block the short-lived overlays or the final composite.
  - Result: Reflected the policy in `docs/design/runtime.md` and added a short summary to `docs/design.md`.
- Topic: `zstarview-export-image` parallel cloud fetch implementation
  - Decision: Start the cloud download in a daemon worker thread as soon as the sky snapshot is ready, then fetch aircraft, satellites, and the static layers on the main path before joining the cloud result for the final composite.
  - Rationale: This preserves the single-shot behavior while allowing the most time-sensitive overlays to finish first and letting cloud download overlap with the rest of the export pipeline.
  - Validation: `uv run -p .venv/bin/python pytest -q tests/test_export_image_layer_gating.py tests/test_cli_arg_helpers.py`
  - Validation: `uv run -p .venv/bin/python ruff check src/zstarview/cli/export_image.py`
- Topic: `zstarview-export-image` splash-style progress logs
  - Decision: Add stage logs that mirror the GUI splash phrasing, using `Fetching initial ...`, `Calculating initial ...`, and `... ready` messages around export-image's sky, cloud, aircraft, satellite, terrain, night light, urban, and water phases.
  - Rationale: The export command can spend minutes in heavy work with no visible progress, so splash-like stage logs make it clear that the process is still advancing rather than hung.
  - Validation: `uv run -p .venv/bin/python pytest -q tests/test_export_image_layer_gating.py tests/test_cli_arg_helpers.py`
  - Validation: `uv run -p .venv/bin/python ruff check src/zstarview/cli/export_image.py`
- Topic: Cloud processing phase logging
  - Decision: Split cloud processing logs into source lookup, tile download, and image projection stages so the slow segment can be identified from the log stream.
  - Rationale: The long pauses in export-image are not caused by cloudiness itself but by a combination of source discovery, tile retrieval, and projection. Separating those phases makes it easier to see whether the delay is network-bound or CPU-bound.
  - Validation: `uv run -p .venv/bin/python pytest -q tests/test_export_image_layer_gating.py tests/test_cli_arg_helpers.py`
  - Validation: `uv run -p .venv/bin/python ruff check src/zstarview/clouddisc/core.py src/zstarview/clouddisc/providers/hima.py src/zstarview/clouddisc/workers/cloud_source.py src/zstarview/cli/export_image.py`
- Topic: Night-light azimuth neighborhood reduction
  - Decision: Clamp the night-light glow neighborhood to +/-25 degrees in azimuth by zeroing gaussian weights beyond that span.
  - Rationale: The previous neighborhood spread was broader than necessary for the observed glow behavior and made the azimuthal accumulation more expensive than needed. A hard cutoff keeps the existing weighting shape near the center while bounding the lateral work.
  - Validation: `uv run -p .venv/bin/python pytest -q tests/test_night_lights.py`
  - Validation: `uv run -p .venv/bin/python ruff check src/zstarview/night_lights.py tests/test_night_lights.py`
- Topic: Night-light final azimuth smoothing removal
  - Decision: Remove the final circular azimuth smoothing pass from the night-light strength aggregation.
  - Rationale: After bounding the neighborhood width to +/-25 degrees, the extra +/-3 bin smoothing added another lateral blur without improving the new constrained profile. Removing it makes the output easier to reason about and reduces the remaining azimuthal work.
  - Validation: `uv run -p .venv/bin/python pytest -q tests/test_night_lights.py`
  - Validation: `uv run -p .venv/bin/python ruff check src/zstarview/night_lights.py tests/test_night_lights.py`
- Topic: Ridge glow mask placement
  - Decision: Keep night light rendering unchanged, and treat ridge glow as a separate intermediate alpha grid that is masked at draw time instead of being hard-clipped during alpha-grid generation.
  - Rationale: The visible jaggedness comes from the main ridge glow edge, not the night-light base layer. Deferring the mask lets the ridge glow carry intensity slightly below the main ridge line and produces a cleaner final edge when terrain masking is applied during composition.
  - Result: Reflected this in `docs/design.md` and `docs/design/runtime.md`.
- Topic: Ridge glow coarse-plus-fine model
  - Decision: Model ridge glow as a coarse full-field base plus a higher-resolution refinement only near the main ridge line; outside the refinement window, fall back to the coarse model rather than extrapolating missing values.
  - Rationale: The jaggedness is localized near the primary ridge, so a local refinement can preserve detail where it matters without forcing the whole field to stay expensive. Using the coarse model as the fallback avoids artificial discontinuities at the refinement boundary that extrapolation would introduce.
  - Result: Reflected this direction in `docs/design.md` and `docs/design/runtime.md`.
- Topic: Ridge glow coarse/fine fade transition
  - Decision: Blend the coarse and fine ridge glow models across a small boundary band instead of switching hard at the refinement limit.
  - Rationale: A fade reduces the chance of visible seams where the high-resolution patch ends. It keeps the coarse field as the background while allowing the fine field to dominate near the ridge crest.
  - Result: Reflected this in `docs/design.md` and `docs/design/runtime.md`.
- Topic: Ridge glow coarse/fine refinement implementation
  - Decision: Build the ridge glow edge layer from a coarse full-field background, then overwrite only the main-ridge neighborhood with locally computed fine samples on a denser altitude grid.
  - Rationale: This preserves the existing coarse scene shape while giving the ridge crest a finer vertical profile where the jaggedness is visible. The coarse field remains the fallback outside the local band, and the merge boundary fades instead of snapping.
  - Validation: `uv run -p .venv/bin/python pytest -q tests/test_night_lights.py tests/test_glow_mask.py`
  - Validation: `uv run -p .venv/bin/python ruff check src/zstarview/night_lights.py src/zstarview/gui/composite.py tests/test_night_lights.py`
- Topic: Ridge glow fine altitude resolution
  - Decision: Reduce the ridge-glow refinement altitude step to 0.5 degrees while keeping the coarse base at 2 degrees.
  - Rationale: The remaining artifacts are predominantly vertical in the zoomed ridge glow, so the finer altitude sampling is the more direct lever. The terrain horizon data itself is not snapped to 1 degree; the only rounding in the cache path is for stable keys, not for the underlying geometry.
  - Validation: `uv run -p .venv/bin/python pytest -q tests/test_night_lights.py tests/test_glow_mask.py`
  - Validation: `uv run -p .venv/bin/python ruff check src/zstarview/night_lights.py src/zstarview/gui/composite.py tests/test_night_lights.py`
- Topic: Ridge glow fill-first merge
  - Decision: Replace the edge-layer blend with a fill-first merge: compute the fine ridge band first, copy exact coarse-altitude rows into remaining gaps, and finish with altitude-neighbor interpolation for any leftover missing cells.
  - Rationale: The previous blend still behaved like an additive mix at the refinement boundary, which preserved visible jaggedness and made the transition hard to reason about. The new order keeps fine samples authoritative, uses the coarse field only as a fallback, and leaves interpolation as a last-resort completion step.
  - Validation: `uv run -p .venv/bin/python pytest -q tests/test_night_lights.py tests/test_glow_mask.py`
  - Validation: `uv run -p .venv/bin/python ruff check src/zstarview/night_lights.py src/zstarview/gui/composite.py tests/test_night_lights.py`
- Topic: Compare sun-alt gating with sky-disc coloring
  - Decision: Treat night light / ridge glow as visibility-amplitude layers gated by the sun altitude, while the sky disc uses the sun altitude as a continuous atmospheric-color input.
  - Rationale: The glow layers are effectively binary-to-smooth overlays that fade out as the sun rises, whereas the sky disc still renders a full color model across a wide altitude range. They share the same sun-alt signal, but they do not use it in the same way.
- Topic: Terrain horizon azimuth refinement
  - Decision: Change the GUI terrain horizon scan default from 1.0-degree azimuth steps to 0.5-degree steps.
  - Rationale: The remaining 1-degree cranks in the screenshots appear to come from the terrain horizon polyline itself rather than the glow layer. Matching the GUI controller to the export-image azimuth resolution gives the horizon a denser sampling grid and reduces the chance of visible angular kinks.
  - Validation: `uv run -p .venv/bin/python pytest -q tests/terrain/test_terrain_horizon_ui.py tests/test_controller_shutdown_waits.py`
- Implemented terrain visibility threshold grid precomputation for night-light and ridge-glow generation.
- Added cached azimuth weight matrices so repeated glow accumulation reuses the same lateral kernel.
- Kept compositor behavior unchanged; the optimization stays inside the profile-generation path.
- Validation: `uv run -p .venv/bin/python ruff check src/zstarview/night_lights.py tests/test_night_lights.py`
- Validation: `uv run -p .venv/bin/python pytest -q tests/test_night_lights.py tests/test_glow_mask.py`
- Validation: `uv run -p .venv/bin/python pytest -q tests/test_export_image_layer_gating.py`
- Topic: Version bump for glow-profile optimization
  - Decision: Bump the package version by one patch step and document the glow-profile speedup in `release-notes.md`.
  - Rationale: The optimization is user-visible and already validated, so it belongs in the public release history rather than only in internal notes. Keeping the version update and release note entry together prevents the metadata from drifting out of sync.
## Session Summary

- Updated the night-light fade window to start at `-9.0` deg and reach zero at `-4.0` deg.
- Aligned GUI/export warm-up gates with the same cutoff so the glow path is skipped once it is fully invisible.
- Added regression coverage for the new fade boundaries.
- Validation: `uv run -p .venv/bin/python pytest -q tests/test_night_lights.py tests/test_glow_mask.py`
- Validation: `uv run -p .venv/bin/python ruff check src/zstarview/night_lights.py src/zstarview/cli/export_image.py src/zstarview/gui/sky_worker.py src/zstarview/gui/window_updates.py tests/test_night_lights.py docs/specification.md`
  - Validation: `uv run -p .venv/bin/python ruff check src/zstarview/gui/terrain_controller.py tests/terrain/test_terrain_horizon_ui.py`
- Topic: Roll back ridge-glow experiment
  - Decision: Revert the ridge-glow fine-grid merge and the GUI terrain azimuth refinement, restoring the earlier coarse-grid-only behavior.
  - Rationale: The experimental refinements did not improve the visible artifact in the screenshots. Returning to the previous simpler path removes the added complexity and gives us a clean baseline for the next attempt.
- Topic: Terrain mask altitude fade
  - Decision: Replace the main-ridge and secondary-ridge binary masks with an altitude fade that linearly ramps from 0 to 1 over one altitude bin width below the threshold, using the existing 2-degree altitude step.
  - Rationale: The aliasing is most visible as stepped ridge edges, so a small vertical fade is a low-risk way to soften both the main ridge and the secondary ridges without changing the underlying horizon geometry.
  - Validation: `uv run -p .venv/bin/python pytest -q tests/test_night_lights.py tests/test_glow_mask.py`
  - Validation: `uv run -p .venv/bin/python ruff check src/zstarview/night_lights.py tests/test_night_lights.py`
- Topic: Design wording cleanup
  - Decision: Remove the explicit "few-bin" wording from the design docs and describe the ridge-glow transition more generally as a smooth boundary.
  - Rationale: The current implementation no longer depends on a multi-bin fade width, so the design text should avoid pinning behavior to a detail that is no longer true.
- Topic: Resize-time sky/cloud image stretching
  - Decision: Replace the compositor's fallback `QImage.scaled(..., Qt.IgnoreAspectRatio, ...)` path with aspect-preserving scaling that centers the result on a transparent canvas.
  - Rationale: Resize-time frames can briefly reuse sky/cloud images whose size no longer matches the current viewport. Stretching those images makes circular sky-disc content appear elliptical, which matches the reported artifact better than any missing FOV propagation.
  - Validation: `uv run -p .venv/bin/python pytest -q tests/clouddisc/test_cloud_hatch.py tests/test_sky_disc_viewport_and_ground_tint.py`
  - Validation: `uv run -p .venv/bin/python ruff check src/zstarview/gui/composite.py tests/clouddisc/test_cloud_hatch.py`
- Topic: Resize-time sky disc regeneration
  - Decision: Clear cached sky-disc images during resize and trigger a fresh sky update immediately with `allow_during_viewport_interaction=True`.
  - Rationale: Keeping the old sky disc around makes the resize path visibly reuse the previous size until the deferred update arrives. Dropping the stale disc and starting a new sky snapshot right away makes the viewport switch to the new geometry as soon as the worker finishes.
  - Validation: `uv run -p .venv/bin/python pytest -q tests/test_window_render_sync.py tests/terrain/test_terrain_horizon_ui.py`
  - Validation: `uv run -p .venv/bin/python ruff check src/zstarview/gui/window.py tests/test_window_render_sync.py tests/terrain/test_terrain_horizon_ui.py`
- Topic: Night-light opacity default
  - Decision: Lower the default `--night-light-opacity` from `0.08` to `0.02` across CLI parsing, GUI user options, and renderer defaults.
  - Rationale: The previous default made the street-light layer stronger than intended for the common case. Aligning the CLI, GUI, and render-layer defaults keeps startup behavior consistent and reduces the amount of manual tuning needed after launch.
  - Validation: `uv run -p .venv/bin/python pytest -q tests/test_cli_arg_helpers.py tests/test_window_render_sync.py`
  - Validation: `uv run -p .venv/bin/python ruff check src/zstarview/cli/args.py src/zstarview/gui/window_inputs.py src/zstarview/render/pipeline.py src/zstarview/gui/composite.py tests/test_cli_arg_helpers.py docs/cli-overlays.md docs/cli-overlays-ja_JP.md docs/specification.md`
- Topic: Remove mouse-press simplified-view override
  - Decision: Drop the background mouse-press override for simplified view and keep `Space` as the only input that changes simplified-view state.
  - Rationale: The press-based override was easy to trigger unintentionally and duplicated the existing keyboard toggle. Removing it simplifies the interaction model, keeps the simplified-view state deterministic, and leaves the drag/resize path focused on window movement only.
  - Validation: `uv run -p .venv/bin/python ruff check src/zstarview tests`
  - Validation: `uv run -p .venv/bin/python pytest -q tests/terrain/test_terrain_horizon_ui.py tests/test_window_render_sync.py`
- Topic: Rename simplified-view internals
  - Decision: Rename the remaining simplified-view suppression flag from `press_pending` to `simplified_view_active`.
  - Rationale: The old name implied a mouse interaction that no longer exists. Using the state directly in the name keeps the render pipeline and tests aligned with the actual behavior and reduces the chance of reintroducing the old mental model.
- Topic: Derive simplified alpha internally
  - Decision: Stop passing a separate simplified-view alpha value into `_draw_terrain_layers()` and derive the `0.4` scale from `simplified_view_active` inside the renderer.
  - Rationale: The alpha is not an independent input; it is just the rendering consequence of simplified view being active. Deriving it locally removes duplicated state, keeps the call sites smaller, and makes the function contract easier to read.
- Topic: Version bump for simplified-view cleanup
  - Decision: Bump the package version to `1.31.11` and add a release note for the Space-only simplified-view behavior.
  - Rationale: The mouse-press override removal is user-visible and belongs in the public release history. The version bump keeps the release notes aligned with the shipped state.
- Topic: Version bump for ridge-glow release
  - Decision: Bump the package version to `1.31.13` and add a release-note entry covering the dedicated ridge-glow opacity control, the tuned glow defaults, and the shared warmup path.
  - Rationale: The public-facing behavior is now shipped and should be reflected in the release history before committing. Keeping the notes short preserves the current release window summary without duplicating the detailed design log above.
- Topic: Time-shifted tropical cyclone gating
  - Decision: Hide the tropical cyclone overlay whenever the view is not real-time, using the same time-mode gate that already disables clouds, aircraft, and artificial satellites.
  - Rationale: The overlay is a live observational aid rather than a historical layer, so keeping it visible in past/future views created an inconsistent UI story. Reusing the existing overlay-availability gate keeps the behavior predictable across the GUI and export-image path.
  - Validation: `uv run -p .venv/bin/python pytest -q tests/test_overlay_time.py tests/test_export_image_layer_gating.py`
  - Validation: `uv run -p .venv/bin/python pytest -q tests/test_window_render_sync.py -k tropical_cyclone_layer_is_disabled_when_opacity_is_zero`
  - Note: `uv run -p .venv/bin/python ruff check src/zstarview/overlay_time.py src/zstarview/cli/export_image.py src/zstarview/gui/window.py tests/test_overlay_time.py tests/test_export_image_layer_gating.py` reported an existing `E731` in `src/zstarview/gui/window.py:2154`.
- Topic: Startup delta handoff for cyclone gating
  - Decision: Re-apply the tropical cyclone availability gate inside `apply_startup_delta_t()` so the GUI startup bootstrap disables the cyclone menu and opacity once the real launch-time delta is known.
  - Rationale: The main window is created before startup resolution finishes, so the initial menu state can be optimistic. The launch-time handoff already re-gates clouds, aircraft, and satellites there; cyclone needed to follow the same path to avoid remaining enabled after startup.
  - Validation: `uv run -p .venv/bin/python pytest -q tests/test_window_render_sync.py -k 'apply_startup_delta_t_disables_tropical_cyclone_layer_when_time_shifted or tropical_cyclone_layer_is_disabled_when_opacity_is_zero'`
- Topic: Cyclone menu-state parity
  - Decision: Keep the tropical cyclone action in the same visible state shape as aircraft and satellites when time-shifted: disabled, but still checked if it was originally enabled.
  - Rationale: The user intent is better represented by the checked state, while the disabled state explains that the live overlay cannot be updated for a non-real-time view. This keeps the menu semantics aligned across the live overlays instead of making cyclone look like a different class of feature.
  - Validation: `uv run -p .venv/bin/python pytest -q tests/test_window_render_sync.py -k 'apply_startup_delta_t_disables_tropical_cyclone_layer_when_time_shifted or tropical_cyclone_layer_is_disabled_when_opacity_is_zero'`
- Topic: Version bump to 1.31.14
  - Decision: Bump the package version to `1.31.14`, rename the top release-notes entry to `1.31.14`, and update the user-agent version examples in the public docs.
  - Rationale: `1.31.13` is intentionally skipped, so the next shipped version should clearly identify the actual release contents. Updating the public version strings keeps the executable metadata and the docs aligned.
