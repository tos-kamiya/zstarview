- Topic: Investigate night light glow alpha not rendering
  - Decision: Start tracing the rendering path and configuration source for the glow alpha.
  - Update: Rolled tracked files back to `71d1f36c` to remove the ridge glow branch, then trimmed ridge glow-specific tests and a few brittle terrain expectations to match the restored code path.
  - Validation: `pytest -q tests/test_glow_mask.py tests/test_window_render_sync.py tests/test_cli_arg_helpers.py tests/test_viewer_startup_errors.py tests/test_export_image_layer_gating.py tests/test_sky_disc_viewport_and_ground_tint.py tests/terrain/test_terrain_horizon_ui.py tests/test_night_lights.py`
  - Update: Replaced the night-light distance attenuation with a linear distance boost that grows toward the 128 km band edge, so the accumulated glow gets stronger with distance instead of decaying.
  - Validation: `pytest -q tests/test_night_lights.py tests/test_glow_mask.py tests/test_window_render_sync.py tests/test_sky_disc_viewport_and_ground_tint.py tests/test_export_image_layer_gating.py tests/test_cli_arg_helpers.py tests/terrain/test_terrain_horizon_ui.py`
  - Update: Split the current effect into two composited layers: keep the existing night light profile as the base layer, and add a separate edge glow layer built from the new edge alpha grid so the additive behavior is explicit in the compositor.
  - Validation: `uv run -p .venv/bin/python pytest -q tests/test_night_lights.py tests/test_glow_mask.py tests/test_window_render_sync.py tests/test_sky_disc_viewport_and_ground_tint.py tests/test_export_image_layer_gating.py tests/test_cli_arg_helpers.py tests/terrain/test_terrain_horizon_ui.py`
  - Update: Reworked the edge glow source so it is driven by DEM-derived terrain heights plus distance only; removed the night-light tile brightness from the edge layer and keep the distance boost as a separate multiplier.
  - Validation: `uv run -p .venv/bin/python pytest -q tests/test_night_lights.py tests/test_glow_mask.py tests/test_window_render_sync.py tests/test_sky_disc_viewport_and_ground_tint.py tests/test_export_image_layer_gating.py tests/test_cli_arg_helpers.py tests/terrain/test_terrain_horizon_ui.py`
  - Update: Reduced the DEM-derived edge glow input by dividing the relative terrain elevation by 3 before the existing accumulation path, so ridge glow stays visibly present but much subtler.
  - Validation: `uv run -p .venv/bin/python pytest -q tests/test_night_lights.py tests/test_glow_mask.py`
  - Update: Separated the base night-light opacity from the edge glow opacity in the compositor, then removed stale cross-wired argument names from `_resolve_edge_glow_mask()` / `_build_edge_glow_mask()` so the edge path now receives only `ridge_glow_opacity`.
  - Validation: `uv run -p .venv/bin/python ruff check src/zstarview/gui/composite.py`; `uv run -p .venv/bin/python pytest -q tests/test_glow_mask.py -k 'build_glow_mask or compositor_builds_edge_glow_mask_separately'`
  - Update: Simplified the public specification and internal design notes so they now describe night light as the base layer and ridge glow as a separate additive layer with its own opacity.
  - Update: Bumped the package version to `1.31.10` and added a release-note entry for the separate night-light / ridge-glow opacity controls.
- Topic: Investigate glow vs cloud draw order
  - Decision: Keep this as a design-only investigation for now and do not change rendering code yet.
  - Rationale: The current base scene compositor builds a single sky/cloud image first, then adds night-light and ridge-glow onto that composited image in `SkyCompositorCache.draw()`. To make glow appear in front of clouds in a controlled way, the glow pass would need to be split out of the shared sky/cloud composite so it can be layered explicitly relative to clouds instead of being baked into the same cached image.
  - Result: Identified the relevant boundaries in `src/zstarview/gui/composite.py` and `src/zstarview/render/pipeline.py`; no implementation or tests were changed.
- Topic: Repaint trigger for glow visibility
  - Decision: The useful design split is not "cloud missing => skip cloud composite", because that branch already exists. The meaningful change would be to separate sky/glow repaint triggers from cloud projection/render triggers so a completed sky snapshot can become visible immediately, even if cloud fetching or cloud projection is still pending.
  - Rationale: `request_client_update()` only schedules a Qt repaint; the actual frame is still governed by the shared frame cache and by whichever background callback last invalidated it. Because the base frame cache currently keys sky, glow, and cloud state together, cloud progress can effectively coalesce the visible update even when glow is already computed.
  - Update: Reflected this separation in `docs/design/rendering-pipeline.md` and added a short note to `docs/design.md` that glow visibility should not wait on cloud download completion.
- Scope: Startup sequencing for static overlay cache warm-up vs delayed dynamic overlays.
- Notes: Investigate whether DEM/terrain/night-light/ridge-glow/water/urban outline can be warmed before the main GUI reveal, while clouds/sky-disc/aircraft/satellites remain deferred until after the window is shown.
- Summary: The current startup gate already excludes clouds from the initial reveal path; next design step is to split "static derived overlays" from "live overlays" more explicitly before any code changes.
- Topic: `zstarview-export-image` layer acquisition order
  - Decision: Allow export-image to prioritize short-lived layers first, with aircraft and satellites ahead of cloud, while keeping cloud as an independent fetch path and leaving static layers to follow afterward.
  - Rationale: export-image is a single-shot command, so maximizing freshness matters more than mirroring the GUI splash sequence. Cloud still benefits from an independent download path, but it does not need to block the short-lived overlays or the final composite.
  - Result: Reflected the policy in `docs/design/runtime.md` and added a short summary to `docs/design.md`.
- Topic: `zstarview-export-image` parallel cloud fetch implementation
  - Decision: Start the cloud download in a daemon worker thread as soon as the sky snapshot is ready, then fetch aircraft, satellites, and the static layers on the main path before joining the cloud result for the final composite.
  - Rationale: This preserves the single-shot behavior while allowing the most time-sensitive overlays to finish first and letting cloud download overlap with the rest of the export pipeline.
  - Validation: `uv run -p .venv/bin/python pytest -q tests/test_export_image_layer_gating.py tests/test_cli_arg_helpers.py`
  - Validation: `uv run -p .venv/bin/python ruff check src/zstarview/cli/export_image.py`
- Topic: `zstarview-export-image` splash-style progress logs
  - Decision: Add stage logs that mirror the GUI splash phrasing, using `Fetching initial ...`, `Calculating initial ...`, and `... ready` messages around export-image's sky, cloud, aircraft, satellite, terrain, night light, urban, and water phases.
  - Rationale: The export command can spend minutes in heavy work with no visible progress, so splash-like stage logs make it clear that the process is still advancing rather than hung.
  - Validation: `uv run -p .venv/bin/python pytest -q tests/test_export_image_layer_gating.py tests/test_cli_arg_helpers.py`
  - Validation: `uv run -p .venv/bin/python ruff check src/zstarview/cli/export_image.py`
- Topic: Cloud processing phase logging
  - Decision: Split cloud processing logs into source lookup, tile download, and image projection stages so the slow segment can be identified from the log stream.
  - Rationale: The long pauses in export-image are not caused by cloudiness itself but by a combination of source discovery, tile retrieval, and projection. Separating those phases makes it easier to see whether the delay is network-bound or CPU-bound.
  - Validation: `uv run -p .venv/bin/python pytest -q tests/test_export_image_layer_gating.py tests/test_cli_arg_helpers.py`
  - Validation: `uv run -p .venv/bin/python ruff check src/zstarview/clouddisc/core.py src/zstarview/clouddisc/providers/hima.py src/zstarview/clouddisc/workers/cloud_source.py src/zstarview/cli/export_image.py`
- Topic: Night-light azimuth neighborhood reduction
  - Decision: Clamp the night-light glow neighborhood to +/-25 degrees in azimuth by zeroing gaussian weights beyond that span.
  - Rationale: The previous neighborhood spread was broader than necessary for the observed glow behavior and made the azimuthal accumulation more expensive than needed. A hard cutoff keeps the existing weighting shape near the center while bounding the lateral work.
  - Validation: `uv run -p .venv/bin/python pytest -q tests/test_night_lights.py`
  - Validation: `uv run -p .venv/bin/python ruff check src/zstarview/night_lights.py tests/test_night_lights.py`
- Topic: Night-light final azimuth smoothing removal
  - Decision: Remove the final circular azimuth smoothing pass from the night-light strength aggregation.
  - Rationale: After bounding the neighborhood width to +/-25 degrees, the extra +/-3 bin smoothing added another lateral blur without improving the new constrained profile. Removing it makes the output easier to reason about and reduces the remaining azimuthal work.
  - Validation: `uv run -p .venv/bin/python pytest -q tests/test_night_lights.py`
  - Validation: `uv run -p .venv/bin/python ruff check src/zstarview/night_lights.py tests/test_night_lights.py`
- Topic: Ridge glow mask placement
  - Decision: Keep night light rendering unchanged, and treat ridge glow as a separate intermediate alpha grid that is masked at draw time instead of being hard-clipped during alpha-grid generation.
  - Rationale: The visible jaggedness comes from the main ridge glow edge, not the night-light base layer. Deferring the mask lets the ridge glow carry intensity slightly below the main ridge line and produces a cleaner final edge when terrain masking is applied during composition.
  - Result: Reflected this in `docs/design.md` and `docs/design/runtime.md`.
- Topic: Ridge glow coarse-plus-fine model
  - Decision: Model ridge glow as a coarse full-field base plus a higher-resolution refinement only near the main ridge line; outside the refinement window, fall back to the coarse model rather than extrapolating missing values.
  - Rationale: The jaggedness is localized near the primary ridge, so a local refinement can preserve detail where it matters without forcing the whole field to stay expensive. Using the coarse model as the fallback avoids artificial discontinuities at the refinement boundary that extrapolation would introduce.
  - Result: Reflected this direction in `docs/design.md` and `docs/design/runtime.md`.
- Topic: Ridge glow coarse/fine fade transition
  - Decision: Blend the coarse and fine ridge glow models across a small boundary band instead of switching hard at the refinement limit.
  - Rationale: A fade reduces the chance of visible seams where the high-resolution patch ends. It keeps the coarse field as the background while allowing the fine field to dominate near the ridge crest.
  - Result: Reflected this in `docs/design.md` and `docs/design/runtime.md`.
