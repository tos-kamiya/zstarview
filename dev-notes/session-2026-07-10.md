# Session 2026-07-10

Scope: Rebuild object-viewer background separation from `df74f0f8`.

- Topic: Split scenic and object-viewer CLI arguments
  - Decision: Keep location, time, search, star, and overlay arguments in shared parser helpers, while registering sky-disc and night-light arguments only for the scenic viewer.
  - Rationale: The object viewer has a deliberately different presentation and should not expose controls for layers it disables. Its `-V` default and maximum are passed as parser configuration rather than hidden post-parse mutations.

- Topic: Object-viewer background isolation
  - Decision: Give the instrument presentation its own solid-background renderer instead of reusing the scenic radial background / window-border helper.
  - Rationale: The object viewer is intentionally flat and opaque, while scenic `zstarview` keeps theme-dependent chrome and gradients. Separate background code prevents object-viewer changes from leaking into scenic themes.

- Topic: Window border responsibility
  - Decision: Remove the theme-level outer-border flag and always draw the scenic window frame in `draw_window_border()`.
  - Rationale: Whether the frame is present is a presentation decision, not a theme decision. The theme should carry border color and chrome colors, while object-viewer uses its own rendering path to omit the frame entirely.

- Verification: `tests/test_object_viewer_profile.py` and `tests/test_viewer_startup_errors.py` passed (`10 passed`); Ruff and `git diff --check` passed. The full suite has two pre-existing failures in launch-profile JSON serialization and the theme error-message expectation.

- Topic: Object-viewer satellite color
  - Decision: Use the object-viewer aircraft red-purple style for artificial satellites, including outline and label colors, while leaving the default/night theme unchanged.
  - Rationale: The object viewer uses marker shape to distinguish aircraft from satellites, so a shared high-contrast hue improves visual grouping without affecting `zstarview`.

- Topic: Object-viewer reference guides
  - Decision: Add a theme-scoped guide style for the object viewer: black, thin, simple lines for the horizon, direction labels, celestial equator, ecliptic, and zenith/pole markers.
  - Rationale: Guide projection and clipping remain shared, while the presentation-specific stroke policy is isolated from the scenic viewer's colored layered guides.

- Topic: Object-viewer guide visibility
  - Decision: Enable guideline overlays by default in the object-viewer profile.
  - Rationale: The requested reference lines must remain visible independently of the scenic viewer's saved guideline preference; explicit CLI or GUI profile overrides remain respected.

- Topic: Object-viewer guide rendering path
  - Decision: Add a dedicated instrument guide-layer routine that draws reference lines before the shared guide annotations.
  - Rationale: The instrument presentation previously drew direction annotations but never called the celestial reference-line renderer, so horizontal/equator/ecliptic lines could not appear regardless of opacity.

- Topic: Object-viewer permanent alt/az grid
  - Decision: Draw the direction grid unconditionally within the instrument guide layer when guidelines are enabled, using the object-viewer guide color.
  - Rationale: The scenic viewer's hover-triggered grid behavior is not useful for the object viewer's instrument-reading purpose; keeping the call in the instrument-specific path avoids changing scenic behavior.

- Topic: Object-viewer ecliptic line
  - Decision: Render only the object-viewer ecliptic as a thin black dotted line; keep the horizon and celestial equator solid.
  - Rationale: The dotted ecliptic remains distinguishable from the other reference guides without changing the scenic viewer's established dash pattern.

- Topic: Object-viewer below-horizon tint
  - Decision: Apply a default 0.15 gray tint below the terrain horizon in the instrument presentation.
  - Rationale: The initial 0.08 alpha produced an almost indistinguishable result over white; 0.15 remains subtle while making the below-horizon region visibly distinct. The existing compositor tint is not used by the instrument path, so the terrain profile is projected and filled directly before guides and scene objects; scenic rendering remains unchanged.

- Verification adjustment: Temporarily used red for the object-viewer below-horizon tint to confirm the rendering path, then restored the gray default after the fill-coordinate bug was fixed.

- Topic: Object-viewer below-horizon tint visibility
  - Decision: Reduce the default gray tint opacity from `0.15` to `0.08`.
  - Rationale: The below-horizon fill should remain visible as a location cue while appearing lighter on the white object-viewer background.

- Topic: Object-viewer ground tint artifacts at high azimuth
  - Decision: Replace the object-viewer polygon fill with a shared inverse-projection pixel mask.
  - Rationale: Polygon closure from projected horizon samples can produce invalid filled regions as the view azimuth changes. The normal viewer already uses a per-pixel altitude-versus-horizon test, which is independent of profile ordering and projection seams.
  - Verification: Reused the inverse projection and horizon interpolation in both compositor and terrain rendering; targeted tests passed (`52 passed`).

- Topic: Object-viewer solar-system marker visibility
  - Decision: Give the instrument presentation filled, opaque discs for the Sun, Moon, and planets, with dark opaque labels; retain the scenic outline and white-blended label behavior.
  - Rationale: Outline-only markers and scenic label blending are difficult to read on the white object-viewer background. The presentation-specific branch keeps the normal viewer unchanged while making object positions readable.

- Topic: Test-suite compatibility after presentation and profile changes
  - Decision: Make launch-profile JSON conversion handle sets and update stale test doubles and expectations for instrument guides, ground tint, theme guide colors, and the expanded theme list.
  - Rationale: These failures reflected newly introduced runtime fields and presentation-scoped colors rather than regressions in the tested behavior. The launch-profile serializer required a real compatibility fix for `_explicit_options` sets.
  - Verification: Full test suite passed (`1232 passed`).

- Topic: Rename the instrument-style viewer to Atlas
  - Decision: Use `zstarview-atlas` as the sole public entry point and describe the application as Atlas in user-facing documentation.
  - Rationale: Atlas suggests a parchment-like celestial map that precisely shows where objects are, while remaining shorter and more evocative than object-viewer. No compatibility alias is provided by request.

- Topic: Atlas parchment-map visual planning
  - Decision: Keep the scenic `zstarview` rendering and the shared `object-white` theme unchanged; introduce an Atlas-only parchment visual style layered onto the existing instrument presentation.
  - Rationale: `object-white` remains selectable through the shared CLI and theme registry, while Atlas already has dedicated background, guide, and instrument-scene entry points. A presentation-scoped style can darken and slim Atlas lines without changing the normal viewer.
  - Scope: Tune a warm paper background and subtle texture, dark sepia/ink layer palettes, and style-driven thin guide/overlay widths. First establish a screenshot comparison and regression tests before implementation.

- Topic: Remove the legacy object-white theme boundary
  - Decision: Remove `object-white` as a public/shared theme and make the new parchment style the Atlas-only default.
  - Rationale: The user confirmed that the normal `zstarview` entry does not need `object-white`; retaining it would preserve an unnecessary public theme and duplicate the Atlas-specific presentation boundary.
  - Compatibility: The implementation should remove the parser choice, theme registry entry, constant, and related tests/docs, while replacing Atlas defaults and references with the new parchment theme. Existing saved profiles or scripts that explicitly request `object-white` will no longer be supported and should receive the normal invalid-theme error.

- Topic: Update Atlas specification and design documents
  - Decision: Describe Atlas as a parchment-style map in `docs/atlas-specification.md` and `docs/design/atlas.md`, including darker thinner map lines, paper texture/background responsibilities, and removal of `object-white`.
  - Rationale: Keep the user-facing appearance requirements separate from the internal theme and rendering-boundary plan before implementation begins.
  - Verification: Documentation diff passed `git diff --check`; no source code or tests were changed.

- Topic: Implement Atlas parchment presentation
  - Decision: Replace the removed `object-white` theme with the internal `atlas-parchment` theme, inject Atlas-specific guide widths, and render a cached deterministic paper texture only for the Atlas instrument background.
  - Rationale: Keep the normal viewer's themes, background, and guide widths unchanged while making Atlas lines darker and thinner on a warm map-like surface.
  - Verification: Targeted tests passed (`90 passed`); full suite passed (`1232 passed`); Ruff and `git diff --check` passed.

- Topic: Atlas default theme parser regression
  - Decision: Allow `atlas-parchment` only when the parser is built with Atlas theme defaults; keep it out of the normal viewer's public theme choices and help text.
  - Rationale: `argparse` applies the theme type converter to Atlas's string default before the Atlas profile runs, so the internal default must be accepted in that parser context without making it a normal `zstarview` theme.
  - Verification: Atlas parser tests passed (`45 passed`), default parsing returned `atlas-parchment`, and the Atlas help output retained the normal public theme list.

- Topic: Atlas background and contrast direction change
  - Decision: Use a plain white Atlas background instead of parchment coloring or texture, rename the internal theme to `atlas-white`, and increase line darkness/alpha while retaining thin strokes.
  - Rationale: The desired map character comes from crisp, high-contrast linework rather than a colored paper surface; removing the texture also keeps the Atlas canvas clean and predictable.
  - Verification: Targeted tests passed (`55 passed`); full suite passed (`1232 passed`); Ruff passed.

- Topic: Atlas Earth guide
  - Decision: Add Earth guide rendering to the Atlas instrument scene through a dedicated single-line mode that skips fill segments and all underlay passes.
  - Rationale: Atlas needs the geographic reference outline, but the normal and fast-mode layered strokes create a soft/glowing effect that conflicts with its crisp map-line presentation.
  - Verification: Earth guide and window rendering tests passed (`130 passed`); full suite passed (`1233 passed`); Ruff passed.
