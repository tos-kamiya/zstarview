# Session 2026-03-08

- Scope: Terrain horizon polyline integration planning for `zstarview` based on `dev-samples/horizon_polyline_demo.py`.

- Topic: Current horizon rendering path
  - Decision: Treat the existing horizon as a geometric baseline and add terrain horizon as a separate data source rather than mutating the current behavior immediately.
  - Rationale: `src/zstarview/astro.py` currently generates `alt=0` horizon points in view space only, and `src/zstarview/ui/sky_worker.py` computes them together with other sky overlays. Keeping the current path intact reduces regression risk while a terrain-backed path is introduced and validated.
  - Alternatives Considered: Directly replacing the geometric horizon was rejected for the first integration step because it would couple DEM/network/runtime risks to an always-on rendering path.

- Topic: Production decomposition of the demo script
  - Decision: Split the demo logic into three layers: DEM access/cache, terrain horizon computation, and UI-facing orchestration.
  - Rationale: `dev-samples/horizon_polyline_demo.py` currently mixes DEM download, raster loading, scan computation, and validation outputs. The app needs only the first three concerns, and they should be reusable from worker threads without CLI-only side effects.
  - Alternatives Considered: Importing the demo script directly was rejected because it keeps validation-only output paths and argument parsing coupled to runtime logic.

- Topic: Worker and responsiveness model
  - Decision: Run terrain horizon computation off the UI thread and isolate it from the fast sky update loop.
  - Rationale: The current `SkyDataWorker` already handles astronomy calculations in a background thread, but the DEM ray scan can be materially heavier and may involve cache misses or tile downloads. A dedicated worker or separately throttled task avoids stalling normal sky refresh.
  - Alternatives Considered: Folding DEM work into every ordinary sky update was rejected because view-center changes and timer-driven updates would trigger unnecessary recomputation.

- Topic: Initial product scope
  - Decision: Design for a terrain horizon overlay whose effective feature switch is opacity. A non-zero opacity enables DEM acquisition and rendering; opacity `0` disables both. The default is enabled. The GUI toggle will switch between `0` and a remembered non-zero default opacity. If the CLI sets opacity to `0` at startup, the GUI must not be allowed to raise it during that run.
  - Rationale: This collapses feature enablement and rendering strength into one user-facing control, matching the requested behavior while preserving the cloud-like “CLI can hard-disable GUI re-enable” rule. The overlay remains observer-location dependent and time independent, so it can be reused across view changes without recomputing unless the observer location changes.
  - Alternatives Considered: Terrain-clipped sky-disc shading was deferred because it requires a broader rendering contract than the current polyline overlay.

- Topic: Cache policy
  - Decision: Persist only DEM data and recompute the terrain horizon polyline on demand.
  - Rationale: The user estimates that the actual scan can complete in roughly five seconds when DEM data is already present. That makes precomputing and serializing observer-specific horizon profiles unnecessary complexity for the first production version.
  - Alternatives Considered: Caching finished horizon polylines per observer/parameter set was rejected because DEM-only caching is simpler and still performant enough.

- Topic: Rendering semantics
  - Decision: Keep the existing geometric horizon unchanged and draw the terrain horizon as an additional green polyline overlay.
  - Rationale: This preserves existing orientation aids and avoids redefining compass-marker behavior, label placement, or ground/sky shading. It also makes the terrain data visually comparable against the current geometric horizon.
  - Alternatives Considered: Replacing the geometric horizon or clipping the sky/ground boundary to the terrain horizon was rejected for v1 because both options have broader rendering consequences.

- Topic: Failure and retry behavior
  - Decision: Report DEM acquisition/render preparation status in the bottom status line alongside cloud status. If DEM acquisition fails, keep a persistent terrain-horizon failure message visible for the rest of the current app session and do not retry automatically. Retry happens only on the next app launch.
  - Rationale: This matches the requested UX and avoids repeated download attempts or noisy retry loops after a failure. It also keeps terrain failure semantics parallel to cloud status presentation while remaining simpler operationally.
  - Alternatives Considered: Silent fallback and background retries were rejected because they obscure failure state and may repeatedly hit the same network or data issue.

- Topic: Fetch timing
  - Decision: Trigger terrain horizon acquisition on the same lifecycle timing as cloud acquisition.
  - Rationale: The app already has infrastructure for kicking off non-blocking background fetch/render work around startup and view idle transitions. Using the same timing model minimizes new orchestration concepts and keeps user expectations aligned.
  - Alternatives Considered: Immediate eager fetch during window construction and fully lazy first-paint fetch were both rejected in favor of matching the existing cloud timing model.

- Topic: Open design questions
  - Decision: Keep the following unresolved until implementation planning: exact CLI option names for terrain-horizon opacity and whether the bottom status line should show cloud and terrain state as a combined single sentence or fixed left-to-right segments.
  - Rationale: The higher-level runtime behavior is fixed, but naming and final presentation wording still need a concrete API/UI choice.

- Topic: Wikidata tower query for viewpoint candidates
  - Decision: Keep the WDQS query broad and perform item-level deduplication and filtering locally instead of forcing one-row-per-item aggregation in SPARQL.
  - Rationale: The initial miss on Tokyo Skytree was caused by incorrect class QIDs, not by the item data itself. After correcting the QIDs, `result2.json` included `Q57965`, but the result naturally expanded into multiple rows per item due to multiple `P31` matches and, for some items, multiple `P2048` values. Attempting to collapse this in WDQS was noted to hit `stack overflow`, so local normalization is the more stable path.
  - Alternatives Considered: Full aggregation in WDQS was rejected because it is brittle for this subclass-heavy query. Narrowing the class set by removing `observation tower` remains an option later if fire-lookout noise becomes too high.

- Topic: Tower-name location input and observer-height defaults
  - Decision: Accept tower names from the packaged Wikidata-derived viewpoint dataset as startup locations. For city names and direct `lat;lon`, default observer height is `1.7m`. For tower-name or `wikidata:Q...` input, default observer height is the stored tower height. A CLI override `--observer-height-m` can replace either default.
  - Rationale: This keeps the user-facing model simple while making tower-top viewing practical immediately. Absolute sea-level elevation is not yet modeled consistently enough to expose as a separate user input, but threading observer-above-ground height through viewer state, terrain horizon, and astronomy calculations already gives the needed behavior and leaves room for later ground-elevation refinement.
  - Alternatives Considered: Exposing absolute observer elevation immediately was rejected because the project does not yet have a stable ground-elevation data path for all location modes. Reusing `dev-samples` data at runtime was rejected in favor of packaging the normalized tower dataset under `src/zstarview/data/`.

- Topic: Hamburger menu grouping and view controls
  - Decision: Split the menu into three action groups separated by horizontal rules: view-direction changes, named-star jump/search, and display toggles. Add altitude raise/lower actions alongside azimuth rotation, and expose sky-disc visibility as another display toggle.
  - Rationale: The window already supports altitude changes on arrow keys, but the menu only exposed azimuth rotation. Mirroring both axes in the menu makes the controls consistent, while explicit separators match the requested information architecture and keep navigation, target jump, and layer toggles visually distinct.
  - Alternatives Considered: Leaving altitude control keyboard-only was rejected because it keeps menu behavior asymmetric. A separate submenu was rejected because the current menu is small enough that flat grouping is clearer.

- Topic: Cloud compositing without sky-disc fallback
  - Decision: Keep the sky-disc toggle, but interpret OFF as a fixed-color flat disc rather than removing the disc entirely. The compositor continues to use the normal cloud-over-sky composition path in both modes.
  - Rationale: The dedicated no-sky path kept producing confusing differences in cloud stripe contrast and opacity. Keeping a disc present in both modes stabilizes cloud appearance while still letting the user disable the full sky-color shading.
  - Alternatives Considered: Tuning the no-sky opacity separately and removing the disc entirely were both rejected because they preserved divergent visual models that were hard to reason about.

- Topic: Ground rendering when sky-disc shading is disabled
  - Decision: Preserve ground tinting in the flat-disc fallback by deriving horizon position from `view_center` and tinting only the below-horizon pixels.
  - Rationale: The compositor still expects a disc image when sky shading is off, but a single flat fill removed the visual ground entirely. Reusing the inverse projection keeps the no-sky mode visually consistent with the shaded mode without reintroducing the full sky gradient.
  - Alternatives Considered: Moving ground rendering into the compositor was rejected for this fix because it would broaden the rendering contract and touch more call sites than needed.

- Topic: Follow-up fixes after validating the ground-tint change
  - Decision: Keep startup city persistence as a single save point in `_startup_resolve_city` and let search-target jumps fall back to `1.7m` when a test double omits `observer_height_m`.
  - Rationale: The persistence test guards against accidental duplicate writes, so consolidating the save point keeps behavior explicit. The jump helper is used in unit tests with minimal `SimpleNamespace` stubs, so a small fallback preserves compatibility without changing normal runtime behavior.
  - Alternatives Considered: Updating the tests only was rejected because both failures reflected brittle production code paths rather than bad assertions.

- Topic: Ground fill boundary for terrain-horizon mode
  - Decision: Move ground tinting out of raw sky-disc generation and apply it in the compositor with a NumPy mask built from the current `view_center` and optional terrain horizon profile.
  - Rationale: The terrain horizon should change the fill boundary as well as the polyline. Doing the tinting in the compositor keeps the raw sky disc reusable, lets the geometric-horizon fallback and terrain-horizon override share one path, and avoids per-frame `QPainter` polygon fills for a dense curved boundary.
  - Alternatives Considered: Filling below the terrain polyline with `QPainter` was rejected because it couples the result to screen-space fragment stitching and is harder to cache cleanly than a single image-space mask operation.

- Topic: Ground tint opacity as a CLI control
  - Decision: Add a `--ground-tint-opacity` CLI option and thread it into the compositor as the final blend opacity for the ground-tint overlay, while keeping the underlying tint color recipe unchanged.
  - Rationale: The existing ground fill had a fixed appearance controlled only by internal constants. Exposing the final blend factor gives a user-facing control that behaves like real opacity and preserves the prior default look at `1.0`.
  - Alternatives Considered: Reusing the terrain-horizon polyline opacity for the fill was rejected because the line and fill now have different semantics and should remain independently tunable.

- Topic: Ground fill below the horizon boundary
  - Decision: Below the geometric or terrain horizon boundary, stop blending with the sky color and instead fill directly with the ground color; `ground_tint_opacity` now scales that fill color in the same way `sky-opacity` scales the sky disc.
  - Rationale: The requested behavior is a hard visual separation: sky above, ground below. Directly assigning the ground color removes residual sky contamination under the terrain horizon and makes the opacity parameter easier to reason about.
  - Alternatives Considered: Ratio-based interpolation with the original sky pixels was rejected because it preserved some sky color below the horizon and did not match the requested “fully ground color” behavior.

- Summary:
  - Work completed: inspected the demo algorithm, current horizon generation, worker/render flow, config persistence, and dependency availability.
  - Notes completed: added a `dev-samples` memo and raw-result sample for the Wikidata tower-query investigation and recorded the decision in this session log.
  - Sample tooling completed: added a local normalizer script and tests to convert raw Wikidata tower-query output into a deduplicated zstarview-friendly JSON dataset.
  - Sample data completed: regenerated the tower-viewpoint JSON with `en`/`ja` labels fetched from the Wikidata API; primary names now prefer English when available.
  - Runtime location completed: packaged the tower dataset, added tower-name startup resolution, and threaded observer-height defaults through viewer state and terrain/astronomy calculations.
  - Implementation completed: added terrain DEM/cache and horizon computation modules, GUI/CLI opacity control, terrain status handling, and green terrain-horizon overlay rendering; updated docs from in-progress to implemented.
  - Commits: none
  - Tags: none
  - Pushes: none
  - Follow-ups: validate end-to-end DEM download behavior and tune default opacity/styling if needed.
