# Session 2026-02-27
- Scope: numpy使用箇所の調査と最適化可能性の評価

- Topic: numpy利用箇所の性能監査（描画・クラウド合成・BTサンプリング）
  - Decision: 即時実装は行わず、CPU負荷の高い順に最適化候補を特定して優先順位を提示する。
  - Rationale: ループ主体の箇所と配列コピー主体の箇所が混在しており、先にボトルネック候補を整理した方が安全に改善効果を出せるため。
  - Alternatives Considered: 一括でNumPy全面ベクトル化を実施する案は、描画品質やQtとの相互変換の退行リスクが高いため見送った。

## End Summary
- Commits: なし
- Tags/Pushes: なし
- Follow-ups:
  - `utils/image.py` の月描画をマスクベースのベクトル化へ置換するPoC
  - `ui/composite.py` のストライプ平均化を `np.bincount` へ変更するPoC
  - 必要であれば `cProfile`/`py-spy` でフレーム時間計測を追加
- Topic: `generate_moon_phase_image` のNumPy最適化
  - Decision: 画素ごとの二重ループを廃止し、法線計算・可視判定・陰影・エッジalphaをマスク付き配列演算へ置換。
  - Rationale: Pythonループ/都度 `np.array` 生成を削減し、月描画時のCPU負荷を下げるため。既存の描画ロジック（可視面カリング、sunlit/dark分岐、tint、縁フェード）は維持した。

- 11:35 Ran: `uv run -p .venv/bin/python -m compileall src`
  Output:
```text
Compiling 'src/zstarview/utils/image.py'...
```
  Result: 構文エラーなしでコンパイル成功。
- Topic: ハッチ適用時のストライプ平均化を `np.bincount` 化
  - Decision: ストライプごとの平均α計算を `np.unique + for` から `np.bincount` ベースのベクトル集計へ変更。
  - Rationale: Pythonループを削減し、ストライプ数が多い場合の処理時間を短縮するため。負の `stripe_id` は最小値シフトで `bincount` に対応した。

- 11:40 Ran: `uv run -p .venv/bin/python pytest tests/test_cloud_hatch.py tests/test_cloud_la_encoding.py`
  Output:
```text
collected 6 items
... 6 passed ...
```
  Result: 変更後も該当テストは全件成功。
- Topic: 合成処理のデータ変換最適化（`ui/composite.py`）
  - Decision: 画像配列変換後の冗長 `astype(np.uint8)` を削除し、グレースケール合成の `np.repeat` をブロードキャストへ置換、円形マスク生成を `np.ogrid` 化。
  - Rationale: 中間配列とメモリコピーを減らし、合成時のメモリ帯域負荷を下げるため。計算式と丸めは維持し、挙動差を避けた。

- 11:52 Ran: `uv run -p .venv/bin/python pytest tests/test_cloud_hatch.py tests/test_cloud_la_encoding.py`
  Output:
```text
collected 6 items
... 6 passed ...
```
  Result: 変更後も関連テストは全件成功。
- Topic: BTサンプラーの配列確保最適化（`bt_sampler.py`）
  - Decision: `sampler()` 呼び出しごとに確保していた `ix_f`/`iy_f` をクロージャ内バッファとして再利用し、shape変更時のみ再確保する方式に変更。
  - Rationale: 2D投影グリッドを毎回処理する際の大きな一時配列確保コストを削減するため。戻り値 `output_bt` は参照安全性を保つため従来どおり都度生成。
  - Alternatives Considered: `output_bt` も再利用する案は、呼び出し側が結果を保持した場合に次回呼び出しで破壊されるため採用しなかった。

- 12:03 Ran: `uv run -p .venv/bin/python -m compileall src`
  Output:
```text
Compiling 'src/zstarview/clouddisc/sampling/bt_sampler.py'...
```
  Result: 構文エラーなし。

- 12:03 Ran: `uv run -p .venv/bin/python pytest tests/test_cloud_hatch.py tests/test_cloud_la_encoding.py`
  Output:
```text
collected 6 items
... 6 passed ...
```
  Result: 関連テストは全件成功。
- 12:14 Ran: `uv run -p .venv/bin/python - <<'PY' ... CloudDisc.render_now(...); img.save('dev-notes/cloud_debug_la_2026-02-27.png')`
  Output:
```text
saved=dev-notes/cloud_debug_la_2026-02-27.png
satellite=HIMAWARI product=HSD-B13 time=2026-02-26T21:50:00+00:00
```
  Result: 現在の実装で雲ディスク（LA）をPNG出力。

- 12:15 Ran: `uv run -p .venv/bin/python - <<'PY' ... alpha preview save ...`
  Output:
```text
saved=dev-notes/cloud_debug_alpha_preview_2026-02-27.png
```
  Result: 目視比較しやすいアルファ可視化PNGを追加出力。
- 12:25 Ran: `uv run -p .venv/bin/python - <<'PY' ... Denver CloudDisc.render_now ...`
  Output:
```text
saved=dev-notes/cloud_debug_denver_la_2026-02-27.png
satellite=G18 product=CMIPF-C13 time=2026-02-26T22:10:00+00:00
```
  Result: デンバー向け雲ディスク（LA）を出力。
- Topic: 小さい星描画のRGBAグルーピング最適化（`render/draw.py`）
  - Decision: `np.unique(rgba, axis=0)` のみ利用していた処理を `return_inverse=True` に変更し、各グループのマスクを `rgba_group == i` で作成する方式に変更。
  - Rationale: グループごとの `np.all(rgba == tuple, axis=1)` 全列比較を避け、グルーピングコストを削減するため。

- 12:40 Ran: `uv run -p .venv/bin/python -m compileall src`
  Output:
```text
Compiling 'src/zstarview/render/draw.py'...
```
  Result: 構文エラーなし。
- Topic: リリースバージョンのパッチバンプ
  - Decision: `src/zstarview/__about__.py` の `__version__` を `0.10.1` から `0.10.2` へ更新。
  - Rationale: ユーザー依頼の +0.0.1 バンプを反映。
- Topic: 雲ハッチを「密度場 + 幾何線描画」方式へ拡張
  - Decision: 固定サイズ雲画像から `StripeDensityField`（正規化45度UV空間）を生成し、描画時はウィンドウサイズで線を再生成して密度サンプリングする経路を追加。
  - Rationale: 画像拡大由来のぼけを避けつつ、大画面でも過密化しない疎なストライプ表示を実現するため。
  - Alternatives Considered: 既存ハッチ画像を単純スケールする方式は、線エッジの劣化と密度制御の難しさから採用しなかった。

- Topic: 組み込み変更点
  - Decision: `CloudImageState` に `stripe_density` を追加し、`window` 側のクラウド取得直後に密度場を生成して `SkyCompositorCache.draw()` へ渡すよう変更。
  - Rationale: 背景スレッド側で重い前処理を済ませ、描画スレッドでは軽量サンプリングのみを行うため。

- 13:05 Ran: `uv run -p .venv/bin/python -m compileall src tests`
  Output:
```text
Compiling 'src/zstarview/ui/composite.py'...
Compiling 'src/zstarview/ui/window.py'...
Compiling 'tests/test_cloud_hatch.py'...
```
  Result: 構文エラーなし。

- 13:05 Ran: `uv run -p .venv/bin/python pytest tests/test_cloud_hatch.py tests/test_cloud_la_encoding.py`
  Output:
```text
collected 7 items
... 7 passed ...
```
  Result: 変更後の関連テストは全件成功。
- Topic: CLIで雲ストライプ指定を追加
  - Decision: `--cloud-stripe COUNT,WIDTH` を追加し、例 `30,0.15` で指定できるよう変更。`COUNT==0` または `WIDTH==0` の場合は雲描画を無効化する仕様を実装。
  - Rationale: 1つのオプションでストライプ本数と幅を同時に操作し、無効化も同じ入力系で扱えるようにするため。

- Topic: 描画設定の受け渡し
  - Decision: `SkyWindow` に `cloud_stripe_style` を追加し、`SkyCompositorCache` へ `cloud_target_stripes` と `cloud_stripe_width_factor` を伝搬。
  - Rationale: CLI値を最終描画ロジックへ直接反映するため。

- 13:27 Ran: `uv run -p .venv/bin/python -m compileall src tests`
  Result: 構文エラーなし。

- 13:27 Ran: `uv run -p .venv/bin/python pytest tests/test_cloud_hatch.py tests/test_cloud_la_encoding.py`
  Result: 7 passed。
- Topic: リリースバージョンのマイナーバンプ
  - Decision: `src/zstarview/__about__.py` の `__version__` を `0.10.2` から `0.11.0` へ更新。
  - Rationale: +0.1.0 バンプ依頼を反映。
- Topic: テーマ `black` の追加（dark + 低透明）
  - Decision: 既存の `day/night/white` に加えて `black` をCLIテーマとして追加し、スプラッシュ/背景/文字/ステータスラインで専用配色を実装。
  - Rationale: `night` より暗く、かつ透け感を抑えた背景を提供する要件を満たすため。既存テーマの挙動を維持しつつ分岐追加で影響範囲を限定した。
  - Alternatives Considered: `night` のalphaのみ引き上げる案は、テーマ名と見た目差分が曖昧になるため不採用。

- 17:43 Ran: `uv run -p .venv/bin/python pytest tests/test_theme_parse.py tests/test_startup_city_persistence.py tests/test_cloud_hatch.py tests/test_cloud_la_encoding.py`
  Output:
```text
collected 10 items
... 10 passed ...
```
  Result: 新規テーマ追加後も対象テストはすべて成功。既存機能の退行なし。

## End Summary
- Commits: なし
- Tags/Pushes: なし
- Follow-ups:
  - `--theme black` 実行時の実画面で視認性（ラベル/星）を目視確認
  - 必要なら `black` 時の `star_visibility_boost` を微調整
- Topic: `night` / `black` テーマの明度バランス再調整
  - Decision: `night` を従来 `black` 寄りのダークトーンへ移動し、`black` はより純黒ベース（低透明）に再設計。あわせて `black` の文字色を高輝度化して可読性を向上。
  - Rationale: `black` の見た目を明確に差別化しつつ、ラベル可読性を確保するため。

- 17:53 Ran: `uv run -p .venv/bin/python pytest tests/test_theme_parse.py tests/test_startup_city_persistence.py`
  Output:
```text
collected 3 items
... 3 passed ...
```
  Result: テーマ再調整後も関連テストは成功。
- Topic: `black` テーマの背景不透明度を増加
  - Decision: `draw_radial_background` の `black` 分岐でアルファ式を `255 - (s + int(45 * t))` に変更し、背景の透けをさらに抑制。
  - Rationale: ユーザー要望どおり `black` をより不透明にし、下地の透過感を減らすため。

- 18:02 Ran: `uv run -p .venv/bin/python pytest tests/test_theme_parse.py`
  Output:
```text
collected 2 items
... 2 passed ...
```
  Result: 変更後もテーマ関連テストは成功。
- Topic: `day` 背景色を `R:144 G:215 B:236` に寄せる調整
  - Decision: `draw_radial_background` の `day` RGB式を `t=0` で `(144,215,236)` になるよう変更し、`zstarview.py` の `day` スプラッシュ配色も同系色へ更新。
  - Rationale: ユーザー指定のRGBトーンへ寄せつつ、半径方向で暗くなる既存グラデーション挙動は維持するため。

- 18:15 Ran: `uv run -p .venv/bin/python pytest tests/test_theme_parse.py tests/test_startup_city_persistence.py`
  Output:
```text
collected 3 items
... 3 passed ...
```
  Result: 変更後も関連テストは成功。
- Topic: `day` テーマを全域で白寄りに再調整
  - Decision: `draw_radial_background` の `day` RGB式を高輝度側へ変更し、半径方向に暗くしても白系を保つように調整。スプラッシュの `day` 配色も同系統の明るい色へ更新。
  - Rationale: ユーザー要望どおり、`day` を全体的に白へ近づけるため。

- 18:22 Ran: `uv run -p .venv/bin/python pytest tests/test_theme_parse.py tests/test_startup_city_persistence.py`
  Output:
```text
collected 3 items
... 3 passed ...
```
  Result: テーマ調整後も関連テストは成功。
- Topic: `day` テーマを白基調からやや青寄りへ再調整
  - Decision: `draw_radial_background` の `day` RGB式を青成分優位に再設定し、`zstarview.py` の `day` スプラッシュも同系の青みある明色へ変更。
  - Rationale: ユーザー要望どおり、白寄りを維持しつつ青みを戻すため。

- 18:30 Ran: `uv run -p .venv/bin/python pytest tests/test_theme_parse.py tests/test_startup_city_persistence.py`
  Output:
```text
collected 3 items
... 3 passed ...
```
  Result: 再調整後も関連テストは成功。
- Topic: `white` / `black` 背景の無彩色化（黒〜白のみ）
  - Decision: `draw_radial_background` の `white` と `black` のRGB生成を `R=G=B` のグレースケールに変更。`zstarview.py` のスプラッシュ配色も同様に無彩色へ統一。
  - Rationale: ユーザー要望どおり、背景を色相なし（無色）で構成するため。

- 18:38 Ran: `uv run -p .venv/bin/python pytest tests/test_theme_parse.py tests/test_startup_city_persistence.py`
  Output:
```text
collected 3 items
... 3 passed ...
```
  Result: 背景色調整後も関連テストは成功。
- Topic: リリースバージョンのマイナーバンプ（+0.1）
  - Decision: `src/zstarview/__about__.py` の `__version__` を `0.11.0` から `0.12.0` へ更新。
  - Rationale: ユーザー依頼の +0.1 バンプを反映するため。
- Topic: `-A` に応じたデフォルトウィンドウ高の可変化
  - Decision: 初期ウィンドウの高さ比を `0.5 + 0.5*(A/90)` で算出し、`A=0` で横長、`A=90` で正方形になるよう `SkyWindow` 初期化時の `setGeometry` と `setMinimumSize` に適用。
  - Rationale: 低高度視点（例: `-A0`）で上部余白が過大になる問題を、起動時のアスペクト調整で解消するため。

- 18:48 Ran: `uv run -p .venv/bin/python -m compileall src/zstarview/ui/window.py`
  Output:
```text
Compiling 'src/zstarview/ui/window.py'...
```
  Result: 構文エラーなし。

- 18:48 Ran: `uv run -p .venv/bin/python pytest tests/test_startup_city_persistence.py`
  Output:
```text
collected 1 item
. 1 passed
```
  Result: 既存テストは成功。
- Topic: `-A0` 時の上側詰まりを緩和（120度相当ヘッドルーム）
  - Decision: 初期高さ比関数を `0.5 + 0.5*(A/90)` から `0.6 + 0.4*(A/90)` へ変更し、`A=0` の初期高さを増加。
  - Rationale: ユーザー報告のとおり `A=0` で上側が詰まるため、低高度時により大きな縦方向余裕を確保するため。

- 18:56 Ran: `uv run -p .venv/bin/python pytest tests/test_startup_city_persistence.py`
  Output:
```text
collected 1 item
. 1 passed
```
  Result: 既存テストは成功。
- Topic: `-A0` の初期高さ比を 0.65 へ引き上げ
  - Decision: `A=0` の高さ比下限を 0.60 から 0.65 に変更し、式を `0.65 + 0.35*(A/90)` へ更新。
  - Rationale: 追加の上側余白を確保し、ユーザー確認結果に合わせて低高度表示の窮屈さをさらに軽減するため。

- 19:02 Ran: `uv run -p .venv/bin/python pytest tests/test_startup_city_persistence.py`
  Output:
```text
collected 1 item
. 1 passed
```
  Result: 既存テストは成功。
- Topic: リリースバージョンのパッチバンプ（+0.0.1）
  - Decision: `src/zstarview/__about__.py` の `__version__` を `0.12.0` から `0.12.1` へ更新。
  - Rationale: ユーザー依頼の +0.0.1 バンプを反映するため。
- Topic: メニューに雲表示トグルを追加
  - Decision: ハンバーガーメニューにチェック可能な `Clouds` アクションを追加し、`cloud_disc_alpha` の ON/OFF 切替を実装。ON時は即時クラウド更新を開始し、OFF時はクラウド定期更新タイマーを停止。
  - Rationale: `Enlarge Moon` と同様に、実行中にUIから雲表示を簡単に切り替えられるようにするため。
  - Alternatives Considered: キーボードショートカット追加は今回は見送り、既存メニュー操作に統一。

- Topic: 非リアルタイム表示時のクラウドトグル制御
  - Decision: `delta_t != 0`（`--datetime` / `--hours` / `--days`）時はクラウド取得非対応のため、メニューの `Clouds` トグルを無効化。
  - Rationale: 仕様上取得できない状態での誤操作を防ぐため。

- 19:16 Ran: `uv run -p .venv/bin/python pytest tests/test_startup_city_persistence.py`
  Output:
```text
collected 1 item
. 1 passed
```
  Result: 既存テストは成功。
- Topic: 都市指定でリアルタイム雲画像を保存するスクリプト追加
  - Decision: `scripts/save_cloud_snapshot.py` を新規追加し、`City/CC/geonameid/lat;lon` 指定で `CloudDisc.render_now()` を実行してPNG保存できるCLIを実装。
  - Rationale: 単発で現在の雲画像を保存する用途に対し、既存のバッチ探索ユーティリティでは目的が過剰だったため。
  - Alternatives Considered: 既存 `scan_cloud_balance` の流用は、都市単発保存向けではなく引数/出力が重いため不採用。

- 19:31 Ran: `uv run -p .venv/bin/python scripts/save_cloud_snapshot.py --help`
  Result: CLIヘルプ表示を確認。

- 19:31 Ran: `uv run -p .venv/bin/python -m compileall scripts/save_cloud_snapshot.py`
  Output:
```text
Compiling 'scripts/save_cloud_snapshot.py'...
```
  Result: 構文エラーなし。
- Topic: 不可視衛星へのGOESフェイルオーバー抑止
  - Decision: `core.py` で地点ごとの可視GOES候補（G16/G18）を算出し、`goes.fetch_bt_c13_with_failover()` に `allowed_sats` として渡す実装へ変更。不可視衛星は探索順から除外。
  - Rationale: 欧州地点で `G16` 失敗時に不可視の `G18` へ落ちて白画像（alpha=0）になる問題を防ぐため。

- Topic: 衛星可視判定APIの整理
  - Decision: `providers/select.py` に `MAX_VISIBLE_CENTRAL_ANGLE_DEG`、`is_satellite_visible()`、`visible_satellites()` を追加し、可視判定を再利用可能な形へ整理。
  - Rationale: 選択ロジックとフェイルオーバー制御を同じ幾何基準で扱うため。

- Topic: 可視衛星フェイルオーバーの回帰テスト追加
  - Decision: `tests/test_satellite_select.py` と `tests/test_goes_failover_visibility.py` を追加し、欧州でのG18不可視判定と `allowed_sats=("G16",)` 時にG18へ遷移しないことを検証。
  - Rationale: 今回の不具合再発を防ぐため。

- 22:13 Ran: `uv run -p .venv/bin/python pytest tests/test_satellite_select.py tests/test_goes_failover_visibility.py tests/test_startup_city_persistence.py`
  Output:
```text
collected 4 items
... 4 passed ...
```
  Result: 新規テストを含め成功。

- 22:13 Ran: `uv run -p .venv/bin/python scripts/save_cloud_snapshot.py Madrid --log-level INFO`
  Output:
```text
Searching GOES (order=G16, ...)
... CloudDisc failed: GOES CMIPF C13 data not found after all attempts
```
  Result: 不可視のG18へフェイルオーバーせず、期待どおり失敗として返すことを確認。
- Topic: 新プロバイダ追加PR1（選択レイヤ先行）
  - Decision: `providers/select.py` に実験衛星マップ `EXPERIMENTAL_SAT_LON`（`METEOSAT`）を追加し、`is_satellite_visible` / `visible_satellites` に `include_experimental` オプションを導入。
  - Rationale: 新プロバイダ未接続の段階でも、幾何可視判定ロジックを先行整備してテスト可能にするため。
  - Alternatives Considered: `AUTO` 選択へ即時投入する案は、`core` 未接続衛星が選ばれて実行時失敗を引き起こすため不採用。

- Topic: PR1段階の挙動ガード
  - Decision: `pick_satellite(AUTO)` は既存3衛星のみを対象に維持し、テストでガード。
  - Rationale: 本番挙動を壊さず段階導入するため。

- Topic: 設計書追記
  - Decision: `docs/design.md` に「実験衛星はcore接続までAUTO対象外」の運用方針を追記。
  - Rationale: 段階導入時の意図を明文化し、将来の誤接続を防ぐため。

- 22:24 Ran: `uv run -p .venv/bin/python pytest tests/test_satellite_select.py tests/test_goes_failover_visibility.py tests/test_startup_city_persistence.py`
  Output:
```text
collected 6 items
... 6 passed ...
```
  Result: 追加テストを含め全件成功。
- Topic: 新プロバイダ追加PR2（Provider層）
  - Decision: `providers/meteosat.py` を追加し、`MeteosatProvider.fetch_bt_c13()` の契約（`DataArray, used_time, src_paths`）と例外正規化フローを実装。実データ取得は `_fetch_slot` フックで段階導入可能にした。
  - Rationale: `core` 接続前に provider 契約とエラーモデルを確定し、単体テスト可能な土台を先に整えるため。
  - Alternatives Considered: `core` まで同時接続する案は、段階導入の切り分けが崩れ回帰範囲が広がるため見送り。

- Topic: PR2テスト追加
  - Decision: `tests/test_meteosat_provider.py` を追加し、成功・データなし・DownloadError伝播・予期しない例外のRenderError変換をモックで検証。
  - Rationale: 実データバックエンド未接続でも provider の契約挙動を固定するため。

- Topic: 設計書更新
  - Decision: `docs/design.md` のクラウド構成に `providers/meteosat.py` を追記し、PR2時点では `core` 未接続であることを明記。
  - Rationale: 段階導入中の状態を開発者が誤認しないようにするため。

- 22:34 Ran: `uv run -p .venv/bin/python pytest tests/test_meteosat_provider.py tests/test_satellite_select.py tests/test_goes_failover_visibility.py tests/test_startup_city_persistence.py`
  Output:
```text
collected 10 items
... 10 passed ...
```
  Result: 追加テストを含め全件成功。
- Topic: 新プロバイダ追加PR3（core接続）
  - Decision: `core.py` に `MeteosatProvider` を接続し、`pick_satellite(..., include_experimental=True)` で手動優先順位から `METEOSAT` を選択可能にした。`render_now()` に `sat == "METEOSAT"` 分岐を追加。
  - Rationale: provider層（PR2）を実行経路へ接続し、段階導入を完了するため。

- Topic: AUTO選択の互換維持
  - Decision: `pick_satellite(AUTO)` は既存3衛星のまま維持し、`METEOSAT` は手動優先順位でのみ選択可能とした。
  - Rationale: 未実装バックエンドによる既存挙動の破壊を避けるため。

- Topic: ドキュメント/CLI同期
  - Decision: `save_cloud_snapshot.py` と `docs/developer/cloud-snapshot-script.md` の `--sat-priority` 説明へ `METEOSAT` を追加。`docs/design.md` の段階導入注記をPR3状態へ更新。
  - Rationale: 実装状態と利用者向け説明を一致させるため。

- 22:43 Ran: `uv run -p .venv/bin/python pytest tests/test_meteosat_provider.py tests/test_satellite_select.py tests/test_goes_failover_visibility.py tests/test_startup_city_persistence.py`
  Output:
```text
collected 11 items
... 11 passed ...
```
  Result: 追加変更を含め全件成功。
- Topic: `AUTO` 選択で `METEOSAT` を候補化
  - Decision: `pick_satellite()` の `AUTO` 分岐を `include_experimental` 対応に拡張し、`core.py` から `include_experimental=True` で呼ぶ経路では `METEOSAT` も自動選択対象にした。
  - Rationale: ユーザー要望どおり、手動指定なしでも新衛星を選択できる段階まで進めるため。

- 22:32 Ran: `uv run -p .venv/bin/python scripts/save_cloud_snapshot.py London --log-level INFO`
  Output:
```text
Selected satellite=METEOSAT ...
CloudDisc failed: Meteosat IR data not found in search window
```
  Result: `AUTO` で `METEOSAT` が選択されることを確認（実データ取得バックエンドは未実装のため失敗）。
- Topic: 未対応地域を「雲表示なし」にする仕様へ変更
  - Decision: `core.py` で可視衛星を `G16/G18/HIMAWARI` に限定し、可視候補が空なら `VisibilityError("No supported satellite for this region")` を即時送出。`pick_satellite` 呼び出しは `include_experimental=False` に固定。
  - Rationale: Meteosat実データ未実装の段階で、誤った自動選択や白画像を避けるため。

- Topic: クラウドステータス欄に衛星種別を常時表示
  - Decision: `CloudImageState` に `current_satellite` を追加し、`window.py` で下部ステータスを常時描画。表示形式を `Clouds [SAT]: ...` に統一（downloading/error/idle/最新product時刻）。
  - Rationale: ユーザー要望どおり、ダウンロード表示位置で衛星種別を常時把握できるようにするため。

- Topic: 対応外地域のUIメッセージ
  - Decision: `VisibilityError` 捕捉時にクラウド画像をクリアし、バナーを `Clouds: no supported satellite for this region` に設定。
  - Rationale: 対象外地域で古い雲画像が残ることを防ぎ、仕様を明示するため。

- 22:48 Ran: `uv run -p .venv/bin/python pytest tests/test_core_visibility_gate.py tests/test_satellite_select.py tests/test_meteosat_provider.py tests/test_goes_failover_visibility.py tests/test_startup_city_persistence.py`
  Output:
```text
collected 12 items
... 12 passed ...
```
  Result: 追加テストを含め全件成功。

- 22:48 Ran: `uv run -p .venv/bin/python scripts/save_cloud_snapshot.py London --log-level INFO`
  Result: `Selected satellite=G16` を確認（AUTOでMETEOSATを選ばない）。

- 22:48 Ran: `uv run -p .venv/bin/python scripts/save_cloud_snapshot.py "0;20" --log-level INFO`
  Result: `CloudDisc failed: No supported satellite for this region` を確認（対応外地域仕様）。
- Topic: スプラッシュ画面に対象地点（都市/座標）を表示
  - Decision: `setup_splash_and_attach_logger()` にコンテキスト行setterを追加し、都市解決直後に `Location: ...` を設定。以後のスプラッシュログ表示にも location 行を常時挿入する方式に変更。
  - Rationale: 起動中に対象地点を即座に認識できるようにするため。

- 22:58 Ran: `uv run -p .venv/bin/python pytest tests/test_startup_city_persistence.py tests/test_theme_parse.py`
  Output:
```text
collected 3 items
... 3 passed ...
```
  Result: 既存テストは成功。
- Topic: リリースバージョン更新（patch）
  - Decision: アプリ版を `0.12.1` から `0.12.2`（+0.0.1）へ更新。
  - Rationale: 直近機能追加（スプラッシュ地点表示等）を反映した小規模リリースとして整合を取るため。
- Topic: 動的タイムシフト機能の検討（設計のみ）
  - Decision: `-H` のGUI動的版を、キーボード押下状態 + `QTimer` 駆動で実装する案を `docs/design.md` に追記。
  - Rationale: OSキーリピート依存を避け、前進/巻き戻しの操作感を一定化するため。
- Topic: CloudController 抽出（PR1）
  - Decision: `ui/cloud_controller.py` を新規追加し、雲更新のバックグラウンド実行・例外正規化・running/pending制御・キャッシュ清掃トリガを `SkyWindow` から移管。
  - Rationale: `SkyWindow` の責務を縮小し、雲更新ロジックをUI描画から分離するため。

- 23:08 Ran: `uv run -p .venv/bin/python pytest tests/test_core_visibility_gate.py tests/test_satellite_select.py tests/test_meteosat_provider.py tests/test_goes_failover_visibility.py tests/test_startup_city_persistence.py`
  Output:
```text
collected 12 items
... 12 passed ...
```
  Result: 既存回帰テスト全件成功。

- 23:09 Ran: `uv run -p .venv/bin/python ruff check src/zstarview/ui/window.py src/zstarview/ui/cloud_controller.py`
  Output:
```text
All checks passed!
```
  Result: 追加/変更したPythonファイルのlintは問題なし。
- Topic: 入力データ契約 明文化プラン1（alt/az順序統一の着手）
  - Decision: `clouddisc/projectors/az.py` に `altaz_to_dir_ecef(alt_deg, az_deg, ...)` を正準APIとして追加し、既存 `azalt_to_dir_ecef(...)` は後方互換ラッパー化。内部呼び出しは新APIに切替。
  - Rationale: 高優先の誤用リスク（alt/az順序取り違え）を、破壊的変更なしで先に低減するため。

- 23:18 Ran: `uv run -p .venv/bin/python ruff check src/zstarview/clouddisc/projectors/az.py`
  Output:
```text
All checks passed!
```
  Result: lint問題なし。

- 23:18 Ran: `uv run -p .venv/bin/python pytest tests/test_core_visibility_gate.py tests/test_goes_failover_visibility.py tests/test_meteosat_provider.py`
  Output:
```text
collected 6 items
... 6 passed ...
```
  Result: クラウド関連回帰テストは全件成功。
- Topic: 入力データ契約 明文化プラン2（型定義への単位/順序反映）
  - Decision: `types.py` に `AltDeg/AzDeg/LatDeg/LonDeg/ViewCenterAltAz` などの型エイリアスを追加し、`PlanetBody` と `ViewerData` に `*_deg` の明示アクセサを導入（既存フィールド互換は維持）。
  - Rationale: 既存APIを壊さずに、型と名前から単位・順序を読み取れるようにするため。

- 23:26 Ran: `uv run -p .venv/bin/python ruff check src/zstarview/types.py`
  Output:
```text
All checks passed!
```
  Result: lint問題なし。

- 23:26 Ran: `uv run -p .venv/bin/python pytest tests/test_startup_city_persistence.py tests/test_core_visibility_gate.py tests/test_satellite_select.py tests/test_meteosat_provider.py`
  Output:
```text
collected 11 items
... 11 passed ...
```
  Result: 主要回帰テストは全件成功。
- Topic: 入力データ契約 明文化プラン3（stars 構造の型化）
  - Decision: `types.py` に `StarsTable(TypedDict)` を追加し、`CelestialData.stars` と `calculate_visible_stars()` の戻り型を `StarsTable` に変更。
  - Rationale: `stars` のキー契約（name/alt/az/vmag/bv）を固定し、暗黙仕様をコード上で明示するため。

- 23:39 Ran: `uv run -p .venv/bin/python ruff check src/zstarview/types.py src/zstarview/astro.py`
  Output:
```text
All checks passed!
```
  Result: 型化変更後のlint問題なし。

- 23:39 Ran: `uv run -p .venv/bin/python pytest tests/test_startup_city_persistence.py tests/test_core_visibility_gate.py tests/test_satellite_select.py tests/test_meteosat_provider.py`
  Output:
```text
collected 11 items
... 11 passed ...
```
  Result: 主要回帰テストは全件成功。
- Topic: 月/食ロジックの `draw_planets` からの分離（段階1）
  - Decision: `render/draw.py` に `_collect_sun_moon_context`, `_moon_eclipse_overlay_color`, `_draw_moon_planet` を追加し、`draw_planets()` から月/食分岐の詳細を抽出。
  - Rationale: 挙動を維持したまま `draw_planets()` の責務を縮小し、月・食の描画ロジックを独立して読めるようにするため。

- 23:51 Ran: `uv run -p .venv/bin/python ruff check src/zstarview/render/draw.py`
  Output:
```text
All checks passed!
```
  Result: lint問題なし。

- 23:51 Ran: `uv run -p .venv/bin/python pytest tests/test_theme_parse.py tests/test_startup_city_persistence.py`
  Output:
```text
collected 3 items
... 3 passed ...
```
  Result: 既存回帰テストは成功。
- Topic: 入力データ契約 明文化プラン4/5 完了
  - Decision: `render/draw.py` の `altaz_to_normalized_xy_vectorized` docstring を実装仕様に一致させ、主要引数名を `*_deg/*_px` に改名（`get_screen_geometry`, `draw_moon` など）。
  - Rationale: 正規化座標仕様の誤読を防ぎ、単位を引数名で明示して読み違いを減らすため。

- 23:59 Ran: `uv run -p .venv/bin/python ruff check src/zstarview/render/draw.py src/zstarview/ui/window.py src/zstarview/ui/sky_worker.py`
  Output:
```text
All checks passed!
```
  Result: 変更に伴うlint問題なし。

- 23:59 Ran: `uv run -p .venv/bin/python pytest tests/test_theme_parse.py tests/test_startup_city_persistence.py tests/test_core_visibility_gate.py tests/test_satellite_select.py`
  Output:
```text
collected 9 items
... 9 passed ...
```
  Result: 主要回帰テストは全件成功。
- Topic: 恒星フレアの連続強度化とサイズ二重強調の解消
  - Decision: フレア適用を固定閾値（`vm<=1.2`）から全等級向け単調関数へ変更し、フレア外側長が1px未満の星は非描画とする。フレア描画時のみコア半径を補正して、フレア分の見かけサイズ増加を二重にしない方式を採用。
  - Rationale: 境界等級での見た目不連続を避けつつ、明るい星の強調を連続制御し、ユーザー要望の「フレア分だけコア拡大を抑える」を満たすため。
  - Alternatives Considered: しきい値ベース維持 + 上限クランプ案は、境界で段差が残るため不採用。

- 02:03 Ran: `uv run -p .venv/bin/python pytest tests/test_star_flare_profile.py`
  Output:
```text
collected 3 items
... 3 passed ...
```
  Result: 単調性・1pxゲート・見かけ半径単調性の回帰テストが成功。

## End Summary (Star Flare Update)
- Commits: なし
- Tags/Pushes: なし
- Follow-ups:
  - 実画面で `-t night/day/white/black` の見え方比較を行い、`0.65` / `0.9` / `0.45` の係数を必要に応じて調整
- Topic: Meteosat対応コードの除去（誤解防止）
  - Decision: `MeteosatProvider` と関連分岐・実験衛星定義・テスト・ドキュメント記載を削除し、クラウド衛星は `G16/G18/HIMAWARI` のみを明示する。
  - Rationale: 実データ取得の見込みがない機能を残すと、利用者と開発者双方に誤解を生むため。実装と文書を一致させる。
  - Alternatives Considered: スタブとして残す案は、将来の拡張余地はあるが現状の期待値調整に失敗しやすいため採用しなかった。

- 06:17 Ran: `uv run -p .venv/bin/python pytest tests/test_satellite_select.py tests/test_core_visibility_gate.py`
  Output:
```text
collected 3 items
... 3 passed ...
```
  Result: Meteosat除去後の衛星選択・可視判定テストは成功。

## End Summary
- Commits: なし
- Tags/Pushes: なし
- Follow-ups:
  - 必要なら README / README_ja-JP に「現在は GOES/HIMAWARI のみ対応」を明記して周知を強化
- Topic: 実験衛星フラグの除去
  - Decision: `providers/select.py` の `include_experimental` 引数と呼び出し側指定を削除し、衛星選択APIを実対応範囲に一致させた。
  - Rationale: 非対応衛星の存在を示唆するAPI痕跡をなくし、実装仕様を明確化するため。

- 06:17 Ran: `uv run -p .venv/bin/python -m compileall src`
  Output:
```text
Compiling 'src/zstarview/clouddisc/core.py'...
Compiling 'src/zstarview/clouddisc/providers/select.py'...
Compiling 'src/zstarview/ui/cloud_controller.py'...
Compiling 'src/zstarview/ui/window.py'...
```
  Result: 変更対象を含むソースのコンパイルは成功。
- Topic: 惑星表示仕様の設計追記（INPROGRESS）
  - Decision: 設計書に「惑星記号を廃止し、惑星はクロスゲージ表示。ホバー時のみ惑星名表示」の方針を `INPROGRESS` として追記した。
  - Rationale: 画面の情報密度を下げつつ、太陽/月との視覚的一貫性を持たせるため。
- Topic: 惑星表示を記号からクロスゲージへ変更
  - Decision: `draw_solar_system_bodies()` の惑星分岐で記号描画を廃止し、太陽/月と同系統のクロスゲージ描画へ統一した。
  - Rationale: 画面の情報密度を抑えつつ、太陽・月・惑星のマーカー表現を統一するため。

- Topic: 惑星ホバー名表示の確認
  - Decision: 既存 `find_highlighted_object` をそのまま活用し、惑星ホバー時に `draw_overlay_info()` で惑星名が表示される経路を維持。
  - Rationale: 追加実装なしで要件を満たせ、既存のハイライトUIと整合するため。

- 06:25 Ran: `uv run -p .venv/bin/python pytest tests/test_planet_marker_style.py tests/test_star_flare_profile.py`
  Output:
```text
collected 5 items
... 5 passed ...
```
  Result: 惑星マーカー変更と既存フレアロジックのテストは成功。

- 06:25 Ran: `uv run -p .venv/bin/python -m compileall src tests`
  Output:
```text
Compiling 'tests/test_planet_marker_style.py'...
```
  Result: 変更後のソース/テストはコンパイル成功。
- Topic: 惑星記号廃止に伴う不要依存の整理
  - Decision: `SkyWindow` での絵文字フォント読み込み（`EMOJI_FONT_PATH` / `EMOJI_FONT_SIZE`）を削除した。
  - Rationale: 惑星記号描画を行わなくなったため、未使用初期化を残さないため。
- Topic: 惑星の見かけ等級を反映したマーカー表示
  - Decision: `astro.calculate_planets()` で Skyfield `planetary_magnitude()` を使って惑星の `vmag` を算出し、`PlanetBody.vmag` として保持。描画側では `vmag` に応じてクロスゲージのサイズ/不透明度/線幅を変化させる方式を採用。
  - Rationale: 太陽・地球・惑星の幾何に基づく見かけの明るさを表示へ反映しつつ、既存のクロスゲージUI方針を維持するため。

- Topic: ホバー情報への等級表示
  - Decision: 惑星ホバー時のラベルに `Vmag` を追記（例: `mars (Vmag -1.3)`）。
  - Rationale: 明るさ計算結果を視認できる形で表示し、挙動確認をしやすくするため。

- 06:30 Ran: `.venv/bin/python -m pytest tests/test_planet_brightness.py tests/test_planet_marker_style.py tests/test_star_flare_profile.py`
  Output:
```text
collected 7 items
... 7 passed ...
```
  Result: 惑星明るさ連動と既存描画ロジックの関連テストは成功。

- 06:30 Ran: `.venv/bin/python -m compileall src/zstarview/astro.py src/zstarview/render/draw.py src/zstarview/types.py tests/test_planet_brightness.py tests/test_planet_marker_style.py`
  Output:
```text
Compiling 'tests/test_planet_brightness.py'...
Compiling 'tests/test_planet_marker_style.py'...
```
  Result: 変更対象のコンパイルは成功。
- Topic: 惑星マーカーをクロスから円（恒星風）へ変更
  - Decision: 惑星の表示はクロスゲージではなく、`vmag` に応じた半径/アルファのソフト円マーカーで描画する方式へ変更した。
  - Rationale: 恒星に近い視覚表現で惑星の明るさ差を直感的に示すため。

- Topic: ホバーラベルから等級文字を削除
  - Decision: 惑星ホバー時の表示は名称のみとし、`(Vmag ...)` の文字追記を削除した。
  - Rationale: 明るさは図形（円マーカー）で表現する要件に合わせるため。

- 06:33 Ran: `.venv/bin/python -m pytest tests/test_planet_brightness.py tests/test_planet_marker_style.py tests/test_star_flare_profile.py`
  Output:
```text
collected 7 items
... 7 passed ...
```
  Result: 惑星円マーカー化後の関連テストは成功。
