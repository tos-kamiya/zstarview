# Session 2026-08-17

- Topic: Star interpolation Gate 1 measurement
  - Decision: Mark the current single-homography approach Not Go under a
    1-pixel maximum-error budget and proceed to the mesh-warp implementation.
  - Evidence: In a 1600x900, 90-degree field with the circular sky-disc area
    sampled at 25x15 points and elapsed times of +/-30 seconds, north-facing
    views reached about 0.59 px maximum error, while south, zenith, and
    low-altitude views reached about 10.5 to 15.8 px.
  - Rationale: A single global homography is sufficiently accurate only for
    some orientations and cannot represent the position-dependent sky motion
    consistently across representative views.

- Topic: Star interpolation Gate 2 mesh experiment
  - Decision: Mark the interior-cell 16x9 mesh result Go under the 1-pixel
    maximum-error budget, while keeping circular sky-disc boundary clipping as
    a separate pending rendering check.
  - Evidence: `dev-samples/measure_star_mesh_gate.py` measured a maximum error
    of about `0.0158 px` for 16x9 square cells across four half-cell mesh
    phases, north, south, zenith, and low-altitude views at +/-30s.
  - Rationale: Piecewise-linear interpolation between correctly reprojected
    mesh vertices is substantially below the 1-pixel budget away from the
    circular clip boundary.

- Topic: Gate measurement interpretation and next implementation scope
  - Decision: Use 1600x900 as the stress-test surface and define the mesh by a
    square pixel cell size rather than by a fixed rows/columns pair. The
    100px baseline therefore produces a 16x9 mesh.
  - Measurement procedure: For each mesh phase `(0.0, 0.0)`, `(0.5, 0.0)`,
    `(0.0, 0.5)`, and `(0.5, 0.5)`, compare triangle-interpolated positions
    against direct 3D sidereal rotation plus screen reprojection. Test the
    north, south, zenith, and low-altitude view directions at `-30s` and
    `+30s`.
  - Results: The square 16x9 mesh produced a worst interior-cell error of
    approximately `0.0158 px`, well below the provisional `1.0 px` Gate 2
    threshold. Earlier reference measurements showed that the single global
    3x3 homography reached approximately `0.59 px` in the north-facing case
    and `10.5` to `15.8 px` in the other representative orientations.
  - Boundary: Cells whose four corners are not inside the circular sky disc
    are excluded from this numerical gate. The transparent surface boundary,
    clipping, triangle seams, and actual QImage composition remain a separate
    rendering validation item.
  - Implementation consequence: Proceed with square-pixel mesh deformation
    in the renderer, retain the 3D rotation/reprojection path as the reference
    oracle, and keep the 1px gate for later visual and numerical validation.

- {Time 00:25} Ran: `uv run -p .venv/bin/python dev-samples/measure_star_mesh_gate.py --fail-on-not-go`
  Output:
  ```text
  mesh=16x9 phase=0.5,0.5 samples=123
  low-altitude elapsed=+30s mean=0.0061px max=0.0137px
  gate_max_error=0.0158px threshold=1.0000px result=GO
  ```
  Result: Gate 2 passed for the interior-cell numerical experiment.

- Topic: Initial renderer mesh-warp implementation
  - Decision: Add a 100px-cell mesh path for the cached dark-star surface. For
    each mesh vertex, rotate the screen ray around the observer's terrestrial
    pole axis and reproject it; render each mesh cell as two clipped affine
    triangles. Keep the existing single-homography fallback when interpolation
    inputs are unavailable.
  - Scope: The initial path is used for the cached dark-star surface. Bright
    stars remain in the existing direct drawing path so their underlay and
    crisp markers are not changed in this increment.
  - Validation: `tests/test_star_interpolation.py` and
    `tests/test_star_overscan_render.py` passed (22 tests); the added mesh
    vertex test and targeted `ruff check` passed.
  - Follow-up: Validate actual QImage composition at the circular sky-disc
    boundary, inspect triangle seams and transparent edges, then decide
    whether labels and twinkle overlays should use the same mesh path.

- Topic: Gate 2 with full 100% coverage
  - Decision: The renderer coverage value was changed from `0.75` to `1.0`
    while preserving the existing multiplication expression and constant, so
    later tuning only needs to change the value. The mesh approximation passes
    the 1px numerical gate with full elapsed sidereal motion.
  - Evidence: Running `dev-samples/measure_star_mesh_gate.py --coverage 1.0`
    with the 1600x900, 100px square-cell 16x9 mesh and four half-cell phases
    produced a worst interior-cell error of approximately `0.0211 px`, versus
    `0.0158 px` at 75% coverage.
  - Rationale: The mesh interpolation error remains far below the 1px budget at
    full motion, but this numerical gate does not measure the visible jump when
    a new one-minute star snapshot replaces the cached surface. That visual
    check remains the next tuning criterion.

- Topic: Full-motion interpolation implementation
  - Decision: Apply `STAR_INTERPOLATION_COVERAGE = 1.0` to both the existing
    homography path and the mesh path. Keep the formula unchanged so the value
    can be tuned back below 1.0 without restructuring the interpolation code.
  - Validation: `tests/test_star_interpolation.py` and
    `tests/test_star_overscan_render.py` passed (23 tests); targeted `ruff
    check` passed.

- Topic: Twinkle mesh integration
  - Decision: Route the transient twinkle dots through a transparent temporary
    QImage and apply the same star-surface triangular mesh before compositing.
    This removes the former 3x3 homography-only path for twinkle.
  - Rationale: Twinkle is attached to faint-star positions and must move with
    the same local position field as the dark star surface, while remaining a
    volatile overlay that does not invalidate the cached surface.
  - Validation: `tests/test_star_interpolation.py` and `tests/test_twinkle.py`
    passed (19 tests); targeted `ruff check` passed.

- Topic: Star-following label mesh integration
  - Decision: Render simplified named-star labels into a transparent temporary
    QImage and warp that image through the same triangular mesh as the dark
    star surface. Non-star HUD and direction overlays remain outside this
    path.
  - Boundary handling: Mesh vertices outside the circular sky disc remain at
    their source coordinates, so transparent overscan does not acquire an
    unstable projected displacement. Actual triangle seams and edge clipping
    still require visual inspection.
  - Validation: Star interpolation, label, and render-overlay tests passed (43
    tests); targeted `ruff check` passed.

- Topic: Move mesh preparation out of normal paint work
  - Decision: Prepare the star interpolation mesh during the even-second
    dynamic display tick and cache it by viewport, view, interval, snapshot,
    and display-time inputs. Normal paint operations reuse the cached vertices
    and targets; a cache miss retains a safe fallback calculation.
  - Rationale: The mesh path performs per-cell affine solves, clip-path setup,
    and image draws on the GUI thread. Moving vertex preparation out of paint
    removes repeated NumPy geometry work from mouse and repaint handling while
    preserving the existing GUI interaction mode.
  - Validation: Relevant GUI/render tests passed (77 tests); targeted `ruff`
    and `git diff --check` passed.

- Topic: Avoid a full viewport surface for twinkle
  - Decision: Transform only the screen positions of the transient twinkle
    targets through the star mesh and draw the dots directly on the painter.
  - Rationale: At most 30 twinkle targets are active, so allocating and
    rasterizing a full viewport-sized transparent `QImage` was unnecessary
    memory use and added avoidable GUI-thread work.
  - Validation: 46 targeted tests passed with `QT_QPA_PLATFORM=offscreen`;
    targeted `ruff check` passed.

- Topic: Avoid a full viewport surface for simplified star labels
  - Decision: Transform each named star's label anchor through the mesh and
    draw the text directly on the destination painter.
  - Rationale: Named-star labels are sparse, and the text should remain sharp
    rather than being rasterized and geometrically distorted with a temporary
    full-screen image.
  - Validation: Label and overlay tests passed (33 tests) with the offscreen
    Qt platform; targeted `ruff check` passed.

- Topic: Remove mesh overscan freeze outside the sky disc
  - Decision: Let all rectangular viewport mesh vertices follow the projected
    sidereal motion, including vertices in the transparent area outside the
    circular sky disc.
  - Rationale: Freezing the outside vertices created a discontinuity at the
    disc boundary and was observed as flicker in semi-transparent pixels.

- Topic: Scheduler probe compatibility with optional star mesh preparation
  - Decision: Treat star interpolation mesh preparation as an optional render
    hook when publishing a dynamic display tick.
  - Rationale: The production window provides the hook through its render
    mixin, while lightweight scheduler probes intentionally compose only the
    update mixin. A missing cache-preparation hook can safely use the existing
    render-time fallback and must not prevent dynamic-layer publication.
  - Validation: `tests/test_scheduler_idle.py` passed (8 tests); the full
    suite passed (1569 tests passed, 5 skipped); targeted Ruff and
    `git diff --check` passed.

- Topic: Star mesh fallback and cache correctness review
  - Decision: Initialize mesh dimensions before the optional interpolation
    branch, and include the projection center and observer location in the
    mesh cache key.
  - Rationale: Interpolation can legitimately be disabled for long update
    intervals or missing times, so downstream star and asterism drawing must
    receive a safe no-mesh state. The mesh rotation axis and projection also
    depend on observer location and screen center, which must invalidate a
    cached mesh when either changes.
  - Validation: Star interpolation, render-overlay, and twinkle tests passed
    (40 tests); targeted Ruff and `git diff --check` passed.

- Topic: Twinkle mesh position filtering
  - Decision: Validate transformed positions against the original twinkle
    target list, then apply the same validity mask to rows, alpha values, and
    positions.
  - Rationale: Zero-alpha targets are retained in the prepared target tuple
    but skipped during drawing. Comparing transformed positions after that
    filtering incorrectly treated the valid input arrays as mismatched.
  - Validation: Twinkle, star interpolation, and render-overlay tests passed
    (41 tests); targeted Ruff and `git diff --check` passed.

- Topic: Consolidate star interpolation mesh state
  - Decision: Introduce a frozen `StarInterpolationMesh` dataclass containing
    source vertices, target vertices, columns, and rows, with construction-time
    shape validation and a `map_points()` operation.
  - Rationale: Mesh vertices and dimensions form one invariant-bearing value.
    Passing them separately duplicated grid-size calculations and allowed an
    absent mesh to be combined with uninitialized or inconsistent dimensions.
  - Validation: The full suite passed (1573 tests passed, 5 skipped); targeted
    Ruff and `git diff --check` passed.

- Topic: Use equal, tighter night-light glow spread in both axes
  - Decision: Apply a `0.45` spread scale to the distance-based sigma, then use
    that reduced value for both altitude and azimuth. Raise the default VIIRS
    night-light opacity from `0.14` to `0.22`.
  - Rationale: This produces a more concentrated isotropic glow while retaining
    the separately tuned default opacity of `0.22`.
  - Validation: Night-light and CLI argument tests passed (78 tests); targeted
    Ruff and `git diff --check` passed.

- Topic: Align night-light activity with illuminated roof fills
  - Decision: Multiply the configured night-light opacity by the illuminated
    roof-fill factor after subtracting its `0.05` daytime floor and clamping at
    zero. Keep ridge glow on its existing solar-altitude-only fade.
  - Rationale: A configured value remains the pre-adjustment maximum, producing
    95% of that value at full night strength while still reaching zero in
    daylight and sharing the roof fill's post-solar-midnight activity change.
  - Validation: Render-overlay, glow-mask, and night-light tests passed (73
    tests); targeted Ruff and `git diff --check` passed.

- Topic: Release 1.54.8
  - Decision: Bump the patch version from `1.54.7` to `1.54.8` and document the
    night-light glow tuning as the complete release window since the previous
    version-bump commit.
  - Rationale: The user-visible default opacity and glow footprint changed and
    require a matching public release-note entry.
  - Validation: Night-light and CLI argument tests passed (78 tests); reported
    package version confirmed as `1.54.8`; `git diff --check` passed.

- Topic: Clarify solar tint physics and propose lunar atmospheric tinting
  - Decision: Correct the documentation to describe the Sun hover tint as
    direct-light Rayleigh extinction, aerosol extinction, and ozone
    absorption, explicitly excluding the Mie phase function and anisotropy.
    Specify a future Moon hover path that reuses the same atmospheric RGB
    transmission instead of adding empirical altitude colour bands.
  - Rationale: The implementation integrates wavelength-dependent loss along
    the direct sightline but does not calculate directionally scattered Mie
    radiance. Sharing that transmission with the Moon keeps the atmospheric
    model consistent, while retaining body-specific source colour and display
    floors avoids applying the Sun's HMI reference tint to Dial-A-Moon data.
  - Moon eclipse handling: Apply the eclipse appearance before observer-side
    atmospheric transmission. If the combination is too dark or red, tune the
    display eclipse overlay or Moon-specific minimum multiplier rather than
    changing the shared atmospheric model only during an eclipse.
  - Scope: Documentation and design proposal only; no rendering code changed.

- Topic: Apply atmospheric transmission to Dial-A-Moon hover images
  - Decision: Generalize the direct solar transmission function for arbitrary
    body altitude, extract shared linear-RGB QImage multiplication, and add a
    Moon-specific tint cache and neutral 35% marker-colour fallback. Apply it
    only to the external Moon hover image; leave `--enlarge-moon` and the
    procedural Moon renderer unchanged.
  - Rationale: Reusing the same Rayleigh extinction, aerosol extinction, ozone
    absorption, observer height, and location/month AOD keeps Sun and Moon
    atmospheric colour consistent. Dial-A-Moon already contains its source
    colour and eclipse appearance, so it receives no HMI reference colour and
    no additional procedural eclipse overlay.
  - Validation: Atmospheric, Moon hover/tint, solar tint, solar-system marker,
    and window HUD/overlay tests passed (`94 passed`); targeted Ruff and
    focused mypy passed. The repository coverage command was unavailable
    because `coverage` is not installed in `.venv`. Three direct full-pytest
    attempts terminated at about 13% without producing a final summary or
    JUnit report in this environment.

End Summary: Committed the atmospheric tint documentation as `3be5b932`, then
implemented shared direct-light transmission for external Dial-A-Moon hover
images. The implementation remains uncommitted; `--enlarge-moon` and the
procedural Moon renderer are unchanged.

- Topic: Reduce maximized-window star mesh composition cost
  - Decision: Cache the faint-star surface at
    `compute_star_render_surface_size()` instead of expanding it to the client
    size first. Scale the full client-coordinate mesh independently in X and Y,
    composite its triangles into a same-size low-resolution image, and upscale
    that completed image to the client rectangle once.
  - Rationale: The previous path repeatedly submitted a client-sized star
    image for every mesh triangle. Keeping the existing approximately 100px
    screen-space cell count preserves interpolation accuracy while reducing
    the source and intermediate pixel area on large windows. Bright stars,
    twinkle targets, and label anchors continue to use the full screen mesh as
    point coordinates.
  - Verification: A 3840x2160 client with a 2160px sky disc now retains a
    2024x1138 faint-star surface, with the 100px screen cell represented by
    about 53px on that surface. Star interpolation, overscan, and window render
    twinkle, asterism, label, and window render cache/context/HUD/overlay tests
    passed (`128 passed`); targeted Ruff passed.

- Topic: Preserve the celestial-pole center in mesh raster transforms
  - Decision: Correct the NumPy row-vector affine to `QTransform` mapping so
    the linear off-diagonal terms retain their X/Y order and translation comes
    from `affine[2, 0:2]` rather than the constant output column.
  - Evidence: For a 2160x2160 Matsue view centered at altitude 40 degrees and
    azimuth north, the numerical mesh places the north celestial pole at
    `(1080.0, 1134.43)` and moves it by only about `0.0002 px` over 30 seconds.
    Points 500px from the pole move approximately `1.09 px` tangentially with
    about `0.001 px` radial motion and less than `0.00025 px` mesh error.
  - Rationale: The former Qt matrix placement discarded affine translation and
    transposed the off-diagonal terms, so correct mesh vertices were rasterized
    about the wrong origin. Image tests now verify both nonzero translation and
    rotation about a nonzero center.
  - Validation: The combined star interpolation, overscan, twinkle, asterism,
    label, and window render suite passed (`128 passed`); targeted Ruff and
    `git diff --check` passed.

End Summary: The uncommitted star-mesh update now keeps the faint-star surface
at internal resolution and fixes the Qt affine mapping so the rendered motion
preserves the projected celestial-pole center. Visual verification remains.

End Summary: Committed the Moon atmospheric tint as `e709fc24`. Implemented an
uncommitted low-resolution faint-star mesh composition change for maximized
window responsiveness; visual verification on the target display remains.

- Topic: Simplify and tint procedural 5x Moon rendering
  - Decision: Keep the normal-size filled Moon's Lambert sphere rendering,
    while using a flat two-colour phase disc for both `--enlarge-moon` and the
    Dial-A-Moon hover fallback. Apply the same altitude, observer-height, and
    location/month AOD atmospheric transmission used by external Dial-A-Moon
    images to both procedural 5x paths.
  - Rationale: The 5x procedural image primarily needs to communicate lunar
    phase now that the API supplies detailed imagery. Restricting the change to
    enlarged rendering preserves the established normal Moon appearance and
    makes atmospheric colour consistent across all enlarged-image sources.
    Eclipse tint is applied before observer-side atmospheric transmission.
  - Validation: Targeted Ruff passed. Moon phase, atmospheric tint,
    solar-system marker, HUD, and overlay tests passed (`80 passed`).

End Summary: Implemented an uncommitted flat phase-readable procedural Moon
for always-on 5x display and API fallback, with shared atmospheric tinting.

- Topic: Separate Moon CLI style and scale
  - Decision: Add planned `--moon-style {marker,sphere,image}` and
    `--moon-scale {1,2,3,4,5,6,7,8}` inputs with defaults of `marker` and 1.
    Remove the Moon from `--bright-bodies`; that option will continue to
    control bright stars, the Sun, and planets. Keep `--enlarge-moon` as an
    exact shortcut for `--moon-style sphere --moon-scale 5` and reject it when
    either new Moon option is also present.
  - Rationale: Style and scale are independent choices, while the current Moon
    marker is not simply the same outline treatment used for other bright
    bodies. Rejecting mixed legacy and new inputs avoids hidden precedence.
  - Scope: Documentation and implementation design only; CLI and rendering
    behavior are not changed yet.

- Topic: Implement independent Moon style and scale
  - Decision: Normalize CLI inputs to `moon_style` and `moon_scale`, propagate
    them through window/export render styles, and select marker, Lambert sphere,
    or Dial-A-Moon rendering independently of `bright_bodies_mode`. Reuse the
    existing asynchronous Moon image controller for persistent image mode and
    retain the flat procedural phase renderer as its fallback.
  - Compatibility: `--enlarge-moon` resolves to sphere at 5x and conflicts with
    either new option. The existing menu action and M key toggle sphere 5x and
    marker 1x. Export rendering uses the procedural fallback when no external
    image has been supplied.
  - Validation: CLI, Moon rendering/orientation, window HUD/cache/overlay,
    export input, startup error, meteor state, and terrain UI tests passed
    (`254 passed`). Targeted Ruff and `git diff --check` passed. A full pytest
    run reached `919 passed, 5 skipped` before exposing a legacy call missing
    newer Moon tint defaults; after fixing that compatibility issue, the next
    full run was interrupted later by an unrelated native-extension
    segmentation fault. Full mypy remains unavailable as a clean gate because
    the repository currently reports pre-existing errors across many modules.

- Topic: Ignore legacy GUI `enlarge_moon` profile values
  - Decision: Remove `enlarge_moon` from GUI profile defaults, loaded values,
    and saved output, and defensively skip it when applying a profile to parsed
    arguments. Continue to support an explicitly supplied CLI
    `--enlarge-moon` outside the saved GUI profile.
  - Rationale: The startup dialog now exposes `moon_style` and `moon_scale`.
    Allowing a hidden legacy value to survive would override those visible
    selections with sphere 5x rendering.

- Topic: Make the M key relative to the configured Moon option
  - Decision: Preserve the startup `moon_style` and `moon_scale` as the toggle
    target. For the default marker 1x configuration, M toggles a temporary
    sphere 5x view. For every non-default configuration, M toggles that exact
    configuration off to marker 1x and restores it on the next press. Rename
    the menu item from `Enlarge Moon` to `Moon Option` to reflect both modes.
  - Rationale: M remains a convenient enlargement shortcut at defaults while
    acting as an enable/disable switch for an explicitly selected Moon style
    or scale.

- Topic: Remove the internal `enlarge_moon` flag
  - Decision: Keep `--enlarge-moon` only as a parser-level compatibility input
    and normalize it immediately to `moon_style="sphere"` and `moon_scale=5`.
    Remove the boolean from window user options, render styles, window state,
    overlay functions, planet-layer calls, and rendering tests. Rename the GUI
    action state and handler to the neutral Moon-option terminology.
  - Rationale: Style and scale fully describe every Moon rendering state. A
    second derived boolean created competing precedence in the renderer and
    made tests exercise a state that no longer belongs to the internal model.
  - Regression fix: Removing the unused overlay argument initially shifted a
    positional HUD call and passed `label_candidates` twice. Convert both
    production `draw_overlay_info()` calls to keyword arguments from
    `vmag_limit` onward and update the forwarding test to assert the keyword.
  - Validation: The combined CLI, GUI profile/dialog, Moon action/rendering,
    overlay/HUD, window cache/context, export, and startup suite passed
    (`299 passed`); targeted Ruff and `git diff --check` passed.
