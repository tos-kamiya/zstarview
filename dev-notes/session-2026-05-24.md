# 2026-05-24
Scope: restore solar-system body rendering during viewport fast mode.

- Topic: Fast-mode solar-system rendering
  - Decision: Draw the existing solar-system layer during viewport interaction fast mode instead of limiting that path to stars and terrain only.
  - Rationale: The fast-mode path currently returns before the normal planet layer runs, which suppresses the Sun, Moon, and planets while panning or zooming. Reusing the existing solar-system renderer keeps the behavior aligned with normal rendering and confines the change to the interaction pipeline.
  - Alternatives Considered: Reimplement a dedicated fast-mode planet marker path. Rejected because it would duplicate body rendering logic and increase divergence between normal and fast paths.

Summary:
- Updated `src/zstarview/render/pipeline.py` so viewport interaction fast mode also draws solar-system bodies, with labels disabled in that path.
- Added regression coverage in `tests/test_window_render_sync.py`.
- Updated `README.md`, `docs/specification.md`, `docs/design.md`, and `docs/implementation-history.md` so the documented fast-mode behavior matches the implementation.
- Bumped `src/zstarview/__about__.py` from `1.28.5` to `1.28.6`.
- Verified with:
  - `uv run -p .venv/bin/python pytest tests/test_window_render_sync.py -k "viewport_interaction_layers"`
  - `uv run -p .venv/bin/python pytest tests/test_planet_marker_style.py`

- Topic: clickpy Top 25 medal display
  - Decision: Add the medal image near the top of `README.md`, directly below the introductory paragraph.
  - Rationale: The award is project-level social proof, so it should be visible without scrolling past the main feature list or screenshots.
  - Alternatives Considered: Adding it near the screenshots section. Rejected because that placement weakens visibility for repository visitors who only read the top section.
- Topic: clickpy medal date labeling
  - Decision: Label the README medal display as `as of 2026-05-23` rather than presenting it as a timeless badge.
  - Rationale: The ranking is likely time-sensitive, so the README should make the snapshot date explicit and avoid implying a permanent standing.
  - Alternatives Considered: Leaving only the date in the asset filename. Rejected because readers do not necessarily inspect image URLs.
- Topic: clickpy page link
  - Decision: Add the project's `clickpy` dashboard URL directly below the medal caption in both READMEs.
  - Rationale: Even if the Top 25 snapshot becomes outdated, the stable page link still gives readers current context and a place to verify the project listing.
  - Alternatives Considered: Relying only on the embedded image. Rejected because an image alone does not provide a durable navigation target.
- Topic: README medal placement
  - Decision: Change the medal from a centered block to a smaller right-aligned image with caption/link text below the intro text.
  - Rationale: The original centered layout dominated the top of the README. Right alignment keeps the recognition visible while reducing interruption to the main project description.
  - Alternatives Considered: Removing the medal entirely from the top section. Rejected because the user still wants it visible near the introduction.
- Topic: README medal grouping
  - Decision: Wrap the medal, snapshot date, and clickpy link in a single right-aligned HTML table in both READMEs.
  - Rationale: Right-aligning only the image caused the caption and link to separate from the badge under GitHub's Markdown/HTML layout rules. A single container keeps them visually attached.
  - Alternatives Considered: Leaving the caption and link in normal Markdown flow. Rejected because the elements drift apart and look broken.

Summary:
- Added a centered medal image reference to `README.md` using `docs/images/clickpy-top25-20260523.png`.
- Added visible `as of 2026-05-23` text below the medal in `README.md`.
- Added the same dated medal display to `README_ja-JP.md` with Japanese caption text.
- Added the `https://clickpy.clickhouse.com/dashboard/zstarview` link below the medal in both READMEs.
- Reduced visual prominence by right-aligning the medal and shrinking it from `220` to `160` pixels in both READMEs.
- Grouped the medal, date, and link into one right-aligned container so they stay together.
- At edit time, the referenced image file was not present in `docs/images/`, so the README change assumes that asset path will exist in the working tree.
