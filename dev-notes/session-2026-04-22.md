# Session 2026-04-22

Scope: Verify the Voyager 1 Horizons result file against the GUI sky-view interpretation.

- Topic: Horizons result inspection for Voyager 1
  - Decision: Treat `horizons_results-2.txt` as the source of truth for the downloaded Horizons observer CSV and compare its azimuth/elevation values with the app's alt/az mapping.
  - Rationale: The app's Horizons parser expects azimuth first and elevation second, so the file must be read in that order before judging whether the GUI position is consistent.

- Topic: JPL status line cleanup
  - Decision: Suppress literal `None` in the JPL small-body status line and keep the `retry` wording for small-body refresh states.
  - Rationale: The GUI should not surface Python sentinel values to users. The status line should say `retry pending` or `retry HH:MMZ` without an error suffix when the error text is missing.
  - Verification: `uv run -p .venv/bin/python pytest tests/terrain/test_terrain_horizon_ui.py -q` and `uv run -p .venv/bin/python pytest tests/test_window_render_sync.py -k jpl_small_body_failure_reschedules_one_hour_later -q`

- Topic: Restrict satellite search route to ISS
  - Decision: Limit `search_satellite_targets()` to ISS only and leave JWST / Voyager / Parker available through the JPL and overlay-specific paths.
  - Rationale: The mixed `satellite` vs `JPL` search identity made Voyager debugging ambiguous. Narrowing the search route removes the misleading `kind="satellite"` result while preserving overlay data fetches.

- Topic: Align design docs with the new search split
  - Decision: Update `docs/design.md`, `docs/specification.md`, and `docs/implementation-history.md` so that search-time `satellite` resolution is documented as ISS-only, while JWST / Voyager / Parker remain Horizons-backed overlay targets and JPL search results.
  - Rationale: The previous docs still described a broader "known artificial satellite" search path, which no longer matched the implementation after narrowing the search route for debugging clarity.
