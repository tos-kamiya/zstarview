# Session 2026-07-19

Scope: Investigate publicly available road datasets for zstarview.

- Topic: Road-data source survey
  - Decision: No implementation change in this session; compare OSM/Overture with Japanese GSI and PLATEAU sources before choosing a runtime source.
  - Rationale: The best source depends on whether zstarview needs a lightweight visual road layer, authoritative Japanese geometry, or 3D road structure.

## Summary

- Reviewed the current building, water, and terrain data paths.
- Checked current official documentation for Overture Transportation, GSI Fundamental Geospatial Data, and PLATEAU distribution.
- Follow-up: propose a lightweight road-centerline overlay first, with OSM/Overture as the global default and GSI/PLATEAU as Japan-specific alternatives.

## End summary

- Recommendation: start with Overture Transportation road segments for a cached centerline overlay; retain GSI Fundamental Geospatial Data as a Japan-specific precision option.
- No source-code or public documentation behavior was changed.
- Confirmed that Overture Transportation `segment` has optional `width_rules`, measured edge-to-edge in meters and optionally varying by normalized position along the segment.
- Updated `docs/specification.md` and `docs/design.md` to define a 50m road-proximity overlay that renders only segments with explicit `width_rules`; no-width data has no fallback rendering.
- Documented that the road overlay is omitted during fast-mode/simplified display, including acquisition and projection work.
- Added `dev-samples/overture_transportation_probe.py` and documented it in `dev-samples/README.md`.
- Probe result for Tokyo Station (`35.681236, 139.767125`, 50m bbox): download succeeded with 156 features and 108 road segments, but 0 road segments contained `width_rules`.
- Size probe at the same location: 50m GeoJSON output was 789,439 bytes; a 100m bbox produced 314 features, 266 road segments, and a 1,005,166-byte GeoJSON output. Both runs had zero `width_rules` road segments.
- Interpretation: output volume is modest for this small-area probe; remote download latency is more noticeable than local feature volume. The 100m bbox is a reasonable first retrieval margin around the 50m drawing radius, subject to wider geographic sampling.
- Follow-up: sample multiple cities and road classes before deciding whether explicit-width rendering is viable.
- Moved the unimplemented road specification and design proposal out of the current documents into `docs/specification-road-overlay-not-implemented.md` and `docs/design-road-overlay-not-implemented.md`; the current specification and design no longer describe an active road feature.

## Edge FOV defaults

- Topic: Separate the regular and Atlas edge FOV defaults
  - Decision: Set the regular `zstarview` default edge FOV back to `90` degrees and explicitly keep the Atlas profile at `90` degrees.
  - Rationale: Both applications use the 90-degree presentation after the brief 80-degree trial.

- Topic: Regular content FOV default
  - Decision: Increase the regular `zstarview` default content FOV from `110` to `115` degrees and keep Atlas at `110` degrees.
  - Rationale: The regular viewer gets five degrees more overscan without changing Atlas's existing profile.

## Ground-outline trial and rollback

- Topic: Nearby building ground outlines as a road-layer alternative
  - Decision: Do not retain the ground-side outline implementation.
  - Rationale: The implementation reused each nearby building footprint at ground elevation and rendered it as a thin, unfilled outline within 50m. In the actual view it was difficult to confirm visually, so it did not provide a sufficiently clear benefit for the added visual complexity.
  - Alternatives Considered: Connecting roof and ground outlines with side polygons was not implemented; the trial intentionally tested the smaller line-only variant first.
- Restored the source, tests, and current specification/design files to their pre-trial state. The road-source survey, probe, and explicitly marked not-implemented road proposal remain preserved.
- Verification after rollback: `pytest tests/test_urban_outline_from_buildings.py tests/test_urban_outline_layer.py tests/test_window_render_sync.py -q` -> `145 passed`.

## Asterism addition

- Topic: Great Diamond と Orion's Sword の追加
  - Decision: 既存の共通アステリズム層に、HIP星IDによる静的な線分定義として追加する。
  - Rationale: Great Diamond は既存の Spring Triangle と近接し、Orion's Sword は既存の Orion's Belt を補完するため、現行の表示・ホバー・検索経路をそのまま利用できる。

- Topic: 地面ティントの既定濃度
  - Decision: 通常ビューの `ground_tint_opacity` を `0.025` に変更する。Atlas の専用プロファイル値 `0.08` は維持する。
  - Rationale: 通常ビューの地面塗りの濃さを明示的な既定値として反映し、CLI・GUI・描画既定値・ドキュメントを一致させる。

## Sky disc zenith color

- Topic: Slightly whiten the zenith base color
  - Decision: Change `ZENITH_DAY_RGB` from `[0.22, 0.43, 0.77]` to `[0.30, 0.49, 0.79]`.
  - Rationale: Increase lightness and reduce saturation slightly while preserving the blue character.
  - Verification: `pytest tests/test_sky_disc_viewport_and_ground_tint.py` -> `38 passed`.
