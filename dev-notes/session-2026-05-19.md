# 2026-05-19 Session Notes

- Scope: scheduler consolidation for idle-only background updates.
- Focus: move timer-driven work behind a single scheduler tick, keep
  startup loading serialized, and preserve latest-wins behavior for view
  updates.
- Outcome: added an idle-gated scheduler tick, routed timer-driven sky /
  cloud / satellite / aircraft / JPL refreshes through due flags, and
  added a regression test for one-task-per-idle-tick behavior.
- Validation: `uv run -p .venv/bin/python pytest tests/test_scheduler_idle.py \
  tests/test_window_cloud_partial_mode.py tests/test_window_cloud_status.py`
  and `uv run -p .venv/bin/python ruff check ...`
- Cleanup: removed dead help-menu and persistent-search overlay helpers
  after confirming they were test-only, and updated the affected tests.
- Version bump: incremented `src/zstarview/__about__.py` from `1.27.3` to
  `1.27.4` as a patch release bump.
- Topic: status line label reduction for terrain, urban, and clouds
  - Decision: Simplified terrain and urban HUD status lines to icon-only
    output, changed the terrain icon from `▲` to `△`, and normalized cloud
    metadata display to `GOES` / `HIMAWARI` plus the UTC time only.
  - Rationale: The footer is easier to scan when it carries only the
    highest-signal token for each layer. Dropping product names and source
    labels keeps the weather-satellite and overlay indicators compact while
    preserving the distinction users care about.
  - Validation: `uv run -p .venv/bin/python pytest tests/test_window_cloud_status.py tests/terrain/test_terrain_horizon_ui.py`
    and `uv run -p .venv/bin/python ruff check src/zstarview/gui/window_updates.py tests/test_window_cloud_status.py tests/terrain/test_terrain_horizon_ui.py`
- Summary: amended the HUD status-line change into commit `c1e9647` and
  prepared the follow-up patch release bump to `1.27.5`.
- Topic: CLI Alt/Az保持チェックの有効条件確認
  - Decision: `Keep CLI-specified Alt/Az` は別の起動オプションではなく、
    `-A` / `-Z` が `raw_argv` に明示的に含まれているときだけ有効になる
    UI チェックボックスとして実装されていると確認した。
  - Rationale: `parse_args()` が `view_center_alt_specified` / `view_center_az_specified`
    を `-A` / `-Z` の存在判定で立てており、検索ダイアログ側はそのフラグが
    1 つでも立っていないとチェックを disabled にするため、設定保存や
    別名オプションは介在しない。
  - Scope: 文書確認のみでコード変更はなし。
- Topic: CLI Alt/Az 明示フラグの起動時伝播
  - Decision: `viewer.main()` で `main_win._search_view_center_alt_specified` と
    `main_win._search_view_center_az_specified` を `args.view_center_*_specified`
    から設定するようにした。
  - Rationale: これで起動時検索とメニュー検索の両方が同じフラグを参照でき、
    `Keep CLI-specified Alt/Az` が CLI で `-A` / `-Z` を指定したときに
    正しく有効化される。
  - Validation: `uv run -p .venv/bin/python pytest tests/test_viewer_startup_errors.py tests/test_place_search_dialog.py`
    と `uv run -p .venv/bin/python ruff check src/zstarview/gui/viewer.py tests/test_viewer_startup_errors.py`
- Topic: design.md の更新スケジューラ記述の集約
  - Decision: `4.4.10` と `6.1` に散っていた更新投入・定期更新・起動後の開始条件を `7. スレッドモデル` に集約し、他章は参照だけに整理した。
  - Rationale: 更新タイミングとスレッド責務は同じ設計判断に属するため、起動フローや個別レイヤー説明に分散させるより、`7` にまとめた方が読み手が責務境界を追いやすい。
  - Scope: `docs/design.md` のみを更新し、機能仕様や実装コードは変更していない。
- Summary: `docs/design.md` の更新スケジューラ関連の記述を 7 章へ集約し、`dev-notes/session-2026-05-19.md` に判断を追記した。commit / push は未実施。
- Topic: Himawari NetCDF crash の切り分け
  - Decision: `_hima_isatss.py` の NetCDF 読み出しを、複数ファイルの同時オープン保持から 1 ファイルずつ即時クローズする形に変更し、`fixedgrid_projection` も backend 参照を持たない in-memory の scalar `DataArray` に置き換えた。
  - Rationale: `free(): invalid pointer` は Python 例外ではなく native 解放経路の破綻に見えるため、HDF5/h5py/xarray の file handle と backend object の寿命を短くするのが最小リスクの対策だった。読み取り内容は同じまま、保持する native オブジェクトだけ減らしている。
  - Alternatives Considered: 取得処理を別プロセス化する案もあるが、今回は最小変更で crash 面を下げることを優先した。
- Topic: 定期更新の優先順位明記
  - Decision: `docs/design.md` の `7. スレッドモデル` に、複数の定期更新が同時に成立した場合は `aircraft -> satellite -> cloud` の順で 1 件ずつ起動する旨を追記した。
  - Rationale: 実装の実際の投入順を設計書に明示しておくと、同時到来時の挙動を読み手が推測しなくて済む。
  - Scope: 文書のみの変更で、挙動変更はない。
- Topic: satellite / aircraft 表示位置の低優先度再投影
  - Decision: 航空機と人工衛星の表示位置再計算を、取得直後だけでなく `refreshed_at_utc + interval` 基準で繰り返す scheduler タスクに戻し、cloud より後ろの最下位優先で実行するようにした。
  - Rationale: 表示位置はキャッシュされた観測値の再投影で更新できるため、重い再取得とは分けて定期更新した方が、画面が古い位置に固定されにくい。
  - Validation: `uv run -p .venv/bin/python pytest tests/test_scheduler_idle.py tests/test_window_render_sync.py -k "satellite_overlay or aircraft_overlay or scheduler_tick"` と `uv run -p .venv/bin/python ruff check src/zstarview/gui/window.py src/zstarview/gui/window_updates.py tests/test_scheduler_idle.py`
- Topic: satellite / aircraft の取得と投影の分離
  - Decision: `SatelliteController` / `AircraftController` から投影処理を外し、取得結果は window 側で受けてから投影する形に分離した。scheduler では cloud / satellite / aircraft のデータ更新を同じ節に並べ、その後ろに satellite / aircraft の再投影を置いた。
  - Rationale: データの取得頻度と表示位置の更新頻度が異なるため、責務を分けた方が再利用しやすく、スケジューラの優先順位も読みやすくなる。
  - Validation: `uv run -p .venv/bin/python pytest tests/test_scheduler_idle.py tests/satellites/test_satellite_controller.py tests/test_controller_shutdown_waits.py` と `uv run -p .venv/bin/python ruff check src/zstarview/gui/window.py src/zstarview/gui/window_updates.py src/zstarview/gui/satellite_controller.py src/zstarview/gui/aircraft_controller.py tests/test_scheduler_idle.py tests/satellites/test_satellite_controller.py tests/test_controller_shutdown_waits.py`
- Topic: satellite / aircraft の投影用語統一
  - Decision: satellite / aircraft の表示位置更新を `projection` / `reprojection` 用語へ寄せ、`reproject_satellite_overlay` / `reproject_aircraft_overlay` を実体のメソッド名にした。旧名は移行互換の薄いラッパーとして残した。
  - Rationale: 取得と投影の責務分離をコード上でも読み取りやすくするため、用語を統一した。
  - Validation: `uv run -p .venv/bin/python pytest tests/test_scheduler_idle.py tests/satellites/test_satellite_controller.py tests/test_controller_shutdown_waits.py tests/test_window_render_sync.py -k "reproject_satellite_overlay or scheduler_tick or satellite_controller_does_not_project_in_fetch_stage or aircraft_controller_does_not_project_in_fetch_stage"` と `uv run -p .venv/bin/python ruff check src/zstarview/gui/window.py src/zstarview/gui/window_updates.py src/zstarview/gui/satellite_controller.py src/zstarview/gui/aircraft_controller.py src/zstarview/gui/window_state.py tests/test_scheduler_idle.py tests/satellites/test_satellite_controller.py tests/test_controller_shutdown_waits.py tests/test_window_render_sync.py`
- Topic: satellite / aircraft の 2 秒再投影が busy 時に止まる問題の修正
  - Decision: scheduler の busy 判定で projection 更新まで早期 return しないようにし、背景更新の開始だけを抑制する一方、`reproject_*` は idle ワークとして継続実行するようにした。
  - Rationale: 取得済みデータの再投影は重いバックグラウンド fetch とは独立して更新できるため、busy で丸ごと止めると 2 秒周期が失われる。今回の症状は取得時に計算した座標の流用ではなく、scheduler 側の早期 return が原因だった。
  - Validation: `uv run -p .venv/bin/python pytest tests/test_scheduler_idle.py -q` と `uv run -p .venv/bin/python ruff check src/zstarview/gui/window_updates.py tests/test_scheduler_idle.py`
- Topic: satellite / aircraft の描画時投影への責務戻し
  - Decision: satellite / aircraft は、取得済みの軌道・状態データを保持し、描画関数が現在時刻と観測条件から画面上の位置を決めて描画する責務へ戻す方針にした。scheduler 上では、これらを fast-mode に似た latest-win の 2 秒タスクとして扱う。
  - Rationale: 投影済み座標を状態として持つよりも、描画直前に再計算した方が、視点変更や現在時刻の変化に対して責務が明確になる。2 秒周期の更新も、描画時投影として整理した方が設計上の整合が取りやすい。
- Topic: satellite / aircraft の描画時投影の実装切り替え
  - Decision: `RenderSceneData` に raw の軌道要素 / snapshot と描画時刻を渡し、`render/satellites.py` と `render/aircraft.py` が描画直前に alt/az と screen position を計算するよう切り替えた。`reproject_*` は座標を保持するのではなく、次回期限の更新と redraw 要求に役割を縮めた。
  - Rationale: 描画責務を render 層へ寄せることで、controller/state に投影済み座標を持つ必要がなくなり、`latest-win` の 2 秒再描画と視点変更の扱いが単純になる。衛星の古い要素は描画時に抑止し、航空機は snapshot の age に応じた予測表示を render 側で継続できる。
  - Validation: `uv run -p .venv/bin/python pytest tests/test_scheduler_idle.py tests/test_window_render_sync.py tests/terrain/test_terrain_horizon_ui.py tests/test_planet_marker_style.py tests/satellites/test_satellite_controller.py tests/test_controller_shutdown_waits.py -q` と `uv run -p .venv/bin/python ruff check src/zstarview/gui/window.py src/zstarview/gui/window_render.py src/zstarview/gui/window_updates.py src/zstarview/render/pipeline.py src/zstarview/render/satellites.py src/zstarview/render/aircraft.py tests/test_scheduler_idle.py tests/test_window_render_sync.py tests/terrain/test_terrain_horizon_ui.py tests/test_planet_marker_style.py tests/satellites/test_satellite_controller.py tests/test_controller_shutdown_waits.py`
- Topic: satellite / aircraft の旧互換データ構造削除
  - Decision: `satellite_overlay_points` / `aircraft_overlay_points` を `RenderSceneData` と GUI state から削除し、描画・hover・export すべてを raw の `records_by_group` / `snapshots` 経路へ統一した。
  - Rationale: 取得済みデータと投影済み座標を分ける方針が固まったため、旧構造を残すと責務が曖昧になる。raw データだけを保持する方が、描画時投影と latest-win 更新の設計に一致する。
  - Validation: `uv run -p .venv/bin/python pytest tests/test_window_render_sync.py tests/terrain/test_terrain_horizon_ui.py tests/test_scheduler_idle.py -q` と `uv run -p .venv/bin/python ruff check src/zstarview/cli/export_image.py src/zstarview/gui/window.py src/zstarview/gui/window_render.py src/zstarview/gui/window_state.py src/zstarview/render/aircraft.py src/zstarview/render/pipeline.py src/zstarview/render/satellites.py tests/terrain/test_terrain_horizon_ui.py tests/test_window_render_sync.py`
- Topic: background update timers の単一化
  - Decision: 個別の `cloud / satellite / aircraft / projection / persistent-search / sky` タイマーをやめ、`scheduler_tick_timer` だけを起床用に残して、各更新は `*_next_refresh_utc` を見て scheduler が起動する形に寄せた。
  - Rationale: 期限管理を state に寄せると、どの更新がいつ動くかを 1 か所で追える。projection と data refresh も分離しやすくなり、`QTimer` の役割を「scheduler を起こす」だけに絞れる。
  - Validation: `uv run -p .venv/bin/python pytest tests/test_scheduler_idle.py tests/test_window_render_sync.py tests/terrain/test_terrain_horizon_ui.py -q` と `uv run -p .venv/bin/python ruff check src/zstarview/gui/window.py src/zstarview/gui/window_updates.py src/zstarview/gui/satellite_state.py src/zstarview/gui/aircraft_state.py tests/test_scheduler_idle.py tests/test_window_render_sync.py tests/terrain/test_terrain_horizon_ui.py`
- Topic: satellite 描画が古い element epoch で止まる問題
  - Decision: `render/satellites.py` の描画・hover から `element_epoch_utc` による非表示判定を削除し、raw データと現在時刻だけで位置決定する形へ戻した。
  - Rationale: いまの設計では衛星データの鮮度管理は scheduler 側で行うため、描画層が古い epoch を理由に全消しするのは責務の重複だった。
  - Validation: `uv run -p .venv/bin/python pytest tests/test_planet_marker_style.py -k satellite_overlay_does_not_hide_old_element_epoch tests/aircraft/test_aircraft_project.py -q` と `uv run -p .venv/bin/python pytest tests/test_window_render_sync.py tests/test_scheduler_idle.py tests/aircraft/test_aircraft_project.py -q`
- Topic: FrameContext の導入
  - Decision: 描画 1 フレーム分の条件を `FrameContext` にまとめ、`viewer`, `time_obj`, `geometry`, `viewport_rect` を GUI と export_image の共通入力として渡す形に寄せた。
  - Rationale: 観測地点と視点中心だけでなく、時刻と viewport サイズも描画系ではほぼ常に必要になるため、フレーム単位の束を作ると引数のばらつきを減らせる。既存の `ViewerData` は残し、互換性のために一部の旧 helper だけは直接 `ViewerData` を組み立てる経路を残した。
  - Validation: `uv run -p .venv/bin/python pytest tests/test_window_render_sync.py tests/test_viewer_startup_errors.py tests/test_place_search_dialog.py` と `uv run -p .venv/bin/python ruff check src/zstarview/gui/window_render.py src/zstarview/render/pipeline.py src/zstarview/cli/export_image.py`
