# 2026-05-27 Session

## Scope

Refresh the Europe infrared gray common mask asset with the newly generated image.

## Notes

- The replacement source image was produced with:
  `uv run -p .venv/bin/python dev-samples/geo_satellite_gray_common_mask.py Europe-IR-20260525130000.png Europe-IR-20260526020000.png Europe-IR-20260526110000.png Europe-IR-20260526201500.png Europe-IR-20260527120000.png -o Europe-IR-gray-common-mask.png`
- The repository keeps the same mask under both `src/zstarview/data/geosatellite/` and `raw-data/` for package/runtime and dev-sample use, so both copies should stay aligned.
- The Geo-satellite raw-data set was reorganized under `raw-data/geosatellite/`, including the five source frames from the mask-generation command plus the companion projection assets (`Europe-IR.png`, `eqdc_lonlat.npz`, `eqdc_grid.png`, and `latlonmap.txt`).
- Only the five source frames that contributed to the current mask are retained under `raw-data/geosatellite/`; the other intermediate `Europe-IR-202605*.png` frames were deleted because they are not needed for traceability.
- The raw-data README now states that the Europe infrared frames were sourced from the MET Norway Geo-Satellite API URL used by the pipeline.
- The specification, design, and implementation-history docs now all cite the same MET Norway Geo-Satellite API URL for the Europe infrared validation assets.
- Added temporary export-image timing logs behind `ZSTARVIEW_EXPORT_IMAGE_TIMING` to separate Geo-sat projection, gray-to-RGBA conversion, and cloud-field binning.
- Expanded the temporary export-image timing logs to cover urban Overture work, water-target DEM setup, and water overlay dot generation.
- Wired the export-image water path to the shared GUI water-overlay cache so repeated runs can reuse cached Overpass footprints.
- Split the export-image urban timing logs into Overture build and outline-resolution stages.
- Documented the fixed-view export-image culling policy in the specification, design, and implementation-history docs.
- Implemented the fixed-view culling path in `zstarview-export-image` for urban outline projection and water overlay sampling, while keeping shared caches unchanged.
- Added regression tests for the export-image front-half culling path in both urban and water helpers.
- Verified the updated tests pass and confirmed the fixed-view export-image run still completes with the new culling path enabled.

## Summary

- Replaced the tracked Europe IR gray common mask in both asset locations with the new generated image.
- Verified that both copies now share the same checksum as the generated source image.
- Updated the Geo-satellite traceability README to include the five-frame generation command and the new source image.
- Added the source API URL to the raw-data README for traceability.
- Aligned the specification, design, and implementation-history docs on the same source URL wording.
- Added temporary timing instrumentation to `zstarview-export-image` for cloud-generation debugging.
- Expanded the temporary timing instrumentation to cover urban and water substeps.
- Connected the CLI water path to the same cached Overpass footprint store used by the GUI.
- Split the CLI urban timing logs so the Overture build and outline resolution costs are visible separately.
- Kept the raw-data archive aligned with the current traceability inputs and projection assets.
- Recorded the fixed-view culling policy for urban and water in the docs so the optimization can be implemented without changing shared cache compatibility.
- Implemented the export-image front-half culling path without changing cache formats.
- Added focused regression tests for the culling helpers and verified them with `pytest`.
- Ran a timed export-image verification pass after the culling change.
- Adjusted the export-image culling threshold from a hard 90-degree cap to `viewer_data.content_fov_deg` so rear-edge buildings in wide FOV views are no longer dropped.
- Re-verified the culling-focused unit tests and one timed `zstarview-export-image` run after the threshold fix.
