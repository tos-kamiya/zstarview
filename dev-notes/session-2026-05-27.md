# 2026-05-27 Session

## Scope

Refresh the Europe infrared gray common mask asset with the newly generated image.

## Notes

- The replacement source image was produced with:
  `uv run -p .venv/bin/python dev-samples/geo_satellite_gray_common_mask.py Europe-IR-20260525130000.png Europe-IR-20260526020000.png Europe-IR-20260526110000.png Europe-IR-20260526201500.png -o Europe-IR-gray-common-mask.png`
- The repository keeps the same mask under both `src/zstarview/data/geosatellite/` and `raw-data/` for package/runtime and dev-sample use, so both copies should stay aligned.
- The Geo-satellite raw-data set was reorganized under `raw-data/geosatellite/`, including the four source frames from the mask-generation command plus the companion projection assets (`Europe-IR.png`, `eqdc_lonlat.npz`, `eqdc_grid.png`, and `latlonmap.txt`).
- Only the four source frames that contributed to the current mask are retained under `raw-data/geosatellite/`; the other intermediate `Europe-IR-202605*.png` frames were deleted because they are not needed for traceability.

## Summary

- Replaced the tracked Europe IR gray common mask in both asset locations with the new generated image.
- Verified that both copies now share the same checksum as the generated source image.
- Moved the Geo-satellite traceability assets into a dedicated raw-data subdirectory and updated the sample/docs defaults to point at the new location.
- Pruned unused intermediate Europe IR frames so the archive only keeps the inputs that matter for reproducing the current mask and projection assets.
