# Session 2026-04-12

- Scope: Reduce octilinear earth-guide line detail by about 20%.

- Topic: Split Earth guide from terrain horizon
  - Decision: Treat Earth guide as an independent layer with its own CLI opacity, GUI toggle, and cache key, while reusing the terrain horizon color value through a separate constant.
  - Rationale: The user wants Earth guide visibility to be controlled separately from terrain horizon, including the startup lockout semantics when opacity is `0`. Keeping the color matched but the state independent preserves the current visual language without coupling the two feature lifecycles.
  - Notes: Implementation work is in progress; terrain horizon fetch/update behavior remains unchanged.

- Summary: Earth guide independence implementation
  - Result: Added `--earth-guide-opacity`, `Earth Guide` menu / hotkey toggle, a separate Earth-guide opacity path through render style and compositor cache keys, and a separate line-color constant alias.
  - Validation: `pytest tests/test_cli_arg_helpers.py tests/test_export_image_layer_gating.py tests/test_earth_guide_render.py tests/test_window_render_sync.py tests/terrain/test_terrain_horizon_ui.py -q`
  - Validation: `ruff check src/zstarview/render/earth_guide.py src/zstarview/gui/window_inputs.py src/zstarview/gui/window.py src/zstarview/gui/viewer.py src/zstarview/gui/window_render.py src/zstarview/gui/composite.py src/zstarview/render/pipeline.py src/zstarview/cli/args.py src/zstarview/cli/export_image.py tests/test_cli_arg_helpers.py tests/test_earth_guide_render.py tests/test_window_render_sync.py tests/terrain/test_terrain_horizon_ui.py`

- Topic: README update for Earth guide
  - Decision: Document Earth guide as an independent layer in both English and Japanese READMEs, including the new CLI flag and the separate GUI toggle.
  - Rationale: The README should match the shipped behavior and show that `--earth-guide-opacity 0` locks only the Earth guide, not terrain horizon.

- Topic: Octilinear preview roughness
  - Decision: Increase the default Douglas-Peucker tolerance in `build_octilinear_earth_guide_svg.py` from `1.8` to `2.16`.
  - Rationale: The linework was still too busy in a few regions, and the user asked for roughly 20 percent more roughness. Raising the simplification tolerance keeps the same rendering pipeline and polar handling while reducing coastline detail enough to make the large-scale landmass shape easier to read.

- Topic: Island preservation while roughening
  - Decision: Cap simplification at `1.8` for rings under `80 deg^2` before applying the broader `2.16` default.
  - Rationale: The global roughening made Japan disappear because a medium-sized island ring was simplified too aggressively. Keeping smaller rings closer to the previous tolerance preserves recognizable archipelagos while still roughening the large continental outlines that dominate the guide.

- Topic: Small-island detail recovery
  - Decision: Use three area buckets for simplification: `2.16` for large continental rings, `1.8` for medium island rings, `1.4` for smaller islands, and `1.0` for tiny island rings.
  - Rationale: The UK still vanished under the two-tier cap because some island rings were being flattened too much. A gentler gradient keeps the continental simplification roughly the same while restoring enough shape detail for islands to remain recognizable.

- Topic: Slightly finer overall mesh
  - Decision: Lower the default simplification tolerance from `2.16` to `2.0` while keeping the island-preserving area buckets.
  - Rationale: The user wanted a modest increase in mesh detail, not a new rendering strategy. Reducing only the global baseline preserves the earlier island fixes and makes the entire outline a little less coarse without undoing the octilinear simplification work.

- Topic: Tiny-island recovery
  - Decision: Lower the tiny-island simplification ceiling from `1.0` to `0.8`.
  - Rationale: Taiwan-sized rings were still being simplified away at `1.0`. Dropping the ceiling a bit more restores the smallest visible islands without changing the coarser treatment for larger landmasses.

- Topic: Taiwan area filtering
  - Decision: Base the ring-area filter on the raw polygon area and make it adaptive by size bucket, with island-sized rings allowed down to `2 deg^2`.
  - Rationale: Taiwan was being dropped by the post-simplification area filter even when the shape itself still had enough geometry to be recognizable. Filtering on the raw ring area keeps the island from disappearing while still suppressing tiny noise rings.

- Topic: Runtime earth-guide data swap
  - Decision: Replace `src/zstarview/data/earth_guide_land_110m.json` with the current octilinear runtime payload generated from `build_octilinear_earth_guide_svg.py`.
  - Rationale: The application should load the same simplified geometry that was validated in the preview script, so the shipped runtime data matches the current guide shape and island-preservation settings.

- Topic: SIXEL terminal capability preflight for export-image
  - Decision: Add a preflight check for `zstarview-export-image --sixel` that first verifies `img2sixel` is present and then queries the controlling terminal with the same device-attributes probe used by `lsix` (`ESC[c`), rejecting terminals that do not report SIXEL support.
  - Rationale: `img2sixel` availability alone does not guarantee the terminal can display the result. Failing fast before image rendering keeps the error clear, avoids wasted work, and matches the terminal capability detection method the user asked us to follow.
  - Notes: Logged the behavior change in `docs/specification.md`, `docs/design.md`, and `docs/implementation-history.md`, and added focused tests for the DA response parser, the terminal probe, and the main preflight path.

- Topic: README feature wording cleanup
  - Decision: Merge the aircraft and artificial-satellite bullets into one feature line, and mention the sky-color disc briefly at the end of the satellite-cloud item in the main and PyPI READMEs.
  - Rationale: The user wanted the feature list to read with the requested granularity, without changing the overall structure or adding new content.

- Topic: Japanese README wording cleanup
  - Decision: Apply the same feature-granularity cleanup to `README_ja-JP.md`, merging aircraft and artificial-satellite wording and adding a short sky-color-disc note to the satellite-cloud item.
  - Rationale: Keeping the Japanese README aligned with the English and PyPI variants avoids drift in the published feature summary.

- Topic: README label tightening
  - Decision: Rename the satellite-cloud feature labels to include the sky-color disc explicitly, and shorten the terrain label to only `terrain horizon and earth guide` / `地形地平線・地球ガイド`.
  - Rationale: The earlier wording buried the disc mention and made the terrain item feel too broad. Tightening the labels makes the two feature boundaries clearer without changing the underlying behavior text.

- Topic: README stars feature clarification
  - Decision: Add an explicit stars feature bullet to the English, Japanese, and PyPI READMEs so the list no longer implies that stars are only covered implicitly by later overlays.
  - Rationale: The feature summary should mention the base star field directly before layering notes, which better matches the product's primary behavior.

- Topic: More detailed island cutoff
  - Decision: Lower the top ring-area cutoff from `12 deg^2` to `8 deg^2` so the effective bucket set is `2 / 4 / 8`.
  - Rationale: The guide was still too coarse to recognize some smaller islands. Reducing the highest cutoff keeps the adaptive island preservation logic intact but allows more mid-sized island rings to survive, which improves recognizability without changing the large-landmass simplification path.

- Summary: Session wrap-up
  - Result: Added SIXEL terminal capability preflight, added `--version` to `zstarview-export-image`, bumped `__version__` to `1.9.5`, and committed the tracked changes as `f7907a3` (`chore: bump version to 1.9.5`).
  - Follow-up: The worktree still has unrelated untracked paths (`.serena/`, `CONTRIBUTING.md`, `dist/`, `raw-data/`) that were left untouched.

- Topic: ip-api.com legal notice propagation
  - Decision: Record that `auto` location resolution now uses the `ip-api.com` API and that the README, specification, design, and implementation history were updated to describe its non-commercial-use restriction and rate limit.
  - Rationale: The new feature is externally visible and its usage constraints need to stay aligned across user-facing and design documentation.

- Topic: `--observation-info` GUI overlay mode
  - Decision: Preserve the new CLI `observation_info` mode string when constructing the window, normalize legacy boolean values to `"auto"`/`"off"` at the window-input boundary, and prevent mouse-based HUD repositioning when the overlay is pinned to the top or bottom.
  - Rationale: This keeps the new option compatible with the existing boolean path while making the overlay pinning behavior explicit in the GUI layer, so the HUD does not fight the CLI-selected mode.

- Topic: Equator and ecliptic dash spacing
  - Decision: Increase the dash/gap spacing for the celestial equator and ecliptic reference lines so they read as distinct dotted guides instead of nearly solid lines at normal zoom.
  - Rationale: The previous dash cadence was visually too dense in screenshots and could merge into a continuous stroke. Widening only those two patterns keeps the horizon guide unchanged while improving legibility for the sky reference overlays.

- Topic: Version bump
  - Decision: Bump the application version to `1.9.7`.
  - Rationale: The user requested a patch-level release after the recent UI and rendering adjustments, so the version metadata should advance by one patch step.
