# Session 2026-06-04

- Scope: GUI Geo-satellite toggle gating for out-of-band observer locations.
- Notes: Track the menu enable/disable behavior and any tests or docs that need to stay aligned.
- Summary:
  - Added GUI gating so the `Geo-satellite` menu item is disabled outside the Europe band determined at startup/resolved viewer location.
  - Kept the toggle state synchronized when startup viewer data is applied, and prevented programmatic toggles from changing state when unsupported.
  - Adjusted the startup path so the temporary placeholder location does not disable the menu before the resolved observer location is known.
  - Moved the startup dialog `Geo-satellite` checkbox from the `Overlays` tab to the `General` tab so it matches the CLI option grouping.
  - Validation: `uv run -p .venv/bin/python ruff check src/zstarview/gui/window.py tests/test_cloud_startup_gating.py`, `uv run -p .venv/bin/python pytest tests/test_cloud_startup_gating.py`.
