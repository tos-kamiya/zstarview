# Session 2026-07-13

Scope: Refresh repository agent instructions against the current project structure and development workflow.

- Topic: Add subtle Atlas day/night background tint
  - Decision: Keep the Atlas background fundamentally white, overlay a low-alpha warm tint at solar altitude `+6°` and above, a low-alpha blue-gray tint below `-6°`, and linearly interpolate between them. Leave the background white when the Sun position is unavailable.
  - Rationale: Provide an at-a-glance day/night cue without adding HUD text, darkening the map, or changing the existing overlay palette. The `-6°`/`+6°` range also avoids an abrupt transition around twilight.
  - Alternatives Considered: A dark night background would require broad contrast and layer-color retuning; a HUD label was rejected because the existing HUD already contains the time.
  - Validation: `uv run -p .venv/bin/python pytest tests/test_sky_disc_viewport_and_ground_tint.py tests/test_window_render_sync.py` (153 passed); focused `ruff check` passed.

- Topic: Move Atlas day/night tint outside the FOV
  - Decision: Keep the FOV interior white and apply the stronger warm/cool tint only outside the content FOV, with a short radial transition at the boundary.
  - Rationale: The visual cue becomes easier to see while preserving the white map surface where sky objects and geographic overlays are drawn.
  - Alternatives Considered: Increasing the tint across the whole background would make the map itself visibly colored and still provided weak contrast in the previous implementation.
  - Validation: `uv run -p .venv/bin/python pytest tests/test_sky_disc_viewport_and_ground_tint.py tests/test_window_render_sync.py` (153 passed); focused `ruff check` passed.

- Topic: Route Atlas tint through the actual instrument background
  - Decision: Apply the FOV-outside tint in `draw_instrument_background()`, which is the background path selected by Atlas's `presentation_id="instrument"`; retain the shared radial helper for other flat-background callers.
  - Rationale: The initial implementation modified `draw_radial_background()`, but Atlas bypasses that function and paints its own full white background. The missing visual effect was therefore a rendering-path mismatch, not an insufficient tint value.
  - Validation: `uv run -p .venv/bin/python pytest tests/test_sky_disc_viewport_and_ground_tint.py tests/test_window_render_sync.py` (154 passed); focused `ruff check` passed.

- Topic: Make the Atlas FOV boundary explicit
  - Decision: Replace the outside-FOV gradient with a hard white-to-solid-color boundary, using a stronger warm daytime color and neutral medium gray at night.
  - Rationale: The previous alpha-blended gradient remained difficult to see in the running Atlas window. A solid outside color makes the state cue unambiguous while leaving the FOV interior white.

- Topic: Use blue, sunset orange, and dark gray for Atlas time-of-day cues
  - Decision: Interpolate through dark gray at solar altitude `-6°`, orange at `0°`, and blue at `+6°` outside the FOV.
  - Rationale: The three colors provide a more intuitive visual progression from night through sunset/sunrise to daytime than the previous warm/gray pair.
  - Validation: `uv run -p .venv/bin/python pytest tests/test_sky_disc_viewport_and_ground_tint.py tests/test_window_render_sync.py` (154 passed); focused `ruff check` passed.

- Topic: Move Atlas time-of-day cue to a corner marker
  - Decision: Remove the FOV-based color area and draw a solid right-isosceles triangle at the left corner opposite the HUD: top-left when HUD is bottom-left, bottom-left when HUD is top-left.
  - Rationale: A compact corner marker avoids changing the map or overlapping sky/terrain content while preserving an immediate visual day/night cue.
  - Validation: `uv run -p .venv/bin/python pytest tests/test_sky_disc_viewport_and_ground_tint.py tests/test_window_render_sync.py` (153 passed); focused `ruff check` passed.

- Topic: Correct Atlas marker side and size
  - Decision: Invert the HUD-side flag before placing the marker and reduce its leg size from `72` px to `44` px.
  - Rationale: The marker must occupy the corner opposite the HUD, and the original triangle was larger than necessary after the placement was confirmed visually.

- Topic: Add an inner margin to the Atlas marker
  - Decision: Place the triangle 4 px inside the selected window corner on both axes.
  - Rationale: A small margin separates the marker from the window frame without changing its corner meaning or HUD avoidance behavior.

- Topic: Improve Atlas text readability
  - Decision: Give the Atlas static HUD and status line a low-alpha white rounded background, and restore a bright white text outline for Atlas labels while leaving other themes unchanged.
  - Rationale: The HUD is a compact fixed block suited to a shared backing panel, while individual label backgrounds would clutter the map; a white outline keeps floating labels readable over dark terrain and overlays.
  - Validation: `uv run -p .venv/bin/python pytest tests/test_theme_window_background.py tests/test_label_layer.py tests/test_planet_marker_style.py tests/test_sky_disc_viewport_and_ground_tint.py tests/test_window_render_sync.py` (201 passed); focused `ruff check` passed.

- Topic: Remove the Atlas HUD backing panel
  - Decision: Remove the semi-transparent HUD and status-line backgrounds; use the same white outline as Atlas labels for all text.
  - Rationale: The backing panel was too visually strong against the white map, while the outline preserves readability without obscuring nearby content.

- Topic: Improve Atlas direction-label readability
  - Decision: Use a medium-weight font for compass direction labels and change their text color to a restrained gray, while retaining the existing outline behavior.
  - Rationale: Direction labels sit on or near the horizon line, so a small weight increase improves legibility without making them compete with object labels.
  - Validation: `uv run -p .venv/bin/python pytest tests/test_color_palette.py tests/test_direction_hover_guide.py tests/test_window_render_sync.py` (122 passed); focused `ruff check` passed.

- Topic: Increase Atlas direction-label weight
  - Decision: Raise compass direction labels from Medium to DemiBold while keeping their gray color and outline unchanged.
  - Rationale: The first weight increase separated them from other labels but did not provide enough contrast against the horizon line.

- Topic: Thicken Atlas satellite cross cursors
  - Decision: Add a theme-level satellite marker width and set the Atlas value to `2.0` px; preserve the existing `1.0` px default for other themes and keep the aircraft-style outline offset of `+2.0` px.
  - Rationale: The short cross segments need more visual weight on the white Atlas surface, while a theme-level value avoids changing the established night-theme rendering.
  - Alternatives Considered: Increasing the shared default would also alter the night, day, and export presentations without being requested.
  - Validation: `uv run -p .venv/bin/python pytest tests/test_color_palette.py tests/test_planet_marker_style.py` (38 passed); focused `ruff check` passed.

- Topic: Update stale AGENTS.md guidance
  - Decision: Correct the entry-point/module references, document the current PySide6 dependency, standardize development commands on uv, and replace the blanket HARD STOP approval exchange with risk-based confirmation guidance.
  - Rationale: Keep durable repository instructions aligned with the current source tree and the GPT-5.6 prompting guidance to preserve outcome-focused autonomy for safe local work while retaining confirmation for destructive or scope-expanding actions.

- Topic: Record rejected design alternatives
  - Decision: Require development notes to record rejected design or implementation options and why they were not chosen.
  - Rationale: Preserve the reasoning behind design decisions, not only the final implementation.

- Topic: Identify the darkest sky-disc RGB
  - Decision: Treat the night branch of the sky-color model as the minimum-color case and report both its normalized model value and the 8-bit rendered value before disc-opacity compositing.
  - Rationale: `disc_opacity` is stored in the RGBA alpha channel, while the sky model applies exposure (and the separately named sky alpha) to RGB before rasterization. Reporting the intermediate stages avoids conflating color dimming with alpha compositing.

- Topic: Temporarily raise sky-disc exposure
  - Decision: Change the sky-disc exposure default from `1.14` to `1.35` without updating tests or documentation.
  - Rationale: Provide a quick, reversible visual comparison of a brighter sky while keeping the experiment limited to the rendering code.

- Topic: Make sky-disc exposure permanent
  - Decision: Keep the sky-disc exposure default at `1.5` and update the focused sky-disc tests that supplied the previous `1.14` value.
  - Rationale: Align the test scenarios with the accepted rendering default while leaving unrelated uses of `1.14` unchanged.

- Topic: Preserve sky-color hue test headroom
  - Decision: Keep the sun-rise warmth comparison at explicit exposure `1.0`.
  - Rationale: At exposure `1.5`, both compared colors clip at the upper RGB bound, making the test unable to observe the intended hue difference. The test is about the direction of the color shift, not the production exposure default.

- Topic: Balance daytime and midnight sky brightness
  - Decision: Lower the sky-disc exposure default to `1.3` and slightly raise `NIGHT_SKY_RGB` to `(0.012, 0.024, 0.06)`.
  - Rationale: Reduce excessive daytime brightness while retaining a subtly more visible blue tone at midnight.
