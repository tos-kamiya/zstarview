# Session 2026-03-25

- Scope: Update the English and Japanese README GUI keybinding documentation to match the current aircraft and satellite shortcuts.

- Topic: Correct README GUI shortcut labels for aircraft and artificial satellite overlays
  - Decision: Change the README keyboard shortcut list so `Aircraft` is documented as `P` and `Artificial satellite` is documented as `I`.
  - Rationale: The implemented GUI bindings were reassigned earlier, but the README still described the old `I` / `L` mapping. This keeps user-facing documentation aligned with the current menu shortcuts.

- Topic: Mirror the same shortcut correction in the Japanese README
  - Decision: Update `README_ja-JP.md` to document `Aircraft` as `P` and `Artificial satellite` as `I` in the GUI keyboard shortcut list.
  - Rationale: The Japanese README had the same stale shortcut mapping, so both user-facing README variants should describe the same current behavior.

- Topic: Clarify that GUI search also supports ISS
  - Decision: Update both README menu descriptions for `Search Stars and Asterisms...` to mention ISS as a searchable target.
  - Rationale: The dialog now supports ISS lookup in addition to named stars and asterisms, so the user-facing README should state that capability briefly where the search action is described.

- Topic: Align export-image past-time satellite gating with the GUI policy
  - Decision: Make `export_image._build_window_inputs_from_args()` disable artificial satellites for past timestamps as well as future timestamps, matching the shared `overlay_time.py` policy and GUI behavior.
  - Rationale: The recent regression exposed that `export-image` tests still encoded an older past-time satellite exception. The intended product behavior is now simpler: only `present` allows realtime overlays, while `past` and `future` disable clouds, aircraft, and satellites consistently.

- Topic: Preserve backward compatibility for search jump targets without `kind`
  - Decision: Make `SkyWindow._jump_to_search_target()` treat targets without a `kind` attribute as ordinary star targets.
  - Rationale: The search-jump flow now supports satellite-specific handling, but older tests and lightweight callers still pass star-like objects with only `label`, `ra_hours`, and `dec_deg`. Defaulting missing `kind` to `star` restores the previous contract without affecting satellite jumps.

- Summary
  - Commits: `docs: update GUI shortcut and search docs`, `fix: align time-shift overlay gating`, `chore: bump version to 1.6.5`
  - Tags: none
  - Pushes: none
  - Follow-ups: none
