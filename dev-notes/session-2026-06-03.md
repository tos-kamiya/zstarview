# 2026-06-03 session log

Scope: Align the tropical cyclone specification and design with an all-storm presentation model.

- Topic: Tropical cyclone overlay scope expansion
  - Decision: Update the user-facing specification and internal design to treat `Active_Hurricanes_v1` as a source of all active storms, not just the single latest observed storm.
  - Rationale: The user wants the overlay to show every concurrently active storm returned by the service, so the docs need to describe multi-storm collection, per-storm grouping, and simultaneous display.
  - Follow-up: Implement the data-fetch and rendering changes after the docs are aligned.
- Summary: Updated `docs/specification.md` and `docs/design.md` for all-storm tropical cyclone handling. No code changes or tests were run in this session.
- Summary: Implemented multi-storm fetching, versioned cache schema `4`, collection-backed tropical cyclone state, and per-storm rendering. Validated with `pytest tests/test_tropical_cyclones.py -q`, `pytest tests/test_window_render_sync.py -q`, and `ruff check` on the touched Python modules.
- Summary: Documented that tropical cyclone cache version changes are non-backward-compatible; stale versions are discarded and re-fetched.
- Topic: Tropical cyclone far-label hover behavior
  - Decision: Keep the `400km`-out-of-range triangular marker visible, but suppress its storm-name label unless the marker is hovered, matching the satellite hover pattern.
  - Rationale: This reduces clutter in scenes with several distant storms while preserving discoverability on demand.
  - Follow-up: Update the specification and design references, and add hover-target tests for the far-label path.
