# 恒星表示補間 引き継ぎ書

最終更新: 2026-07-19

## 目的

zstarview の通常ビューアで、60 秒ごとの恒星・空スナップショット交換時に発生する画面の「カチッ」とした切り替えを緩和する。

重い天文計算の更新間隔は維持し、恒星の表示だけを軽量に補間する。まず通常ビューアの GUI を対象とし、Atlas と `zstarview-export-image` は従来経路を維持する。

## 現状

- sky worker が恒星、惑星、空色ディスクなどを含むスナップショットをバックグラウンドで生成する。
- GUI の既定の sky 更新間隔は 60 秒。
- 約 700ms 周期のスケジューラが更新期限を確認し、Qt の `update()` を通じて再描画を予約する。
- 人工衛星・航空機の位置予測は約 2 秒周期の短命レイヤーとして扱われる。
- 通常ビューアでは、恒星描画はベースシーンの描画中に行われる。
- 大きなウィンドウでは、恒星用の低解像度 `QImage` に恒星を描いてから画面へ拡大 blit する。
- 現状の描画コードでは、`vmag <= 4` と `vmag > 4` の両方が恒星描画経路に含まれる。明るい恒星の暗色下敷きとダイヤモンドは別パスだが、恒星補間用の独立レイヤーとしては未分離である。
- `vmag` は数値が小さいほど明るい。明るい恒星は `vmag <= 4`、暗い恒星は `vmag > 4`。

## 合意済みの目標構成

```text
空色ディスク・背景・地形・雲
        ↓
暗い恒星サーフェイス（vmag > 4、透明画像）
        ↓
明るい恒星の暗色下地（vmag <= 4、SourceOver）
        ↓
明るい恒星本体・ダイヤモンド（Plus 等）
        ↓
惑星、人工衛星、航空機、星座線、ラベルなど
```

- 暗い恒星サーフェイスは 60 秒スナップショット単位で生成し、補間中は画像を再生成しない。
- 明るい恒星、暗色下地、ダイヤモンドはサーフェイスへ焼き込まず、合成時に現在位置へ直接描画する案を第一候補とする。
- 空色ディスクは恒星サーフェイスから分離する。
- 暗色下地は半透明の黒または濃色として `SourceOver` で合成する。
- 暗色下地を恒星の加算画像へ混ぜて `Plus` 合成してはならない。黒が暗くならないためである。
- 暗い恒星サーフェイスには、スナップショット基準時刻からの経過時間に応じた 3x3 射影変換を適用する。
- 行列は画面中心だけで決めず、四隅付近、辺中央、中央など複数点を使って近似する。
- 四隅付近には `content_fov_deg` のオーバースキャン領域が含まれるため、誤差評価の対象にする。
- 星座線や恒星ラベルなど恒星位置に追随する要素は、同じ変換を適用するか、同じ補間時刻で再投影する。

## 時間更新方針

初期評価では、次の 60 秒サイクルを想定する。

- 0 秒: 新しいスナップショットを基準にする。行列をリセットする。
- 10 秒: 補間行列を更新する。
- 20 秒: 補間行列を更新する。
- 30 秒: 補間行列を更新する。
- 40 秒: 補間行列を更新する。
- 50 秒: 補間行列を更新する。
- 60 秒: 新しいスナップショットへ交換する。

ただし、行列は固定 10 秒値として実装せず、経過時間から算出できる形にする。これにより、2 秒周期または 100〜250ms 程度の短周期表示へ比較できるようにする。

重要な分離は次の通り。

- sky worker: 60 秒ごとの重い天文計算
- 補間状態: 経過時間と 3x3 行列の更新
- repaint: `request_client_update()` による Qt の非同期再描画要求

行列の更新では、空色ディスク、地形、雲、都市アウトライン、夜間光などの重いキャッシュを無効化しない。

## 主要な対象ファイル

- `src/zstarview/gui/sky_worker.py`
  - 60 秒スナップショットの生成。原則として大きな変更対象にしない。
- `src/zstarview/render/pipeline.py`
  - `_draw_star_layer()` が恒星サーフェイスと画面描画を分ける主要候補。
- `src/zstarview/render/stars.py`
  - 恒星のラスター生成、明るい恒星の暗色下地、ダイヤモンド、キャッシュ。
- `src/zstarview/gui/window_render.py`
  - ベース／表示フレームのキャッシュと `paintEvent()`。
- `src/zstarview/gui/window_updates.py`
  - 約 700ms 周期のスケジューラ、2 秒周期オーバーレイ、再描画要求。
- `src/zstarview/gui/window.py`
  - `request_client_update()` とタイマー初期化。
- `src/zstarview/render/geometry.py`
  - 現在の天球投影と画面座標変換。

## 実装前のTODO

- [ ] 現行の恒星描画時間と `paintEvent()` 時間を大画面で測定する
- [ ] 60 秒スナップショット交換時の画面差分を保存する
- [ ] 厳密な球面再投影を基準にした位置計算を用意する
- [ ] 四隅付近、辺中央、中央の対応点で 3x3 行列を推定する
- [ ] 画面全体の格子点で行列近似誤差を測定する
- [ ] `edge_fov_deg=90` と `content_fov_deg=115` の表示範囲で誤差を比較する
- [ ] 暗い恒星（`vmag > 4`）だけを透明サーフェイスへ描く試作を作る
- [ ] 明るい恒星（`vmag <= 4`）の本体・暗色下地・ダイヤモンドを直接描く試作を作る
- [ ] `SourceOver` の暗色下地と `Plus` の恒星光を分離して合成する
- [ ] 恒星サーフェイスを空色ディスク・地形・雲から分離する
- [ ] 恒星サーフェイスの行列変換だけで再描画できるキャッシュ経路を作る
- [ ] 明るい恒星の位置を同じ行列で変換する
- [ ] 星座線の補間方式を決める
- [ ] 恒星ラベルの補間方式を決める
- [ ] 10 秒、2 秒、100〜250ms 相当の更新頻度を比較する
- [ ] 補間行列または行列世代を表示キャッシュキーへ反映する
- [ ] 視点変更、resize、簡易表示、`sky-opacity=0` の動作を確認する
- [ ] Atlas と export-image が意図せず変更されていないことを確認する
- [ ] 失敗時に従来の 60 秒更新へ戻せる条件を定義する

## 推奨する実装順

1. **測定と数学モデル**
   - 実装前に球面再投影と 3x3 近似の誤差を測る。
   - 行列近似の受け入れ基準を決める。

2. **恒星レイヤーの独立化**
   - まず補間なしで、暗い恒星サーフェイスと明るい恒星の直接描画を分離する。
   - 変更前後の見た目とピクセル差分を確認する。

3. **合成モードの整理**
   - 暗色下地を `SourceOver`、恒星光を `Plus` で扱う。
   - 黒い縁、透明端、画像拡大時の黒フリンジを確認する。

4. **補間行列の導入**
   - 暗い恒星サーフェイスにだけ行列を適用する。
   - 明るい恒星は同じ行列で直接描画する。

5. **再描画・キャッシュ統合**
   - sky worker を再実行せず、行列更新だけで表示を更新する。
   - 古い行列の表示画像がキャッシュから返らないことを確認する。

6. **追随要素と頻度評価**
   - 星座線、恒星ラベルを対応させる。
   - 10 秒、2 秒、短周期の各方式を比較する。

## 検証基準

### 視覚

- [ ] 60 秒ごとの「カチッ」とした切り替えが軽減される
- [ ] 四隅付近で恒星が大きくずれない
- [ ] 明るい恒星とダイヤモンドが過度にぼけない
- [ ] 明るい恒星の黒い下地が空色ディスク上で正しく暗くなる
- [ ] 恒星の二重描画、消失、重複した下地が発生しない
- [ ] サーフェイス境界やオーバースキャン境界で恒星が欠けない
- [ ] 星座線・恒星ラベルが恒星からずれない

### 性能

- [ ] 恒星補間中の repaint が sky worker の再計算を誘発しない
- [ ] 大画面での再描画時間が許容範囲に収まる
- [ ] 明るい恒星の直接描画数が増えても UI が固まらない
- [ ] 60 秒スナップショット更新の所要時間が悪化しない

### 回帰

- [ ] 視点変更と fast-mode が従来どおり動作する
- [ ] window resize 後に古いサーフェイスや行列を使わない
- [ ] `sky-opacity=0` で暗色下地が意図せず残らない
- [ ] Atlas の描画に影響しない
- [ ] export-image がアニメーション状態に依存しない

## 受け入れ基準の初期案

数値基準は実測後に確定する。初期評価では次を目安にする。

- 画面全体の95パーセンタイル位置誤差: 0.5px 以下を目標
- 四隅付近の最大位置誤差: 1px 前後を目標
- 明るい恒星の輪郭: 変換前後で明らかな二重像・黒フリンジがない
- 補間中の追加処理: sky worker の再計算を発生させない

数値基準を満たさなくても、実際の表示で違和感がない場合は、視覚評価と併記して判断する。逆に数値基準を満たしていても、明るい星の輪郭や下地に違和感があれば採用を保留する。

## 未決事項

- [ ] 3x3 行列を厳密な球面回転から求めるか、対応点フィッティングから求めるか
- [ ] 4点最小構成と9点以上の最小二乗構成のどちらを採用するか
- [ ] 行列更新を10秒、2秒、短周期のどれにするか
- [ ] 恒星サーフェイスをQImageで保持するか、別のラスター形式を使うか
- [ ] 星座線を画像変換するか、折れ線を再投影するか
- [ ] ラベルの位置だけを変換するか、補間中に再配置するか
- [ ] 行列近似の誤差が大きい場合に、球面再投影へ切り替えるか

## 進捗ログ

### 2026-07-19

- [x] 現行のスケジューラ、sky worker、フレームキャッシュ、2秒周期オーバーレイを確認
- [x] 60秒スナップショットと軽量な恒星表示補間を分離する方針を整理
- [x] 3x3行列は画面中心だけでなく、四隅・辺中央・中央を含めて評価する方針を整理
- [x] 明るい恒星は `vmag <= 4`、暗い恒星は `vmag > 4` と確認
- [x] 暗色下地は `SourceOver`、恒星光は `Plus` として分離する方針を整理
- [x] 仕様書・レンダリング設計書・GUIキャッシュ設計書を更新
- [x] ドキュメント更新を `c27f6879`（`docs: define star interpolation design`）としてコミット

### 次回セッション開始時

- [ ] このファイルのTODOと未決事項を確認する
- [ ] まず誤差評価の方法を決め、実装前の基準測定から始める
- [ ] 実装を始める前に、変更対象ファイルとテスト方針を更新する

## 関連文書

- [`docs/specification.md`](../docs/specification.md)
- [`docs/design/rendering-pipeline.md`](../docs/design/rendering-pipeline.md)
- [`docs/design/gui-screen-update-and-cache.md`](../docs/design/gui-screen-update-and-cache.md)
- [`dev-notes/session-2026-07-19.md`](session-2026-07-19.md)
