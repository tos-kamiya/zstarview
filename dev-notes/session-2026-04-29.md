# Session 2026-04-29

- Topic: Preserve a working copy of the Wikidata tower query result
  - Decision: Saved `wiki-query-result3.json` to `src/zstarview/data/viewpoints/tower_viewpoints_2.original.json` as a raw source snapshot for further processing.
  - Rationale: The direct SPARQL query is timing out when retried, so keeping the current successful result allows incremental filtering and normalization without re-querying Wikidata.
  - Notes: The snapshot includes the desired tall-building candidates such as Shanghai Tower, Ping An Finance Centre, Shanghai World Financial Center, and Jin Mao Tower.

- Topic: Derive a filtered tower candidate bundle from the raw query result
  - Decision: Flattened `wiki-query-result3.json` into `src/zstarview/data/viewpoints/tower_viewpoints_2.rows.json`, filtered out missing and sub-100m heights, and merged it with the bundled tower dataset into `src/zstarview/data/viewpoints/tower_viewpoints_2.json`.
  - Rationale: This keeps the working query result intact while producing a reusable normalized candidate file for review and further curation.
  - Notes: The merged output currently includes the desired candidates and keeps `Q168575` excluded. `Q80813` was renamed to `Jin Mao Tower` in the processed file to restore a stable display name and English alias.

- Topic: Promote One World Trade Center into the bundled tower dataset
  - Decision: Added `Q11245` to `src/zstarview/data/viewpoints/tower_viewpoints.json` as a bundled high-observation viewpoint.
  - Rationale: It belongs in the same curated set as the other tall buildings with observation decks, and the QID-based merge path keeps the addition stable.
  - Notes: The other temporary extra candidates remain in the review-oriented `tower_viewpoints_2.json` for now.

- Topic: Merge the four recommended tall-building viewpoints
  - Decision: Added `Q18547`, `Q1077308`, `Q80852`, and `Q80813` into `src/zstarview/data/viewpoints/tower_viewpoints.json` using QID as the merge key.
  - Rationale: These are the originally recommended high-observation buildings, and they fit the existing bundled viewpoint model without requiring a schema change.
  - Notes: The bundled dataset now includes Shanghai Tower, Ping An Finance Centre, Shanghai World Financial Center, Jin Mao Tower, and One World Trade Center alongside the existing tower set.
