# Session 2026-03-01
- Scope: Repository overview check (structure, runtime stack, key modules, tests/docs)

- Topic: Repository overview walkthrough for zstarview
  - Decision: Keep the architecture as a package-centered desktop app with clear separation between CLI bootstrap, UI, rendering, astronomy logic, and satellite cloud pipeline.
  - Rationale: Current module split in `src/zstarview` is coherent and supports testability of pure logic (astro/math/cloud processing) while containing GUI concerns under `ui/`.
  - Alternatives Considered: None (inspection-only session).

- Summary:
  - Commits:
    - `docs: add INPROGRESS plan for star rendering responsiveness` (`2dbad6a`)
    - `perf: add interaction-time star LOD rendering` (`1e33f5c`)
  - Tags: none
  - Pushes: none
  - Follow-ups:
    - 次段として、視点回転時の星再計算スキップ（補助線データ再設計）を検討する。

- Topic: 高密度恒星表示（vmag<=10）の応答改善計画を文書化
  - Decision: `docs/design.md` の「今後の設計課題」に INPROGRESS 項目として、操作中軽量化 + 停止後精細化（LOD）方針を追記してから実装に着手する。
  - Rationale: 実装前に方針・受け入れ条件・テスト観点を固定し、段階導入時の判断基準を明確化するため。

- Topic: 操作中の星描画を軽量化する LOD の初期実装
  - Decision: `SkyWindow` に interaction mode（300ms アイドル復帰）を導入し、操作中は `vmag` 描画上限を `min(user_limit, 7.0)` に下げる。
  - Rationale: 既存ロジックへの影響を最小化しつつ、キー連打・リサイズ時の体感応答を改善するため。
  - Alternatives Considered: 視点回転時の星計算スキップは、補助線データ依存の整理が必要なため今回スコープ外とした。

- 17:20 Ran: `uv run -p .venv/bin/python pytest tests/test_star_draw_lod.py tests/test_theme_parse.py tests/test_planet_marker_style.py`
  Output:
  ```text
  collected 7 items
  tests/test_star_draw_lod.py ..
  tests/test_theme_parse.py ..
  tests/test_planet_marker_style.py ...
  7 passed in 4.12s
  ```
  Result: 新規 LOD テストを含め、関連テストは全件成功。

- Topic: 応答改善項目の INPROGRESS 表記を解消
  - Decision: `docs/design.md` の該当見出しを `INPROGRESS` から `一部実装済み` へ変更し、実装済み/未実装を分離して明記した。
  - Rationale: 実装済み範囲を正確に反映し、次段の残課題を追跡しやすくするため。

- Topic: design.md セクション再編（今後課題と実装済みの分離）
  - Decision: セクション9を今後課題に限定し、実装済み項目をセクション10へ分離。未実装のタイムシフト案はセクション11（継続課題）へ分割した。
  - Rationale: 「予定」と「完了」を同一セクションで混在させないため。

- Topic: 星カタログ正本化のステータス更新（vmag<=10 完了）
  - Decision: `docs/design.md` の「星カタログ生成の正本化」を今後課題から削除し、実装済みセクションへ移動した。
  - Rationale: `vmag <= 10` まで完了した現状を設計書に反映し、未完了タスクとの混在を防ぐため。

- Topic: 回転操作後にFOV外起源の欠落が残る不具合の修正
  - Decision: `SkyDataWorker` busy 時に捨てていた更新要求を pending フラグで保持し、計算完了後に追従更新を1回必ず実行する。
  - Rationale: 視点回転連打後に最新 `view_center` で再計算されず、黒い欠落領域が残る症状を防ぐため。

- 18:02 Ran: `uv run -p .venv/bin/python pytest tests/test_star_draw_lod.py tests/test_theme_parse.py tests/test_planet_marker_style.py`
  Output:
  ```text
  collected 7 items
  7 passed in 1.39s
  ```
  Result: 修正後も関連テストは全件成功。

- Topic: キー操作中は LOD6 を再生成して即時反映する更新経路を追加
  - Decision: 星空更新ワーカーへ `star_vmag_limit` オーバーライドを導入し、回転時は `vmag<=6` の星データを再生成して反映する。アイドル復帰時は通常上限で再計算を必ず要求する。
  - Rationale: 操作中の体感応答を上げつつ、停止後に完全データへ確実に戻すため。

- 18:16 Ran: `uv run -p .venv/bin/python pytest tests/test_star_draw_lod.py tests/test_theme_parse.py tests/test_planet_marker_style.py`
  Output:
  ```text
  collected 7 items
  7 passed in 1.49s
  ```
  Result: 変更後も関連テストは全件成功。

- Topic: LOD6 専用カタログの常駐化
  - Decision: 起動時に `vmag<=6` の `star_catalog_lod6` を作成して保持し、回転時の LOD6 更新ではこれを直接使用する。
  - Rationale: `-V10` など高上限指定時でも、LOD6 更新で毎回フルカタログをフィルタしないようにするため。

- 18:28 Ran: `uv run -p .venv/bin/python pytest tests/test_star_draw_lod.py tests/test_theme_parse.py tests/test_planet_marker_style.py`
  Output:
  ```text
  collected 7 items
  7 passed in 1.44s
  ```
  Result: 変更後も関連テストは全件成功。

- Topic: 高密度恒星表示セクションを確定仕様へ更新
  - Decision: `docs/design.md` で当該項目を「今後の設計課題」から削除し、「実装済みの主な変更」に確定仕様として再記述した。
  - Rationale: 現在の実装（LOD6専用データ常駐、キー操作時のLOD6再生成、busy時pending追従）を設計書に一致させるため。

- Topic: full/LOD6 の NumPy 前処理キャッシュ導入（ステップ1）
  - Decision: 起動時に `prepare_star_catalog_arrays()` で full と LOD6 を NumPy 化して保持し、更新時は Polars 変換を回避する。
  - Rationale: `calculate_visible_stars` 内の `cast(...).to_numpy()` を毎回実行するオーバーヘッドを削減するため。

- 18:41 Ran: `uv run -p .venv/bin/python pytest tests/test_star_catalog_prepare_arrays.py tests/test_star_draw_lod.py tests/test_theme_parse.py tests/test_planet_marker_style.py`
  Output:
  ```text
  collected 9 items
  9 passed in 1.36s
  ```
  Result: 新規前処理ヘルパーテストを含め、関連テストは全件成功。

- Topic: 回転時の先描画を停止して黒い三日月を抑止
  - Decision: `_rotate_view()` での即時 `update()` を削除し、LOD6 再計算結果の到着後に反映する挙動へ変更。
  - Rationale: 前視点FOVでクリップ済みキャッシュを一瞬描画して欠けが見える現象を避けるため。

- 18:53 Ran: `uv run -p .venv/bin/python pytest tests/test_star_catalog_prepare_arrays.py tests/test_star_draw_lod.py tests/test_theme_parse.py tests/test_planet_marker_style.py`
  Output:
  ```text
  collected 9 items
  9 passed in 1.38s
  ```
  Result: 変更後も関連テストは全件成功。

- Topic: LOD6 回転更新を FOV 非依存化して欠け表示を抑止
  - Decision: 回転時の LOD6 更新要求では `apply_fov_filter=False` を渡し、`vmag<=6` 全天データを返す経路を追加した。
  - Rationale: 再計算待ちやbusy追従時でも、前視点FOVで切られたデータを新視点へ投影して欠ける現象を防ぐため。

- 19:05 Ran: `uv run -p .venv/bin/python pytest tests/test_star_catalog_prepare_arrays.py tests/test_star_draw_lod.py tests/test_theme_parse.py tests/test_planet_marker_style.py`
  Output:
  ```text
  collected 9 items
  9 passed in 1.42s
  ```
  Result: 変更後も関連テストは全件成功。

- Topic: LOD6先行反映を撤回し、キー操作ごとに通常品質再計算へ復帰
  - Decision: 回転時の `LOD6` 優先更新を停止し、`request_sky_data_update()`（全LOD/FOVフィルタ）を毎キー入力で要求する挙動へ戻した。
  - Rationale: 体感品質優先のため、部分品質の先行反映をやめて一貫した通常品質更新に統一する。

- 19:20 Ran: `uv run -p .venv/bin/python pytest tests/test_star_catalog_prepare_arrays.py tests/test_star_draw_lod.py tests/test_theme_parse.py tests/test_planet_marker_style.py`
  Output:
  ```text
  collected 9 items
  9 passed in 1.39s
  ```
  Result: 変更後も関連テストは全件成功。

- Topic: 回転中のクラウド更新起因再描画を抑止
  - Decision: `view-change` ごとのクラウド更新を停止し、インタラクション終了時に1回だけクラウド更新する方式へ変更。`cloud_started` 受信時の `update()` も削除。
  - Rationale: 星再計算完了前に古い星データで再描画される経路を減らし、黒い三日月の一瞬表示を抑えるため。

- 19:33 Ran: `uv run -p .venv/bin/python pytest tests/test_star_catalog_prepare_arrays.py tests/test_star_draw_lod.py tests/test_theme_parse.py tests/test_planet_marker_style.py`
  Output:
  ```text
  collected 9 items
  9 passed in 1.50s
  ```
  Result: 変更後も関連テストは全件成功。

- Topic: ダウンロード中クラウド更新による再描画割り込みの抑止
  - Decision: インタラクション中は `cloud_ready`/`cloud_failed` で即時再描画せず deferred フラグへ退避し、星更新後（またはインタラクション終了後）に反映する。
  - Rationale: 雲更新イベントが星更新より先に再描画を起こして欠けフレームが見える現象を抑えるため。

- 19:47 Ran: `uv run -p .venv/bin/python pytest tests/test_star_catalog_prepare_arrays.py tests/test_star_draw_lod.py tests/test_theme_parse.py tests/test_planet_marker_style.py`
  Output:
  ```text
  collected 9 items
  9 passed in 1.39s
  ```
  Result: 変更後も関連テストは全件成功。

- Topic: 高密度恒星表示仕様へクラウド遅延反映ルールを明記
  - Decision: `docs/design.md` の確定仕様へ、回転中クラウド更新抑止・クラウド再描画deferred反映の仕様を追記した。
  - Rationale: 実装済みの挙動を設計書に一致させ、再発防止の判断基準を明確化するため。

- Topic: 高密度恒星表示の確定仕様を現行実装へ同期
  - Decision: `docs/design.md` の当該節を更新し、キー操作時は full品質再計算（FOVあり）であること、LOD6専用データは前処理キャッシュとして保持していることを明記した。
  - Rationale: 以前の LOD6先行描画前提の記述が残っていたため、現在の実装と仕様書を一致させるため。

- Topic: パッチバージョンを 0.16.1 へ更新
  - Decision: `src/zstarview/__about__.py` の `__version__` を `0.16.0` から `0.16.1` へ更新した。
  - Rationale: 直近の機能/挙動修正を次の配布単位として識別可能にするため。

- Topic: 暗い星の最小面積下限を 0.35 から 0.1 へ調整
  - Decision: `draw_stars()` の `min_area_px` を `0.35 -> 0.1` に変更した。
  - Rationale: 暗い星の持ち上げを弱め、`-V10` での過密感と描画負荷を緩和するため。

- 20:13 Ran: `uv run -p .venv/bin/python pytest tests/test_star_draw_lod.py tests/test_planet_marker_style.py`
  Output:
  ```text
  collected 5 items
  5 passed in 1.52s
  ```
  Result: 関連テストは全件成功。

- Topic: パッチバージョンを 0.16.2 へ更新
  - Decision: `src/zstarview/__about__.py` の `__version__` を `0.16.1` から `0.16.2` へ更新した。
  - Rationale: 直近の描画調整を新しい配布単位として識別可能にするため。

- Topic: 有名恒星ジャンプUIの仕様化（INPROGRESS）
  - Decision: `docs/design.md` の「今後の設計課題」に、メニュー起点の「Jump to Famous Star...」ダイアログ案を `INPROGRESS` で追記。初期スコープは `Vmag<=2.0` かつ赤緯3分割（北/赤道/南）とした。
  - Rationale: 代表恒星への素早い移動を可能にしつつ、候補数を過密にしないUI（北12/赤道15/南21）を先に固定するため。

- Topic: 有名恒星ジャンプ機能とジャンプ後3秒ハイライトの実装
  - Decision: `SkyWindow` に「Jump to Famous Star...」メニューと選択ダイアログを追加し、`Vmag<=2.0` の有名恒星を北/赤道/南で選択できるようにした。選択後は `RA/Dec -> Alt/Az` 変換で視点を移動し、3秒間はマウスホバー時と同じUI（円マーカー+星名）で対象を強調表示する。
  - Rationale: 代表星への移動操作を簡単にしつつ、ジャンプ直後に対象星を見失わないようにするため。

- 19:33 Ran: `uv run -p .venv/bin/python pytest tests/test_famous_star_shortcuts.py tests/test_star_catalog_prepare_arrays.py tests/test_star_draw_lod.py tests/test_planet_marker_style.py`
  Output:
  ```text
  collected 9 items
  9 passed in 1.49s
  ```
  Result: 新規ショートカット抽出ロジックを含め、関連テストは全件成功。

- 19:34 Ran: `uv run -p .venv/bin/python -m compileall src`
  Output:
  ```text
  Listing 'src'...
  ...
  ```
  Result: 追加モジュールを含め、コンパイルエラーはなし。

- Topic: 有名恒星ジャンプUIのステータス確定
  - Decision: `docs/design.md` の「有名恒星ジャンプUI」から `INPROGRESS` 表記を外し、仕様確定として更新した。
  - Rationale: ユーザー確認が完了し、実装仕様として固定できる状態になったため。

- Topic: バージョンを 0.17.0 へ更新
  - Decision: `src/zstarview/__about__.py` の `__version__` を `0.16.2` から `0.17.0` へ更新した。
  - Rationale: 有名恒星ジャンプ機能の追加を minor バージョンとして明示するため。

- Topic: 有名恒星の検索専用メニュー追加
  - Decision: 既存の「Jump to Famous Star...」とは別に「Search Famous Star...」を追加し、検索語入力で恒星名を絞り込む専用ダイアログを導入した。
  - Rationale: ユーザー要望どおり、ジャンプ一覧操作と検索操作をメニュー導線レベルで分離するため。

- Topic: 有名恒星検索メニューの対象を全固有名へ拡張
  - Decision: `Search Famous Star...` は `Vmag<=2.0` 制限を適用せず、固有名付き恒星（約443件）を全件検索対象に変更した。`Jump to Famous Star...` は従来どおり代表星集合を維持。
  - Rationale: ユーザー要望どおり、検索導線では代表星に限定せず固有名全件へ到達できるようにするため。

- Topic: READMEに固有名星ジャンプ/検索操作を追記
  - Decision: `README.md` と `README_ja-JP.md` の操作説明へ、`Jump to Named Star...` と `Search Named Stars...`、およびジャンプ後3秒ハイライト仕様を追記した。
  - Rationale: 新規UIの導線と挙動を利用者向けに明確化するため。

- Topic: バージョンを 0.17.1 へ更新
  - Decision: `src/zstarview/__about__.py` の `__version__` を `0.17.0` から `0.17.1` へ更新した。
  - Rationale: 直近のドキュメント更新を含むリリース単位をパッチ番号で識別するため。

- Topic: バージョンを 0.17.2 へ更新し、sdist構成を固定
  - Decision: `__version__` を `0.17.2` へ更新し、`pyproject.toml` の sdist include 制限を同時にコミットして公開手順に進める。
  - Rationale: 0.17.1 を取り下げたため再配布版として 0.17.2 を発行し、PyPIサイズ制限超過の再発を防ぐため。

- Topic: 配布サイズ削減のため巨大zipをビルド成果物から除外
  - Decision: `pyproject.toml` の `sdist` / `wheel` ターゲットに除外設定を追加し、`Noto_Sans.zip`・`cities1000.zip`・`stars/hip_main.dat.zip` を成果物へ含めないようにした。
  - Rationale: PyPI のファイルサイズ上限（100MB）に対して十分な余裕を確保するため。

- Topic: バージョンを 0.17.3 へ更新
  - Decision: `src/zstarview/__about__.py` の `__version__` を `0.17.2` から `0.17.3` へ更新した。
  - Rationale: 0.17.2 は既に PyPI に登録済みのため、追補修正を新バージョンで公開する必要があるため。

- Topic: VR版の有名恒星ジャンプ仕様（大円弧ガイド）を文書化
  - Decision: `../zstarview-vr/README.md` / `README-ja_JP.md` / `DEVELOPER_NOTES.md` に、固有名恒星選択後「現在の正面方向から目標恒星までの大円弧を3秒表示する」仕様案を具体値付きで追記した。
  - Rationale: VR版では視点強制回転を避け、ユーザーが向きを変える操作モデルを維持したまま、方向誘導を明確にするため。

- Topic: VRメニューの実装と大円弧ジャンプのフロー化
  - Decision: `../zstarview-vr/src/main.js` に「左コントローラメニューボタン / Mキーでパネル表示」「12本の明るい恒星リスト」「パネル上で名前に向けてトリガーを引くと3秒間の大円弧 + ホバーラベルで誘導」「アークのフェード処理」などのロジックを追加した。
  - Rationale: Spec を具現化するため、VR 内で実際に名有名星を選んで回転することなく正しい方向が示される体験を作る必要があったため。

- Topic: 恒星カタログの Alt/Az 変換高速化
  - Decision: `prepare_star_catalog_arrays()` で RA/Dec のラジアン、sin/cos、ICRS 単位ベクトルをキャッシュし、`calculate_visible_stars()` では AltAz 行列（`build_icrs_to_altaz_matrix`）を使って各星の Alt/Az を生成し、元の RA/Dec を残したまま FOV 判定を続ける形に設計書&計算ルートを更新した。
  - Rationale: `SkyCoord.transform_to` を毎回呼ぶ高コストを避けつつ、`ra_h`/`dec` のデータ契約を壊さずに視点更新応答を改善するため。

- Topic: BACKGROUND_FIELD_OF_VIEW_DEG1 以遠の小星描画の整理
  - Decision: `BACKGROUND_FIELD_OF_VIEW_DEG1`（90度）より外の半透明背景領域では、描画する矩形星の長辺が 2 ピクセル以下になる星は除外し、従来のサブピクセルノイズを抑制する。
  - Rationale: 背景の半透明処理を汚さないようサブピクセルサイズの星を削減しつつ、中心付近の視認性を維持するため。

- 06:06 Ran: `uv run -p .venv/bin/python pytest tests/test_star_draw_lod.py`
  Output:
  ```text
  ============================= test session starts ==============================
  platform linux -- Python 3.12.11, pytest-9.0.2, pluggy-1.6.0
  collected 2 items

  tests/test_star_draw_lod.py ..                                           [100%]

  ============================== 2 passed in 1.43s ===============================
  ```
  Result: `tests/test_star_draw_lod.py` passed after the star rendering filter change.

- Topic: Fixed hover highlight to require named stars
  - Decision: `find_highlighted_object()` now filters the star candidates to those whose `name` field contains non-empty text before computing the nearest highlight, so anonymous stars no longer trigger an empty label/circle.
  - Rationale: When `vmag` is high the sky is dense, making it hard to land on a named star; restricting the hover to named stars keeps the interaction focused on identifiable targets.
- 06:30 Ran: `uv run -p .venv/bin/python pytest tests/test_planet_marker_style.py`
  Output:
  ```text
  ============================= test session starts ==============================
  platform linux -- Python 3.12.11, pytest-9.0.2, pluggy-1.6.0
  collected 5 items

  tests/test_planet_marker_style.py .....                                  [100%]

  ============================== 5 passed in 1.48s ===============================
  ```
  Result: Named-star hover regression test passes after the filter change.

- Topic: Planet hover shows circle but no label
  - Decision: `draw_overlay_info()` now skips drawing the textual label when the highlighted object is a `PlanetBody`, preserving only the circle marker while stars still get labeled.
  - Rationale: Planets already have persistent labels elsewhere, so hiding the hover label prevents redundant text while keeping the circle feedback.
- 06:35 Ran: `uv run -p .venv/bin/python pytest tests/test_planet_marker_style.py`
  Output:
  ```text
  ============================= test session starts ==============================
  platform linux -- Python 3.12.11, pytest-9.0.2, pluggy-1.6.0
  collected 6 items

  tests/test_planet_marker_style.py ......                                 [100%]

  ============================== 6 passed in 1.69s ===============================
  ```
  Result: Planet hover regression test passes while keeping star highlight coverage.
