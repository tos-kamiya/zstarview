# Session 2026-07-14

Scope: Update zstarview-atlas Space-key simplified views.

- Topic: Atlas simplified-view rendering
  - Decision: When Atlas is in either simplified state, skip the below-horizon ground tint and urban-outline layer. Reuse the shared simplified named-star label pass only in the labels-enabled state.
  - Rationale: Atlas uses a separate instrument presentation path, so the existing scenic simplified-view flags did not reach its ground and building layers. Keeping terrain horizon, water, and other positional context intact preserves the map's orientation while removing the requested visual clutter.

- Verification: `QT_QPA_PLATFORM=offscreen UV_CACHE_DIR=/tmp/zstarview-uv-cache uv run -p .venv/bin/python pytest tests/test_window_render_sync.py -q` -> `117 passed`
- Verification: `.venv/bin/ruff check src/zstarview/render/pipeline.py tests/test_window_render_sync.py` -> `All checks passed!`
- Follow-up: Atlas の実画面では簡易ラベル状態の恒星名がキャッシュ済み instrument フレームに含まれない経路があったため、instrument 本体で名前付き恒星ラベルを描画し、HUD 側の二重描画を抑止した。
- Follow-up verification: `QT_QPA_PLATFORM=offscreen UV_CACHE_DIR=/tmp/zstarview-uv-cache uv run -p .venv/bin/python pytest tests/test_window_render_sync.py -q` -> `117 passed`
- Follow-up: Atlas の白背景では通常ビュー用の白ブレンド / 40% alpha の恒星ラベルが背景に溶けるため、Atlas の名前付き恒星ラベルをテーマの濃色・不透明文字で描画するよう調整した。カタログの名前メタデータは Atlas にも伝播していることを確認した。
- Follow-up verification: focused window-render tests -> `117 passed`; ruff and `git diff --check` passed.
- End summary: Updated Atlas rendering and its specification/design notes; no commit or push performed.
