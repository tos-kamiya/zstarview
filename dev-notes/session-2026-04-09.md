# Session 2026-04-09

- Scope: Bump the application version by `+0.0.1` and commit the release metadata change.

- Topic: Patch version bump
  - Decision: Update `src/zstarview/__about__.py` from `1.8.12` to `1.8.13`.
  - Rationale: `pyproject.toml` uses Hatch dynamic versioning from that file, so a single-source patch bump keeps CLI, GUI, and package metadata aligned.
