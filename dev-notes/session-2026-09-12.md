# Session 2026-09-12

- Scope: Spaceで切り替える「見えるものだけ」表示モードの追加。

- Topic: 表示モードの循環に新モードを追加
  - Decision: `visible-only` を既存の簡易表示とは独立したモードとして、簡易表示（ラベル付き）の次に追加する。
  - Rationale: 実際の天体・地形・環境レイヤーは維持しつつ、文字・方位や天文ガイド線・never-risesだけを抑制するため。

- Validation: `ruff check`、`compileall`、`git diff --check` は成功。関連58テストは57件成功し、1件は既存のinstrument描画期待値不一致。

- Topic: visible-onlyの実景レイヤー
  - Decision: 地形地平線・副稜線・水面・建物は再表示し、アステリズムだけを抑制する。
  - Validation: visible-only変更後の対象41テストは40件成功。残る1件は既存の描画呼び出し順期待値不一致。

- Topic: 利用者確認後の調整
  - Decision: window renderの別経路で描かれるアステリズムもvisible-onlyでは抑制し、Earth guideは表示する。
  - Validation: Ruff成功。関連42テストは41件成功、既存のinstrument描画期待値不一致が継続。

- Topic: 高度リングの抑制
  - Decision: visible-onlyでは背景と合成画像のalt 30/60度リングを無効化する。
  - Validation: Ruff、compileall、diff check成功。

- Topic: visible-onlyのステータス行
  - Decision: 下部の通常ステータス行を表示し、モード表示はその上に配置する。
  - Validation: Ruff、compileall、diff check成功。
