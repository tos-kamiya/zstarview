# Session 2026-09-20

Scope: Determine whether the Open-Meteo forecast model used for Matsue can be identified.

- Topic: Open-Meteo model provenance for Matsue
  - Decision: Treat the current application as using Open-Meteo `best_match`/automatic selection, while documenting that the exact contributing model is not currently exposed by the JSON response or retained by the application.
  - Rationale: The request omits `models`, so Open-Meteo applies its automatic selection. The current endpoint response contains coordinates and forecast arrays but no selected-model field. Open-Meteo describes the result as a stitched combination in which the source can vary by location, time step, variable, and forecast horizon.
  - Follow-up: If model provenance is required, add a diagnostic comparison/reporting path and decide whether to pin a specific model such as a JMA model, accepting that this changes the forecast semantics.

- Topic: Snapshot comparison of automatic forecast and JMA MSM
  - Decision: Compare eight Japanese cities from Naha to Sapporo using both 24-hour/current variables and the application's 15-minute precipitation request, without changing application code.
  - Result: In the captured snapshot, 15-minute precipitation differed only in Naha; Matsue matched for the sampled current and hourly values. The raw JSON and derived JSON/CSV summaries are saved in the repository root with the `open_meteo_` prefix.
  - Follow-up: Treat these results as a time-specific sample; identical values do not prove that `auto` always selected JMA MSM.

- Summary: Saved a repeatable experiment handoff at `dev-notes/open-meteo-auto-vs-jma-msm-handoff-2026-09-20.md`. No application code was changed.
