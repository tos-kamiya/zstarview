# Session 2026-03-20

- Scope: Adjust the top-right menu button styling for better readability.
- Topic: Menu button icon style
  - Decision: Replace the top-right menu button glyph from a hamburger (`☰`) to a vertical ellipsis (`⋮`).
  - Rationale: The current button is implemented as a plain `QPushButton` text glyph, so swapping the glyph is the smallest change and matches the requested "three vertical dots" affordance without introducing custom painting.
- Topic: Menu button visibility
  - Decision: Make the vertical-ellipsis menu button glyph bold via the shared button stylesheet.
  - Rationale: The `⋮` glyph reads visually lighter than `☰`, so increasing font weight improves discoverability without changing size, spacing, or button behavior.
- Topic: Menu button icon follow-up
  - Decision: Revert the menu button glyph to the hamburger (`☰`) and reduce the shared button font size from 18px to 16px.
  - Rationale: The vertical ellipsis remained hard to read in practice. Returning to the hamburger preserves recognizability, while a slightly smaller font keeps the icon from feeling oversized.
- 10:43 Ran: `uv run -p .venv/bin/python -m compileall src/zstarview/ui/window.py`
  Output:
  ```text
  Compiling 'src/zstarview/ui/window.py'...
  ```
  Result: Menu button styling changes compiled without syntax errors.
- Summary: Restored the hamburger menu glyph and reduced the button font size to 16px after testing a vertical-ellipsis variant.
