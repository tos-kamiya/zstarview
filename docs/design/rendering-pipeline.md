# レンダリング・パイプライン

この文書は、`zstarview`、`zstarview-gui`、`zstarview-export-image` で共通する描画パイプラインをまとめる。
主要なデータモデルは [data-model.md](data-model.md) を参照する。

## 1. 役割

- GUI と export-image が同じ入力から同じ scene を作れるよう、共通パイプラインを使う。
- GUI固有の一時的な表示モードは、共有scene dataやexport-imageの入力へ持ち込まず、GUIの描画段階で適用する。
- ベースシーン描画と HUD / 一時オーバーレイ描画を分ける。
- ラベル、ガイド、オーバーレイは、ウィジェットへ直結させず、合成可能な部品として扱う。
- renderer 固有の ad hoc な判断より、前処理済みの scene data と state を優先する。
- sky/glow と cloud は、見た目の base frame が近くても再描画トリガーと cache key を分けて考えてよい。

## 2. 合成の順序

描画は、概ね次の層を順に重ねる。

1. 背景
2. sky disc
3. 雲と欠損ティント
4. sky guides
5. 地形地平線と地面ティント
6. Earth guide
7. 星と天体
8. 都市アウトライン
9. 水面、航空機、人工衛星、夜間光、台風・サイクロン
10. ラベルと HUD

この順序は厳密な固定ではなく、レイヤーの種類ごとの責務を表す。
たとえば、雲は sky disc の上に乗るが、地平線下のティントや HUD はさらに上に置く。

通常ビューアの星・惑星の局所コントラストは、星と天体の層の内部で次の順に扱う。

1. `vmag <= 4.0` の恒星・通常惑星の暗色下敷きまたは輪郭
2. 恒星本体・惑星本体
3. `vmag <= 2.0` の恒星用ひし形マーカー、その他の天体マーカー

空色ディスクが無効な場合は、この局所コントラスト層を追加しない。これにより、
`--sky-opacity 0` は空色とそれに伴う補助的なコントラスト強調を同時に無効化する。

### 2.0.1 恒星サーフェイスと時間補間

恒星の 60 秒スナップショット更新によるフレーム切り替えを目立たなくするため、恒星の
描画資産を空色ディスクおよび重い base scene から分離する構成を採用してよい。

- 天文計算済みの恒星データと、そこから生成した暗い恒星（`vmag > 4.0`）の透明サーフェイスを、60 秒スナップショット単位で保持する。
- 恒星サーフェイスは更新間隔の中間時刻に生成し、その基準時刻からの経過時間（`-更新間隔/2`〜`+更新間隔/2`）に応じて矩形メッシュ変形を適用してから合成する。
- `sky_update_interval` が 90 秒を超える場合は、この射影変換による補間を無効にする。更新間隔が長い場合に60秒用の補間量を拡張して適用しないためである。
- 実際に行列またはメッシュへ与える経過時間は、現在は基準時刻からの値を100%適用する。補間係数は定数として保持し、平面近似またはスナップショット境界で逆戻りが確認された場合に値だけを下げて調整できるようにする。
- メッシュの誤差評価では、四隅付近、辺中央、中央を含む複数の位置を測定する。四隅付近は、`content_fov_deg` のオーバースキャン領域を含む実際の表示範囲の評価点とする。
- 四隅そのものはクリッピングやサーフェイス境界の影響を受けるため、測定点は必要に応じて数％内側へ置く。
- 1枚の3x3射影変換で画面位置による残差が見える場合の次段階として、恒星サーフェイスを矩形メッシュへ分割する。各メッシュ頂点を画面座標から3D天球方向へ戻し、観測地点における地球の自転軸を中心に恒星時の回転角だけ回転し、同じ画面投影で変換後の頂点位置を求める。
- メッシュ頂点では、移動ベクトル `(dx, dy)` を補助値として保持してよいが、描画の基準は変換前後の頂点位置とする。各セルを2枚の三角形へ分割し、恒星サーフェイスを変換後の三角形へ貼り付ける。これにより、北極星付近の小さい移動を中心とした回転場と、極から離れた位置の大きい移動を局所変形として表現する。
- メッシュの分割数は、最終クライアント領域の周囲にガード領域を加えた expanded viewport における約100px四方のセルから決める。暗い恒星サーフェイスも expanded viewport 全体を内部描画解像度へ変換して生成し、恒星、明るい恒星、asterism、twinkle、恒星ラベルなど恒星に追随する全てのメッシュ利用レイヤーは同じ expanded 座標系を使う。メッシュの変換前後の頂点をサーフェイスの幅・高さへ独立に縮小して変形し、変形結果を最終 viewport へ1回だけ拡大合成する。サーフェイスは expanded viewport のアスペクト比を保持した矩形であり、正方形を前提にしない。
- 三角形ごとのアフィン行列は、NumPyの行ベクトル式`[x, y, 1] @ A = [X, Y, 1]`からQtの`QTransform`規約へ変換する。`A[2, 0:2]`を平行移動へ、`A[0:2, 0:2]`をQtのX/Y係数へ正しい順序で設定し、回転中心を原点へ誤って固定してはならない。
- expanded viewport のガード幅は表示上の恒星移動量とフィルタ半径を吸収できる値とし、既定値は最終画面換算で32pxを初期候補とする。expanded 座標の原点は最終 viewport の左上からガード幅だけ外側に置き、最終合成時は元の viewport でクリップする。透明な空ディスク外の矩形領域も全メッシュで変形し、境界頂点を固定しない。各セル境界の連続性、expanded 領域から viewport へ入る恒星、viewport 端の画像アーティファクトがないことを確認する。
- 明るい恒星（`vmag <= 4.0`）は恒星サーフェイスへ焼き込まず、合成時に変換後の位置へ個別描画してよい。`vmag <= 2.0` のひし形も同じ経路で扱う。
- 明るい恒星の暗色下敷きは、恒星本体・ひし形と位置を共有する独立の透明レイヤーとして扱う。下敷きは `SourceOver`、恒星本体と加算的な光は `Plus` など、合成モードを分ける。
- 下敷きレイヤーを恒星の加算画像へ単純に混ぜて `Plus` 合成してはならない。黒成分が暗くならず、コントラスト下敷きとして機能しないためである。
- 新しいスナップショットが到着したら、恒星サーフェイスを交換し、中間時刻を補間の基準時刻としてメッシュ頂点を更新する。
- メッシュ頂点の更新は、重い天文計算とは独立した偶数秒tickの軽量な表示更新として扱う。メッシュの更新は惑星、航空機、人工衛星、瞬きと同じ対象時刻でpublishし、Qt repaint要求はtick末尾の1回にまとめる。
- 星座線、名前付き恒星ラベルなど恒星位置に追随する要素は、同じ変換を適用するか、同じ補間時刻で再投影する。簡易表示の恒星ラベルはアンカー位置だけをメッシュ変換し、文字は直接描画する。
- 惑星、太陽、月は恒星サーフェイスへ含めず、通常ビューアでは次の偶数秒tickの時刻について位置を専用workerで先行計算し、表示レイヤーで描画する。直接計算済みのalt/azには恒星スナップショット用の時間補間行列を適用せず、通常の画面座標投影だけを行う。
- 空色ディスク、雲の色、夜間光は60秒スナップショット側のキャッシュを維持し、惑星・太陽・月の表示位置更新では再生成しない。

この構成は、空色ディスク、地形、雲など地球側に固定されたレイヤーを動かさず、
恒星に関係する画像だけを変換するための現行設計である。実装検証では、画面中央、辺中央、
四隅付近の残差と、明るい星の輪郭・下敷きの見え方を検証する。

#### 2.0.2 瞬き（シンチレーション）の表示演出

瞬きは大気状態の物理シミュレーションではなく、通常GUIの恒星表示へ加える
軽量な一時演出である。Atlasと`zstarview-export-image`には持ち込まない。

- 通常GUIでは既定で有効とし、既存の2秒周期の軽量表示更新と同期して30個の対象を更新する。
- viewport interaction中のfast-modeでは瞬き対象を恒星描画へ渡さない。
- 通常GUIのCLIでは`--twinkle-count`で1回の更新あたりの候補数を指定できる。既定値は30で、
  0を指定すると無効になる。このオプションはAtlasと`zstarview-export-image`には追加しない。
- 通常GUIの起動時ダイアログでは、`Celestial`タブに瞬き候補数を、`Atmosphere`タブの
  `Meteor Trails`セクションにMeteor trailsの不透明度と最大候補数を配置する。Atlasでは瞬き
  項目を表示しない。AtlasのDisplayメニューにあるMeteor trails切り替えは維持する。
- 現在の表示フィールドからランダムなalt/azを選び、角距離3度以内で最も近い可視恒星を選ぶ。
  1回の更新では候補maskと恒星単位ベクトルを一度だけ作り、全抽選方向との内積を行列演算で
  一括評価する。結果は可視恒星配列のrow indexとして保持し、再検索せず描画に使う。
- 抽選方向は、azimuthを0〜360度から一様に選び、altitudeを`10 + 80 * x**2.5`（`x`は0〜1の
  一様乱数）で選ぶ。視野外の抽選結果は破棄する。
- 地平線下、描画対象外、個別描画される明るい恒星、および太陽系天体は候補から除外する。
- 選択した恒星のスナップショット側の座標へ、描画後サイズ（最小1ピクセル）の1.0倍の暗色半透明ドットを
  恒星本体の上に重ねる。ドットは恒星を完全に隠さなくてよい。
- 恒星サーフェイスのcache keyには瞬き対象を含めない。キャッシュ済みサーフェイスを合成した後、
  ドットだけをvolatile overlayとして描き、既存の恒星補間行列を適用する。2秒周期の更新間では、
  対象恒星、ドット位置、ドット不透明度を補間しない。
- altが10〜90度の対象の不透明度はaltに対して線形に`0.5`から`0`へ変化させる。
  altが0〜10度の場合は10度と同じ`0.5`を使い、altが0度未満の場合は適用しない。
- 1回の更新内では同じ恒星を重複して選ばないが、更新をまたいだ同じ恒星の再選択は抑制しない。
  候補が見つからない対象については一時レイヤーを描画しない。

### 2.1 Sky disc の色モデル

昼間のsky discは、球形地球上のRayleigh散乱とMie散乱を積算するモデルで生成する。実装の詳細と数値計算方針は、以下の球形大気モデル節にまとめる。旧来の高度・太陽角度・夕焼け補正を個別に重ねる経験的モデルは、本番描画経路から削除した。

夜間は、大気散乱の結果へ最低限のアンビエントRGBを加える。通常時は `[1, 2, 5]`、深夜2〜4時頃は `[2, 4, 10]` とし、AKARI IR bandsや人工照明とは独立した成分として扱う。補間には人工照明と同じローカル時刻ベースの深夜活動量カーブを使い、時刻を含む補正値をsky discのキャッシュキーへ反映する。

大気散乱RGBへ表示露出の指数トーンカーブを適用した後、`--sky-opacity`を総合表示強度として適用する。強度を`p`、散乱RGBを`C`、Rec. 709係数`[0.2126, 0.7152, 0.0722]`で求めた輝度を`Y`とし、`d = 0.18 * p^1.5`、`C' = p * ((1 - d) * C + d * Y)`で表示色を求める。これにより輝度は`p`に比例させながら、色差は高い強度ほど相対的に抑える。夜間アンビエントRGBはこの変換後に加え、独立した最低輝度として維持する。

### 2.1.1 球形大気・Rayleigh/Mie散乱モデル

`sky_disc.py` のMieモデルは、球形地球上の単一散乱を数値積分して昼間側の色を求める。移行前の挙動確認用スクリプトは `dev-samples/atmospheric_sky_experiment.py` と `dev-samples/atmospheric_sky_mie_experiment.py` に置く。

観測地点を `o`、視線方向を単位ベクトル `v`、地球半径を `R`、大気上端を `R + H` とすると、視線上のサンプル点は次で求める。

```text
x(s) = o + s v
```

`s` は視線と半径 `R + H` の球面との前方交点までを範囲とする。地球内部へ向かう視線は大気の積算対象外とし、各サンプル点の高度は `|x(s)| - R` から求める。太陽方向 `u_s` についても、各サンプル点から大気上端までの光路を同じ球形交差計算で求める。太陽光路が地球内部を通るサンプル点は照明されないものとして除外する。

各RGB成分 `lambda` の散乱放射は、概念的には次の積分で定義する。

```text
L_lambda(v) = integral[
    I_sun_lambda
    * beta_lambda(x)
    * P_lambda(theta)
    * exp(-tau_sun_lambda(x) - tau_view_lambda(x))
    ds
]
```

- `I_sun_lambda` は入射太陽光のRGB強度。
- `beta_lambda` はその地点の散乱係数で、Rayleigh成分とMie成分を持つ。
- `tau_sun_lambda` は太陽から散乱地点まで、`tau_view_lambda` は散乱地点から観測者までの光学的厚さ。
- `P_lambda(theta)` は散乱位相関数。Rayleigh成分には `1 + cos(theta)^2` 型、Mie成分には前方散乱の強いHenyey–Greenstein型を使用する。
- 太陽から観測者への直接光を表す項はこの積分に含めない。

#### スペクトル計算への移行設計

現行のRGB係数は表示調整を含む近似であり、Rayleigh散乱のG成分やエアロゾルのRGB係数を個別に調整すると色再現の一貫性を失いやすい。将来は次のスペクトル計算へ移行する。実装が完了するまでは、既存のRGB計算を正規の実行経路として維持する。

- 可視域を12〜16個程度の代表波長へ離散化する。初期仕様の候補範囲は380〜780 nmで、波長間隔または帯域幅を計算資産のメタデータへ記録する。
- 配列の基本形は、波長数を `L`、視線方向数を `N`、視線積分ステップ数を `V` として、密度を `(N, V)`、波長別光学的厚さを `(N, V, L)`、スペクトル光量を `(N, L)` とする。波長方向はNumPyのブロードキャストで計算し、視線方向は現行と同じチャンク処理を使う。
- 各波長で、太陽光スペクトル、Rayleigh散乱係数、Mie散乱係数、太陽方向・視線方向の透過率、位相関数を計算する。現在の地球形状・高度密度・太陽遮蔽の処理は共有する。
- CAMSのAOD550は、Ångström指数などの既定の波長依存則で各波長のエアロゾル光学的厚さへ変換する。AOD550だけでは吸収特性や粒径分布を決定できないため、採用した指数・単一散乱アルベド・位相関数のパラメータを設計定数として明示する。
- 波長別光量にCIE 1931 XYZカラーマッチング関数を掛けて `(N, 3)` のXYZへ積算し、D65白色点に基づくXYZ→sRGB変換を行う。D65は白色点であり、RGBの3波長や波長別の単純なRGB重みとしては扱わない。
- XYZ積分後のsRGB値は、現行と同じ表示露出・クリップ・QImage変換へ渡す。スペクトル計算の検証では、昼間の青空、太陽高度0〜10度の夕焼け、太陽高度下の残光、AODの地域差をRGB版と比較する。
- 12波長版で現行の空色ディスク更新時間とメモリ使用量を測定し、必要なら波長数を減らす。視線方向チャンクを増やしてもGUIスレッドでは実行せず、既存のsky workerで処理する。

大気密度は地表から指数関数的に減少させ、Rayleigh成分は大きいスケールハイト、エアロゾル成分は約 `1.4 km` の小さいスケールハイトを使用する。これにより、Mie成分は地平線付近の夕焼けの広がりへ寄与し、上空の青色は主にRayleigh成分が担う。実験では大気上端を100 kmと500 kmで比較し、画像差がほぼないことを確認したため、組み込み時の実質的な上端は100 kmを基本とする。

#### CAMSエアロゾル入力

- 配布物には、CAMSから事前抽出した全球約80 km（ADSの0.75度格子）・12か月分のAOD550だけを収録した軽量な派生データを同梱する。元のNetCDF/GRIBや複数の高度層・エアロゾル種は同梱しない。現在の生成物は `src/zstarview/data/aerosol/cams_aod550_climatology.npz` である。
- 派生データは `latitude/longitude/month -> aod550` の参照表として扱い、地点キー、データ版、生成元のCAMSデータ期間をメタデータへ含める。生成時の出典・加工表示は同じディレクトリの `cams_aod550_climatology.txt` に記録する。全球格子を収録するため、通常の地点は必ず対応セルを持つ。
- 実行時は緯度を全球範囲へ収め、経度を `0..360` へ正規化したうえで、緯度・経度の最近傍セルと暦月の値を参照する。補間は行わず、0.75度格子の解像度をそのまま利用する。
- 同梱する値は気候値であり、現在の天気や将来予測を表さない。観測地点の緯度経度と暦月でセルを選び、AODを散乱・消散量の補正入力へ変換する。
- RGB近似では代表波長を `R=650 nm`、`G=550 nm`、`B=450 nm` とする。Rayleigh係数は `1/lambda^4`、エアロゾル係数はÅngstrom指数 `alpha=0.7` の `1/lambda^alpha` から算出し、係数の由来をコード定数で追跡できるようにする。Mieの位相関数パラメータはRGB共通とする。
- 既存の固定エアロゾルプロファイルをAOD550 `0.15` 相当の基準とし、参照したAOD550との比率をエアロゾル密度へ適用する。Rayleigh成分は変更せず、エアロゾルの視線方向・太陽方向の光学的厚さとMie散乱量を同じ比率で調整する。エアロゾル消散の表示スケールは `0.018` とする。
- 薄明時は単一散乱結果へ高次散乱のRGB近似を加える。太陽高度 `+3`〜`-12` 度で滑らかに有効化し、視線高度に対する smoothstep を掛けて天頂側を強くする。実効RGBは `[0.032, 0.080, 0.280]` とし、近似値は表示変換前の放射輝度へ加え、深夜のアンビエント空色とは別に扱う。
- 空色ディスクは恒星・DSOのsky snapshot更新とは分離した低解像度専用workerで再生成する。太陽高度 `+15`〜`-15` 度では15秒、それ以外では60秒を固定周期とし、`--sky-update-interval` は恒星・DSO側だけに適用する。
- 左上または左下の時刻マーカー（三角形）の色は、太陽方位における高度 `0..10` 度の6サンプルを空色計算し、そのRGB平均を使用する。水平線上の単一点ではなく、夕焼け色が強くなりやすい少し上の空も含める。
- 通常のsky workerはネットワーク取得を行わない。これによりAPI認証、通信待ち、レート制限、サービス停止が空色表示を妨げない。
- AODは鉛直積算量であるため、単独で粒子の高度分布や吸収特性を決定しない。実装では、既定のエアロゾル鉛直プロファイル、波長依存係数、Mie位相関数と組み合わせる。
- 同梱データの検証に失敗した場合だけは、清浄寄り標準プロファイルへ退避して描画を継続する。これは配布データが正常な場合には発生しない防御的な異常系である。
- 将来、利用者が明示的に設定した認証情報で最新CAMS値を取得する機能を追加してもよい。その場合も、通常経路は同梱気候値とし、API認証情報をアプリケーションへ埋め込まない。
- CAMSの派生データを公開・配布する場合は、CAMSライセンスに従い、出典表示と改変データに関する表示を行う。生成スクリプトと取得対象のデータ版をリポジトリで追跡できるようにする。

#### 数値計算と表示解像度

GUIでは空色ディスクを最終画面の `0.25` 倍の内部サーフェスへ生成する。試作の基準値は視線方向64分割、太陽方向24分割だったが、視線方向を32分割へ下げても、240×100の比較画像で平均RGB差は `0.14/255`、最大差は `2/255` だった。処理時間は同条件で約2.61秒から約1.42秒へ短縮したため、組み込みでは視線方向32分割を使用する。

積分はGUIスレッドで行わず、既存のsky workerで実行する。太陽位置、観測地点、表示解像度、視線投影条件が同じ間は計算結果をキャッシュし、恒星・惑星の軽量な位置更新では空色を再計算しない。NumPy配列からQImageへの変換は主なボトルネックではなく、太陽光路と視線光路の光学的厚さを求める積分が計算量の中心である。

### 2.2 AKARI IR bands の色変換

AKARI IR bands は、準備済みの90 / 140 / 160 µm表示用データを銀河座標から画面の視線方向へサンプリングし、RGB画像として独立レイヤーにする。
色変換と合成は次の順序で行う。

1. 90 µmをB、140 µmをG、160 µmをRへ割り当てる。
2. 内部テーマに応じてRGBチャンネルを変換する。現在のテーマは
   `akari` で、90 / 140 / 160 µmをそれぞれB / G / Rへ直接割り当てる。
   `akari-four-band` は65µmをB、90µmをG、140µmと160µmの平均をRへ割り当て、
   `akari-two-band` は90µmをB、140µmをR、両者の平均をGへ割り当てる。
   `akari-two-band-color-adjusted` は`akari-two-band`のRGBへ
   それぞれ`0.7、0.8、1.0`を乗算する。
   比較用に `creative-hubble` のチャンネル混合と `jwst` の固定色加算も実装している。
3. RGB各成分へガンマ補正（現行値 `0.35`）を適用する。
4. RGBの最大成分をHSVのV値として扱い、V値のknee
   `v' = v / (1 + v)` で明るい領域を圧縮した後、固定倍率 `2` で表示用の
   `0`〜`1` へ再スケールする。色相と相対的な成分比は維持する。
5. 太陽高度による夜間フェードとレイヤーopacityをRGBへ反映する。人工照明が減少する深夜は、ローカル市民時に基づく活動量係数に応じて、AKARIの最終opacityを既定設定で`0.05`〜`0.10`の範囲へ変化させる。色相やRGBチャンネル自体は変更しない。
6. 画面サイズの0.25倍で生成したRGBAレイヤーを平滑拡大し、最終合成時に加算合成する。

この色変換は可視光の色や物理温度を再現するものではなく、遠赤外線バンドの分布を見やすくするための疑似カラー処理である。

## 3. ベース描画と HUD

- ベース描画は、scene の視覚的な土台を作る。
- HUD は、hover、jump highlight、static observation overlay、status line のような一時的または情報密度の高い表示を担当する。
- ベース描画と HUD を分けることで、視点変更時に base frame を再利用しやすくなる。
- `zstarview-export-image` は、既定では HUD を入れずにベース描画中心で出力してよい。

### 3.1 Display tone curve

Display tone curveはレイヤーの描画パラメータではなく、通常ビューアの最終表示段に
置くディスプレイ補償である。通常経路の合成順は次のようにする。

```text
base frame
  -> stars / planets / regular overlays
  -> present frame
  -> display tone curve (enabled only)
  -> window drawImage
  -> volatile hover / labels / HUD / status line
```

`present_frame`そのものを変更せず、補正が有効な場合だけRGB変換済みの
`display_frame`を作る。補正が`off`の場合は`present_frame`をそのまま
`drawImage()`へ渡し、追加サーフェイスも全画素変換も発生させない。

補正LUTは256要素とし、R/G/Bへ同じ値を適用してalphaを維持する。LUTは単調増加を
保証し、入力0を出力0、入力255を出力255へ固定する。利用者が指定した
`BLACK,WHITE`から暗部のliftと明部のcompressionを滑らかに生成し、中間調は
可能な限りidentityへ合流させる。LUT生成規則はversionを持ち、規則変更時に古い
表示用キャッシュを再利用しない。

この処理はICC profileの適用や`QColorSpace`による色空間変換ではない。
ディスプレイ側で既に失われた値を復元せず、アプリ出力を識別されやすい端部階調へ
再配置する。既存のstar、asterism、Earth guideなどのvisibility boostは、形状、
線幅、alphaを調整するレイヤー表現としてこの経路へ移動しない。

スクリーンショット、周期デバッグ画像、headless exportは補正前の描画資産を使う。
これにより、あるディスプレイ用の補正が別環境で見る画像ファイルへ混入しない。

### 3.2 Display tone curve calibration

キャリブレーション表示は、RGB 0背景上のnear-blackとRGB 255背景上のnear-whiteを
番号付きの独立した無彩色パッチとして暗い順に並べる再利用可能なPySide widgetと
する。値は2階調刻みとし、輪郭線のない短い矩形を8px間隔で背景領域の中央寄りに
配置して、その上下へ基準背景を残す。黒側は
左端寄りの最暗識別値、白側は右端寄りの最明識別値を選ぶ案内を各列の近くに置き、
クリック時はパッチ外のマーカーと現在値を更新する。パッチ値は製品内で共有し、
GUI起動ダイアログとCLI専用モードで同じ選択結果になるようにする。

- キャリブレーションサーフェイスにはdisplay tone curveを適用しない。
- `zstarview-gui`はstartup dialogの`General`タブから約800x600のcalibration dialogを
  開き、確定値をGUI launch profileへ戻す。
- `zstarview --calibrate-display-tone-curve`は地点解決や通常scene準備より前に
  約800x600の通常ウィンドウでcalibration dialogだけを実行する。
- CLI専用モードは`--display-tone-curve BLACK,WHITE`という引数断片をterminalへ
  ASCIIで出力し、同じ断片をQt clipboardへコピーする操作を提供して終了する。
- CLI専用モードは通常ビューアを起動せず、再実行、子プロセス起動、他のCLI引数の
  再構築、設定ファイルへの暗黙保存を行わない。

### 3.3 sky/glow と cloud の再描画分離

- `night light` と `ridge glow` のデータ生成は cloud の有無とは独立して扱う。ただし通常ビューアでは、night light と共有する太陽高度係数を使って、夜間の cloud 合成強度を補助的に持ち上げてよい。
- cloud の source ready / render ready は cloud overlay の更新だけを起こし、sky/glow の再計算や再描画を待たせない設計としてよい。source ready 自体は repaint の直接トリガーにせず、投影完了や別の更新契機に任せてよい。
- 逆に、sky snapshot 完了や terrain 完了は sky/glow base の再描画だけを起こし、cloud のダウンロード完了を待たずに可視化してよい。
- 実装上は、base sky/glow cache と cloud overlay cache を分け、invalidate の粒度も別にしてよい。
- cloud データが未到着でも、sky/glow base は単独で描画してよい。

### 3.4 簡易表示

- GUI は `simplified_view_enabled` のような全体簡易表示フラグを持ってよい。
- `Space` はInverted Cityを含む全体表示モードを循環する入力として扱ってよい。
- 起動時に`default_display_mode`と`current_display_mode`を同じ値で初期化する。`--inverted-city`が指定された場合だけ両方を`inverted-city`にする。循環の論理順は常に`normal -> inverted-city -> no-labels -> labels -> normal`であり、起動モードはその循環上の開始位置を決める。
- 実効表示は `simplified_view_enabled` とラベル設定から決めてよい。
- 実効表示の解決は、`normal` / `no-labels` / `labels` の 3 状態として扱ってよい。
- Urban Outlineが有効な場合、`Space`は `normal -> inverted-city -> no-labels -> labels -> normal` の順で循環してよい。Urban Outlineが無効またはロックアウト中は `normal -> no-labels -> labels -> normal` とし、inverted-cityをスキップする。
- Urban Outlineが無効化されたときにinverted-cityだった場合はnormalへ正規化し、再有効化時に自動復帰させない。
- 実効表示が簡易側のときは cloud / night-light / Earth guide / secondary ridges / water / urban outline を抑止してよい。
- 主稜線は簡易表示のときだけ細く描いてよく、通常表示では省略してよい。
- 実効表示が簡易側のときも、hover 解決は止めず、hover ラベルは維持してよい。
- 簡易表示が有効なときは、HUD に `Simplified: with labels [Space]` または `Simplified: no labels [Space]` を短く出してよい。
- `current_display_mode == default_display_mode` の間は通常の固定・動的ステータス行だけを描画し、異なる場合は黒文字と通常文字色背景のモード行を追加して3行にする。循環を一周して起動時のdefaultへ戻った場合はモード行を省略する。

## 4. オーバーレイの責務

### 4.1 天体とガイド

- 星、太陽、月、惑星は主要天体として描画する。
- Sky guides は、地平線、天の赤道、黄道、方位ラベル、天頂マーカー、天の極マーカーを含む。
- never-rises 領域は、Sky guides の一部として扱ってよい。

### 4.2 通常ビューアの明るい天体描画

通常ビューアでは、空色ディスクの既定不透明度を `0.2` とする。空色の明度によって
明るい天体が埋もれることを防ぐため、`vmag <= 4.0` の恒星は通常の一括ラスタ描画から
除外し、恒星単位の個別描画パスへ渡す。4.0 は対象に含める。

個別描画は次の2段階の恒星帯を持つ。

- `vmag <= 2.0`：暗色下敷き、恒星本体、既存の明るい恒星用ひし形
- `2.0 < vmag <= 4.0`：暗色下敷き、恒星本体

`vmag > 4.0` の恒星は従来の一括描画パスに残す。個別描画パスでは、まず対象恒星の
暗色下敷きを全件描画し、その後に対象恒星の本体とひし形を描画する。近接した恒星の
下敷きが、既に描いた恒星本体を覆わないようにするためである。

個別描画の下敷きは、空全体を塗り替える背景ではなく、恒星の見かけサイズに応じた
小さな局所形状とする。`vmag <= 2.0` のひし形には四角形の暗色角を残さず、ひし形に
整合する形状を使う。惑星は `vmag <= 4.0` の通常の惑星円盤に暗色の輪郭または下敷き
を付ける。太陽のクロスゲージと月の位相描画は、初期設計ではこの個別恒星下敷きの
対象外とする。

月の通常 `outline` 描画は、月の外周を一周させず、太陽光が当たる側の外周弧だけを
描く。その内側に、太陽方向と観測方向から求めた可視半球上の明暗境界を弧として描き、
月相と満ち欠けの向きを示す。正確な満月では外周円全体を、正確な新月では明るい側の
外周弧を描かない。`sphere`は倍率にかかわらずLambert陰影による球体月相画像を使う。
`image`のDial-A-Moon取得前・取得失敗時と月ホバーのフォールバックは、同じ太陽方向計算から
明部と暗部を二色で分け、明暗境界だけをサブピクセルで平滑化した簡略月相画像を使う。
月食色を簡略画像へ適用した後、月高度・観測標高・地点／月別AODによる共通の直達光
大気透過率を線形RGBで乗算する。

月表示CLIは、描画方式と倍率をそれぞれ `moon_style` と `moon_scale` として
保持する。`moon_style` は `marker`、`sphere`、`image` の三値、`moon_scale` は1から8の
整数とし、既定値はそれぞれ `marker` と1とする。月は `bright_bodies_mode` の分岐から
切り離し、同設定は明るい恒星、太陽、惑星だけに適用する。既存の
`--enlarge-moon` は `sphere` と倍率5を同時に選ぶ互換入力としてCLI解決時に正規化する。
新しい月オプションとの併用は優先順位を設けず入力エラーとする。正規化後のGUI入力、
描画スタイル、描画関数には `enlarge_moon` の真偽値を保持せず、`moon_style` と
`moon_scale` だけを渡す。

月ホバー時の拡大画像はHUD側で通常月の上に重ねる。ベース描画が既にクロスゲージを
描いているため、ホバー用の拡大月描画ではクロスゲージを追加せず、二重表示を避ける。

外部画像を使う月ホバーでは、NASA SVS Dial-A-Moon APIの730x730 JPEGを共有GUI worker
poolで取得し、APIが返す時刻・月の見かけの直径・位置角とともにメモリおよびディスクへ
キャッシュする。画像は黒背景で透過情報を持たないため、画像の明るさではなく、満月
フレームで校正した画像中心と`diameter`に基づく円形マスクで月面を抽出する。これにより
月の暗い側を背景として誤って削除しない。取得中または取得失敗時は既存の自前月相描画を
使う。取得完了後は、同じ月へのホバーが継続している場合、または常時 `image` 表示が
選択されている場合だけ再描画する。
NASA画像は天の北を上にした座標系なので、自前月相画像の天頂基準回転角をそのまま使わず、
観測緯度における天の北極（高度＝観測緯度、方位＝0度）を月の接平面へ投影し、その方向を
画面へ写す回転角を使う。画像座標の上方向は負のY軸なので、画面上の天の北方向角に90度を
加えた値を`QPainter`の回転角とする。画像に含まれる月の位置角を追加で回転して二重適用して
はならない。

月ホバー画像の大気色には、高度区分ごとの固定色や経験的な暖色補間ではなく、太陽ホバーと
共通の`direct_atmospheric_transmission_rgb()`による直達光RGB透過率を使う。月高度、観測
地点の標高、レイリー消散、エアロゾル消散、オゾン
吸収、AODを同じ光路積分へ渡す。Dial-A-Moon画像は元の月面色を持つため、太陽用の基準色は
重ねず、入力と出力のsRGB変換の間で透過率だけを乗算し、alphaは維持する。水平線付近または
水平線下の表示下限は天体共通にせず、月マーカーに対応する中立的な最低乗算色を月用に定める。

Dial-A-Moonの月食画像は月食による月面色を入力画像自体に含むため、その画像へ観測者側の大気
透過率を乗算する。既存の自前月相向け月食オーバーレイは外部画像へ重ねない。月食と低高度が
重なって暗さや赤みが過剰になる場合も、大気モデル
を月食時だけ弱めない。調整対象は表示用の月食オーバーレイまたは月用最低乗算色とし、共通の
大気透過率の意味を維持する。

外部画像を使う太陽ホバーでは、HelioViewerのSDO/HMI Continuum `getClosestImage` と
`takeScreenshot`画像を共有GUI worker poolで取得し、実観測時刻、データソース、画像スケール、出力範囲とともにメモリおよび
ディスクへキャッシュする。ホバーイベントはキャッシュ済み状態の表示だけを切り替え、HTTP
要求や画像デコードを直接開始しない。現在時刻付近では5〜15分の更新間隔を目安とし、過去時刻
では表示対象時刻に最も近い観測フレームをキーにする。更新失敗時は時刻を保持した古い正常
キャッシュを許容し、キャッシュがなければ通常の太陽描画を維持する。

取得したPNGは未加工のままディスクへ保存し、デコード後に表示用alphaを生成する。画像中心から
メタデータの太陽半径以内はalpha 255とする。円板外では`m = max(R, G, B)`とし、`m <= 3`を
alpha 0、それ以外をalpha `m`とする。straight alphaのRGBは`255 * (R, G, B) / m`として、
合成時のpremultiplied RGBが入力画素の強度へ戻るようにする。円板半径から外側4画素の範囲は
smoothstepで円板側の不透明画素と円板外のblack-to-alpha結果をpremultiplied空間で補間し、
境界の硬い切れ目を避ける。同じ変換を新規取得とディスクキャッシュの双方へ適用する。

HMI Continuumはほぼ単色の強度画像として届くため、表示色は散乱光の加算ではなく、画像輝度への
RGB乗算で生成する。入力を線形RGBへ変換した後、太陽光の基準スペクトルと大気のRGB別透過率を
各画素へ乗じ、最後にsRGBへ戻す。したがって、黒点の低輝度は黒点のまま残り、光球の輝度構造を
保ったまま黄白色、橙色、赤色の色温度を表現できる。alpha計算と乗算色付けは分離し、円板外の
black-to-alpha処理やコロナ相当の弱い発光の透明度を色付け処理で変更しない。

RGB透過率は太陽高度に応じた太陽方向の光路について計算する。既存の大気モデルが持つ
`RAYLEIGH_SCATTERING_RGB`、`AEROSOL_SCATTERING_RGB`、レイリー／エアロゾルのスケール高度、
オゾン吸収係数、観測地点の標高、および`bundled_aod550_or_default()`のAODを再利用する。
この計算が求めるのは直達光から失われるRGB別の消散量であり、散乱光の方向分布を求めない。
したがって、空の散乱光計算で使用するミー位相関数と`MIE_ANISOTROPY`は、太陽ホバー画像の
色付けには使用しない。ここでエアロゾル項はミー領域の粒子による消散の近似であって、ミー
散乱光の方向別放射輝度を計算するものではない。
`atmospheric_sky_samples()`は散乱して視線へ入る空の放射輝度を返す加算型の関数で、直射太陽光を
意図的に含まないため、太陽ホバー画像の色付けには直接使用しない。太陽画像用には、同じ係数と
光路計算からRGB別の直射光透過率を求める専用の純粋関数を設ける。
透過率乗算後のRGB係数は、既存の`planet_marker_color("sun")`を線形化して45%に抑えた
フォールバック係数をRGBチャンネルごとの下限として適用する。これにより、水平線下で直接光の
透過率が0になる場合も画像全体が黒くならず、太陽マーカーとの視覚的な連続性を保つ。フォール
バックは加算ではなく乗算係数の下限であり、黒点の輝度差とalphaは維持する。

HelioViewerのHMI画像は`HPLN-TAN` / `HPLT-TAN`、`CROTA2=0`、
`HV_ROTATION=0`の太陽北基準へ正規化された入力として扱う。ROI指定の`x0` / `y0`は
HelioViewerの表示画像座標であり、特に正の`y0`はPNGの下方向になるため、WCSのHPLT符号を
表す値として使用しない。通常取得では太陽中心の`x0=0`、`y0=0`だけを使う。

画面回転は入力画像の北上化と分離する。表示対象時刻について、IAU太陽北極方向を観測者の
見かけの座標系へ変換し、太陽中心方向に直交する接平面へ射影して太陽北ベクトルを得る。
太陽中心と、そのベクトル側へ微小角だけ移動した方向を同じAlt/Az・画面投影関数へ通し、
両画面座標の差から画面上の太陽北方向角を求める。画像上端の負Y方向がこの方向角へ一致する
よう`QPainter`で一度だけ回転する。HelioViewer画像へP角を追加して再び北上化してはならず、
天の北を入力とする月ホバー用回転関数も流用しない。

向きの回帰テストでは、太陽北と太陽東西を明記した人工画像を使い、視線中心、FOV、観測地点、
季節を変えて画面投影を検証する。実画像については、HelioViewer JP2/FITSヘッダー、同一時刻の
`downloadImage`、NASA SDO公式browse画像との比較を基準にし、外観だけで上下左右を判定しない。

この個別描画は通常ビューアの表示方針であり、Atlas の明背景ラスターパスとは別に
管理する。実装時は、候補抽出、下敷き、恒星本体、ひし形の合成モードを分離し、
`vmag` 境界、形状、サイズ、アルファ、表示モードを星レイヤーのキャッシュキーへ
含める。export-image でも同じ条件と描画順を使用する。

### 4.3 雲と地平線

- 雲は sky disc の上に合成し、欠損領域は別ティントで表現してよい。
- 雲画像の生成は 2 段階に分ける。
  1. **取り込み段階（ingestion）**：衛星データを取得した直後に、観測者から見た (altitude, azimuth) グリッド `CloudAltAzGrid` に変換する。このグリッドはカメラ視点に依存せず、観測地点・衛星・時刻・雲シェルの組み合わせで一意に決まる。
  2. **描画段階（render）**：カメラが変わるたびに、`CloudAltAzGrid` から現在の視線中心・FOV・画面サイズに合わせて RGBA 雲画像を生成する。
- Geo-satellite もこの 2 段階に含めてよく、原画像の再利用と投影を別工程として扱ってよい。
- 取り込み段階では、衛星画素の (lat, lon, 雲シェル高度) を ECEF 上の点とみなし、観測者からの方向ベクトルをローカル ENU 座標に変換して altitude/azimuth を求める。
- 輝度温度（BT）から雲量への変換は、既存の `bt_warm` / `bt_cold` 正規化を流用してよい。
- 雲量の正規化では、赤道中心の単線ではなく、おおむね `±5°` の赤道帯サンプルを warm-threshold の基準に使ってよい。
- warm-threshold は、赤道帯サンプルを主軸に、視野内の局所サンプルを少量混ぜた補正としてよい。局所視野が安定しているときでも、赤道帯参照を主軸に残す。
- この補正は共通 render 経路で使われ、現時点では GOES / Himawari のどちらにも適用される。
- 複数の雲シェル（既定では 3 km / 5 km / 7 km）から来たサンプルは、シーン全体の雲量に応じた重みでブレンドして 1 つの雲量値 per (alt, az) セルにまとめる。
- 同一セルに複数サンプルが入った場合は `max` または平滑化された平均を使ってよい。MVP では `max` 寄りの統合で、薄い高層雲も視覚的に残すことを優先する。
- 描画段階では、`CloudAltAzGrid` から 4 つのストライプモードを生成できる。
  - `halftone`: 45° 傾いた画面固定の正方形グリッドを用い、各セルの雲量を 8 段階に量子化して独立した円を描く。各円の外周約 2px は放射状アルファグラデーションで滑らかに減衰させ、別個の輪郭線は描かない。
  - `width`: 中心対称のストライプを描き、雲量に応じて帯の見かけ幅を変える。
  - `width-quantized`: `width` の変形。雲量を5段階に量子化し、幅の変化点に隙間を設けた丸端ストロークとして描く。
  - `alpha`: 帯幅を固定し、雲量に応じて帯のアルファ値を変える。
- `--cloud-stripe MODE[,COUNT[,WIDTH]]` の `COUNT` はディスクを横切る絶対的な本数である。ウィンドウサイズや星レイヤーのレンダリング面サイズに応じてスケールせず、大きなウィンドウでは間隔が広がる。
- 既定の雲描画スタイルは `halftone,30,1.7` とし、既定の雲不透明度は `0.11` とする。`COUNT` または `WIDTH` を `0` にすると雲描画を無効化する。
- 通常ビューアでは、雲は恒星の縮小レンダーサーフェイスと同じ解像度で生成する。ウィンドウ基準のグリッド間隔・ドット径・線幅は、サーフェイスの縮小率に応じて変換し、拡大後の見かけの密度を維持する。暗い恒星の後に雲を合成し、明るい恒星はその後に画面解像度で描画するため、雲のアルファは空と暗い恒星にだけ作用する。
- `CloudAltAzGrid` は観測地点・衛星・時刻・シェルに対してキャッシュしてよい。カメラの回転やズームでは取り込みをやり直さず、軽量な描画段階だけ再実行する。
- 欠損領域ティントも、alt/az グリッドに付随する `missing_mask` を同じ alt/az → 画面投影で描画してよい。
- 地形地平線は空と地面の境界を決める。
- Earth guide は地平線下の方向案内として、地形地平線とは独立した補助レイヤーとする。
- 簡易表示では Earth guide を抑止し、主稜線だけを細く再描画してよい。
- sky/glow は cloud source の到着を待たずに更新してよい。cloud overlay の再投影は、sky/glow base とは独立に遅延または再実行してよい。

### 4.3 地表系レイヤー

- 都市アウトラインは、観測地点周辺の建物輪郭を示し、閉じた輪郭には薄い面塗りを重ねてよい。
- 閉じた屋根ポリゴンの面塗りは、人工照明の視覚成分としてnight lightと同じ深夜活動量係数を最終塗り強度へ乗算する。最低アルファを例外扱いせず、深夜に面塗りが消えることを許容する。
- 建物の輪郭線は面塗りとは別の地物基準であり、面塗りと同じ係数で消さず、独立した強度で残す。
- 画面上で小さく潰れた閉輪郭は、面塗りだけ省略してよい。
- GUIの`Inverted City`が有効な場合、都市アウトラインの投影済み点列に対して、すべての表示点を幾何学的な水平面 `alt=0` より下へ収める変換 `alt_inverted = -abs(alt)` を描画直前に適用する。輪郭線、屋根ポリゴン、遠距離スカイスクレーパー補助レイヤーには同じ変換を適用する。これにより、タワーなど高所の観測地点から見て元々負の高度にある建物も、反転後に上空へ現れない。
- `Inverted City`は都市レイヤーの表示座標だけを変更し、建物データの取得、キャッシュ、観測者中心の投影、DEM、天体計算、地形地平線の生成を変更しない。
- 道路夜間光、水面、地形地平線、地面ティント、Earth guideは反転都市の変換対象外とする。
- 反転都市の状態はGUIの一時状態として扱い、通常のexport-image共通sceneには含めない。export-imageは常に通常の都市アウトライン表示とする。
- 水面レイヤーは、地形地平線や地面ティントと合わせて地表の読み取りを助ける。
- 通常描画時の水面遮蔽判定は、水面点または境界点の方位に最も近い terrain horizon profile の点を参照する。地形の高度が水面より一定量以上高く、地形距離が水面距離より近い場合だけ、その水面の alpha を副稜線の遮蔽時と同じ係数で下げる。地形プロファイルまたは距離情報がない場合は遮蔽せず、従来の水面描画を維持する。ビューポート移動中の高速描画では、この判定を省略する。

### 4.4 移動体と補助情報

- 航空機は予測軌跡として描画する。
- 描画表現は折れ線に限定せず、帯状ポリゴンにしてよい。帯状ポリゴンを使う場合は、進行方向に沿った軌跡を細かくサンプリングし、投影後の点列からリボン状の面を構成してよい。
- その帯の視覚的な半幅は、概ね「2 秒程度で機体が移動する量」を目安に決めてよい。近距離の機体ほど太く、遠距離ほど細く見える段階的な幅制御を続けてよい。
- 帯幅の基準はスクリーン座標ではなく、機体の局所水平面で進行方向に直交する向きとしてよい。その後で投影してもよい。
- 地平線付近や旋回中は、局所水平面で左右境界を作ってから投影する方が、画面上の見かけよりも意味を保ちやすい。
- 人工衛星は小さなマーカーとして描画する。
- 夜間光と台風・サイクロンは、補助的な地理・気象情報として重ねる。
- 夜間光の glow は、街灯系の補助レイヤーとして扱い、色推定と合成を分けてよい。
- 簡易表示では、night-light 系の glow を抑止してよい。

## 5. ラベルの扱い

- ラベルは、星、惑星、DSO、アステリズム、衛星、航空機、検索結果など複数の候補源から集約する。
- まず候補を正規化し、その後に配置順を決める。
- 重なりやすいラベルは、局所グループを作ってから配置してよい。
- グループ内では、対象に近い候補、または上側の候補から先に配置してよい。
- 既に置かれたラベルの予約矩形を共有し、後続のラベルほど回避するようにしてよい。
- hover ラベルと persistent marker ラベルは、短い jump highlight とは別に扱ってよい。
- export-image では、必要なら marker/label を 1 件だけ持ち込むが、永続状態は残さなくてよい。
- 簡易表示で出す名前付き恒星ラベルは、`B-V` 由来の色をそのまま使い、文字不透明度を `0.7` 程度に抑えてよい。
- その簡易表示ラベルは、星座標に対して文字矩形の左下隅を合わせる固定配置としてよく、重なり回避は行わなくてよい。
- 人工衛星の簡易表示ラベルも `draw_satellite_overlay()` 側で同様に固定配置してよく、hover 用のラベル経路とは分けてよい。
- その衛星ラベルは、クロスマーカーの近傍に固定で置き、重なり回避を行わなくてよい。
- 簡易表示ラベルが有効なときは、hover 側の衛星名ラベルは抑止して重複を避けてよい。
- 太陽ラベルは、body marker とは別に Moon と同程度の muted なテキストとして描いてよい。
- 太陽ラベルには、装飾マーカーや囲みを追加しなくてよい。
- 航空機の帯状ポリゴンを使う場合、label collision の対象は帯の外形ではなく機体本体の基準点としてよい。
- 全体表示モードは、`normal`、`inverted-city`、`no-labels`、`labels`の状態で管理してよい。`inverted-city`はUrban Outlineが有効な場合だけ有効な状態とし、無効時は状態循環から除外する。

## 6. オーバーレイと HUD の境界

- static observation overlay は HUD 側で描画する。
- status line も HUD 側で描画する。
- hover の一時表示は、ベース描画ではなく HUD 側で足す。
- ベースフレームのキャッシュから mouse position、hover 対象、jump highlight、status 文言は除外してよい。
- 簡易表示では、HUD の固定情報ブロックを抑止し、代わりに `Simplified: with labels [Space]` のような最小状態ラベルだけ残してよい。

## 7. 外部依存

- GUI toolkit と描画プリミティブ
- 天文・暦関連ライブラリ
- 数値処理
- ラスタ・地理空間処理
- 衛星、地形、建物、航空機、夜間光の取得ヘルパ

## 8. 設計意図

renderer は、正規化済みの scene data を受け取り、入口ごとの分岐を最小限にして最終画像を生成する。
それにより、3 つのアプリの見え方を揃えつつ、起動方法や永続化だけを入口ごとに変えられる。

### 4.2a 雲ストライプ描画モード

`CloudAltAzGrid` から画面への雲描画には、以下 3 つのストライプモードがある。
いずれも右下 45°（`u = x - y` 方向）の線を基本とし、`SkyCompositorCache` の
`cloud_stripe_mode` で切り替える。

- `halftone`
  - 画面に固定の正方形 2D グリッド（`Δu = Δv`、45° 回転）を置く。グリッド原点はビューポート左上。セル間隔は `target_stripes` で調整可能。
  - `target_stripes` はディスクを横切る絶対的な本数であり、ウィンドウサイズや星レイヤーのレンダリング面サイズに応じてスケールしない。大きなウィンドウではセル間隔が広がり、円/線も太くなる。
  - 各セルの中心点で `CloudAltAzGrid.amount` をサンプルし、width スタイルと同じパーセンタイル正規化（非ゼロ値の 12%ile / 92%ile）を適用後、8 段階の非線形量子化を行う。閾値は `1.4⁻ⁱ`（i=7..1）。
  - 量子化テーブル（[0, 1] を 8 分割）:

    | 雲量範囲    | レベル | 円直径（delta=30px 時） |
    |-------------|--------|------------------------|
    | 0.000 – 0.095 | 0    | 描画なし |
    | 0.095 – 0.133 | 1    | 2.8 px |
    | 0.133 – 0.186 | 2    | 5.7 px |
    | 0.186 – 0.260 | 3    | 8.5 px |
    | 0.260 – 0.364 | 4    | 11.3 px |
    | 0.364 – 0.510 | 5    | 14.1 px |
    | 0.510 – 0.714 | 6    | 17.0 px |
    | 0.714 – 1.000 | 7    | 19.8 px |

  - 円直径は `width_factor` でスケール可能。直径は `delta / 30 * wf / √2 * 0.95` で計算され、並行チェーン間の垂直距離（`delta / √2`）に対して隙間が確保される。
  - レベル > 0 の各セルは、セル中心に量子化レベルに対応する直径の一様な円として描画する。円のアルファは `hatch_cfg.strength` とする。
  - 色は白、不透明度は `hatch_cfg.strength`。円形クリップパスで円盤外を切り落とす。
  - `CloudAltAzGrid` 自体は変更せず、描画時にスクリーン空間で量子化する。

- `width`
  - `CloudAmountField`（正規化 (u, v) 空間にビン化した雲量場）からサンプル。
  - 中心対称のストライプを描き、雲量に応じて帯の見かけ幅を変える。

- `alpha`
  - 同じく `CloudAmountField` からサンプル。
  - 帯幅は固定し、雲量に応じて帯のアルファ値を変える。

- `--cloud-stripe MODE[,COUNT[,WIDTH]]` の既定値は `halftone,30,1.7` とする。`COUNT` または `WIDTH` を `0` にすると雲描画を無効化する。
- 既定の雲不透明度は `0.11` とする。通常ビューアでは、night light と同じ太陽高度遷移（`-9°` 以下で最大、`-4°` 以上で無効）を使い、cloud 用の boost `0.3` により実効値を最大 `1.3` 倍まで持ち上げる。
- 通常ビューアの雲サーフェイスは恒星の低解像度サーフェイスを共有し、ウィンドウ解像度からの縮小率でグリッド間隔を変換する。描画順は暗い恒星、雲、明るい恒星の順とする。
- 雲のグレースケール下地への寄せ量は、`cloud_opacity * 4.0` を `1.0` でクリップした係数を使う。白色 RGB の加算は `cloud_opacity` をそのまま係数に使う。
