# 白背景オブジェクトビューア仕様書

最終更新: 2026-07-10

この文書は、`zstarview` と同じドメイン計算・描画部品を共有する白背景オブジェクトビューアの利用者向け仕様である。
`zstarview` 本体の星空表示仕様は `docs/specification.md` に置き、この文書では別入口アプリとしての外部動作だけを扱う。

## 1. 位置づけ

白背景オブジェクトビューアは、暗い星を含む星空シミュレーションではなく、指定地点から見える空中・天体・地理補助オブジェクトを白背景上で読み取るためのビューアである。

利用者には `zstarview` の表示モードではなく、別アプリ入口として見せる。
ただし、地点解決、時刻解釈、天体計算、雲・航空機・人工衛星・地形・都市アウトラインなどの取得処理は `zstarview` と共有してよい。

起動コマンドは `zstarview-object-viewer` とする。

## 2. 表示目的

- 白背景上で、雲、明るい恒星、太陽、月、惑星、航空機、人工衛星を読み取りやすくする。
- 都市アウトライン、地形地平線、水面、Earth guide を場所の特定を助ける補助レイヤーとして表示する。
- 暗い恒星の再現性より、対象物の識別、位置関係、ラベルの読みやすさを優先する。

## 3. 既定表示

- 背景は完全な白とする。
- sky color disc は既定で描かない。
- ラベルは黒文字を基本とする。
- 恒星は明るいものに限定し、白背景で点描ノイズになりにくい密度に抑える。
- 恒星の等級上限は既定値を `4.0` とし、`-V` で指定できる上限を `6.0` とする。
- 都市アウトライン、地形地平線、水面、Earth guide は既定で表示する。
- 稜線より下の地面 tint は既定で有効にし、夜間光と ridge glow は既定で無効化する。
- 空色ディスクと night light のコマンドライン引数は提供しない。
- 水平線、方位ラベル、天の赤道、黄道、天頂マーカーは、黒の細い実線またはマーカーで描く。
- 黄道だけは黒の細い点線で描く。
- これらのガイドは既定で表示する。
- 方位・高度のグリッドは、マウスホバーを待たず常時表示する。
- 稜線より下の領域には、既定で確認可能な薄いグレー（不透明度 `0.15`）の tint を重ねる。
- 雲は白背景で見えるよう、白ではなく淡い青灰色のハッチ、ドット、線で描く。
- 航空機と人工衛星は、白背景上で識別しやすい赤紫系の線色・マーカー色を使う。両者は形状で区別する。

## 4. ラベルと補助レイヤーの視認性

白背景オブジェクトビューアでは、ラベルと HUD は黒または濃いグレーを基本とする。
文字の縁取りは原則不要とし、対象物の種類を示す必要があるラベルだけ、航空機や人工衛星などのマーカー色に合わせてもよい。

航空機、人工衛星、地形地平線、都市アウトライン、水面、Earth guide などの補助レイヤーは、白背景で主対象に負けない色、線幅、不透明度を使う。
必要に応じて、線やマーカーの本体とは別に薄い縁取りまたは下敷き線を描いて視認性を補う。
この縁取りは文字の縁取りとは別の概念であり、軌跡、十字マーカー、都市アウトラインなどの図形を白背景から分離するために使う。

## 5. 恒星描画

白背景オブジェクトビューアでは、恒星描画は次の規則に従う。

- 描画対象は等級上限でまず絞り込む。
- 投影後の見かけ直径が 1.0 px 未満の恒星は描画しない。
- 1.0 px 以上の恒星は、アウトラインレイヤーと本体レイヤーを分けて描く。
- アウトラインレイヤーは恒星本体より先に描く。
- 本体レイヤーはアウトラインの上に描き、恒星色を維持する。
- アウトラインは黒または濃いグレーとし、恒星本体より少し大きくする。
- すべての恒星に一律の太い縁取りを付けず、見かけサイズや等級に応じて控えめにする。

この分離により、近接した恒星がある場合でも、後描きのアウトラインで恒星本体の色を潰さない。

## 6. レイヤー順

概ね次の順序で描画する。

1. 白背景
2. 稜線下の地面 tint
3. 常時表示の方位・高度グリッド
4. 水平線、天の赤道、黄道、方位ラベル、天頂マーカー
5. 地理補助レイヤー
6. 雲
7. 恒星アウトライン
8. 恒星本体
9. 太陽、月、惑星
10. 航空機、人工衛星
11. 検索マーカー、ラベル、HUD

地理補助レイヤーには、都市アウトライン、地形地平線、水面、Earth guide を含めてよい。
各レイヤーは白背景で主対象を妨げない低い不透明度を既定とする。

## 7. zstarview との差分

- `zstarview` は暗背景または空色 disc を使い、暗い星を含む星空把握を重視する。
- 白背景オブジェクトビューアは、白背景、黒ラベル、明るい星、地理補助レイヤー、移動体・雲・天体の識別を重視する。
- 既定値の違いはテーマだけでなく、sky color、恒星密度、補助レイヤーの色、ラベル色、雲色に及ぶ。
- そのため、利用者向けには `zstarview` の単なるテーマや一時モードではなく、別入口アプリとして扱う。

## 8. 未確定事項

- 起動前ダイアログを `zstarview-gui` と共有するか、白背景アプリ用の簡略ダイアログを用意するか。
