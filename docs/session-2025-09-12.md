Session: 2025-09-12 — Remove python -m note in README Troubleshooting

- 00:34 Ran: `rg -n "python -m zstarview|トラブル|troubleshoot|Troubleshoot|起動" README.md || true`
  Output:
  ```text
  29:> Note: Troubleshooting tips (including X11 libraries and slow network) are summarized below.
  63:   See Troubleshooting for tips on slow networks or offline use (e.g., disabling clouds with `-c 0`).
  168:## Troubleshooting
  185:Launching from a terminal shows startup messages and errors: `zstarview` or `python -m zstarview.zstarview`.
  ```
  Why: Locate the line mentioning `python -m zstarview.zstarview`
  Result: Found the target line in Troubleshooting

- 00:34 Ran: `nl -ba README.md | sed -n '180,189p'`
  Output:
  ```text
     181	### Sky Update Interval and CPU Load
     182	Frequent sky updates can be CPU‑intensive on lower‑end machines. Increase the interval to reduce load (e.g., `-i 300` for every 5 minutes). Lower it only if your machine can keep up.
     183	
     184	### Viewing Logs
     185	Launching from a terminal shows startup messages and errors: `zstarview` or `python -m zstarview.zstarview`.
     186	Logs are also written to a file (platform‑dependent). Examples:
     187	- Linux: `~/.cache/zstarview/logs/app.log`
     188	- macOS: `~/Library/Logs/zstarview/app.log`
  ```
  Why: Confirm exact context to edit
  Result: Ready to patch the line

- 00:35 Ran: apply_patch (update README)
  Output:
  ```text
  Success. Updated the following files:
  M README.md
  ```
  Why: Remove `python -m zstarview.zstarview` mention, keeping `zstarview` only
  Result: README updated

- 00:35 Ran: `rg -n "python -m zstarview" README.md || true`
  Output:
  ```text
  
  ```
  Why: Verify removal
  Result: No matches — change effective

Summary: Removed the `python -m zstarview.zstarview` launch instruction from Troubleshooting to align with pipx-based installation and CLI usage.

---

- 00:36 Ran: `rg -n "python -m zstarview|トラブル|トラブルシュート|起動|Troubleshoot|ログ|Viewing Logs" README-ja_JP.md || true`
  Output:
  ```text
  README-ja_JP.md: No such file or directory (os error 2)
  ```
  Why: Attempt to find Japanese README by common filename
  Result: File not found; try discovering actual filename

- 00:36 Ran: `rg -n "README.*ja|ja_JP|日本語|日本語版|Japanese" -S`
  Output:
  ```text
  README_ja-JP.md:206:→ [2025年の月食, 2026〜2028年の皆既日食](docs/appendix-eclipses-ja_JP.md)
  ```
  Why: Locate the Japanese README filename
  Result: Found `README_ja-JP.md`

- 00:36 Ran: `nl -ba README_ja-JP.md | sed -n '168,176p'`
  Output:
  ```text
     169	### 星空の更新間隔とCPU負荷
     170	CPU性能によっては星空の自動更新が負荷になる場合があります。様子を見ながら更新間隔を長く（数値を大きく）して負荷を下げてください（例: `-i 300` で5分ごと）。余裕があれば短くして構いません。
     171	
     172	### ログの確認
     173	ターミナル（端末）から起動すると起動メッセージやエラーを確認できます: `zstarview` または `python -m zstarview.zstarview`
     174	併せてログファイルにも出力されます（OSに依存）。例:
     175	- Linux: `~/.cache/zstarview/logs/app.log`
     176	- macOS: `~/Library/Logs/zstarview/app.log`
  ```
  Why: Confirm the exact line to edit
  Result: Ready to patch

- 00:36 Ran: apply_patch (update README_ja-JP.md)
  Output:
  ```text
  Success. Updated the following files:
  M README_ja-JP.md
  ```
  Why: Remove `python -m zstarview.zstarview` from Japanese README Troubleshooting
  Result: Updated successfully

- 00:36 Ran: `rg -n "python -m zstarview" README_ja-JP.md || true`
  Output:
  ```text
  
  ```
  Why: Verify removal
  Result: No matches — change effective

Summary: Updated README_ja-JP.md to remove the `python -m zstarview.zstarview` instruction in the ログの確認 section; aligns with pipx and CLI usage.

---

- 00:41 Ran: apply_patch (add offline first-run note to README.md)
  Output:
  ```text
  Success. Updated the following files:
  M README.md
  ```
  Why: Document that the first run downloads `de440s.bsp` and needs network
  Result: Note added under “Slow or Unstable Network / Offline Use”

- 00:41 Ran: apply_patch (add offline first-run note to README_ja-JP.md)
  Output:
  ```text
  Success. Updated the following files:
  M README_ja-JP.md
  ```
  Why: 同様の注記を日本語READMEの「ネットワークが遅い/オフラインで使いたい」に追記
  Result: 注記を追加

Summary: Added a first-run ephemeris download note (de440s.bsp) to both EN/JA offline-use sections.

---

- 00:46 Ran: `rg -n "de4\\d\\ds\\.bsp|\\.bsp\\b" src | sed -n '1,200p'`
  Output:
  ```text
  src/zstarview/data/check_moon_eclipse.py:48:eph = load("de440s.bsp")
  src/zstarview/astro.py:154:    planets = _starfield_load("de440s.bsp")
  src/zstarview/astro.py:249:    planets = _starfield_load("de440s.bsp")
  src/zstarview/astro.py:315:    planets = _starfield_load("de440s.bsp")
  ```
  Why: Find hardcoded ephemeris filenames to refactor
  Result: Identified call sites

- 00:47 Ran: apply_patch (add EPHEMERIS_FILENAME const to paths.py)
  Output:
  ```text
  Success. Updated the following files:
  M src/zstarview/paths.py
  ```
  Why: Centralize ephemeris kernel filename
  Result: EPHEMERIS_FILENAME = "de440s.bsp"

- 00:47 Ran: apply_patch (use EPHEMERIS_FILENAME in astro.py)
  Output:
  ```text
  Success. Updated the following files:
  M src/zstarview/astro.py
  ```
  Why: Replace magic strings with constant
  Result: All three load sites updated

- 00:47 Ran: apply_patch (use EPHEMERIS_FILENAME in data/check_moon_eclipse.py)
  Output:
  ```text
  Success. Updated the following files:
  M src/zstarview/data/check_moon_eclipse.py
  ```
  Why: Keep internal script consistent
  Result: Uses constant via package import

- 00:47 Ran: `rg -n "de4\\d\\ds\\.bsp|\\.bsp\\b" src | sed -n '1,200p'`
  Output:
  ```text
  src/zstarview/paths.py:110:EPHEMERIS_FILENAME = "de440s.bsp"
  ```
  Why: Verify no remaining magic literals
  Result: Only the centralized constant remains

Summary: Refactored ephemeris filename into src/zstarview/paths.py:EPHEMERIS_FILENAME and updated all usages.

---

- 00:52 Ran: apply_patch (revert change in data/check_moon_eclipse.py)
  Output:
  ```text
  Success. Updated the following files:
  M src/zstarview/data/check_moon_eclipse.py
  ```
  Why: This is a verification script, not production; keep the literal `"de440s.bsp"` to avoid coupling
  Result: Import of EPHEMERIS_FILENAME removed; literal restored

Summary: Left the verification script with its original magic literal by request; production code uses the centralized constant.
